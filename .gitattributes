# 바이너리 산출물은 diff·검사 대상이 아니다.
# (2026-08-16: 재생성된 dist/·docs/ 의 PDF 가 `git diff --check` 에서 「줄 끝
#  공백」으로 잡히던 것을 여기서 막는다 --- 텍스트로 훑을 파일이 아니다.)
*.pdf   binary
*.zip   binary
*.woff2 binary
*.png   binary
*.jpg   binary

# 생성물의 줄 끝 공백은 예제 출력에서 온다 --- 손으로 고칠 수 없으므로 검사에서 뺀다
docs/**/*.html -whitespace
