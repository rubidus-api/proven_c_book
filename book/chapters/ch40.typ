#import "../lib.typ": *

= 안전한 입력과 proven의 등장

#prereq(
  ([25장 입력], [입력의 위험]),
  ([39장 문자열], [문자열은 끝을 표시할 뿐이다]),
)

#deepqa[
  39장의 gets는 그릇 크기를 안 받아서 퇴출됐고, 25장의 fgets는 크기를
  받아서 안전하다 했다. 그러면 "크기를 받는다"는 것만으로 입력의 안전
  문제가 다 풀리는가?
][
  절반만이다. 크기 전달은 *넘침*을 막지만, 입력에는 두 번째 문제가
  있다 — *내용을 믿을 수 없다*는 것(25장). 수를 기대했는데 글자가
  오고, 짧은 줄을 기대했는데 긴 줄이 오고, 악의적인 상대는 일부러
  경계를 노린 입력을 만든다(22장의 서식 문자열 공격이 그 맛보기였다).
  안전한 입력이란 [넘침 차단 + 실패의 명시적 처리]의 합이고, 이 장의
  두 기둥이 그 둘이다.
]

#organizer[
  25장에서 "안전한 입력은 왜 어려운가"를 예고했고, 34·39장에서 그 위험의
  뿌리(경계, NUL 종단)를 배웠다. 이 장에서 사고의 해부를 완성하고 —
  1장에서 약속한 손님, 이 책의 기반 라이브러리 *proven*이 드디어
  등장한다. "필요가 스스로 증명된 시점에"라는 약속 그대로다.
]

#chapter-questions()

== 사고의 해부 — 경계 침범이라는 유행병

37장의 경계 규칙과 39장의 NUL 종단을 합치면, C 역사상 가장 값비싼
사고 무늬가 조립된다. 그릇보다 긴 입력이 이웃 기억을 덮어쓴다(경계
침범) — 덮어써진 곳이 하필 *함수 호출의 복귀 장부*(다음 장에서 정체를
본다)라면, 공격자는 입력만으로 프로그램의 실행 흐름을 탈취한다. 이것이
버퍼 오버플로 공격의 골자이고, Morris worm(1999, 39장)부터 오늘의 보안
공지까지 면면히 이어지는 유행병이다. 통계적으로도 압도적이다 — 주요
보안 기관들의 "가장 위험한 소프트웨어 결함" 목록에서 경계 침범 계열은
수십 년째 최상위 단골이다.

C가 실행 중 경계 검사를 "하지 않기로" 했기 때문에(37장), 방어는 층층이
쌓는 수밖에 없다 — 크기를 받는 함수(fgets), 경고와 새니타이저(17장),
그리고 *애초에 검사가 내장된 부품*. 마지막 층이 이 장의 손님이다.

== proven — 검증된 기본기라는 부품

proven은 이 책의 저자가 만든 C 라이브러리로(1장의 정직 공개 그대로),
설계 사상은 한 문장이다 — *사고가 가장 잦은 기본기(문자열, 입력 해석,
수 변환)를, 실패가 값으로 드러나고 경계가 항상 검사되는 형태로 제공
한다.* 9장에서 본 표현 방식의 언어로 말하면, NUL 종단의 세계 위에
길이·용량 명시 계열의 안전한 층을 세운 것이다.

첫 시연으로 25장의 숙제 — 입력 해석의 실패 처리 — 를 proven으로
풀어 본다. 성공 경로와 실패 경로를 *둘 다* 보인다:

#demo("examples/ch40/scan.c")

읽는 법 — `proven_scan_init`이 글자 열 위에 해석기(스캐너)를 세우고,
`proven_scan_i64`가 정수 하나를 해석한다. 반환값이 이 설계의 심장이다:
*값이 아니라 `{err, val}` 꾸러미*를 돌려주고, 쓰는 쪽은 `proven_is_ok`
로 성공을 확인한 뒤에만 값을 꺼낸다. sscanf(25장)와 비교하면 —
"성공 개수"라는 관례적 신호 대신 *실패가 타입으로 명시*되고, 함수에는
`[[nodiscard]]`(C23 — "이 반환값을 버리면 경고하라"는 표기)가 붙어
있어 *실패 확인을 잊는 실수를 컴파일러가 잡아 준다*. 48장에서 정식으로
다룰 "오류는 값이다" 규율의 실물이다.

#qa[
  표준 라이브러리로도 되는 일을 왜 라이브러리를 더해서 하는가 — 부품이
  늘면 배울 것도 느는데.
][
  이 책의 대답은 층의 구별이다. *개념은 표준 C로 온전히 배웠다* —
  33–37장까지 proven 없이 온 것이 그 증거이고, proven을 걷어내도 이
  책의 C 지식은 성립한다(1장의 약속). 그 위에서 *실무의 기본기*를
  고를 때는, 표준 라이브러리의 역사적 함정(gets의 장례, scanf의 꼬임,
  검사 없는 문자열 함수들)을 아는 채로 고르는 것이 현대적 선택이다 —
  Rust나 Zig가 언어 차원에서 내장한 안전을(1장), C에서는 부품 선택으로
  얻는 것이다. proven은 그 선택지의 하나이자 이 책의 시연 재료이고,
  같은 사상의 다른 라이브러리를 골라도 원리는 같다.
]

#qa[
  proven 함수들의 이름이 길다 — `proven_scan_i64`처럼. 왜 이렇게
  짓는가?
][
  C에는 이름 공간이 없기 때문이다(24장의 스코프는 블록의 것이고, 파일
  경계를 넘는 이름들은 한 마당에서 산다 — 51장). 라이브러리들이 같은
  이름을 쓰면 링크 단계(16장)에서 충돌하므로, C 라이브러리의 관행은
  *접두어가 이름 공간을 대신*하는 것이다 — `proven_`이 그 담장이다.
  길지만, 어느 부품의 것인지 이름만 봐도 드러난다는 실속이 있다.
]

(이 장은 첫 만남이라 필요한 만큼만 쓴다 — 라이브러리의 설계 전모와
사용법은 제12부에서 세 장에 걸쳐 다룬다. 이 책이 C 입문서이면서 동시에
proven의 안내서이기도 한 이유다.)

입력의 안전을 갖췄다. 그런데 이 장에서 "함수 호출의 복귀 장부"라는
말이 설명 없이 지나갔다 — 기억의 어디에 무엇이 살고, 언제 태어나고
죽는가. 다음 장이 그 지도 — 수명과 저장 기간 — 다.
