#import "../lib.typ": *

= 개발환경 구축

#organizer[
  14장의 릴레이를 내 컴퓨터에서 돌릴 도구를 갖춘다. 어느 플랫폼에서든
  통하는 일반론이 본문이고, 특정 운영체제에 묶이는 구체적 설치법은
  "플랫폼 노트" 상자로 격리해 두었다 — 상자는 이 책의 예시 플랫폼인
  Windows 기준이며, 건너뛰어도 본문은 이어진다. 버그를 실행 중에
  잡아 주는 그물(새니타이저)까지 장만하면 이 부가 끝난다.
]

#deepqa[
  14장에서 컴파일러에게 `-E`, `-S` 같은 부탁을 하며 명령줄에서 일했다.
  요즘은 클릭으로 다 되는 세상인데, 왜 명령줄인가?
][
  일반론이기 때문이다. 그래픽 개발 도구는 저마다 생김새가 다르고 몇 해
  마다 바뀌지만, `cc hello.c -o hello`라는 문장은 반세기째 모든
  플랫폼에서 통하는 공용어다. 그리고 어떤 그래픽 도구를 쓰든 그 "빌드"
  버튼의 밑바닥에서 실제로 도는 것이 바로 이 명령들이다 — 밑바닥을
  아는 사람은 도구를 갈아타도 길을 잃지 않는다. 이 책이 명령줄을
  기준으로 삼는 이유다.
]

== 필요한 것은 셋뿐이다

개발환경이라는 말이 거창하지만, C 프로그래밍에 필요한 것은 셋뿐이다.

- *컴파일러* — 14장의 네 주자 일체. 이 책은 gcc와 clang 두 계열을
  기준으로 한다(둘 다 무료이고 모든 주요 플랫폼에 있다).
- *텍스트 편집기* — 소스 코드는 그냥 텍스트 파일이므로(6장), 글자를
  적을 수 있는 도구면 무엇이든 된다. 어떤 편집기를 쓰는가는 취향의
  영역이라 이 책은 정하지 않는다.
- *터미널* — 명령을 적어 넣는 창. 모든 운영체제에 기본으로 있다.

일과의 모양도 단순하다 — *편집하고, 컴파일하고, 실행한다.* 이 세 박자
의 반복이 프로그래밍의 기본 사이클이고, 이 책의 모든 예제가 이 사이클
위에서 돈다(물론 독자는 지면으로 읽기만 해도 된다 — 1장의 약속대로,
이 장의 설치조차 시연이지 숙제가 아니다).

#platform[
  Windows에서 컴파일러 설치하기
][
  이 책의 예시 플랫폼은 Windows다. 두 갈래 중 하나(또는 둘 다)를
  고르면 된다 — 이 책의 예제는 어느 쪽으로도 동일하게 빌드된다.

  *갈래 1 — LLVM clang (권장).* LLVM 프로젝트의 공식 Windows 배포판을
  설치한다. 터미널(PowerShell)에서 한 줄이면 된다:

  ```text
  > winget install LLVM.LLVM
  ```

  (또는 llvm.org 의 릴리스 페이지에서 설치 프로그램을 받아도 같다.)
  설치 후 새 터미널에서 `clang --version`이 판 번호를 답하면 준비 끝
  이다. 이후 이 책의 `cc` 자리에 `clang`을 쓰면 된다:

  ```text
  > clang hello.c -o hello.exe
  > .\hello.exe
  안녕, 세상!
  ```

  *갈래 2 — MinGW-w64 gcc.* gcc의 Windows 이식판이다. MSYS2라는 꾸러미
  관리 환경(msys2.org)을 설치한 뒤, 그 터미널에서:

  ```text
  $ pacman -S mingw-w64-ucrt-x86_64-gcc
  $ gcc --version
  ```

  *셋째 길 — Visual Studio(MSVC)에 관하여.* Microsoft의 자체 컴파일러와
  통합 환경도 훌륭한 선택지이지만, 이 책의 명령·선택지 표기(gcc/clang
  계열)와 달라서 여기서 다루지는 않는다. 관심 있는 독자는 Microsoft의
  공식 문서(`learn.microsoft.com/cpp`)가 설치부터 첫 프로그램까지
  자세히 안내하므로 그쪽을 따르면 된다.
]

#qa[
  컴파일러가 두 계열이나 되는 이유가 있는가? 하나만 있으면 헷갈릴 일도
  없을 텐데.
][
  경쟁이 곧 품질이기 때문이다 — 그리고 프로그래머에게는 공짜 검증
  도구이기 때문이다. 같은 코드를 gcc와 clang 양쪽으로 컴파일해 보면,
  한쪽이 놓친 경고를 다른 쪽이 잡아 주는 일이 흔하다. 두 독립적인
  구현이 모두 통과시키는 코드는 표준(계약서)에 맞게 쓰였을 가능성이
  훨씬 높다 — 10장의 표현을 빌리면, 방언이 아니라 표준어로 말하고
  있다는 교차 증거다. 실제로 이 책의 수록 예제들도 두 컴파일러로 교차
  검증한다.
]

== 경고 — 공짜 검토를 켜 두어라

설치 직후에 들일 습관이 하나 있다. 컴파일러에게 *경고*를 넉넉히
켜 달라고 부탁하는 것이다:

```text
$ cc -Wall -Wextra hello.c -o hello
```

`-Wall -Wextra`는 "수상한 구석을 최대한 알려 달라"는 뜻이다(이름과
달리 `-Wall`이 전부는 아니라서 관행상 둘을 함께 쓴다). 경고는 오류가
아니라 *컴파일러의 무료 코드 검토*다 — 계약(표준) 위반까지는 아니어도
사고 나기 좋은 무늬를 짚어 준다. 이 책의 모든 예제는 이 두 선택지를
켠 채, 경고 0개로 검증된다.

== 새니타이저 — 실행 중에 치는 그물

현대 C 개발환경의 필수 장비가 하나 더 있다. 컴파일 때가 아니라 *실행
중에* 사고를 잡는 도구, *새니타이저*(sanitizer)다.

원리는 이렇다 — 컴파일할 때 특별한 선택지를 주면, 컴파일러가 프로그램
곳곳에 *감시 코드*를 심어 준다. 그 프로그램을 실행하면 감시 코드가
메모리 접근과 연산을 지켜보다가, 잘못이 *일어나는 순간* 현장을 짚으며
프로그램을 세운다. 대표 선수는 셋이다.

- *ASan*(AddressSanitizer) — 메모리 사고 담당. 경계를 넘는 접근, 해제
  한 기억을 다시 만지기 같은, 제7부에서 만날 위험들을 현장에서 잡는다.
- *UBSan*(UndefinedBehaviorSanitizer) — 계약 위반 담당. 부호 있는
  오버플로(5장), 폭 이상 시프트(5장) 같은 *정의되지 않은 동작*이
  실행 중에 실제로 일어나면 그 자리에서 알린다. 41장의 주역이다.
- *TSan*(ThreadSanitizer) — 경쟁 사고 담당. 여러 코어(10장)가 같은
  데이터를 다투는 문제를 잡는다. 이 책의 범위 밖이지만 이름은 알아
  둔다.

쓰는 법은 컴파일 선택지 하나다:

```text
$ cc -fsanitize=address,undefined -g hello.c -o hello
$ ./hello
```

(`-g`는 "사고 지점을 소스 몇 번째 줄인지로 알려 달라"는 부가 정보
선택지다.) 이렇게 빌드한 프로그램은 조금 느려지는 대신, 사고를 조용히
지나치지 않는다 — 5장에서 본 "오버플로는 조용하다"에 대한 현대의
응답이 바로 이 도구들이다. 이 책의 뒷부분(제7부, 제9부)에서 위험한
코드를 다룰 때, 새니타이저가 사고를 잡아내는 장면을 지면에서 여러 번
보게 된다.

#platform[
  Windows에서 새니타이저 쓰기
][
  플랫폼마다 지원 폭이 달라서, Windows의 사정을 정직하게 적어 둔다.

  - *ASan·UBSan*: 갈래 1의 *clang으로 쓸 수 있다* — 위의
    `-fsanitize=address,undefined` 선택지가 그대로 통한다. 갈래 2의
    MinGW-w64 gcc는 Windows에서 새니타이저를 지원하지 않는다 —
    새니타이저를 쓰려면 clang을 고르면 된다(두 갈래를 다 설치해 두고
    평소엔 아무 쪽, 검사할 땐 clang을 쓰는 것도 좋은 배치다).
  - *TSan*: Windows 자체를 지원하지 않는다(리눅스·macOS 전용). Windows
    에서 굳이 쓰려면 WSL(Windows 안의 리눅스 환경, Microsoft 공식
    기능)을 통하는 길이 있다 — 다만 이 책의 범위에서는 필요하지 않다.

  참고로 MSVC도 자체 ASan을 지원한다(`/fsanitize=address`) — 상세는
  앞서 언급한 Microsoft 공식 문서에 있다.
]

#qa[
  경고도 켜고 새니타이저도 있는데, 그래도 버그가 남는 이유는 무엇인가?
][
  각 도구가 보는 범위가 다르기 때문이다. 경고는 *컴파일 시점*에 코드의
  생김새를 보고, 새니타이저는 *실행 시점*에 실제로 일어난 일을 본다 —
  즉 실행해 보지 않은 경로의 사고는 새니타이저도 모른다. 그래서 현대적
  C 개발은 그물을 겹친다: 경고(항상) + 새니타이저(시험 실행) + 두
  컴파일러 교차(습관) + 그리고 애초에 사고 나기 어려운 부품을 쓰는 것.
  마지막 항목이 이 책 후반의 proven이 서는 자리다(34장) — 도구는
  그물이고, 좋은 부품은 애초에 떨어지지 않는 발판이다.
]

== 제3부를 닫으며

첫 프로그램을 읽었고(13장), 그것이 실행 파일이 되는 릴레이를
구경했으며(14장), 그 릴레이를 내 책상에서 돌릴 도구와 그물까지
갖췄다(15장). 이제 준비는 끝났다.

다음 부부터 본격적인 언어 공부다 — 단, 이 책의 방식대로 간다. 새
문법을 나열하는 것이 아니라, 13장의 헬로 월드 *한 편을 완전히 읽어
내는 것*이 제4부의 목표다. 프로그램의 뼈대(문장과 블록)부터, 수식,
함수를 부르는 법, 그리고 출력까지 — 이미 본 여섯 줄이 전부 "아는
문장"이 될 때까지.
