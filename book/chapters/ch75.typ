#import "../lib.typ": *

= 문자열과 텍스트

#organizer[
  70장의 첫째 버그 — 그릇의 크기를 모르는 문자열 함수 — 에 대한 답이다.
  길이를 지니고 다니는 문자열, 소유와 빌림의 구분, 그리고 이 라이브러리
  에서 가장 논쟁적인 결정인 *거부하되 자르지 않는다*까지. 9장의 인코딩
  이야기와 38장의 경계 이야기가 여기서 하나로 모인다.
]

#deepqa[
  38장에서 C 문자열은 "NUL을 만날 때까지"가 곧 길이라, 길이를 알려면
  매번 세어야 한다고 했다. 그러면 길이를 함께 들고 다니면 무엇이
  좋아지는가 — 그리고 무엇을 잃는가?
][
  얻는 것이 셋이다. 길이를 세는 비용이 사라지고($O(n)$이 $O(1)$이 된다),
  중간에 0바이트가 들어 있는 자료도 다룰 수 있으며, 무엇보다 *경계를
  검사할 수 있다* — 길이를 모르면 검사할 것 자체가 없다. 잃는 것은
  둘이다. 문자열 하나가 한 워드가 아니라 두 워드가 되고, `printf("%s")`
  처럼 NUL 종단을 기대하는 세상과 만날 때 변환이 필요해진다. 이
  라이브러리는 그 변환 비용을 없애려고 내부적으로 NUL도 함께 유지한다 —
  둘 다 가지는 절충이다.
]

== 두 개의 타입, 하나의 규칙

문자열 어휘는 둘뿐이다.

- `proven_u8str_t` — *소유하는* 문자열. 버퍼와 길이와 용량을 가진다.
- `proven_u8str_view_t` — *빌린* 문자열. 포인터와 길이뿐이다.

이름에 `view`가 있으면 빌린 것이고, 빌린 것은 파괴하지 않는다. 74장에서
세운 규칙이 문자열에 그대로 적용된 것이다. `u8`은 UTF-8을 뜻한다 —
9장에서 본 그 인코딩이 이 라이브러리의 기본 텍스트 표현이다.

소유하는 문자열을 얻는 길은 둘이다. 할당자에게 받거나(`_create`), 남의
버퍼를 빌려 쓰거나(`_borrow`).

#demo("examples/ch75/ops.c")

첫 줄이 74장의 규칙을 다시 보여 준다. `proven_u8str_borrow`에는 할당자가
없다 — 그러므로 할당하지 않는다. 스택에 잡은 16바이트 배열 위에서 문자열
연산을 하는 것이고, 임베디드에서 힙 없이 문자열을 다루는 방식이 정확히
이것이다.

== 거부하되, 자르지 않는다

두 번째 `append`가 이 부에서 가장 중요한 한 줄이다. 16바이트 그릇에
`"hello"` 다음으로 `" world, and more"`를 붙이려 하자 라이브러리는
*아무것도 쓰지 않고* `PROVEN_ERR_OUT_OF_BOUNDS`를 돌려주었다. 그리고
그다음 줄이 보여 주듯 원래 내용 `hello`는 그대로다 — 72장에서 배운 실패
원자성이다.

70장에서 본 `snprintf`의 선택과 정반대다. 왜 자르지 않는가?

*잘린 값은 짧은 값이 아니라 다른 값이기 때문이다.* 잘린 경로는 다른
파일을 가리키고, 잘린 명령은 다른 명령이며, 잘린 사용자 이름은 다른
사람이다. 자르고 성공을 말하면 호출자는 자기가 받은 것이 요청한 것인지
알 수 없다. 그래서 이 라이브러리는 *결정을 호출자에게 되돌려준다* —
"자리가 모자랍니다. 어떻게 할까요?"

#qa[
  그래도 로그 한 줄처럼 잘려도 괜찮은 자리가 있지 않은가?
][
  있다. 그래서 *명시적으로 부분만 쓰는* 함수가 따로 있다 —
  `proven_u8str_append_partial`은 들어간 바이트 수를 돌려준다. 이름이
  길고 반환값이 다르다는 점이 핵심이다. 자르기는 여전히 가능하지만, 그것을
  하려면 *그렇게 적어야* 한다. 기본값은 언제나 안전한 쪽이고 위험한 선택은
  눈에 보이는 이름으로만 할 수 있다는 원칙(73장의 `_unchecked`와 같다)이
  여기서도 지켜진다.
]

#antipattern[
  성장과 고정 용량을 헷갈리기
][
  ```c
  proven_u8str_t s = proven_u8str_borrow(buf, sizeof buf);
  proven_err_t e = proven_u8str_append_grow(alloc, &s, view);  /* 위험 */
  ```
  `_append`는 용량 안에서만 쓰고, `_append_grow`는 모자라면 할당자에게
  더 달라고 한다 — 그래서 후자에만 할당자 인자가 있다. 문제는 빌린
  버퍼(`_borrow`) 위에서 성장을 요구하는 경우다. 스택 배열은 자랄 수
  없으므로 이것은 설계 오류다. 시그니처를 읽는 습관이 여기서 그대로
  방어가 된다 — *할당자가 있으면 자랄 수 있고, 없으면 자랄 수 없다.*
]

== 찾기와 자르기 — 복사 없는 텍스트 처리

예제의 ③·④가 뷰의 진짜 쓸모다. 부분 문자열을 얻는 데 복사가 필요 없다 —
원본을 가리키는 포인터와 길이를 새로 계산할 뿐이다. CSV 한 줄을 세 필드로
가르는 동안 할당은 한 번도 일어나지 않았다.

이 무늬는 파서에서 특히 강력하다. 26장에서 `sscanf`로 줄을 해석할 때는
결과를 담을 버퍼를 미리 준비해야 했지만, 뷰로 자르면 *원본 위에 표시만*
남긴다. 대신 지켜야 할 것이 하나 늘어난다 — 73장에서 본 대로 *원본이
살아 있는 동안만 유효하다*.

`find`가 못 찾았을 때 돌려주는 값에도 규약이 있다. 0이나 음수가 아니라
`PROVEN_INDEX_NOT_FOUND`라는 이름의 센티널이다. 70장에서 센티널 값의
위험을 이야기했는데, 여기서는 *이름이 붙어 있고 문서화되어 있다*는 점이
다르다 — 이름 없는 마법의 수와 이름 붙은 계약은 다른 물건이다.

#misconception[
  "길이를 들고 다니면 글자 수를 아는 것이다"
][
  아니다. 길이는 *바이트 수*다. 9장에서 배운 대로 UTF-8에서 한 글자는
  1\~4바이트이므로, 한글 "가"는 3바이트이고 이모지는 4바이트다. 예제의
  출력이 굳이 "(5 bytes)"라고 밝히는 이유다. 그래서 "n번째 글자"를 다루는
  일은 여전히 조심스럽고, *바이트 경계에서 아무 데나 자르면* 9장에서 본
  깨진 글자가 나온다. 문자열 라이브러리가 해결해 주는 것은 경계 침범이지
  인코딩의 본질적 난이도가 아니다.
]

== 두 세계의 경계 — NUL 종단과 UTF-16

바깥 세계와 만나는 자리마다 변환이 필요하다.

- `proven_u8str_as_cstr` — 소유 문자열에서 NUL 종단 포인터를 얻는다.
  내부에 NUL을 유지해 두므로 복사도 할당도 없다. `printf("%s")`나 파일
  API에 넘길 때 쓴다.
- `proven_u8str_view_to_cstr` — 뷰에서 NUL 종단 문자열을 만든다. 이쪽은
  *할당자를 받는다* — 뷰는 원본 중간을 가리킬 수 있어 그 자리에 NUL을 쓸
  수 없기 때문이다. 시그니처가 다시 한번 사실을 말해 준다.
- `proven_u16str_t` / `proven_u16str_view_t` — UTF-16 세계와의 다리다.
  Windows API가 이 인코딩을 쓰므로 변환이 필요하고, 변환은 실패할 수
  있다(짝이 맞지 않는 서러게이트 같은 것). 그래서 결과는 꾸러미로 온다.

#realcase[
  "추측하지 말고 거부하라" — 인코딩 처리의 원칙
][
  9장에서 본 텍스트 보안 문제들의 뿌리는 대개 *너그러운 디코더*다.
  잘못된 UTF-8을 만났을 때 "비슷한 것으로 고쳐 읽는" 구현은 과거 여러 번
  보안 사고의 통로가 됐다 — 특히 지나치게 긴 인코딩(overlong)을 허용한
  디코더들이 검사를 우회당했다. 그래서 오늘의 규범은 한 문장이다.
  *유효하지 않은 입력은 고쳐 읽지 말고 거부하라.* proven의 인코딩 변환이
  `PROVEN_ERR_INVALID_ENCODING`을 돌려주며 멈추는 것이 그 규범의 구현이고,
  이 원칙은 이 장 전체의 "자르지 않는다"와 정확히 같은 정신이다.
]

#recap[
  문자열 어휘 요약.

  #dtable(
  columns: 3,
    [*함수*], [*하는 일*], [*실패·소유*],
    [`u8str_create(alloc, cap)`], [소유 문자열 생성], [꾸러미 반환, `_destroy` 필요],
    [`u8str_borrow(buf, cap)`], [남의 버퍼 위에 문자열], [할당 없음, 파괴 없음],
    [`u8str_append(&s, v)`], [용량 안에서 덧붙임], [모자라면 거부(원본 보존)],
    [`u8str_append_partial`], [들어가는 만큼], [넣은 바이트 수 반환],
    [`u8str_append_grow(alloc,…)`], [모자라면 늘려서], [할당 실패 가능],
    [`u8str_view_find`], [부분 문자열 위치], [없으면 `PROVEN_INDEX_NOT_FOUND`],
    [`u8str_view_slice`], [부분 뷰], [복사 없음 — 원본 수명에 묶임],
    [`u8str_as_cstr`], [NUL 종단 포인터], [복사 없음],
    [`u8str_view_to_cstr`], [뷰 → C 문자열], [할당자 필요],
)
]

문자열을 안전하게 담고 자를 수 있게 됐다. 그런데 아직 *만드는* 일이
남았다 — 수와 값을 글자로 바꾸고, 글자를 다시 수로 되돌리는 일. 70장의
셋째 버그가 기다리는 자리이자, 이 라이브러리에서 가장 눈에 띄게 다른
문법이 나오는 자리다.
