#import "../lib.typ": *

= 수명과 저장 기간

#prereq(
  ([24장 함수 선언과 정의], [함수가 도는 동안]),
  ([2장 기억의 구역들], [스택과 정적 구역]),
)

#deepqa[
  24장에서 스코프 — 이름이 *보이는* 범위 — 를 배웠다. 그런데 41장에서
  "함수 호출의 복귀 장부"라는 말이 나왔다. 이름이 안 보이게 되는 것과,
  그 기억이 *사라지는* 것은 같은 일인가?
][
  다른 일이고, 이 구별이 이 장의 뼈대다. *스코프*는 컴파일 시점의
  개념(어디서 그 이름을 쓸 수 있는가)이고, *수명*은 실행 시점의
  개념(그 기억이 언제부터 언제까지 유효한가)이다. 대개는 함께 가지만,
  어긋나는 순간이 사고가 된다 — 이름은 사라졌는데 주소가 남아 있는
  경우다(아래 오개념 블록).
]

#organizer[
  기억의 지도를 그린다 — 변수는 어디에 살고, 언제 태어나 언제 죽는가.
#idx("스택")  자동 수명과 정적 수명, 스택이라는 장부, 그리고 이 부에서 가장 조심
  해야 할 사고(사라진 것의 주소를 간직하기)까지.
]

#chapter-questions()

== 표준의 네 축 — 저장 기간, 유효범위, 연결, 저장 클래스

여기서 용어를 정식으로 정리하고 간다. C 표준은 이름과 객체에 대해 *서로
독립적인 네 가지 성질*을 정의하는데, 이것을 뭉뚱그리면 뒤에서 반드시
헷갈린다(특히 `static`의 두 얼굴이 그렇다).

#idx("저장 기간")#idx("유효범위")#idx("연결")
#dtable(
  columns: 3,
  [*표준 용어*], [*무엇을 정하는가*], [*언제의 개념인가*],
  [*저장 기간*(storage duration)], [객체가 *언제부터 언제까지* 존재하는가], [실행 시점],
  [*유효범위*(scope)], [그 이름을 *어디서* 쓸 수 있는가], [번역 시점],
  [*연결*(linkage)], [다른 곳의 같은 이름과 *같은 것인가*], [번역·링크 시점],
  [*저장 클래스 지정자*(storage-class specifier)], [위 셋을 *어떻게 적는가*], [문법],
)

마지막 줄이 중요하다. `static`·`extern`·`auto`·`register`·`typedef`,
그리고 C11의 `_Thread_local`(C23의 `thread_local`)은 문법적으로 *한 자리*를
차지하는 낱말들이고, 그중 무엇을 적느냐에 따라 앞의 세 성질이 정해진다.
한 선언에 이 자리는 하나뿐이라 `static extern int x;`는 문법 오류다.

=== 저장 기간 — 넷

#dtable(
  columns: 3,
  [*저장 기간*], [*언제 태어나 언제 죽는가*], [*어떻게 만드나*],
  [자동(automatic)], [블록에 들어설 때 ~ 나갈 때], [블록 안의 보통 선언],
  [정적(static)], [프로그램 시작 전 ~ 끝날 때], [파일 유효범위 선언, 또는 `static`],
  [스레드(thread)], [그 갈래가 시작 ~ 끝날 때], [`thread_local`(C11)],
  [할당(allocated)], [`malloc` ~ `free`], [44장],
)

표준의 낱말은 *동적*이 아니라 *할당 저장 기간*이다. 흔히 "동적 할당"이라
부르는 것이 이것이고, 이 책도 관행을 따라 그렇게 부르되 표준 용어를 여기
적어 둔다.

=== 유효범위 — 넷

이름이 보이는 범위다. C 표준은 넷으로 나눈다.

#dtable(
  columns: 3,
  [*유효범위*], [*어디까지 보이는가*], [*예*],
  [블록(block)], [선언한 자리부터 그 블록의 끝까지], [함수 안의 지역 변수],
  [파일(file)], [선언한 자리부터 그 번역 단위의 끝까지], [함수 밖의 선언],
  [함수(function)], [그 함수 전체], [*레이블 이름만*(`goto`의 대상)],
  [함수 원형(function prototype)], [원형의 괄호 안], [원형에 적은 매개변수 이름],
)

세 번째가 낯설 텐데, 레이블은 함수 안 어디에 적혀 있든 그 함수 전체에서
보인다는 특례다. 네 번째는 `void f(int count);`처럼 원형에만 적은 이름이
어디까지 사는지를 정한 것 — 괄호를 벗어나면 사라지므로, 원형의 매개변수
이름은 사실상 *주석*이다.

*안쪽이 바깥을 가린다.* 같은 이름이 겹치면 안쪽 블록의 것이 이긴다.

```c
int n = 1;                  /* 파일 유효범위 */
void f(void) {
    int n = 2;              /* 블록 유효범위 — 바깥의 n 을 가린다 */
    { int n = 3; use(n); }  /* 여기서는 3 */
    use(n);                 /* 여기서는 2 */
}
```

=== 연결 — 셋

같은 이름이 여러 곳에 나올 때 *하나의 같은 객체를 가리키는가*를 정한다.
52장(여러 파일)의 밑바탕이다.

#dtable(
  columns: 3,
  [*연결*], [*뜻*], [*어떻게 되나*],
  [외부(external)], [번역 단위 *전체를 가로질러* 같은 것], [파일 유효범위 기본값, `extern`],
  [내부(internal)], [*이 번역 단위 안에서만* 같은 것], [파일 유효범위의 `static`],
  [없음(none)], [선언마다 별개], [블록 안의 보통 변수, 매개변수, `typedef` 이름],
)

#antipattern[
  `static`의 두 얼굴을 같은 것으로 읽기
][
  같은 낱말이 *어디에 적히느냐*에 따라 전혀 다른 일을 한다. 표준 용어로
  보면 헷갈릴 여지가 없다.

  ```c
  static int counter;        /* 파일 유효범위: 연결을 *내부*로 (수명은 원래 정적) */

  void f(void) {
      static int calls;      /* 블록 유효범위: 저장 기간을 *정적*으로 (연결은 없음) */
  }
  ```

  첫 번째 `static`은 *수명을 바꾸지 않는다* — 파일 유효범위 변수는 어차피
  정적 저장 기간이다. 바꾸는 것은 *연결*이고, 뜻은 "이 파일 밖에서는 이
  이름을 못 쓴다"다. 두 번째 `static`은 *연결과 무관하다* — 지역 이름은
  원래 연결이 없다. 바꾸는 것은 *저장 기간*이다.

  한 문장으로 외우면 된다: *파일 유효범위의 `static`은 숨기는 것, 블록
  유효범위의 `static`은 살려 두는 것.*
]

=== 나머지 저장 클래스 지정자

- *`extern`* — "이 이름은 어딘가 다른 곳에 정의되어 있다"는 선언이다.
  정의가 아니라 *예고*이므로 기억을 잡지 않는다(16장의 링커가 실물을 잇는다).
- *`auto`* — 자동 저장 기간을 명시하는 옛 낱말. 어차피 기본값이라 아무도
  적지 않았고, *C23은 이 자리를 타입 추론에 재활용했다* — `auto x = 1 + 2;`
  처럼 쓰면 초기식에서 타입을 가져온다.
- *`register`* — "되도록 레지스터에"라는 옛 부탁(10장). 오늘날 최적화에는
  영향이 없지만 규칙 하나는 남아 있다: *`register` 변수의 주소는 얻을 수
  없다*(`&x`가 오류가 된다).
- *`typedef`* — 문법적으로 이 자리에 오지만 하는 일은 전혀 다르다. 변수
  대신 *타입 이름*을 만든다(58장에서 다뤘다).
- *`thread_local`* — 갈래마다 따로인 객체를 만든다(76장). 정적 저장 기간과
  섞어 적을 수 있는 유일한 예외다(`static thread_local`).

#qa[
  "선언"과 "정의"는 또 무엇이 다른가?
][
  *정의*는 실물을 만드는 선언이다. 변수라면 기억을 잡고, 함수라면 몸통을
  적는다. 그냥 *선언*은 "이런 이름이 이런 타입으로 어딘가 있다"는 예고일
  뿐이다.

  ```c
  extern int total;      /* 선언 — 기억을 잡지 않는다 */
  int total = 0;         /* 정의 — 여기서 실물이 생긴다 */
  ```

  규칙은 하나다: *정의는 프로그램 전체에서 한 번, 선언은 필요한 만큼.*
  이것을 어기면 16장에서 본 링커의 오류(`undefined reference`는 정의가
  없을 때, `multiple definition`은 둘 이상일 때)가 난다. 헤더에는 선언을
  두고 소스 파일 하나에 정의를 두는 관행이 여기서 나왔다(52장).
]

== 두 가지 수명

#idx("저장 기간")C의 지역 변수는 기본적으로 *자동 저장 기간*(automatic)을 갖는다 —
블록에 들어설 때 태어나고 나갈 때 죽는다. 함수 호출마다 매개변수와
지역 변수가 새로 태어나는 것이 33장 재귀가 성립한 근거였다.

여기에 `static`을 붙이면 *정적 저장 기간*(static)이 된다 — 프로그램 시작 전에
한 번 태어나 끝날 때까지 산다(초기화도 딱 한 번). 시연으로 둘을
대조한다.

#demo("examples/ch42/life.c")

`next_ticket`의 `issued`는 호출 사이에도 값을 간직해 1, 2, 3으로
자라고, `fresh_count`의 `n`은 호출마다 새로 태어나 언제나 1이다. 같은
"함수 안의 변수"인데 수명이 다른 것이다. (덧붙여, 시연이 호출을 문장
으로 나눈 것에 주목하면 된다 — 부수효과 있는 호출을 한 수식에 몰면
평가 순서가 미지정이라는 33장의 수칙 그대로다.)

== 기억의 지도 — 구역들을 눈으로

이쯤에서 프로그램의 기억이 실제로 어떻게 놓이는지 한 장의 그림으로 보아 둔다.
아래는 이 책의 검증 기계에서 직접 찍은 주소들이다.

#demo("examples/ch42/regions.c")

읽는 법은 *값이 아니라 순서*다. 낮은 주소부터 이렇게 늘어선다.

#dtable(
  columns: 3,
  [*구역*], [*무엇이 사는가*], [*수명*],
  [코드], [함수의 기계 명령], [프로그램 내내(읽기 전용)],
  [읽기 전용 자료], [문자열 리터럴, `const` 자료], [프로그램 내내(쓰면 붕괴)],
  [`data`], [초깃값이 있는 전역·`static`], [프로그램 내내],
  [`bss`], [초깃값이 없는(=0인) 전역·`static`], [프로그램 내내],
  [힙], [`malloc`이 준 것(43장)], [해제할 때까지],
  [스택], [지역 변수·매개변수·복귀 주소], [함수가 끝날 때까지],
)

예제가 확인해 준 것이 셋이다. *`data`와 `bss`가 나란히 있고*, *힙은 그
위쪽에서 커지며*, *스택은 아주 멀리 떨어진 높은 주소에서 아래로 자란다*
(예제의 마지막 세 줄이 프레임을 하나 더 쌓아 그 방향을 실측한다).

#qa[
  `bss`라는 이상한 이름은 무엇인가? 그리고 `data`와 왜 나누는가?
][
  이름은 1950년대 어셈블러의 명령어 `Block Started by Symbol`의 약자다 —
  뜻은 잊혔고 이름만 반세기를 건너왔다.

  나누는 이유는 실용적이다. `int table[1000000];`처럼 *초깃값이 0인* 큰
  전역을 생각해 보자. `data`에 넣으면 실행 파일 안에 0이 백만 개 들어가
  파일이 4MB 커진다. `bss`에 넣으면 파일에는 *"이만큼을 0으로 채워 달라"는
  숫자 하나*만 적히고, 실제 채우기는 프로그램이 시작될 때 일어난다. 그래서
  `bss`의 변수는 "명시적으로 초기화하지 않아도 0"이라는 C의 약속을 공짜로
  얻는다 — 그 0은 운영체제(또는 임베디드의 시작 코드)가 채워 준 것이다.
]

#platform[
  스택은 얼마나 큰가 — 리눅스와 윈도우
][
  *C 표준에는 "스택"이라는 말도, 그 크기에 대한 약속도 없다.* 자동 저장
  기간만 정해 두고, 그것을 어디에 어떻게 놓을지는 구현에 맡긴다. 그래서
  크기는 *운영체제와 도구가* 정한다.

  - *리눅스* — 주 스레드의 기본 한도는 대개 *8 MiB*다(`ulimit -s`로 확인·
    변경한다. 이 책의 검증 기계도 8388608바이트였다). 필요하면 늘릴 수
    있고, 스레드마다 만드는 스택은 `pthread_attr_setstacksize`로 따로
    정한다.
  - *윈도우* — 기본은 *1 MiB*다. 게다가 그중 실제로 확보(commit)되는 것은
    처음 4 KiB뿐이고, 쓰는 만큼 늘어난다. 실행 파일을 만들 때 링커 옵션
    `/STACK:예약[,커밋]`으로 바꾸고, 스레드는 `CreateThread`의 인자로
    지정한다.

  차이가 실무에서 드러나는 자리가 있다. 리눅스에서 잘 돌던 코드가 윈도우
  에서 스택 넘침으로 죽는 것 — *같은 코드, 여덟 배 좁은 그릇*이기 때문이다.
  큰 지역 배열(`char buf[2*1024*1024];`)이나 깊은 재귀가 그 후보다.
  더 자세한 지도와 임베디드의 사정은 80장에서 다룬다.
]

== 스택 — 호출의 장부

자동 변수들이 사는 곳에는 이름이 있다 — *스택*(stack)이다. 함수를
부를 때마다 그 호출을 위한 칸 묶음(스택 프레임)이 얹히고, 함수가
끝나면 통째로 걷힌다. 프레임에는 지역 변수·매개변수와 함께 *돌아갈
주소*(복귀 주소)가 들어 있다 — 41장에서 이름만 스쳤던 "복귀 장부"의
정체다.

그림이 갖춰지면 두 가지가 한 번에 설명된다. 첫째, 33장의 재귀가 왜
겹겹이 쌓이는지 — 호출마다 프레임이 하나씩 얹히기 때문이다. 그리고
너무 깊이 재귀하면 스택 공간이 바닥나 프로그램이 붕괴한다(*스택
오버플로* — 무한 재귀의 대표 증상이다). 둘째, 41장의 경계 침범 공격이
왜 그토록 위험한지 — 스택에 놓인 배열을 넘쳐 쓰면 *같은 프레임의 복귀
주소*를 덮어쓸 수 있고, 그러면 함수가 끝날 때 공격자가 정한 곳으로
"돌아간다". 배열 하나의 경계가 프로그램의 통제권과 이어져 있는 것이다.

#misconception[
  "함수가 끝나도 그 안의 변수를 가리키는 주소는 쓸 수 있다"
][
  포인터를 배운 직후 가장 흔한 사고이고, 무서운 점은 *한동안 잘 도는
  것처럼 보인다*는 것이다. 함수가 끝나면 프레임이 걷히지만 — 그 자리의
  비트가 즉시 지워지는 것은 아니라서, 죽은 변수의 주소를 따라가도
  잠시는 예전 값이 읽힌다. 그러다 다른 함수가 그 자리를 프레임으로
  쓰는 순간 값이 뒤바뀐다 — 시간이 지나서, 엉뚱한 곳에서 터지는
  버그가 되는 것이다. 이런 주소를 *댕글링 포인터*(dangling pointer)라
  하고, 규칙은 하나다: *지역 변수의 주소는 그 함수보다 오래 살아서는
  안 된다.* 함수가 만든 결과를 오래 살려 보내려면 정적 수명을 쓰거나,
  다음 장의 동적 메모리를 쓰거나, 호출자가 준 그릇에 채워 주는(35장의
  `&` 관용구) 셋 중 하나다. 17장의 ASan이 실행 중에 이 사고를 잡아
  주는 대표 도구이기도 하다.
]

#qa[
  전역 변수 — 함수 밖에 선언한 변수 — 도 정적 수명인가?
][
  그렇다. 함수 밖의 변수는 정적 저장 기간을 갖고 프로그램 내내 산다.
  다만 수명과 별개로 *가시성*의 문제가 붙는데 — 여러 파일에서 보이게
  할지 이 파일에만 둘지를 정하는 것이 52장의 주제(연결)다. 그리고
  실무의 권고는 오래된 것이다: *전역 가변 상태는 최소로.* 어디서든
  바뀔 수 있는 값은 추적이 어렵고, 12장의 멀티코어 세계에서는 사고의
  근원이 된다. 함수 안의 `static`도 같은 성질을 작게 가진 것이라(시연의
  `issued`), 편리하지만 남용하지 않는 것이 관행이다.
]

기억의 지도에서 두 자리 — 스택(자동)과 정적 영역 — 를 익혔다. 남은
한 자리가 이 부의 마지막이다: 크기를 실행 중에 정하고, 원하는 만큼
살려 두는 기억. 다음 장의 동적 메모리다.
