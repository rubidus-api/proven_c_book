#import "../lib.typ": *

= 컴파일러의 지형 — 현역 C 컴파일러들

#organizer[
#idx("컴파일러")  15장에서 깐 gcc·clang 말고 세상에 어떤 C 컴파일러가 있는지, 그리고
  각각이 어디에 쓰이는지 지도를 그린다. 데스크톱과 서버의 큰 셋, 윈도우의
  작고 단정한 대안(Pelles C), 성능을 파는 벤더 컴파일러들, 그리고 다음
  장에서 자세히 볼 임베디드 세계의 입구까지. *지금 현역인 것만* 싣는다 —
  은퇴한 이름과 은퇴 중인 이름도 함께 밝힌다.
]

#deepqa[
  14장에서 컴파일이 전처리·번역·어셈블·링크의 네 걸음이고, 15장에서는
  그 걸음을 대신 밟아 주는 도구로 gcc나 clang을 깔았다. 그러면 컴파일러가
  다르면 무엇이 달라지는가 — 같은 표준 C인데?
][
  세 가지가 달라진다. 첫째, *만들어 내는 기계어의 품질*(속도와 크기)이다.
  둘째, *어떤 기계를 겨냥할 수 있는가* — 세상의 CPU 중 gcc나 clang이 아예
  모르는 것들이 있고, 그 자리는 그 칩을 만든 회사의 컴파일러가 채운다.
  셋째, *표준의 어느 판까지 지원하는가*와 *진단(경고)의 성격*이다. 표준이
  같아도 도구는 여럿이고, 각자 잘하는 자리가 다르다 — 그 지도를 그리는
  것이 이 장이다.
]

== 세상의 큰 셋 — GCC, Clang/LLVM, MSVC

오늘날 데스크톱과 서버 C의 대부분은 이 셋 위에서 돈다.

*GCC*(GNU Compiler Collection). 1987년부터 이어져 온 자유 소프트웨어
컴파일러이고, 리눅스 세계의 기본값이다. 겨냥할 수 있는 CPU의 수가 압도적
으로 많다는 것이 가장 큰 강점이다 — x86·Arm·RISC-V는 물론이고 AVR,
MSP430, SuperH, PowerPC 같은 임베디드 칩까지 하나의 계보로 덮는다. 임베디드
벤더들이 자기 도구의 속을 gcc로 채우는 일이 흔한 이유다(17장).

*Clang/LLVM*. 2000년대에 나온 후발 주자로, 컴파일러를 *부품으로 쓸 수 있게*
설계한 LLVM 기반이다. 진단 메시지가 친절하다는 평판으로 자리를 잡았고,
오늘날에는 그 부품성 덕에 생태계 전체가 그 위에 섰다 — 정적 분석기
(`clang-tidy`), 서식 도구(`clang-format`), 편집기의 자동완성 서버(`clangd`),
그리고 여러 벤더의 새 컴파일러가 전부 LLVM에서 갈라져 나왔다. macOS의
기본 컴파일러이기도 하다.

*MSVC*(Microsoft Visual C++). 윈도우의 표준 도구다. 이름은 C++이지만 C도
컴파일하며, 오랫동안 C99조차 미완이라는 평을 들었으나 근래에는 C11·C17을
지원하고 C23으로 나아가는 중이다. 윈도우 API와 가장 잘 맞물린다는 것이
존재 이유다.

#qa[
  하나만 골라 배워도 되는가?
][
  배우는 것은 하나로 시작하되, *가끔 다른 것으로도 컴파일해 보는* 습관을
  권한다. 컴파일러마다 잡아 주는 실수가 다르기 때문이다 — 한쪽이 지나친
  코드를 다른 쪽이 경고로 잡는 일이 흔하다. 이 책의 예제도 그래서 두
  컴파일러로 교차 검증한다(머리말). 무료이고 설치가 쉬우니, 리눅스·macOS
  라면 gcc와 clang을, 윈도우라면 MSVC와 clang을 함께 두는 것이 좋다.
]

== 윈도우의 단정한 대안 — Pelles C

윈도우에서 무거운 개발 환경 없이 C만 다루고 싶을 때 오래 사랑받아 온
도구가 *Pelles C*다. 스웨덴의 펠레 오를린드(Pelle Orinius)가 혼자 이어 온
프리웨어로, 편집기·컴파일러·링커·자원 편집기·디버거·설치 파일 생성기까지
한 묶음으로 들어 있다. 다운로드 하나로 끝나고 용량도 작다.

주목할 점은 표준 지원이 성실하다는 것이다 — 이 책이 쓰이는 시점의 최신판
(14.50, 2026년 7월)은 C99부터 *C23*까지 지원한다고 밝히고 있다. 규모가
큰 도구들보다 새 표준을 빨리 따라간 시기도 있었다. 대상은 윈도우로 한정
된다(32비트·64비트, ARM64 윈도우 호스트 포함).

#dtable(
  columns: 2,
  [*맞는 자리*], [*맞지 않는 자리*],
  [윈도우에서 C만 가볍게 시작할 때], [윈도우 밖으로 이식할 코드],
  [작은 유틸리티·학습용], [거대한 빌드 시스템과의 통합],
  [최신 표준 문법을 시험해 볼 때], [리눅스·macOS 대상],
)

== 성능을 파는 컴파일러들 — 벤더 컴파일러

같은 소스라도 자기 회사 CPU에서 더 빨리 돌게 만들어 주는 것을 파는 컴파일러
들이 있다. 대형 계산(HPC)과 서버 쪽에서 주로 만난다.

#dtable(
  columns: 3,
  [*이름*], [*누가*], [*지금 상태*],
  [`icx` (oneAPI DPC++/C++)], [인텔], [현역. LLVM 기반],
  [`icc` (C++ Compiler Classic)], [인텔], [*은퇴* — oneAPI 2024.0에서 제거],
  [AOCC], [AMD], [현역. LLVM 기반],
  [NVIDIA HPC SDK (`nvc`)], [엔비디아], [현역. GPU 가속 코드와 함께],
  [IBM Open XL C/C++], [IBM], [현역(AIX·z/OS). LLVM 기반으로 갈아탐],
  [Cray/HPE CCE], [HPE], [현역(슈퍼컴퓨터)],
)

표를 관통하는 흐름이 하나 보인다 — *다들 LLVM으로 모였다.* 자기만의
컴파일러를 처음부터 유지하는 비용이 너무 커서, 앞단(문법 해석)과 최적화의
공통부는 LLVM에서 가져오고 자기 칩에 맞는 뒷단만 붙이는 방식이 표준이 됐다.
인텔의 고전 `icc`가 물러나고 LLVM 기반 `icx`가 그 자리를 이은 것이 이 흐름의
가장 상징적인 장면이다.

#misconception[
  "인텔 컴파일러(icc)를 쓰면 코드가 빨라진다더라"
][
  오래 회자된 말이지만 두 겹으로 낡았다. 첫째, *그 `icc`는 이제 없다.*
  인텔은 고전 컴파일러를 폐기 예고한 뒤 oneAPI 2024.0에서 제거했고, 지금
  받는 것은 LLVM 기반의 `icx`다. 둘째, 격차 자체가 줄었다. 예전에는 인텔
  컴파일러의 자동 벡터화가 크게 앞선 시기가 있었지만, 지금은 GCC와 Clang도
  같은 일을 잘한다. *컴파일러를 바꿔서 얻는 몇 %*보다, 9장에서 본 기억의
  사다리에 맞게 자료를 배치해 얻는 몇 배가 훨씬 크다는 사실은 변하지 않았다.
]

== 임베디드 — 다음 장의 입구

세상의 C 컴파일러 중 수적으로 가장 많은 갈래는 임베디드 쪽이다. 칩을 만드는
회사마다 자기 컴파일러가 있고, 안전 인증(자동차·항공·의료)이 걸린 자리에는
그 인증을 함께 파는 전문 회사들이 있다. IAR, Arm의 Keil 계열, TI, 마이크로칩,
르네사스, 그린힐스, 윈드리버 — 그리고 8비트 칩을 위한 자유 소프트웨어 SDCC
까지. 이 지형은 도구 전체(디버그 프로브, 온칩 디버깅, 정적 분석)와 함께
보아야 뜻이 서므로 다음 장에서 따로 다룬다.

== 그 밖에, 그리고 은퇴한 이름들

*작지만 살아 있는 것* — `tcc`(Tiny C Compiler)는 몇백 KiB짜리 초소형
컴파일러로, C 소스를 즉석에서 컴파일해 바로 실행하는 용도로 여전히 쓰인다.
`cproc`·`chibicc` 같은 작은 컴파일러들은 "컴파일러를 배우는 교재"로서 값이
있다. 실무의 주력은 아니다.

*이름은 유명하지만 이제 현역이 아닌 것* — 옛 윈도우의 Borland/Turbo C
계열, 왓콤(Watcom), 그리고 위에서 본 인텔의 고전 `icc`. 오래된 교재나
인터넷 글에서 이 이름들을 만나면 *시대를 감안해 읽어야 한다.* 특히
Turbo C 시절의 관행(예: `conio.h`, `void main()`)은 오늘의 표준 C가 아니다.

#realcase[
  이 책이 두 컴파일러로 예제를 검증하는 이유
][
  머리말에서 밝힌 대로 이 책의 예제는 gcc와 clang 양쪽으로 컴파일해 검증
  한다. 실제로 집필 중 여러 번, 한쪽만 잡아 준 실수가 있었다 — 서식 문자열의
  잘림 가능성(`-Wformat-truncation`), `strncpy`의 잘림(`-Wstringop-truncation`),
  부호 있는 값과 없는 값의 비교(`-Wsign-compare`), 배열 매개변수에 `sizeof`를
  쓴 실수(`-Wsizeof-array-argument`) 같은 것들이다. 한 컴파일러의 침묵은
  "이 코드가 옳다"는 증거가 아니다 — 그저 *그 도구가 그 실수를 보는 눈을
  갖고 있지 않다*는 뜻일 수 있다. 도구를 하나 더 두는 것은 리뷰어를 하나 더
  두는 것과 같다.
]

#qa[
  컴파일러를 고르는 실무의 기준을 한 줄로 요약하면?
][
  *대상 기계가 먼저 고르고, 나머지를 사람이 고른다.* 겨냥할 칩이 정해지면
  선택지는 대개 두어 개로 줄고(그 칩을 아는 컴파일러만 남으므로), 그 안에서
  인증·가격·진단 품질·팀의 익숙함으로 정한다. 배우는 단계에서는 반대다 —
  아무 데서나 도는 표준 C를 쓰고, 흔한 도구 둘로 검증하는 것으로 충분하다.
]

#recap[
  #dtable(
    columns: 2,
    [*이름*], [*한 줄*],
    [GCC], [자유 소프트웨어. 겨냥하는 칩이 가장 많다],
    [Clang/LLVM], [부품으로 쓰이는 컴파일러. 도구 생태계의 바탕],
    [MSVC], [윈도우의 표준. C11·C17 지원, C23 진행 중],
    [Pelles C], [윈도우 전용 프리웨어 한 묶음. C23까지 지원],
    [`icx`·AOCC·`nvc`·Open XL], [벤더의 성능 컴파일러. 대부분 LLVM 기반],
    [`icc`], [은퇴(oneAPI 2024.0에서 제거)],
    [임베디드 계열], [다음 장],
  )
]

컴파일러의 지도를 폈다. 다음 장은 그 지도의 가장 넓은 구역 — 임베디드로
들어가, 그곳의 컴파일러들과 make·git 곁에 놓이는 도구들을 함께 본다.
