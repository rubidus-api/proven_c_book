#import "../lib.typ": *

= 표준 라이브러리의 지형

#organizer[
#idx("표준 라이브러리")  지금까지 쓰기만 해 온 표준 라이브러리의 지도를 편다 — 어떤 도구
  상자가 있고, 무엇을 믿고 무엇을 조심할 것인가. 그리고 이 라이브러리가
  왜 "얇고 오래된" 모습인지, 그 역사적 이유까지.
]

#deepqa[
  14장에서 링커가 `printf`의 몸통을 표준 라이브러리에서 찾아 이어 준다고
  했고, 35장에서 `gets`는 표준에서 *삭제*됐다고 했다. 표준 라이브러리는
  누가 정하고 어떻게 바뀌는가?
][
  언어 표준이 함께 정한다 — C 표준 문서는 언어 문법만이 아니라 *제공
  되어야 할 라이브러리 목록과 각 함수의 계약*까지 못박는다(그래서 "C
  표준 라이브러리"다). 바뀌는 속도는 언어만큼 느리고, 무언가를 빼는
  일은 훨씬 더 느리다 — `gets`의 장례가 수십 년 걸린 이유이자, 이
  라이브러리의 성격을 설명하는 열쇠다.
]

== 지도 — 도구 상자들

자주 만나는 헤더를 갈래로 묶으면 지형이 한눈에 들어온다.

#align(center, table(
  columns: 2,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left),
  [*갈래*], [*대표 헤더와 내용*],
  [입출력], [`<stdio.h>` — 스트림, printf/scanf 계열, 파일],
  [문자열·기억], [`<string.h>` — 복사·비교·검색, `<ctype.h>` — 문자 분류],
  [수], [`<stdlib.h>` — 변환·난수, `<math.h>` — 수학 함수, `<stdint.h>`·`<limits.h>`·`<float.h>` — 타입의 한계],
  [기억 관리], [`<stdlib.h>` — malloc/free 계열],
  [시간·환경], [`<time.h>`, `<stdlib.h>`의 환경 접근],
  [계약·진단], [`<assert.h>`, `<errno.h>`],
  [현대의 보탬], [`<stdbool.h>`(C99, C23에서 불필요해짐), `<stdatomic.h>`·`<threads.h>`(C11), `<stdckdint.h>`(C23 검사 산술)],
))

이 목록에서 두 가지가 눈에 띈다. 첫째, *얇다* — 자료구조(목록·해시
표), 정규식, 네트워크, 그래픽이 없다. 둘째, *오래됐다* — 대부분이
C89에 있던 것들이고, 새로 들어온 것은 손에 꼽는다.

== 왜 얇은가 — 설계와 역사

이유는 C가 자란 자리에 있다. C는 운영체제를 만드는 언어로 태어났고
(2장), 운영체제 *위에서* 도는 프로그램만이 아니라 운영체제 *자신*과
임베디드 펌웨어까지 같은 언어로 짜야 했다. 그런 곳에는 파일 시스템도,
동적 할당도, 심지어 운영체제도 없을 수 있다 — 그래서 표준 라이브러리는
"어디서나 있을 수 있는 최소한"으로 좁혀졌다(표준이 프리스탠딩 환경을
따로 정의해 두는 이유다). 풍부함을 포기한 대신 이식성을 얻은 것이다.

그리고 얇음에는 대가가 따랐다 — 세상은 결국 필요한 것을 만들었고,
그 결과가 플랫폼마다 다른 API들과 서드파티 라이브러리들의 숲이다.
"C는 라이브러리를 고르는 일이 절반"이라는 말이 나온 배경이고, 이
책이 35장에서 proven을 고른 것도 그 선택의 한 예다.

== 출력 서식 문자열의 해부

가장 많이 쓰는 함수를 정면으로 뜯어볼 자리다. 19장에서는 최소 집합
(`%d`, `%s`, `%%`)만 익혔지만, `printf`의 서식은 사실 다섯 조각으로 된
작은 언어다.

#align(center, block(inset: (y: 4pt))[
  `%` `[플래그]` `[폭]` `[.정밀도]` `[길이]` `변환`
])

뒤에서부터 읽는 것이 이해가 빠르다. *변환*(conversion)이 "무엇으로
찍을 것인가"를 정하고 — 이것만 필수다 — 나머지는 그 위에 얹는 장식이다.

- *플래그*(flag): `-` 왼쪽 정렬, `0` 남는 자리를 0으로, `+` 양수에도
  부호, 빈칸 양수 앞에 빈칸, `#` 대체 형식(`%#x`는 `0x`를 붙인다).
- *폭*(width): 최소 글자 수. 모자라면 채우고, 넘치면 *자르지 않는다* —
  폭은 하한이지 상한이 아니다.
- *정밀도*(precision): `.`으로 시작한다. 실수에는 소수 자릿수, 문자열
  에는 *최대* 길이, 정수에는 최소 자릿수다.
- *길이 수식어*(length modifier): 인자의 폭을 알려 준다. `l`(long),
  `ll`(long long), `z`(`size_t`), `h`(short로 줄여 해석).

폭과 정밀도 자리에 `*`를 쓰면 그 값을 인자로 넘길 수 있다. 8장의 뷰처럼
"포인터와 길이"로 다니는 문자열을 찍을 때 `%.*s`가 요긴하다.

#demo("examples/ch46/fmtspec.c")

읽을거리가 몇 개 있다. `%06d`가 `000042`가 되는 것은 `0` 플래그가 빈칸
대신 0으로 채우기 때문이고, `%.3s`가 `pro`에서 멈추는 것은 문자열에서
정밀도가 *최대 길이*이기 때문이다. `%#x`의 `0x`는 `#`가 붙인 것이다.
`%g`가 `3.14159`로 짧아진 것은 이 변환이 `%e`와 `%f` 중 짧은 쪽을
자동으로 고르기 때문이다.

길이 수식어는 장식이 아니라 *계약*이다. 45장에서 본 대로 가변 인자에는
타입 정보가 실려 가지 않으므로, `printf`는 서식이 말한 폭 그대로 스택을
읽는다. `size_t`를 `%d`로 찍는 코드가 64비트에서 조용히 어긋나는 이유가
이것이다 — 8바이트를 넣었는데 4바이트만 꺼내 읽는다.

#align(center, table(
  columns: 3,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left, left),
  [*데이터형*], [*출력 서식*], [*메모*],
  [`int`], [`%d` `%i`], [기본형],
  [`unsigned int`], [`%u` `%x` `%o`], [비트를 볼 때는 16진],
  [`short`], [`%d`], [승격되므로 `%hd`는 선택],
  [`long`], [`%ld`], [],
  [`long long`], [`%lld`], [],
  [`size_t`], [`%zu`], [★ `%d`로 찍으면 어긋난다],
  [`ptrdiff_t`], [`%td`], [],
  [`double`], [`%f` `%e` `%g`], [`float`도 승격되어 같다],
  [`long double`], [`%Lf`], [],
  [`char`(문자로)], [`%c`], [인자는 int로 승격된다],
  [`char *`], [`%s`], [NUL 종단이어야 한다],
  [`void *`], [`%p`], [형식은 구현 정의(implementation-defined)],
  [`bool`], [`%d`], [0 또는 1로 찍힌다],
))

#misconception[
  "`float`을 찍을 때는 `%f`, `double`은 `%lf`"
][
  절반만 맞다. *출력*에서는 `float`이 가변 인자 기본 진급(25장)으로
  `double`이 되어 넘어가므로 둘 다 `%f`다 — `%lf`도 표준이 허용하지만
  같은 뜻이다. 헷갈림의 진짜 뿌리는 *입력*에 있다. `scanf`는 주소를
  받으므로 진급이 일어나지 않고, `float *`와 `double *`를 반드시 구별
  해야 한다 — `%f`는 `float *`, `%lf`는 `double *`다. 같은 글자가 방향에
  따라 다른 뜻이 되는, 이 두 함수의 가장 유명한 비대칭이다.
]

== 입력 서식 문자열 — 무엇이 다른가

`scanf` 계열의 서식은 출력과 글자가 겹쳐서 같은 언어처럼 보이지만,
하는 일은 반대이고 규칙도 다르다. 다른 점을 다섯으로 정리한다.

*첫째, 인자는 값이 아니라 담을 곳의 주소다.* `&n`을 빠뜨리면 정수를
주소로 착각해 그 번지에 쓰려 든다 — 22장에서 만난 그 `&`가 여기서
필수인 이유다.

*둘째, 서식의 공백은 "공백 몇 개든(없어도 좋다)"이다.* 출력에서 공백은
그저 공백 한 칸이지만, 입력에서는 *건너뛰기 지시*다. 대부분의 변환은
앞선 공백을 알아서 건너뛴다 — 예외가 `%c`와 `%[`로, 이 둘은 공백도
글자로 읽는다.

*셋째, 서식 안의 보통 글자는 입력과 그대로 맞아야 한다.* `"x=%d"`는
입력이 `x=`로 시작할 것을 요구한다. 맞지 않으면 *매칭 실패*로 멈춘다.

*넷째, 반환값은 찍은 글자 수가 아니라 성공한 항목 수다.* 그래서 셋을
읽으려 했는데 2가 돌아오면 하나는 채워지지 않았다는 뜻이고, 그 인자는
*손대지 않은 채로 남는다*. 입력이 아예 없으면 EOF(음수)가 돌아온다 —
0과 구별해야 한다.

*다섯째, `%s`에는 반드시 최대 폭을 준다.* 폭 없는 `%s`는 목적지 크기를
모르는 채로 쓰는 것이라 35장의 `gets`와 같은 위험이다.

#demo("examples/ch46/scanspec.c")

출력을 한 줄씩 짚으면 규칙이 눈에 보인다. `mismatch`와 `nonnum`은 둘 다
`k=0`인데, 인자가 그대로 남아 있다는 점이 중요하다 — 값을 확인하지 않고
쓰면 *이전 값*을 새 입력으로 착각한다. `empty`의 `-1`은 "입력이 끝났다"
이지 "형식이 틀렸다"가 아니다. `partial`은 앞의 정수만 읽히고 뒤에서
멈춘 경우다. `set`의 `%15[^,]`는 "쉼표가 아닌 글자를 최대 15개"라는
뜻으로, 구분자가 있는 줄을 자를 때 쓴다.

#align(center, table(
  columns: 3,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left, left),
  [*데이터형*], [*입력 서식*], [*인자*],
  [`int`], [`%d`], [`int *`],
  [`unsigned`], [`%u` `%x`], [`unsigned *`],
  [`long`], [`%ld`], [`long *`],
  [`long long`], [`%lld`], [`long long *`],
  [`size_t`], [`%zu`], [`size_t *`],
  [`float`], [`%f`], [★ `float *`],
  [`double`], [`%lf`], [★ `double *`],
  [`long double`], [`%Lf`], [`long double *`],
  [문자 하나], [`%c`], [`char *`(공백도 읽는다)],
  [단어], [`%99s`], [`char[100]` — 폭 필수],
  [글자 집합], [`%15[^,]`], [`char[16]`],
  [건너뛰기], [`%*d`], [읽되 저장하지 않는다],
))

#realcase[
  서식 하나가 무너뜨린 것 — 형식 문자열 취약점
][
  19장에서 이름만 스쳤던 사고를 여기서 완성한다. 사용자가 준 문자열을
  서식 자리에 그대로 넣은 코드(`printf(user)`)는 공격자가 `%s`나 `%x`를
  입력해 스택을 읽게 만들 수 있고, 옛 `%n`(찍은 글자 수를 인자가 가리
  키는 곳에 *쓴다*)까지 살아 있던 시절에는 임의의 메모리 쓰기로까지
  이어졌다. 1999년 무렵 이 부류가 대규모로 발견되면서 wu-ftpd를 비롯한
  여러 서버가 원격 침해됐다. 교훈은 한 줄이다 — *서식 문자열은 언제나
  프로그램이 쓴 상수여야 하고, 사용자 입력은 인자로만 들어간다*
  (`printf("%s", user)`).
]

#qa[
  그러면 입력 해석은 늘 `sscanf`로 하면 되는가?
][
  간단한 형식에는 충분하다. 다만 `sscanf`는 *어디에서 왜 실패했는지*를
  알려 주지 않는다 — 돌려주는 것은 성공한 항목 수뿐이라, "세 번째 필드가
  숫자가 아니었다"와 "줄이 일찍 끝났다"를 구별할 수 없다. 게다가 정수
  오버플로를 감지하지 못하고(`%d`에 `99999999999`를 주면 계약 밖이다),
  실패한 자리까지 몇 글자를 소비했는지도 알 수 없다. 형식이 복잡해지고
  입력이 남이 준 것이라면, 실패가 값으로 드러나고 남은 입력을 손에 쥘 수
  있는 도구가 필요하다 — 제11부의 스캐너가 그 답이다.
]

== 조심할 자리들

이 책이 지나오며 만난 함정들을 라이브러리 관점에서 모으면 이렇다.

- *크기를 안 받는 함수들* — `strcpy`, `strcat`, `sprintf` 계열은 목적지
  그릇의 크기를 모른다(35장의 `gets`가 극단이었다). 대안은 크기를 받는
  판(`snprintf`)이나 경계를 관리하는 부품이다.
- *반환값이 실패를 알리는 함수들* — `malloc`, `fopen`, `fgets`는 실패
  시 널을 준다(41장의 규율: 확인 없이 쓰지 않는다).
- *`errno`라는 전역 상태* — 많은 함수가 실패의 이유를 전역 변수에 남긴다.
  성공해도 지우지 않는 함수가 있어 "호출 직전에 0으로 초기화하고 직후에
  읽는" 규약을 지켜야 한다 — 전역 가변 상태의 불편함을 보여 주는 교과서
  사례다(36장).
- *지역/문화 의존 함수들* — `toupper` 같은 문자 함수와 `strtod`의 소수점
  해석은 로케일 설정에 따라 동작이 달라진다. 데이터 형식을 다룰 때는
  로케일 독립적인 처리를 쓰는 것이 안전하다(7장의 "인코딩을 추측하지
  말라"와 같은 결).
- *정적 버퍼를 돌려주는 함수들* — `asctime`, `strtok` 계열은 내부의
  고정된 자리를 재사용해서, 다음 호출이 이전 결과를 덮는다. 여러 갈래로
  도는 프로그램(10장의 멀티코어)에서는 특히 위험하다.

#qa[
  그러면 표준 라이브러리는 낡아서 안 쓰는 것이 좋은가?
][
  전혀 아니다 — *어디에나 있다*는 것이 압도적인 미덕이다. 어떤 플랫폼,
  어떤 컴파일러에서도 같은 계약으로 존재하는 부품은 이것뿐이고, 이
  책의 예제 대부분도 표준 라이브러리만으로 돌았다. 옳은 태도는 "쓰지
  않기"가 아니라 *어느 함수가 어떤 계약인지 알고 고르기*다 — 크기를
  받는 판을 고르고, 반환값을 확인하고, 전역 상태에 기대는 함수는 규약을
  지켜 쓰는 것. 그리고 반복되는 위험 지대(문자열 조립, 입력 해석)에는
  검사가 내장된 부품을 얹는 것 — 다음 장이 그 실천이다.
]

지도를 폈으니, 이제 그 위에 얹을 층을 본다. 다음 부(제11부)는 이 책의
기반 라이브러리 proven을 세 장에 걸쳐 다룬다 — 설계의 뼈대인 다섯 계약,
문자열과 텍스트, 그리고 할당과 컨테이너까지. C 입문서로 읽어 온 이 책이
그 대목에서는 proven의 안내서가 된다.
