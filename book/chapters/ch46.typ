#import "../lib.typ": *

= 수식과 연산자

#prereq(
  ([20장 수식], [값이 되는 것과 우선순위]),
  ([33장 대입과 부수효과], [평가 순서와 시퀀스 포인트]),
  ([37장 배열], [첨자와 포인터 산술]),
  ([43장 구조체], [멤버 접근]),
)

#deepqa[
  20장에서 "표를 외우지 말고 괄호를 쓰라"고 했고, 그 뒤로 연산자를 필요한
  자리마다 조금씩 꺼내 썼다 — 27장의 시프트, 29장의 비교, 33장의 대입,
  37장의 포인터 산술처럼. 그러면 지금 이 장에서 무엇을 새로 배우는가?
][
  *같은 것을 다른 눈으로* 본다. 지금까지는 "이 자리에서 필요한 만큼"이었다면,
  이제는 연산자 하나하나를 *계약*으로 읽는다 — 무엇을 받고(피연산자 제약),
  무엇을 돌려주며(결과의 타입과 값 범주), 어디서부터 계약 밖인가(회색지대).

  이 눈이 필요한 이유는 실무에 있다. 남의 코드를 읽다 막히는 자리, 컴파일러가
  왜 그렇게 최적화했는지 알아야 하는 자리, 그리고 "왜 여기서만 이상하게
  도는가"를 추적하는 자리 — 전부 연산자의 계약을 봐야 풀린다.
]

#organizer[
#idx("수식")  흩어져 배운 연산자를 한자리에 모아 다시 본다. 수식이란 무엇인가(값·타입·
  값 범주·부수효과)에서 시작해, 우선순위와 결합의 전체 표, 연산자별 계약,
  평가 순서와 시퀀스 포인트, 그리고 회색지대 총람까지. 이 장을 지나면 부록 A는
  조회표로만 쓰면 된다.
]

#chapter-questions()

== 수식이 가진 네 가지

C의 수식은 언제나 네 가지를 함께 가진다. 이 넷을 갈라 놓고 보는 습관이 이 장의
전부라 해도 좋다.

#dtable(
  columns: 2,
  [*가진 것*], [*무엇인가*],
  [값], [계산의 결과. `2 + 3`의 값은 5],
  [타입], [그 값이 어떤 그릇에 담기는가. 컴파일 시간에 정해진다(23장)],
  [값 범주], [*좌변값*(자리를 가리킴)인가 아닌가. `x`는 좌변값, `x + 1`은 아니다],
  [부수효과], [객체를 바꾸거나 바깥 세계에 영향을 주는가(33장)],
)

*값 범주*라는 낱말이 낯설 텐데, 실은 이미 쓰고 있었다. 대입의 왼쪽에 올 수
있는 것이 좌변값이고(33장), `&`를 붙일 수 있는 것도 좌변값이다(34장). 배열
이름은 좌변값이지만 대입의 왼쪽에는 올 수 없는 특수한 경우다(37장).

#qa[
  "좌변값"이라는 이름은 왼쪽에 있다는 뜻인가?
][
  역사적으로는 그랬다(left value). 그러나 지금은 *위치를 가리키는 수식*이라는
  뜻으로 쓰는 편이 정확하다 — 대입의 왼쪽에 올 수 있는 것이 그 성질의 한 가지
  귀결일 뿐이다. `*p`는 대입의 오른쪽에 있어도 좌변값이고, `const int c`는
  좌변값이지만 *수정 가능한* 좌변값은 아니라서 대입의 왼쪽에 올 수 없다.

  그래서 표준은 "수정 가능한 좌변값(modifiable lvalue)"이라는 말을 따로 쓴다.
  이 장의 표에서 대입과 증감의 피연산자 칸에 그 말이 나오는 이유다.
]

== 우선순위와 결합

위쪽이 더 강하게 묶는다. *결합 방향*은 같은 세기의 연산자가 나란히 있을 때
어느 쪽부터 묶느냐다 — `a - b - c`가 `(a - b) - c`인 것은 왼쪽 결합이기
때문이고, `a = b = 0`이 `a = (b = 0)`인 것은 오른쪽 결합이기 때문이다.

#dtable(
  columns: 4,
  [*묶음*], [*연산자*], [*결합*], [*결합이 그런 이유*],
  [후위], [`() [] . -> ++(후위) --(후위)`, 복합 리터럴], [왼→오], [`a.b.c`처럼 왼쪽부터 파고들어야 뜻이 선다],
  [단항], [`++ -- + - ! ~ (타입) * & sizeof alignof`], [오→왼], [피연산자에 가장 가까운 것부터 붙는다: `- -x`, `*&x`],
  [곱셈류], [`* / %`], [왼→오], [산수의 관례],
  [덧셈류], [`+ -`], [왼→오], [뺄셈은 왼쪽 결합이라야 뜻이 맞는다],
  [시프트], [`<< >>`], [왼→오], [`a << 1 << 2`는 차례로 민다],
  [관계], [`< <= > >=`], [왼→오], [그래서 `x < y < z`가 수학과 달라진다],
  [상등], [`== !=`], [왼→오], [],
  [비트 AND], [`&`], [왼→오], [],
  [비트 XOR], [`^`], [왼→오], [],
  [비트 OR], [`|`], [왼→오], [],
  [논리 AND], [`&&`], [왼→오], [단락 평가가 왼쪽부터라야 뜻이 산다],
  [논리 OR], [`||`], [왼→오], [같은 이유],
  [조건], [`?:`], [오→왼], [`a ? b : c ? d : e`가 사다리로 읽힌다],
  [대입], [`= += -= *= /= %= &= ^= |= <<= >>=`], [오→왼], [`a = b = 0`이 "둘 다 0"이 되려면],
  [쉼표], [`,`], [왼→오], [왼쪽을 먼저 하고 버린다],
)

#misconception[
  "우선순위가 높으면 먼저 계산된다"
][
  우선순위는 *묶는 규칙*이지 *계산의 시간 순서*가 아니다. `f() * g() + h()`에서
  곱셈이 덧셈보다 강하게 묶이는 것은 맞지만, `h()`가 `f()`보다 먼저 불려도
  아무 문제가 없다. 세 호출의 순서는 *미지정*이다.

  이 구별은 33장에서 이미 만났다 — 우선순위는 "무엇과 무엇이 한 덩어리인가"를
  정하고, 시퀀스 포인트는 "무엇이 무엇보다 먼저 끝나는가"를 정한다. 둘은 다른
  질문이고, 뒤의 절에서 후자를 따로 다룬다.
]

== 자주 미끄러지는 자리들

#dtable(
  columns: 3,
  [*쓴 것*], [*실제로 묶이는 방식*], [*의도한 것이라면*],
  [`a & b == c`], [`a & (b == c)`], [`(a & b) == c`],
  [`a << 1 + 2`], [`a << (1 + 2)`], [`(a << 1) + 2`],
  [`*p++`], [`*(p++)`], [`(*p)++`],
  [`*p.x`], [`*(p.x)`], [`(*p).x` 또는 `p->x`],
  [`(int)x + y`], [`((int)x) + y`], [`(int)(x + y)`],
  [`a = b = 0`], [`a = (b = 0)`], [(그대로다 — 오른쪽 결합)],
  [`x < y < z`], [`(x < y) < z`], [`x < y && y < z`],
  [`!x & y`], [`(!x) & y`], [`!(x & y)`],
  [`sizeof a + 1`], [`(sizeof a) + 1`], [`sizeof(a + 1)`],
  [`a ? b : c = d`], [`(a ? b : c) = d`(대개 오류)], [`a ? b : (c = d)`],
)

특히 첫 두 줄은 C의 유명한 설계 흠으로 꼽힌다 — 비트 연산자가 비교
연산자보다 *약하게* 묶이는 것은 초기 C가 `&&`·`||`를 갖기 전의 흔적이다.
그래서 비트 검사에는 괄호가 사실상 필수다. 표준도 각주에서 `a<b<c`가
수학과 다르게 읽힌다는 것을 따로 짚어 둔다.

== 연산자별 계약

이제 갈래별로 하나씩 본다. 각 표는 다섯 칸이다.

- *피연산자* — 표준이 요구하는 것. 어기면 컴파일러가 진단해야 한다(제약 위반).
- *결과* — 값의 타입, 그리고 좌변값인지.
- *회색지대* — 49장의 세 낱말로 구분한다. *UB*는 정의되지 않은 동작,
  *미지정*은 몇 갈래 중 하나이되 어느 쪽인지 정해지지 않은 것,
  *구현 정의*는 구현이 정해 문서로 밝히는 것.
- *자세히* — 그 연산자를 서사로 다룬 장.

#platform("이 절의 근거")[
  규칙은 ISO/IEC 9899:2024(C23)의 수식 조항(§6.5)과 대조해 적었다. 판마다
  달라진 자리는 그 사실을 함께 적었다. 인용이 필요하면 부록 D의 안내대로
  발행된 표준을 근거로 삼아야 한다.
]

== 후위 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`a[i]` 첨자], [한쪽은 완전한 객체 타입을 가리키는 포인터, 다른 쪽은 정수], [가리키는 타입의 좌변값. `a[i]`는 `*(a+i)`와 같다], [*UB*: 배열의 경계 밖 접근(한 칸 지난 자리를 따라가는 것 포함)], [37장],
  [`f(...)` 호출], [함수 또는 함수를 가리키는 포인터], [함수의 반환 타입. 좌변값이 아니다], [*미지정*: 인자들의 평가 순서. *UB*: 원형과 어긋난 인자], [21·24·53장],
  [`s.m` 멤버], [구조체·공용체 값과 그 멤버 이름], [멤버의 타입. 왼쪽이 좌변값이면 좌변값], [*UB*: 공용체에서 마지막에 쓴 것이 아닌 멤버 읽기(공통 초기 순서는 예외)], [43·45장],
  [`p->m` 멤버], [구조체·공용체를 가리키는 포인터와 멤버 이름], [멤버의 타입, 좌변값], [*UB*: 널이거나 유효하지 않은 포인터], [43장],
  [`x++` 후위 증가], [수정 가능한 좌변값 — 실수형 또는 포인터형], [*바꾸기 전의 값*. 좌변값이 아니다], [*UB*: 한 시퀀스 포인트 안 이중 수정, 부호 있는 정수의 넘침], [31장],
  [`x--` 후위 감소], [같음], [바꾸기 전의 값], [같음], [31장],
)

== 단항 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`++x` `--x`], [수정 가능한 좌변값(실수형 또는 포인터형)], [*바꾼 뒤의 값*], [`++E`는 `(E += 1)`과 같다 — 넘침 규칙도 같다], [31장],
  [`&x` 주소], [함수 지시자, `[]`·`*`의 결과, 또는 *비트필드가 아니고 `register`로 선언되지 않은* 객체의 좌변값], [그것을 가리키는 포인터], [제약을 어기면 컴파일 오류다], [34장],
  [`*p` 역참조], [포인터 타입], [가리키는 타입의 좌변값], [*UB*: 널·수명이 끝난 객체·정렬을 어긴 주소·출처 밖], [34·36·41장],
  [`+x`], [산술 타입], [승격된 값], [], [20·28장],
  [`-x`], [산술 타입], [승격된 값], [*UB*: 부호 있는 정수의 넘침(`-INT_MIN`)], [26장],
  [`~x`], [정수 타입], [승격 뒤의 비트 반전], [승격된 폭에서 일어난다 — 좁은 타입에 담을 때 주의], [27장],
  [`!x`], [스칼라(산술 또는 포인터)], [`0` 또는 `1`, 타입은 `int`], [], [29장],
  [`(타입)x` 캐스트], [스칼라끼리], [그 타입의 값. 좌변값이 아니다], [*구현 정의*: 포인터↔정수 변환. *UB*: 정렬을 어긴 포인터를 따라가기, 함수 포인터와 객체 포인터의 상호 변환], [28·36장],
  [`sizeof`], [완전한 객체 타입이나 수식. *함수 타입·불완전 타입·비트필드에는 쓸 수 없다*], [`size_t` 값], [가변 길이 배열이면 실행 중에 평가된다 — 그 밖에는 피연산자가 평가되지 않는다], [34·37장],
  [`alignof`], [완전한 객체 타입의 *이름*(수식이 아니다)], [`size_t` 값], [], [36장],
)

== 산술 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`*` 곱셈], [산술 타입], [통상 산술 변환의 공통 타입], [*UB*: 부호 있는 정수의 넘침], [26·28장],
  [`/` 나눗셈], [산술 타입], [같음], [*UB*: 제수가 0, `INT_MIN / -1`], [27·47장],
  [`%` 나머지], [*정수 타입만*], [같음], [*UB*: 제수가 0, `INT_MIN % -1`], [27장],
  [`+` 덧셈], [둘 다 산술, 또는 완전한 객체 타입 포인터와 정수], [산술이면 공통 타입, 포인터면 포인터], [*UB*: 정수 넘침, 배열 범위를 벗어난 포인터 산술], [26·37장],
  [`-` 뺄셈], [둘 다 산술, 포인터와 정수, 또는 *같은 배열 안의* 두 포인터], [포인터끼리면 `ptrdiff_t`], [*UB*: 다른 배열의 포인터끼리 빼기, 결과가 `ptrdiff_t`에 안 담기는 경우], [26·37장],
)

정수 나눗셈은 0을 향해 버린다(C99 이후 확정). 그래서 몫이 표현 가능한 한
`(a/b)*b + a%b == a`가 성립한다.

== 시프트 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`E1 << E2`], [둘 다 *정수 타입*], [승격된 *왼쪽* 피연산자의 타입], [*UB*: `E2`가 음수이거나 승격된 `E1`의 폭 이상. *UB*: `E1`이 부호 있는 음수이거나, 부호 있는 양수인데 `E1 × 2^E2`가 결과 타입에 안 담기는 경우], [7·27장],
  [`E1 >> E2`], [둘 다 정수 타입], [같음], [*UB*: `E2`가 음수이거나 폭 이상. *구현 정의*: `E1`이 부호 있는 음수일 때의 결과], [7·27장],
)

#misconception[
  "C23이 두 보수를 강제했으니 음수 시프트도 이제 정의됐다"
][
  두 보수 표현이 강제된 것은 맞다(C23). 그러나 시프트 조항은 그대로다 —
  *부호 있는 음수의 좌시프트는 C23에서도 UB*이고, *음수의 우시프트는 구현
  정의*다. 대부분의 컴파일러가 산술 시프트로 동작하지만 그것은 표준의 약속이
  아니라 그 구현의 약속이다.

  실무의 규칙은 하나다 — *시프트는 부호 없는 타입 위에서 한다.* 부호 있는
  값을 밀어야 하면 무부호로 옮겨 밀고 되돌린다. 횟수는 언제나
  `0 <= n < 폭`인지 확인한다.
]

== 관계·상등 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`< <= > >=`], [둘 다 실수 타입, 또는 *호환되는 객체 타입을 가리키는 두 포인터*], [`0` 또는 `1`, 타입은 `int`], [*UB*: 같은 배열(또는 같은 객체)에 속하지 않는 두 포인터의 크기 비교], [29·36장],
  [`== !=`], [둘 다 산술, 호환되는 포인터끼리, 한쪽이 `void*`, 한쪽이 널 포인터 상수·`nullptr_t` 등], [`0` 또는 `1`, `int`], [*미지정*: 어떤 객체의 "한 칸 지난" 포인터와 바로 뒤 객체의 포인터가 같게 비교될 수 있다], [29·35장],
)

두 연산자군은 계약이 다르다. *상등 비교는 서로 다른 객체 사이에도 허용*되지만
*크기 비교는 같은 배열 안에서만* 뜻이 있다. 배열이 아닌 객체는 "길이 1짜리
배열"처럼 취급된다. 실수에서 `+0.0`과 `-0.0`은 같다고 비교된다(47장).

== 비트·논리 연산자

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`&` `^` `|`], [둘 다 정수 타입], [통상 산술 변환의 공통 타입], [부호 비트까지 다루게 되므로 무부호 위에서 쓴다], [27장],
  [`&&`], [둘 다 스칼라], [`0` 또는 `1`, `int`], [*보장*: 왼쪽이 0이면 오른쪽은 평가되지 않고, 그 사이에 시퀀스 포인트가 있다], [29장],
  [`||`], [둘 다 스칼라], [`0` 또는 `1`, `int`], [*보장*: 왼쪽이 0이 아니면 오른쪽은 평가되지 않는다], [29장],
)

== 조건·대입·쉼표

#dtable(
  columns: 5,
  [*연산자*], [*피연산자*], [*결과*], [*회색지대*], [*자세히*],
  [`c ? a : b`], [`c`는 스칼라. `a`·`b`는 둘 다 산술이거나, 호환되는 구조체·공용체이거나, 둘 다 `void`이거나, 호환되는 포인터이거나, 한쪽이 널 포인터 상수], [두 갈래의 *공통 타입 하나*], [*보장*: 조건 평가 뒤 시퀀스 포인트가 있고, 고르지 않은 쪽은 평가되지 않는다], [32장],
  [`=`], [왼쪽은 수정 가능한 좌변값], [왼쪽 타입으로 변환된 값. *좌변값이 아니다*], [*UB*: 겹치는 객체 사이의 대입(정확히 겹치고 호환되는 타입이면 허용), 한 시퀀스 포인트 안 이중 수정], [23장],
  [복합 대입 `op=`], [`E1 op= E2`], [`E1 = E1 op E2`와 같되 *`E1`을 한 번만 평가한다*], [연산 자체의 회색지대(넘침·0 나누기)는 그대로 적용된다], [32장],
  [`,` 쉼표], [아무 수식 둘], [오른쪽의 타입과 값], [*보장*: 왼쪽을 평가해 버린 뒤 시퀀스 포인트. 함수 인자 목록의 쉼표는 *연산자가 아니다*], [32장],
)

== 평가 순서와 시퀀스 포인트

우선순위는 *묶는 규칙*이지 *평가의 시간 순서*가 아니다(13·32장). 순서가
보장되는 자리는 다음 다섯뿐이다.

- `&&`의 왼쪽과 오른쪽 사이
- `||`의 왼쪽과 오른쪽 사이
- `?:`의 조건과 고른 가지 사이
- 쉼표 *연산자*의 왼쪽과 오른쪽 사이
- 함수 호출에서 인자들의 평가와 본체 실행 사이(*인자들끼리의 순서는 미지정*)

그 밖에는 아무 순서도 보장되지 않는다.

#dtable(
  columns: 2,
  [*수식*], [*판정*],
  [`f() + g()`], [*미지정* — 어느 쪽이 먼저 불릴지 정해지지 않는다],
  [`h(f(), g())`], [*미지정* — 인자 평가 순서],
  [`i = i++`], [*UB* — 한 시퀀스 포인트 안에서 `i`를 두 번 바꾼다],
  [`a[i] = i++`], [*UB* — 같은 이유],
  [`i++ + i++`], [*UB*],
  [`f(i++, i++)`], [*UB* — 인자 사이에는 시퀀스 포인트가 없다],
  [`i++, i++`], [정상 — 쉼표 *연산자* 사이에는 시퀀스 포인트가 있다],
  [`(i++) && (i++)`], [정상 — `&&` 사이에 시퀀스 포인트가 있다],
)

C11 이후 표준은 이 규칙을 "시퀀스 포인트" 대신 *sequenced-before*라는
관계로 다시 적었지만, 실무의 결론은 같다 — *한 수식 안에서 같은 객체를 두 번
건드리지 않는다.* gcc의 `-Wsequence-point`가 흔한 경우를 잡아 주지만 전부는
아니다.

== 회색지대 총람

49장의 세 낱말로 연산자 관련 항목만 모았다. 전체 목록은 표준의 부속서 J에
있다.

=== 정의되지 않은 동작(UB)

#dtable(
  columns: 2,
  [*자리*], [*조건*],
  [`/` `%`], [제수가 0. `INT_MIN / -1`, `INT_MIN % -1`],
  [`+ - *` `++ --`], [부호 있는 정수의 넘침],
  [`<<`], [횟수가 음수이거나 폭 이상. 부호 있는 음수의 좌시프트. 부호 있는 양수라도 결과가 안 담기는 경우],
  [`>>`], [횟수가 음수이거나 폭 이상],
  [`*` 역참조], [널·수명이 끝난 객체·정렬 위반·출처 밖 포인터],
  [`[]`], [배열 경계 밖 접근],
  [`+ -` 포인터 산술], [배열의 범위(한 칸 지난 자리까지)를 벗어난 결과],
  [`-` 포인터끼리], [서로 다른 배열의 포인터],
  [`< <= > >=`], [같은 배열·객체에 속하지 않는 포인터끼리의 크기 비교],
  [`.` `->` 공용체], [마지막에 쓴 멤버가 아닌 멤버 읽기(공통 초기 순서는 예외)],
  [수식 일반], [한 시퀀스 포인트 안에서 같은 객체를 두 번 바꾸거나, 바꾸면서 그 값을 다른 목적으로 읽는 것],
)

=== 미지정(unspecified)

#dtable(
  columns: 2,
  [*자리*], [*무엇이 정해지지 않았나*],
  [부분식], [`f() + g()`의 평가 순서],
  [함수 인자], [인자들끼리의 평가 순서],
  [`==` `!=`], [한 객체의 "한 칸 지난" 포인터가 이웃 객체의 포인터와 같게 비교될 수 있는지],
  [채움 바이트], [구조체의 채움(padding) 값 — 비교에 `memcmp`를 쓰면 안 되는 이유(44장)],
)

=== 구현 정의(implementation-defined)

#dtable(
  columns: 2,
  [*자리*], [*구현이 정하는 것*],
  [`>>`], [부호 있는 음수를 밀 때의 결과(대개 산술 시프트)],
  [정수 변환], [부호 있는 타입에 담기지 않는 값을 변환한 결과(C23에서도 그렇다)],
  [포인터 ↔ 정수], [변환 결과와 왕복 가능성(`uintptr_t`가 있을 때의 왕복만 표준이 보장한다)],
  [`char`], [부호 있음/없음 — `>>`와 비교에서 갈린다(9장)],
  [비트필드], [배치 순서와 채움],
)

== 연산자가 아닌 것들

문법에서 같은 글자를 쓰지만 연산자가 아닌 것들이 있다.

#dtable(
  columns: 3,
  [*모양*], [*정체*], [*자세히*],
  [`f(a, b)`의 쉼표], [함수 호출 문법의 구분자 — 시퀀스 포인트가 없다], [32장],
  [`int a, b;`의 쉼표], [선언 문법의 구분자], [23장],
  [`{1, 2}`의 쉼표], [초기화 목록의 구분자], [37·43장],
  [`(타입){...}`], [복합 리터럴 — 캐스트가 아니라 *객체를 만드는 문법*], [44장],
  [`sizeof(int)`의 괄호], [타입 이름을 감싸는 문법 — 함수 호출이 아니다], [34장],
  [`#` `##`], [전처리기 연산자 — 번역의 다른 단계에서 동작한다], [52장],
  [`{.x = 1}`의 점], [지정 초기화 문법 — 멤버 접근이 아니다], [43장],
  [`int *p;`의 별표], [선언자 문법 — 역참조가 아니다], [55장],
)

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [수식이 가진 것], [값·타입·값 범주·부수효과 넷],
    [우선순위], [묶는 규칙일 뿐, 계산 순서가 아니다],
    [결합], [같은 세기끼리 어느 쪽부터 묶는가],
    [순서가 보장되는 곳], [`&&`·`||`·`?:`·쉼표 연산자·함수 호출 다섯 자리뿐],
    [회색지대], [UB·미지정·구현 정의를 낱말로 구별해 읽는다],
    [실무 수칙], [괄호를 쓰고, 한 수식에서 같은 객체를 두 번 건드리지 않는다],
  )
]

연산자를 계약으로 읽는 눈이 갖춰졌다. 다음 장부터는 그 계약이 가장 미묘해지는
자리들로 들어간다 — 근사의 수학(47장), 실패를 다루는 법(48장), 그리고 계약을
어겼을 때 무슨 일이 일어나는가(49장)다.
