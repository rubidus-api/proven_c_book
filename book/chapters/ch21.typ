#import "../lib.typ": *

= 함수 선언과 정의

#organizer[
  18장에서 함수를 *부르는* 법을 배웠다 — 이제 *만드는* 법이다. 정의와
#idx("함수 원형")  선언(원형)의 구별, 매개변수와 `return`, 스코프의 첫걸음까지. 그리고
  이 장의 끝에서, 13장부터 끌고 온 외상 — `int main(void)`와
  `return 0` — 을 완전히 청산한다.
]

#deepqa[
  14장에서 "컴파일 단계는 *선언*(예고)만 있으면 만족하고, 몸통을 찾아
  잇는 것은 링커"라고 했다. 그때의 "선언"과, 20장에서 배운 "변수
  선언"은 같은 낱말을 쓴다 — 우연인가?
][
  우연이 아니다. C에서 *선언*이란 언제나 같은 일 — "컴파일러에게
  이름과 그 생김새(타입)를 미리 알리는 것" — 이다. 변수 선언은 값의
  이름을, 함수 선언은 일의 이름을 알린다. 이 장에서 그 두 번째 종류를
  만들고 나면, `#include`가 가져오던 것의 정체(선언들의 묶음)까지
  한 줄로 꿰어진다.
]

== 정의 — 일꾼을 만드는 문법

18장의 `abs`나 `printf` 같은 일꾼을 이제 직접 만든다. 함수 *정의*의
문법은 이렇다:

```c
반환타입 이름(매개변수 목록)
{
    몸통 — 문장들의 목록
}
```

시연부터 보면 빠르다. 정수를 받아 그 제곱을 돌려주는 일꾼이다:

#demo("examples/ch21/fn.c")

`square`의 정의를 부분별로 읽는다.

- *반환타입* `int` — 이 일꾼이 내놓는 결과가 정수라는 약속이다.
- *매개변수* `int n` — 재료를 받는 *변수 선언*이다(20장의 문법
  그대로). 호출될 때, 재료의 값이 이 변수에 담긴 채 몸통이 시작된다.
  `square(12)`라 부르면 `n`에 12가 담기고 몸통이 돈다.
- *`return` 문* — 결과를 내놓고 *즉시 퇴장*한다. `return n * n;`은
  수식을 평가해 그 값을 호출한 쪽에 돌려준다 — 18장에서 배운 "호출
  자리가 반환값으로 바뀐다"의 공급자 쪽 모습이다.

이것으로 호출의 그림이 완성됐다. 18장은 소비자의 눈(부르기)이었고
이 장은 생산자의 눈(만들기)이다 — `printf("%d\n", square(12))`에서
값 12가 `n`으로, `n * n`의 144가 호출 자리로, 다시 `printf`의 재료로
흐르는 릴레이 전체가 이제 보인다.

== 선언 — 계약 서명만 미리

위 예제에서 `square`의 정의는 `main`보다 *위에* 있다. 우연이 아니다 —
컴파일러는 파일을 위에서 아래로 읽으므로(14장), `main` 안의
`square(12)`를 만나는 시점에 square가 *무엇인지*(재료와 결과의 타입)를
이미 알아야 호출이 맞는지 검사할 수 있다.

그런데 정의 전체를 미리 보여 줄 필요는 없다. *계약 서명만* 미리 알리면
된다 — 그것이 함수 *선언*, 관용적으로 *원형*(prototype)이다:

```c
int square(int n);    /* 선언: 이런 일꾼이 (어딘가에) 있다 */
```

몸통 없이 서명과 세미콜론뿐이다. 파일 위쪽에 원형을 적어 두면 정의는
아래 어디에 있어도 되고 — 더 중요하게는 — *다른 파일*에 있어도 된다.
14장의 수수께끼가 여기서 완전히 풀린다: `#include <stdio.h>`가 붙여
넣던 수만 줄의 정체가 바로 이런 *원형들의 묶음*이다. `printf`의 계약
#idx("헤더 파일")서명을 컴파일러에게 미리 보여 주는 것 — 그것이 헤더 파일의 일이고,
몸통은 링커가 잇는다(정의와 선언의 분업이 여러 파일 프로젝트로 자라는
이야기는 43장이다).

이 원형이라는 발명품의 나이도 이제 말할 수 있다 — 10장에서 본 대로,
C89가 표준화하며 들여온 것이다. 그전의 C는 재료의 타입을 검사하지
않았고, 틀린 재료로 부른 호출이 소리 없이 통과해 사고가 났다. 원형은
"계약을 컴파일 시점에 검사한다"는, C가 계약의 언어로 자라는 첫걸음
이었다.

#realcase[
  원형 없는 호출이 남긴 상처 — 암묵 선언
][
  원형이 없던 시절(10장)의 C에는 관대한 규칙이 하나 있었다. 선언한 적
  없는 이름을 함수처럼 부르면, 컴파일러가 "정수를 돌려주는 함수겠거니"
  하고 *멋대로 선언을 지어내* 통과시킨 것이다(암묵 선언). 편해 보이지만
  결과는 참혹했다 — 실제로는 실수를 돌려주는 함수를 정수 함수로
  오해하거나, 포인터를 받는 함수에 정수를 넘겨도 아무 경고 없이 컴파일
  됐다. 특히 정수와 포인터의 크기가 다른 기계로 옮길 때 조용히 무너지는
  코드가 쏟아졌다 — 32비트에서 64비트로 넘어가던 시기에 이 부류의
  버그가 대거 드러났다. C99가 마침내 암묵 선언을 표준에서 제거했고,
  오늘의 컴파일러는 이를 오류로 잡는다. "쓰기 전에 선언한다"는 규칙이
  성가신 격식이 아니라 사고의 방벽이라는 것 — 반세기의 상처가 남긴
  교훈이다.
]

== 스코프 — 이름이 보이는 범위

`square`의 `n`과 `main`의 어떤 이름이 부딪히지 않을까 — 걱정할 필요가
없다. *블록 안에서 선언된 이름은 그 블록 안에서만 보인다.* 이 보이는
범위를 *스코프*(scope)라 한다. `n`은 `square`의 몸통에서만 존재하고,
`main`은 그 이름을 아예 모른다. 함수마다 자기만의 작업대가 있는 셈이라,
일꾼들이 서로의 도구를 건드릴 걱정 없이 이름을 자유로이 짓는다 —
프로그램이 커져도 무너지지 않는 비결의 절반이 이 칸막이다. (작업대의
더 깊은 사정 — 이름의 수명, 함수 사이에 값이 어떻게 오가는지의 정밀한
그림 — 은 29·36장에서 이어진다.)

== 외상 청산 — int main(void)

이제 이 순간을 위해 아껴 둔 일을 한다. 13장의 헬로 월드에서 마지막까지
"주문"으로 남겨 둔 두 줄을, 오늘 배운 문법으로 다시 읽는다.

```c
int main(void)
{
    ...
    return 0;
}
```

이것은 그냥 *함수 정의*다. 이름이 `main`이고, 매개변수 목록의 `void`는
"재료 없음"을 명시하는 표기이며, 반환타입은 `int` — 즉 "재료 없이
일하고 정수 하나를 내놓는 일꾼"이다. 특별한 것은 문법이 아니라 *호출자*
다: `main`을 부르는 것은 우리 코드가 아니라 *운영체제*다. 프로그램
실행이란 운영체제가 `main`을 호출하는 일이고, `return 0;`은 그
호출자에게 결과값을 돌려주는 것이다 — 관례로 0이 "무사히 마쳤음",
0 아닌 값이 "문제 있었음"이다. 그 값은 버려지지 않는다 — 운영체제와
셸이 받아서, 프로그램들을 이어 붙인 자동화가 성공·실패를 가리는 데
쓴다(8장의 스트림처럼, 이것도 프로그램을 부품으로 잇는 장치다).

13장의 외상 장부가 이것으로 *전부 청산되었다*. 헬로 월드의 여섯 줄에
이제 모르는 것이 없다 — 전처리 지시, 함수 정의, 블록, 호출 수식문,
문자열 리터럴, 서식, 반환까지. 입문의 첫 산 하나를 넘은 것이다.

#qa[
  `main`도 함수라면, 우리가 `main()`이라고 직접 불러도 되는가?
][
  문법상 가능하지만 하지 않는 것이 관행이고, 할 이유도 없다 — `main`은
  "운영체제가 부르는 입구"라는 역할이 본질이라서, 코드 안에서 다시
  부르는 것은 건물의 정문을 방 안에 또 만드는 격이다. 재사용하고 싶은
  일이 있다면 그 일을 별도 함수로 빼서 부르면 된다 — 사실 그것이
  함수를 만드는 이유의 전부다: *이름 붙일 가치가 있는 일에 이름을
  붙이는 것.*
]

이름 붙은 값(20장)과 이름 붙은 일(21장)을 다 갖췄다. 이제 19장에서
미뤄 둔 마지막 약속을 지킬 수 있다 — 값을 담을 곳이 생겼으니, 드디어
*입력*이다.
