#import "../lib.typ": *

= 공용체와 표현

#prereq(
  ([43장 구조체], [구조체의 배치]),
  ([5장 워드와 주소], [표현을 바이트로 본다]),
  ([13장 컴파일러 최적화], [엄격한 앨리어싱]),
)

#deepqa[
  13장에서 "float의 비트를 uint32의 눈으로 읽는" 옛 기법이 엄격한
  앨리어싱 위반이며, 옳은 방법은 memcpy나 공용체라고 했다. 그러면
  공용체란 정확히 무엇이길래 그 자리에 서는가?
][
  *여러 멤버가 같은 기억을 공유하는 타입*이다. 구조체가 멤버들을
  나란히 놓는다면(43장), 공용체는 멤버들을 *겹쳐* 놓는다 — 크기는 가장
  큰 멤버에 맞고, 어느 순간에든 실제로 담긴 것은 하나다. "같은 비트를
  이 눈으로도 저 눈으로도 본다"는 5장의 관점이 문법이 된 것이다.
]

#organizer[
#idx("공용체")  같은 기억을 다른 눈으로 보는 장치 — 공용체다. 그리고 이 장은 표현의
  장이기도 하다: 5장에서 예약한 엔디안 관찰 시연을 실행하고, 구조체의
#idx("패딩")  숨은 빈틈(패딩)을 눈으로 확인한다. 표현과 추상의 분리라는 이 책의
  후렴구가 마지막으로 크게 울리는 자리다.
]

#chapter-questions()

== 공용체 — 겹쳐 놓기

문법은 구조체와 쌍둥이다. `struct`를 `union`으로 바꾸면 된다:

```c
union bits32 {
    uint32_t as_int;
    float    as_float;
};
```

멤버 접근도 같다(`u.as_int`). 다른 것은 기억의 배치뿐이다 — 두 멤버가
같은 4바이트를 공유하므로, `as_float`에 쓰고 `as_int`로 읽으면 *같은
비트를 다른 해석으로* 보게 된다. C 표준은 이 "쓴 멤버와 다른 멤버로
읽기"(타입 퍼닝)를 공용체에 한해 허용한다 — 13장의 포인터 캐스트
방식과 달리 계약 안이라는 점이 결정적 차이다(다만 읽은 값이 그 타입의
유효한 값이 아닐 수 있다는 주의는 남는다).

== 표현을 눈으로 — 엔디안과 패딩

5장에서 "이 확인을 C로 실제로 해 보이는 것이 이 장의 시연"이라 예약해
두었다. 이제 갚는다. 여기서는 공용체 대신 가장 이식성 좋은 방법 —
36장에서 배운 바이트의 눈(`unsigned char`)과 `memcpy` — 을 쓴다.

#demo("examples/ch45/endian.c")

첫 부분이 5장의 그림 그대로다. `0x12345678`이 메모리에 `78 56 34 12`
순서로 놓였다 — 이 책의 검증 기계는 리틀 엔디안이라는 뜻이고, 5장의
도해가 실물로 확인된 것이다. (빅 엔디안 기계에서 이 예제를 돌리면
`12 34 56 78`이 찍히고 판정 문장도 바뀐다 — 코드는 그대로다.)

둘째 부분은 구조체의 숨은 사정이다. `char` 하나(1바이트)와 `int`
하나(4바이트)를 담은 구조체의 크기가 5가 아니라 8이다 — 6장의 정렬
규칙 때문에 `char` 뒤에 3바이트의 *패딩*(padding, 빈틈)이 끼워진 것이다. int
멤버가 4의 배수 자리에서 시작해야 기계가 한 움큼에 집을 수 있기
때문이다(6장). 그래서 실무 관행 하나가 나온다 — *큰 멤버부터 배치하면
빈틈이 줄어든다.* 구조체 수백만 개를 다루는 코드에서는 이 배치 하나가
메모리와 캐시 효율(11장)을 좌우한다. 배치 규칙과, 빈틈을 없애거나 정렬을
강제하는 방법(`pack`·`alignas`)은 44장에서 자세히 다뤘다 — 여기서는 그
빈틈이 *실제로 존재한다*는 사실을 눈으로 확인하는 것이 목적이다.

#realcase[
  빈틈이 흘린 비밀 — 커널의 패딩 정보 유출
][
  패딩은 아무도 쓰지 않는 빈칸이라 무해해 보이지만, 보안의 눈으로 보면
  *초기화되지 않은 기억*이다. 운영체제 커널이 구조체를 사용자 프로그램
  에게 통째로 복사해 줄 때 이 빈틈이 함께 건너가는데 — 멤버들은 전부
  채워 넣었어도 빈틈에는 그 자리에 남아 있던 *다른 데이터의 잔해*가
  들어 있다. 공격자는 이 부스러기를 모아 커널 메모리의 내용이나 주소
  배치를 엿볼 수 있고, 그것이 다음 공격의 발판이 된다. 리눅스를 비롯한
  주요 커널들이 이 부류의 정보 유출 취약점을 수십 건 고쳐 왔고, 오늘의
  대응은 단순하다 — 사용자에게 넘기는 구조체는 *멤버를 채우기 전에
  통째로 0으로 지운다*. 23장에서 "선언과 동시에 초기화한다"고 한 규칙이,
  보이지 않는 빈칸에까지 적용되는 순간이다.
]

#misconception[
  "구조체를 그대로 파일에 쓰거나 네트워크로 보내면 된다"
][
  솔깃한 생각이고, 실제로 많이 시도된 방법이다 — 구조체의 바이트를
  통째로 저장하면 코드가 짧아지니까. 그러나 이 장이 방금 보인 두 사실이
  그것을 가로막는다: 바이트 순서가 기계마다 다르고(엔디안), 빈틈의
  크기와 위치도 컴파일러·플랫폼마다 다르다(패딩). 한 기계에서 쓴 파일이
  다른 기계에서 깨지는 것이다 — 5장의 NUXI 사건이 파일 형식의 세계에서
  재현되는 셈이다. 정답은 *직렬화*(serialization)다: 멤버를 하나씩,
  약속된 크기와 바이트 순서로(네트워크 바이트 순서 — 5장) 적고 읽는
  코드를 명시적으로 쓰는 것. 표현은 기계의 사정이고, 파일과 통신은
  약속의 세계라는 것 — 두 세계를 잇는 다리는 손으로 놓아야 한다.
]

#qa[
  공용체는 그러면 언제 쓰는 것이 정석인가?
][
  두 가지 자리다. 첫째, 방금 본 *표현 들여다보기*(타입 퍼닝) — 부동
  소수점 비트를 검사하거나 하드웨어 레지스터를 여러 눈으로 보는 저수준
  코드다. 둘째, 더 흔한 쓰임인 *태그된 공용체*(tagged union): 구조체
  안에 "지금 어느 멤버가 유효한가"를 알리는 표시(태그)와 공용체를 함께
  넣어, "이 값은 정수이거나, 실수이거나, 문자열이다" 같은 선택형 데이터를
  표현하는 것이다. 인터프리터의 값 표현, 설정 파일 파서 같은 곳의 기본
  도구이고 — 6장에서 본 태그 포인터가 비트 차원의 같은 발상이었다.
  현대 언어들의 열거형(Rust의 enum, Swift의 associated value)이 이
  패턴을 언어 차원으로 끌어올린 것이다.
]

== 비트 필드 — 한 워드를 쪼개 쓰기

구조체 멤버 뒤에 콜론과 수를 적으면 *비트 필드*(bit field)가 된다 —
그 멤버가 차지할 비트 수를 직접 지정하는 것이다. 여기에 공용체를 겹치면
"통째로 한 워드로 보는 눈"과 "필드로 나눠 보는 눈"을 동시에 갖게 된다.

#demo("examples/ch45/bitfield.c")

앞부분이 하드웨어 레지스터를 다루는 전형적인 무늬다. `r.f.mode = 5`처럼
필드에 쓰면 라이브러리 도움 없이도 비트 자리에 값이 들어가고, `r.raw`로
읽으면 그 결과가 한 워드로 보인다. 반대로 `r.raw`에 통째로 쓰고 필드로
읽는 것도 된다 — 출력의 세 번째 줄이 그 확인이다.

편해 보이지만 *이식성의 대가*가 크다. 표준이 정해 주지 않는 것이 많기
때문이다.

- *비트가 놓이는 순서* — 낮은 자리부터 채울지 높은 자리부터 채울지는
  구현 정의(implementation-defined)다. 그래서 같은 선언이 컴파일러마다
  다른 배치를 낼 수 있다.
- *경계를 넘는 필드* — 저장 단위를 걸치는 필드를 허용할지도 구현이
  정한다. 패딩이 얼마나 끼는지도 마찬가지다.
- *부호* — 그냥 `int x : 1;`은 부호 있는 1비트라 값이 0과 −1이 된다.
  의도한 것이 아니라면 `unsigned`를 명시해야 한다.
- *주소를 얻을 수 없다* — 비트 필드에는 `&`를 쓸 수 없다. 배열로 만들
  수도 없다.
- *원자적이지 않다* — 같은 워드에 놓인 두 필드를 서로 다른 스레드가
  건드리면 12장의 거짓 공유와 같은 부류의 사고가 난다.

#misconception[
  "비트 필드로 파일·네트워크 형식을 그대로 표현할 수 있다"
][
  가장 흔한 오해이고, 이식성 사고의 단골이다. 파일 형식이나 프로토콜은
  *바이트와 비트의 배치가 규격으로 정해져 있는데*, 비트 필드의 배치는
  구현이 정한다. 컴파일러를 바꾸거나 다른 기계로 옮기는 순간 필드가
  엉뚱한 자리에서 읽힌다 — 엔디안(앞 절)까지 겹치면 더 나빠진다.
  외부 형식을 다룰 때의 정공법은 *바이트 배열을 놓고 시프트와 마스크로
  직접 뽑아 쓰는 것*이다(7장). 비트 필드는 어디까지나 *한 프로그램 안에서
  기억을 아끼는 용도*로 본다.
]

오늘날 비트 필드를 권하지 않는 이유가 이것이다. 그럼에도 이 문법을 알아야
하는 이유는 분명하다 — 임베디드 SDK의 레지스터 정의, 오래된 코드베이스의
플래그 묶음, 커널 자료구조에서 여전히 만나기 때문이다. *읽을 줄은 알되,
새로 쓸 때는 한 번 더 생각한다*가 실무의 감각이다.

== 구조체와 공용체를 섞는 실무 무늬

예제의 뒷부분은 다른 이야기다. 구조체 안에 태그와 공용체를 함께 넣은
*태그된 공용체*(tagged union)로, 앞의 문답에서 이름만 나온 그 무늬다.

```c
struct message {
    enum msg_kind kind;      /* 어느 눈으로 볼지 알려 주는 태그 */
    unsigned      flags : 4; /* 작은 상태들 — 비트 필드가 여기서 쓰인다 */
    unsigned      urgent : 1;
    union {                  /* 이름 없는 공용체 (C11) */
        int  number;
        char text[16];
        struct { int x, y; } point;
    };
};
```

세 가지가 겹쳐 있다. 태그, 비트 필드로 아낀 상태 플래그, 그리고 *이름
없는 공용체*(anonymous union, C11)다. 이름이 없으면 멤버를 한 단계 덜
거쳐 `m->number`처럼 바로 쓸 수 있어서, 태그된 공용체가 훨씬 읽기 좋아
진다.

규율은 하나뿐이고, 그것이 전부다 — *태그가 말하는 멤버만 읽는다.*
`kind`가 `MSG_TEXT`인데 `number`를 읽으면 45장 첫 절에서 본 "다른 눈으로
보기"가 되어 의미 없는 값이 나온다. 그래서 이런 자료를 다루는 코드는
거의 언제나 예제의 `show`처럼 *태그로 분기하는 `switch` 하나*를 통과
하도록 만든다. 접근을 한 곳에 모으면 규율을 지킬 자리도 한 곳이 된다.

`sizeof(struct message)`가 24바이트로 나온 것도 읽어 둘 만하다 — 태그
4바이트 + 비트 필드가 든 워드 + 16바이트 공용체에, 정렬을 맞추는
패딩(앞 절)이 더해진 결과다. 표현은 언제나 이렇게 *선언한 것보다 조금
더* 차지한다.

=== 실물 하나 — 2바이트 조합형 한글

이 무늬가 교과서 속 이야기만은 아니다. 한글을 컴퓨터에 담던 시절, 한
워드를 셋으로 쪼개 쓰는 방식이 실제 표준이 된 적이 있다 — *조합형*이다.

#demo("examples/ch45/johab.c")

설계는 이 장에서 배운 그대로다. 16비트를 넷으로 나눠, 맨 앞 한 비트는
"이것은 한글"이라는 표시로 쓰고 나머지 15비트를 5비트씩 셋으로 잘라
*초성·중성·종성*에 준다.

#dtable(
  columns: 4,
  keycol: false,
  [*비트*], [*15*], [*14–10 / 9–5 / 4–0*], [*뜻*],
  [자리], [표시], [초성 / 중성 / 종성], [각 5비트],
  [`가` = `0x8861`], [1], [2 / 3 / 1], [ㄱ + ㅏ + (없음)],
  [`한` = `0xD065`], [1], [20 / 3 / 5], [ㅎ + ㅏ + ㄴ],
)

낱자에 붙은 번호에는 규칙이 있다. 초성은 ㄱ부터 ㅎ까지 2–20으로
이어지고(0·1은 채움과 예약), 종성은 "없음"이 1이며 2부터 29까지 이어진다.
중성만 8·9, 16·17, 24·25가 비어 있는데 — 모음을 넉 자리씩 묶어 배치한
흔적이다. 예제의 이름표에서 그 빈자리가 보인다.

이 방식이 준 것은 분명했다. 낱자만 조합하면 *현대 한글 11,172자를 모두*
적을 수 있었다. 같은 시기의 *완성형*(KS C 5601-1987)은 자주 쓰는 2,350자만
표에 담았고, 그래서 "펲시"·"똠방각하" 같은 글자를 아예 적지 못하는 유명한
사고가 있었다. 조합형은 표를 늘리는 대신 *규칙으로 만들어 내는* 길을
택한 것이다.

#realcase("조합형이 남긴 세 가지 교훈")[
  *첫째, 둘째 바이트가 ASCII와 겹친다.* `가`는 `88 61`인데 뒤 바이트
  `0x61`은 그대로 `'a'`다. 그래서 바이트 단위로 `'a'`를 찾는 코드가 글자
  한복판을 짚는다 — 예제의 마지막 줄이 그 오검출을 실제로 보여 준다.
  9장에서 본 "바이트와 글자는 다르다"가 여기서 사고로 나타난다.

  *둘째, 배치는 표준이 정해 주지 않는다.* 예제의 공용체는 이 컴파일러에서
  마침 시프트/마스크와 같은 값을 냈지만, 그것은 이 구현이 비트를 낮은
  자리부터 채우기 때문이다. 앞 절의 규칙 그대로 — *외부 형식은 시프트와
  마스크로 다룬다.*

  *셋째, 규칙이 표를 이긴 자리가 있다.* 유니코드는 조합형과 같은 발상을
  더 단정하게 가져갔다. 한글 음절의 코드는
  `0xAC00 + (초성 * 21 + 중성) * 28 + 종성`으로 *계산된다* — 비트를 자르는
  대신 곱셈으로 자리를 정할 뿐, "낱자를 조합해 글자를 만든다"는 생각은
  같다. 조합형은 사라졌지만(윈도우 95가 완성형 계열의 통합 코드를 택하면서
  자리를 잃었다) 그 발상은 오늘의 표준 속에 남았다.
]

== 제8부를 닫으며

타입을 만드는 두 문법을 얻었다 — 나란히 놓는 구조체(43장)와 겹쳐 놓는
공용체(45장). 그리고 그 과정에서 표현의 현실(엔디안·패딩)을 눈으로
확인했다. 제2부의 배경지식이 문법으로 완전히 회수된 것이다.

다음 부는 정밀의 부다 — 8장에서 배운 근사의 수학이 C의 실수 타입으로
내려오고(47장), 32장에서 씨앗을 심은 계약의 관점이 오류 처리로 자라며
(48장), 이 책이 곳곳에서 예고해 온 "계약 밖"의 세계 — 정의되지 않은
동작 — 를 정면으로 만난다(49장).
