#import "../lib.typ": *

= 가변 인자 함수

#organizer[
  `printf`가 인자를 몇 개든 받을 수 있었던 비밀을 연다. `<stdarg.h>`의
  네 도구, 이 장치가 왜 타입 안전하지 않은지, 그 역사(K&R의 `varargs`
  에서 C89의 `stdarg`, C23의 정리까지), 그리고 오늘의 대안까지. 19장의
  "서식은 계약"이 왜 그토록 엄격한 계약이었는지가 여기서 밝혀진다.
]

#deepqa[
  25장에서 가변 인자로 넘어가는 값에는 기본 진급(float→double,
  작은 정수→int)이 적용된다고 배웠다. 그런데 왜 하필 그 자리에만 특별한
  진급 규칙이 있는가?
][
  받는 쪽이 *타입을 모르기 때문이다.* 보통의 함수는 원형(21장)에 인자
  타입이 적혀 있어 컴파일러가 양쪽을 맞춰 준다. 그러나 가변 인자 자리는
  원형에 `...`뿐이라 맞출 상대가 없다 — 그래서 "일단 다들 큰 타입으로
  통일해서 보내자"는 약속으로 혼란을 줄인 것이다. 이 장은 그 "타입을
  모르는 자리"에서 어떻게 값을 꺼내는지, 그리고 그 대가가 무엇인지의
  이야기다.
]

== 네 개의 도구

`<stdarg.h>`가 주는 것은 타입 하나와 매크로 셋이다 — `va_list`(읽개),
`va_start`(시작), `va_arg`(하나 꺼내기), `va_end`(닫기). 시연으로
본다.

#demo("examples/ch45/va.c")

핵심은 `va_arg(ap, int)`의 둘째 인자다 — *꺼낼 값의 타입을 프로그래머가
알려 준다.* 함수는 무엇이 왔는지 모르므로, "지금 꺼낼 것은 int다"라고
말해 주는 사람이 필요하고 그 사람이 호출자와 약속을 공유한 작성자다.
이 약속이 어긋나면 — 실제로는 double이 왔는데 int로 꺼내면 — 계약
밖이다(42장). 기억의 잘못된 자리를 읽는 것이니 값이 쓰레기가 되거나
그 뒤의 꺼내기가 전부 어긋난다.

그리고 함수는 *인자가 몇 개인지도* 모른다. 그래서 개수를 알아내는 길을
호출 규약으로 만들어야 하는데, 시연이 두 관행을 보인다 — 첫 인자로
개수를 받거나(`sum_n`), 끝을 알리는 표지 값을 약속하거나
(`sum_until_zero`). `printf`는 세 번째 길을 쓴다: *서식 문자열이 개수와
타입을 동시에 알려 주는 명세*인 것이다. 19장에서 "서식은 계약"이라 한
말의 정확한 무게가 이것이다 — 그 계약이 깨지면 printf는 있지도 않은
인자를 꺼내며 메모리를 뒤진다(19장의 서식 문자열 취약점이 바로 이
메커니즘이었다).

== 역사 — varargs에서 stdarg로

이 장치의 역사는 C가 계약의 언어로 자란 과정의 축소판이다.

*표준 이전(K&R 시절).* 원형이 없던 시절(10장)의 C에서는 사실상 모든
함수가 인자 개수를 검사받지 않았다 — `printf` 같은 함수가 특별할 것도
없었다. 인자를 꺼내는 방법도 이식성이 없었다: 첫 인자의 주소에서
스택을 따라 훑는 식의 요령을 각자 썼는데, 인자를 스택이 아니라 레지스터로
넘기는 기계가 나오면서 이 요령이 깨졌다. 그래서 나온 것이 Unix 계열의
`<varargs.h>`였다 — 요령을 매크로로 감싸 기계 차이를 숨긴 첫 시도다.

*C89 — `<stdarg.h>`.* 표준화 위원회가 원형을 도입하면서(10·21장) 가변
인자도 정식 문법을 얻었다: 원형에 `...`을 적고, 값 꺼내기는 `va_*`
매크로로 한다. 옛 `varargs`와의 결정적 차이는 *이름 있는 인자가 최소
하나 필요하다*는 것이다(`va_start`가 그 인자를 기준점으로 삼는다) —
그래서 `printf(const char *fmt, ...)`처럼 서식이 앞에 오는 서명이
표준이 됐다.

*C23 — 다듬기.* 기준점 인자가 필요하다는 제약이 완화되어(`va_start`의
둘째 인자가 선택적) 인자 없는 가변 함수도 적을 수 있게 됐고, 옛
`varargs.h`는 역사 속으로 완전히 사라졌다. 반세기에 걸쳐 "이식성 없는
요령 → 표준 매크로 → 군더더기 제거"로 다듬어 온 것이다.

#misconception[
  "가변 인자 함수는 편리하니 자주 쓰면 좋다"
][
  편리해 보이지만, C에서 가변 인자는 *타입 안전이 사라지는 자리*다.
  컴파일러는 `...` 자리의 인자를 검사하지 않고, 받는 쪽은 타입을
  추측할 방법이 없다. `printf` 계열이 그나마 안전한 것은 컴파일러가
  *특별히* 서식 문자열을 읽어 검사해 주기 때문이지(15장의 경고), 언어의
  일반 규칙 덕분이 아니다 — 직접 만든 가변 인자 함수에는 그 그물이
  없다. 실무 권고는 분명하다: *꼭 필요하지 않으면 만들지 않는다.*
  대안이 대개 더 낫다 — 개수가 정해졌으면 그냥 인자를 나열하고, 여럿
  이면 배열과 길이를 받고(33장의 관행), 종류가 다양하면 39장의 태그된
  공용체로 명시적인 값 목록을 받는다. C 라이브러리 설계의 현대적 취향은
  "가변 인자는 로깅·서식처럼 도구의 검사가 붙는 자리에만"이다.
]

#qa[
  `printf` 같은 서식 함수를 감싸는 내 함수를 만들려면 어떻게 하는가 —
  예컨대 로그 함수?
][
  그 자리를 위해 `v` 붙은 짝이 표준에 준비되어 있다 — `vprintf`,
  `vfprintf`, `vsnprintf`처럼 *`va_list`를 그대로 받는* 판들이다.
  내 함수가 `...`으로 받아 `va_start`로 읽개를 만든 뒤, 그 읽개를
  통째로 넘겨주면 된다. 서식 검사 경고를 내 함수에서도 받고 싶다면
  컴파일러 확장 표기(gcc·clang의 `format` 속성)를 붙이는 것이 관행이다
  — 언어가 못 주는 안전을 도구가 메우는, 이 책에 여러 번 나온 무늬다.
]

가변 인자의 정체를 알았다. 다음 장은 지금까지 쓰기만 해 온 표준
라이브러리의 지형을 펼친다 — 어떤 도구 상자가 있고, 무엇을 믿고 무엇을
조심할 것인가.
