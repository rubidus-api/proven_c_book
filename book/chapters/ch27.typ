#import "../lib.typ": *

= 정수의 연산 — 나눗셈, 비트

#organizer[
#idx("나눗셈")  20장에서 미뤄 둔 약속을 지킨다 — 정수 나눗셈 `/`와 나머지 `%`의
  진실, 특히 음수에서의 규칙까지. 그리고 7장에서 개념으로 배운 비트
  연산들이 C의 연산자로 합류한다. 형변환의 첫걸음도 여기서 뗀다.
]

#deepqa[
  20장에서 "7 / 2는 3.5가 아니다"라고 예고만 했다. 7장의 지식으로
  스스로 답해 보면 — 정수 그릇끼리의 나눗셈 결과가 3.5일 수 *없는*
  이유는 무엇인가?
][
  3.5를 담을 자리가 없기 때문이다. 정수 타입의 값 집합에는 3과 4만
  있고 그 사이가 없다 — 그래서 정수끼리의 나눗셈은 결과도 정수여야
  하고, 소수 부분을 어느 쪽으로든 *버려야* 한다. 문제는 "어느 쪽으로"다
  — 그 규칙이 이 장의 첫 절이다.
]

== 나눗셈과 나머지 — 버림의 방향

시연부터 본다.

#demo("examples/ch27/divmod.c")

양수는 직관대로다 — `7 / 2`는 3, 나머지는 1. 규칙이 필요한 곳은
음수다. C는 *0을 향해 버린다*(truncation toward zero) — `-7 / 2`는
$-3.5$에서 0 쪽으로 버린 $-3$이고, 나머지는 그에 맞춰 $-1$이다.
"수학의 몫·나머지(나머지는 항상 0 이상)"와 다른 선택이라서, 음수
나머지를 쓰는 코드는 이 차이를 모르면 사고가 난다.

#mathbox[
  나눗셈-나머지 복원 불변식
][
  방향이 무엇이든, C가 반드시 지키는 등식이 하나 있다:

  $ (a / b) times b + (a % b) = a $

  몫과 나머지는 이 등식을 만족하도록 *한 쌍으로* 정의된다 — 시연의
  마지막 줄이 이 등식의 확인이다. 0을 향한 버림을 택하면 나머지의
  부호가 피제수($a$)를 따르게 되는 것도 이 등식의 귀결이다. 덧붙여
  역사 한 줄 — C89까지는 버림의 방향이 구현마다 달라도 됐고, C99가
  "0을 향해"로 못박았다. 7장에서 본 "기계들이 갈리면 표준은 비워
  둔다"가, 기계들이 수렴하자 약속으로 승격된 또 하나의 사례다.
]

#qa[
  0으로 나누면 어떻게 되는가?
][
  계약 밖 — 정의되지 않은 동작이다. 수학에서 정의되지 않은 것이 C
  에서도 정의되지 않은 드문 일치 사례인데, 결과는 수학처럼 얌전하지
  않다: 많은 기계에서 프로그램이 그 자리에서 붕괴하고, 어떤 최적화
  아래서는 더 기묘한 일도 벌어진다(46장). 나누기 전에 0인지 확인하는
  것은 프로그래머의 몫이고 — 30장에서 분기를 배웠으니 이제 그 확인을
  코드로 적을 수 있게 된다.
]

== 비트 연산 — 7장의 세계, C의 문법

7장에서 개념으로 배운 비트 다루기가 연산자로 합류한다 — AND `&`,
OR `|`, XOR `^`, 반전 `~`, 그리고 시프트 `<<` `>>`. 시연의 뒷부분이
그 맛보기다: `5 & 3`은 `101 & 011 = 001`이라 1, `5 | 3`은 `111`이라
7, `1 << 4`는 7장의 약속대로 $2^4 = 16$이다.

실무의 기본 무늬는 7장에서 예고한 그대로 *시프트+마스크*다 — 원하는
자리로 밀고(`<<`, `>>`), 필요한 비트만 남긴다(`&`). 다만 두 가지
수칙을 함께 챙긴다. 첫째, *비트 연산은 부호 없는 타입에서 한다* —
부호 있는 수의 시프트에는 7장에서 본 함정들(폭 이상, 음수 왼쪽
시프트=계약 밖)이 있어서, `unsigned`나 `uint32_t` 위에서 노는 것이
안전 관행이다. 둘째, `&`(비트 AND)와 `&&`(논리 AND, 다음 장)는 완전히
다른 연산자다 — 한 글자 차이가 값 전체를 바꾼다.

== 형변환 — 그릇 사이를 건너다

타입 가족(26장)이 생기니 새 질문이 따라온다 — 다른 그릇끼리 섞어
계산하면 무슨 일이 일어나는가. C의 답은 *암묵 변환*이다: 작은 그릇의
#idx("부호 확장")값은 계산 전에 자동으로 더 큰 그릇으로 넓혀지고(7장의 부호 확장이
바로 이때 일어나는 일이다), 정수와 실수가 만나면 정수가 실수로
승격된다. 대부분은 뜻대로 되지만, 자동이라는 것은 *안 보인다*는
뜻이기도 하다 — 특히 부호 있는 것과 없는 것이 섞이는 비교는 고전적
함정이라(음수가 거대한 양수로 둔갑한다), 컴파일러 경고(-Wall)가 지켜
주는 대표 지점이다.

변환을 *명시*하고 싶으면 캐스트 표기를 쓴다 — `(double)7 / 2`는
"7을 실수 그릇으로 옮긴 뒤 나눠라"이므로 3.5가 된다. 규칙의 전모
(정수 승격, 통상 산술 변환)는 부록 참조 자료로 두고, 본문 수칙은
둘이다: *섞이는 계산은 의도를 캐스트로 명시하고, 부호 섞인 비교는
피한다.*

정수의 그릇(26장)과 연산(27장)을 갖췄다. 다음 장부터는 *흐름*이다 —
비교하고 판단하는 값, 불리언부터.
