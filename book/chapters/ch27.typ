#import "../lib.typ": *

= 결정 — if와 switch

#organizer[
  프로그램이 갈림길을 만난다. 조건에 따라 다른 길을 가는 `if`와, 여러
  갈래를 한 번에 벌이는 `switch` — 두 분기 장치를 익힌다. 2장의 계산
  모델("결과가 0이면 건너뛰어라")이 드디어 C 문법의 얼굴로 나타나는
  장이다.
]

#deepqa[
  10장에서 "if 는 공짜가 아니다"라는 감각을 예고했다 — 분기 예측이
  틀리면 파이프라인을 비우는 벌금을 문다고. 그러면 프로그래머는 분기를
  피해야 하는가?
][
  아니다 — 분기는 프로그램의 뼈대이고, 예측기는 일상 코드에서 90%
  넘게 맞힌다. 그 감각의 쓸모는 "분기를 쓰지 말라"가 아니라 *분기의
  비용이 일정하지 않음을 아는 것*이다 — 규칙적인 갈림길은 싸고,
  무작위한 갈림길은 비싸다(정렬된 배열 사건). 성능이 문제 되는 극히
  일부 코드에서만 꺼내는 지식이고, 오늘 배우는 분기는 마음껏 쓰면
  된다.
]

== if — 조건이 참이면

문법은 읽는 그대로다:

```c
if (조건) {
    조건이 참일 때의 문장들
} else {
    거짓일 때의 문장들   /* else 부분은 없어도 된다 */
}
```

조건 자리에는 bool이 되는 수식(26장)이 오고, 갈래마다 블록(16장)이
온다. 갈래가 여럿이면 `else if`로 사다리를 만든다 — 시연이 그
모습이다. 22장의 입력 관행(fgets+sscanf)이 다시 나오는 것도 눈여겨보면
된다.

#demo("examples/ch27/grade.c", stdin: true)

사다리는 *위에서부터 차례로* 검사되고, 처음 참이 되는 갈래 하나만
실행된다 — 87점이 "우"에서 멈추고 아래로 내려가지 않는 것이 그
보장이다. 그래서 사다리의 순서가 곧 논리다: 좁은 조건을 위에, 넓은
조건을 아래에 두는 것이 정석이다.

#qa[
  갈래가 하나뿐이면 중괄호를 생략해도 된다고 들었다 — 써도 되는가?
][
  문법상 허용된다 — 그리고 이 책은 쓰지 않기를 권한다. 중괄호 없는
  분기는 "다음 문장 하나만" 갈래에 속하는데, 나중에 문장을 한 줄 보태며
  중괄호 없음을 잊는 실수가 고전적 사고 경로다. 실제로 애플의 SSL
  라이브러리에서 이 무늬로 검증 코드가 분기 밖으로 밀려나 보안 검사가
  통째로 건너뛰어진 사건("goto fail" 사고, 2014)이 있었다 — 들여쓰기는
  사람 눈에만 보이고 컴파일러에게는 안 보인다는 것(16장)이 값비싸게
  증명된 날이다. 갈래에는 언제나 중괄호 — 이 책의 전 예제가 따르는
  수칙이다.
]

== switch — 값으로 갈라서는 판

갈림길이 "어느 범위인가"가 아니라 "어느 *값*인가"로 갈릴 때는 전용
장치가 따로 있다 — `switch`다.

#demo("examples/ch27/season.c", stdin: true)

읽는 법 — `switch (month)`가 값을 하나 들고 판에 들어서면, 실행은
그 값과 일치하는 `case` 이름표로 *건너뛴다*. 그리고 여기가 switch의
가장 중요한 성질이다: `case`는 담장이 아니라 *이름표*라서, 일단
건너뛴 뒤에는 다음 `case`를 지나쳐 *아래로 계속 흐른다*. 이 흐름을
*폴스루*(fall-through)라 부르고, 멈추고 싶은 자리에 `break`(판을
나가라)를 명시한다.

폴스루는 양날이다. 시연처럼 여러 값이 한 답을 공유하게 묶는 데는
제격이지만(12·1·2월이 겨울 하나로), `break`를 *잊으면* 의도치 않은
갈래까지 흘러 들어가는 고전적 사고가 된다 — 그래서 컴파일러 경고와
C23의 명시 표기(`[[fallthrough]]` — "의도된 흐름임"을 적는 문서)가
이 자리를 지킨다. `default`는 어느 이름표에도 안 맞을 때의 갈래다 —
"그런 달은 없다"처럼, *예상 밖의 값을 잡는 그물*로 항상 두는 것이
관행이다.

#qa[
  `if`의 사다리로도 다 되는데, `switch`가 굳이 필요한 이유는 무엇인가?
][
  둘의 차이는 표현력이 아니라 *의도의 전달*이다. switch는 "하나의 값을
  여러 상수와 맞춰 본다"는 의도를 문법으로 못박아서, 읽는 사람과
  컴파일러 모두에게 구조가 보인다 — 컴파일러는 그 구조를 이용해 점프
  표 같은 빠른 코드로 바꾸기도 하고(11장의 편집자가 좋아하는 모양이다),
  경고 기능은 "열거된 값 중 빠진 case"를 짚어 줄 수 있다. 값 하나로
  갈라서면 switch, 범위·복합 조건이면 if 사다리 — 도구를 의도에 맞추는
  것이다. 그리고 다음 장에서, 이 폴스루라는 성질을 극한까지 악용(?)한
  전설의 코드를 드디어 만난다.
]

갈림길을 배웠다 — 그러나 프로그램의 진짜 힘은 갈라서는 것이 아니라
*되풀이하는 것*에서 나온다. 2장에서 "단순한 걸음도 수십억 번이면"이라
#idx("반복문")했던 그 반복을, 다음 장에서 손에 넣는다.
