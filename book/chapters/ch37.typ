#import "../lib.typ": *

= 동적 메모리

#organizer[
  제7부의 마지막 자리 — 실행 중에 크기를 정하고, 원하는 때까지 살려
  두는 기억이다. `malloc`과 `free`, 소유라는 개념, 그리고 이 세계의
  세 가지 대표 사고(누수·이중 해제·해제 후 사용)까지. 기억의 지도가
  여기서 완성된다.
]

#deepqa[
  36장에서 자동 수명(함수와 함께 죽는다)과 정적 수명(프로그램 내내
  산다)을 배웠다. 그러면 "입력에 따라 크기가 정해지고, 필요한 동안만
  살아야 하는" 기억은 — 둘 중 어디에 두는가?
][
  어느 쪽도 맞지 않는다. 크기가 실행 중에 정해지고 수명이 프로그램의
  논리에 따라 갈리는 기억이 필요하다 — 그래서 세 번째 자리가 있다:
  *동적 저장 기간*, 흔히 *힙*(heap)이라 부르는 창고다. 이 자리의 규칙은
  앞의 둘과 결정적으로 다르다 — *태어남과 죽음을 프로그래머가 직접
  지시한다.*
]

== 빌리고, 돌려준다

문법은 함수 두 개다. `malloc(바이트 수)`는 창고에서 그만큼의 연속된
칸을 빌려 그 시작 주소를 돌려주고, `free(주소)`는 빌린 것을 돌려준다.

#demo("examples/ch37/dyn.c")

세 가지 관행이 시연에 박혀 있다.

*크기 계산은 `sizeof *포인터`로.* `count * sizeof *scores`는 "원소
하나의 크기 × 개수"인데, 타입 이름 대신 *대상*으로 크기를 물었다 —
나중에 타입이 바뀌어도 이 줄은 고칠 필요가 없다.

*빌리기는 실패할 수 있다.* `malloc`은 창고가 부족하면 널 포인터를
돌려준다 — 그래서 *쓰기 전에 확인한다*(31장의 수칙이 여기서 실전이
된다). 확인 없이 쓰면 널 역참조로 붕괴다.

*빌린 것은 돌려준다.* `free`를 잊으면 그 칸은 프로그램이 끝날 때까지
묶여 있다 — *메모리 누수*(leak)다. 짧은 프로그램에서는 티가 안 나지만,
오래 도는 서버에서는 조금씩 새어 나가 결국 기계를 잡아먹는다.

== 소유 — 누가 돌려줄 책임을 지는가

`malloc`이 준 주소는 여러 변수에 복사될 수 있고, 함수 사이를 오갈 수도
있다. 그런데 `free`는 *정확히 한 번* 불려야 한다 — 여기서 C 프로그래밍
의 핵심 규율이 나온다: 어느 시점에든 그 기억을 해제할 책임을 지는 주체
하나를 정해 두는 것, *소유*(ownership)라는 관념이다. C에는 소유를
강제하는 문법이 없다 — 그래서 소유는 *주석과 이름과 규약*으로 표현
된다("이 함수는 반환한 포인터의 소유권을 넘긴다"처럼). 현대 언어들이
소유를 타입 체계로 끌어올린 것(Rust의 소유권, C++의 스마트 포인터)은
이 규율을 기계가 강제하게 만든 결과다 — 1장에서 본 이웃 언어들의
문제의식이 바로 이 자리에서 나왔다.

#realcase[
  세 가지 대표 사고 — 누수, 이중 해제, 해제 후 사용
][
  동적 메모리의 사고는 세 얼굴로 온다. *누수*는 돌려주기를 잊는 것 —
  느리게 죽는 병이다. *이중 해제*(double free)는 같은 주소를 두 번
  `free`하는 것으로, 창고 관리 장부를 망가뜨려 그 뒤의 모든 할당이
  오염된다. 가장 위험한 것은 *해제 후 사용*(use-after-free) — 돌려준
  칸의 주소를 계속 쓰는 것이다. 36장의 댕글링 포인터가 힙에서 재현되는
  것인데, 창고는 그 칸을 곧 다른 요청에 내주므로 *공격자가 그 자리에
  자기 데이터를 놓을* 수 있다. 브라우저와 커널의 심각한 취약점 목록에서
  use-after-free는 오늘도 최상위 단골이고, 그래서 현대 시스템 프로그래밍
  전체가 "소유를 언어가 강제하게 하자"는 방향으로 움직여 왔다. C에서의
  방어는 규율 + 도구다 — `free` 직후 포인터에 `nullptr`를 대입하는
  관행, 그리고 15장의 ASan(이 세 사고를 모두 잡아내는 것이 ASan의
  주특기다).
]

#qa[
  그러면 동적 메모리를 되도록 피하는 것이 상책인가?
][
  실제로 그것이 첫 번째 전략이고, 이 책이 여기까지 `malloc` 없이 온
  이유이기도 하다 — 크기를 아는 데이터는 자동 변수(스택)로 두는 것이
  가장 빠르고 가장 안전하다(해제할 것이 없으니 세 사고가 원천 봉쇄된다).
  임베디드나 안전 필수 분야에서는 아예 동적 할당을 금지하는 규약도
  흔하다. 그러나 실행 중에 크기가 정해지는 데이터(사용자가 준 목록,
  파일 내용)는 결국 창고가 필요하고 — 그때는 소유를 명확히 하고,
  도구로 검사하고, 잘 만들어진 부품(35장의 proven처럼 할당과 경계를
  함께 관리하는 계열)을 쓰는 것이 실무의 답이다.
]

== 제7부를 닫으며

기억의 지도가 완성됐다 — 레지스터와 캐시라는 사다리(9장) 위에, C의
세 저장 기간(자동·정적·동적)이 놓이고, 그 위를 포인터가 다닌다.
30장에서 개념을 얻고, 30~32장에서 규칙을, 32~34장에서 연속된 기억과
문자열을, 35장에서 안전한 부품을, 35~37장에서 수명과 창고를 배웠다.
C의 힘과 위험이 함께 사는 자리를 한 바퀴 돈 것이다.

다음 부는 짧지만 오래 미뤄 온 곳이다 — 제5부에서 "타입을 만드는
선언은 메모리 모델을 갖춘 뒤"라며 미뤄 두었던 구조체와 공용체. 이제
그 조건이 갖춰졌다.
