#import "../lib.typ": *

= 기억의 분화 — 레지스터, 캐시, 다층의 사다리

#prereq(
  ([4장 단순한 기계 모델], [기계는 기억에서 값을 가져와 계산한다]),
  ([5장 워드와 주소], [주소 공간]),
)

#organizer[
  고백으로 시작하는 장이다 — 4장의 세 부품 그림이 사실 얼마나 과감한
#idx("레지스터")  단순화였는지 밝히고, 그림을 수리한다. CPU 안에 원래부터 있던 레지스터,
  바깥의 큰 메모리, 그리고 둘 사이의 속도 격차가 벌어지며 그 틈에
#idx("캐시")  끼어든 다층의 캐시까지 — 기억이 하나가 아니라 사다리라는 것을 알게
  된다.
]

#deepqa[
  4장에서 "CPU가 메모리에서 다음 할 일을 가져와 그대로 한다"고 했다.
  그러면 계산 자체도 메모리 위에서 하는가 — 100번 칸과 104번 칸을
  더한 결과가 바로 108번 칸으로 가는가?
][
  아니다 — 그리고 이 질문이 이 장의 문을 연다. CPU는 메모리 위에서
  직접 계산하지 않는다. 계산은 CPU *안*의 별도 공간에서 일어난다.
  4장의 그림에는 그 공간이 아예 그려져 있지 않았다. 이제 정직해질
  시간이다.
]

#chapter-questions()

== 고백 — 그 그림은 과하게 단순했다

4장의 CPU·메모리·클록 모델은 배움의 첫 계단으로는 훌륭하지만, 오늘의
컴퓨터에 대해서는 *과하게 간소화된* 그림이다. 어느 정도인가 하면 —
그 그림대로라면 오늘의 CPU는 시간 대부분을 *멍하니 기다리며* 보내야
한다. 클록은 반세기 동안 수십만 배 빨라졌는데 메모리는 그만큼 빨라지지
못했기 때문이다. 그런데 실제 컴퓨터는 멍하니 있지 않다. 그 사이에 무슨
장치가 들어섰는지가 이 장과 다음 장의 이야기다. 먼저, 처음부터 그림에서
빠져 있던 것부터 채워 넣는다.

== 레지스터 — 원래부터 있던, CPU의 손안

CPU 안에는 *레지스터*(register)라는 작은 기억 장소들이 있다. 처음부터
있었다 — 4장의 그림이 생략했을 뿐이다. 개수는 겨우 수십 개, 크기는
워드 하나씩(5장). 대신 속도는 *즉시*다. 클록 박자 안에서 바로 읽고
쓴다. 손안에 쥔 동전이라 해도 좋다 — 주머니(메모리)보다 가깝고, 꺼내는
데 시간이 걸리지 않는다.

중요한 것은 이것이다. *모든 계산은 레지스터 위에서 일어난다.* 두 수를
더하려면 먼저 메모리에서 레지스터로 가져오고(load), 레지스터끼리
더하고, 결과를 메모리로 되돌린다(store). "100번 칸 + 104번 칸"처럼
보이는 계산의 실제 동선은 언제나 [메모리 → 레지스터 → 계산 →
메모리]다.

#qa[
  그렇게 중요한 것이 왜 겨우 수십 개뿐인가? 잔뜩 만들면 안 되나?
][
  빠름과 많음은 맞바꿈 관계이기 때문이다. 레지스터가 즉시인 이유는 CPU의
  계산 회로 바로 곁에, 가장 비싼 방식으로 만들어져 있어서다. 수를 늘리면
  거리가 멀어지고 고르는 시간이 붙어서 "즉시"가 무너진다. 그래서 설계는
  "아주 빠른 것을 조금"으로 귀결됐고 — 이 맞바꿈이 이 장 전체를 지배하는
  원리다: *빠른 기억일수록 작고, 큰 기억일수록 느리다.*
]

C의 표면에도 이 지층의 흔적이 있다. C에는 `register`라는 키워드가
있는데 — "이 변수를 되도록 레지스터에 두라"는 옛 시절의 부탁이다.
오늘날에는 컴파일러가 사람보다 배치를 훨씬 잘해서 이 부탁은 장식이
됐지만, 변수를 누가 레지스터에 올리고 언제 메모리로 내리는가라는 문제
자체는 여전히 살아 있고, 13장(컴파일러 최적화)의 핵심 소재가 된다.

== 벌어지는 격차 — 레지스터와 메모리 사이

이제 두 기억을 나란히 세워 보면 그림이 선명해진다.

- *레지스터*: 수십 개 × 워드 크기. 즉시.
- *메모리(DRAM)*: 수백억 칸. 오늘의 CPU 기준으로 *수백 클록 박자*를
  기다려야 답이 온다.

처음부터 이랬던 것은 아니다. C가 태어난 시절(4장)에는 CPU와 메모리의
걸음이 얼추 맞았다. 그런데 이후 수십 년, CPU의 클록은 폭주하듯
빨라졌고 메모리는 커지는 쪽으로 진화하느라 속도가 그만큼 따라오지
못했다. 격차는 해마다 벌어져 수백 배가 됐다 — 손안의 동전과, 왕복
한 시간짜리 창고의 관계가 된 것이다. 계산은 즉시인데 재료 조달이 수백
박자라면, 기계는 일하는 시간보다 기다리는 시간이 길어진다.

#misconception[
  "메모리 접근은 어디를 읽든 같은 비용이다"
][
  단순한 그림이 심어 주는 자연스러운 오개념이고, 프로그램의 속도를
  이해하려 할 때 가장 먼저 부수어야 할 착각이다. 실제로는 방금 만졌던
  근처를 다시 만지는 것과 처음 가 보는 먼 곳을 만지는 것의 비용이
  수십–수백 배 다르다 — 왜 그런지가 바로 다음 절의 캐시다. 같은 일을
  하는 두 프로그램이 메모리를 *어떤 순서로 만지느냐*만으로 몇 배씩
  차이 나는 이유가 여기에 있다.
]

== 캐시 — 격차의 틈에 끼어든 중간층

수백 배 격차의 해법으로 공학이 내놓은 것이 *캐시*(cache)다. 착상은
도서관에서 일하는 사람의 요령 그대로다. 서고(메모리)까지 왕복이
느리다면 — *방금 가져온 책을 책상 위에 얼마간 두라.* 다시 찾을 때
책상에 있으면(적중) 즉시고, 없으면(미스) 그때만 서고에 다녀온다.

이것이 통하는 이유는 프로그램의 버릇 때문이다. 프로그램은 메모리를
아무 데나 무작위로 만지지 않는다 — *방금 만진 곳을 곧 다시 만지고*
#idx("지역성") (시간 지역성, temporal locality), *만진 곳의 이웃을 이어서
만진다*(공간 지역성, spatial locality). 루프가
같은 변수를 반복해 만지고, 배열을 앞에서부터 차례로 훑는 것을 떠올리면
된다. 이 버릇 덕에, 작은 책상 하나로도 서고 왕복의 대부분을 없앨 수
있다.

공간 지역성을 활용하는 요령이 하나 더 있다. 캐시는 메모리에서 물건을
*한 칸씩 가져오지 않는다*. 요청한 칸의 이웃까지 묶어 — 오늘날 대개
64바이트, 사물함 예순네 칸을 상자째 — 한 번에 나른다. 이 운반 단위가
#idx("캐시 라인")*캐시 라인*(cache line)이다. 배열을 차례로 훑을 때 첫 칸에서 한 번만
서고에 다녀오면 다음 예순세 칸이 공짜인 이유이고, 거꾸로 멀리멀리
건너뛰며 만지는 프로그램은 상자를 매번 새로 나르느라 느려지는 이유다.
이 "상자" 개념은 다음 장에서 뜻밖의 사고(false sharing)의 주인공으로
다시 나온다.

== 캐시의 분화 — 다층의 사다리

캐시에도 레지스터와 같은 맞바꿈이 적용된다 — 빠르려면 작아야 하고,
크려면 느려진다. 그래서 캐시는 하나가 아니라 *층*으로 분화했다. CPU
바로 곁의 작고 가장 빠른 L1, 그 뒤의 중간 L2, 여러 코어가 함께 쓰는
크고 상대적으로 느린 L3 — 그리고 그 너머의 메모리. 대략의 감각만
숫자로 적으면 이렇다(기계마다 다르다).

#dtable(
  columns: 4,
  [*층*], [*크기의 규모*], [*대략의 대기*], [*비유*],
  [레지스터], [수십 개], [0 (즉시)], [손안의 동전],
  [L1 캐시], [수십 KiB], [박자 몇 개], [책상 위],
  [L2 캐시], [수백 KiB–MiB], [십수 박자], [책장],
  [L3 캐시], [수–수십 MiB], [수십 박자], [같은 층 서가],
  [메모리(DRAM)], [수십 GiB], [수백 박자], [창고],
)

읽는 법은 하나다 — *아래로 한 층 내려갈 때마다 크게 느려진다.* 기억은
"한 덩어리"가 아니라 이 사다리 전체이고, 프로그램의 속도는 상당 부분
"필요한 것이 사다리의 몇 층에서 발견되는가"로 정해진다.

#realcase[
  같은 일, 다섯 배의 시간 — 2차원 표 훑기
][
  큰 2차원 표(예: 수천×수천의 수)를 전부 더하는 단순한 일을 생각하면
  된다. 행을 따라 — 메모리에 놓인 순서 그대로 — 훑으면 캐시 라인
  상자가 매번 알뜰하게 쓰인다. 열을 따라 — 매번 멀리 건너뛰며 —
  훑으면 상자를 나르고 버리기를 반복한다. 하는 일(덧셈의 횟수)은 완전히
  같은데, 실측하면 몇 배에서 수십 배까지 차이가 난다. 코드 두 줄의 순서
  차이가 낳는 이 격차는, 기억이 사다리라는 사실을 모르면 설명할 길이
  없다 — 그리고 이런 실측을 이 책의 예제로 직접 보게 된다(31장).
]

#qa[
  프로그래머가 캐시를 직접 조종할 수는 없는가 — "이건 L1에 넣어 둬"
  같은 지시는?
][
  일반적으로는 없다. 캐시는 하드웨어가 자동으로 운용하고, C 언어에도
  캐시를 직접 만지는 표준 문법은 없다. 프로그래머가 하는 일은 조종이
  아니라 *협조*다 — 데이터를 이웃끼리 모아 두고, 만질 때 순서대로
  만지는 것. 기억이 사다리임을 아는 사람의 코드는 자연스럽게 그렇게
  생긴다. 그것으로 충분히, 방금 본 몇 배의 차이가 난다.
]

그림 수리의 전반부가 끝났다 — 기억은 하나가 아니라 레지스터에서
창고까지의 사다리다. 그러나 아직 절반이다. CPU는 기다림을 줄이는 것으로
만족하지 않고, *실행 자체를 겹치는* 쪽으로도 진화했다 — 조립 라인처럼
명령들을 포개고, 갈림길을 미리 짐작하고, 끝내 일꾼(코어)의 수를 늘렸다.
그 이야기, 그리고 그 요동의 한복판에서 C가 처음으로 계약서(C89)를 쓰게
된 사연이 다음 장이다.
