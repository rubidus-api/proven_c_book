#import "../lib.typ": *

= 기억의 구역들 — 프로그램은 어디에 무엇을 두는가

#prereq(
  ([1장 배경설명], [프로그래밍은 단순한 지시의 목록을 적는 일이다]),
)

#deepqa[
  1장에서 프로그래밍이란 "단순한 지시들의 목록을 적는 일"이라 했다. 그러면
  그 지시들과, 지시가 다루는 값들은 실행 중에 어디에 있는가?
][
  둘 다 기억(메모리) 안에 있다 — 그런데 *같은 자리에 섞여 있지는 않다*.
  프로그램의 기억은 성격이 다른 구역들로 나뉘고, 구역마다 규칙이 다르다.
  어떤 구역은 프로그램이 끝날 때까지 그대로 있고, 어떤 구역은 함수 하나가
  도는 동안만 살며, 어떤 구역은 프로그래머가 직접 빌리고 돌려준다. 이
  지도를 먼저 그려 두면 이후의 이야기가 전부 제자리를 찾는다.
]

#organizer[
#idx("메모리 구역")  프로그램이 쓰는 기억이 한 덩어리가 아니라 *용도가 다른 몇 구역*으로
  나뉘어 있다는 것을 먼저 익힌다. 코드, 붙박이 값, 작업대(스택), 창고(힙) —
  이름과 성격만 먼저 알아 두면 이후의 거의 모든 장이 이 지도 위에서 읽힌다.
  아직 C 문법은 하나도 나오지 않는다.
]

#chapter-questions()

#figure-svg("regions", caption: [한 프로그램이 쓰는 기억의 지도. 이 그림 위에서 이후의 거의 모든 장이 읽힌다.])

== 네 구역, 하나의 비유

책상에서 일하는 사람을 떠올리면 그림이 선다.

#dtable(
  columns: 4,
  [*구역*], [*비유*], [*무엇이 놓이는가*], [*언제까지 사는가*],
  [코드], [작업 지시서], [프로그램의 명령들], [프로그램 내내(대개 못 고친다)],
  [정적 구역], [붙박이 선반], [프로그램 전체가 공유하는 값], [프로그램 내내],
  [스택], [작업대], [지금 하는 일에 필요한 임시 값], [그 일이 끝날 때까지],
  [힙], [창고], [크기·수명을 직접 정하는 값], [돌려줄 때까지],
)

*코드*는 읽기만 한다. 지시서를 실행 중에 고쳐 쓰는 일은 보통 금지되어 있다.

*정적 구역*은 프로그램이 시작될 때 자리를 잡고 끝날 때까지 그대로다. 어디서든
보이고 언제나 살아 있다는 것이 장점이자 위험이다 — 편리하지만, 아무 데서나
바뀔 수 있는 값은 추적하기 어렵다.

*스택*은 작업대다. 일 하나를 시작하면 그 일에 필요한 것들을 작업대에 펼치고,
일이 끝나면 통째로 치운다. 빠르고 자동이지만 *좁다* — 그리고 치운 뒤에는
그 자리의 물건을 다시 찾을 수 없다.

*힙*은 창고다. "이만큼 빌려 주시오"라고 청하면 자리를 내주고, 다 쓰면
돌려줘야 한다. 크기를 실행 중에 정할 수 있고 원하는 만큼 오래 살릴 수 있는
대신, *빌린 것을 돌려주는 책임*이 사람에게 있다.

#qa[
  왜 굳이 나누는가? 기억은 그냥 한 덩어리 아닌가?
][
  물리적으로는 그렇다 — 5장에서 볼 사물함 복도 하나다. 나누는 것은 *약속*
  이고, 그 약속이 두 가지를 준다.

  첫째, *자동 관리*다. 함수 하나가 쓰는 임시 값들을 한 구역(스택)에 모아
  두면, 그 함수가 끝날 때 통째로 치울 수 있다. 하나하나 챙길 필요가 없다.

  둘째, *보호*다. 코드 구역을 읽기 전용으로 두면 프로그램이 실수로(또는
  공격자가 일부러) 자기 명령을 고쳐 쓰는 일을 막을 수 있다. 구역마다 권한을
  다르게 주는 것이 현대 운영체제의 기본 방어다.
]

== 정적 구역이 둘로 나뉘는 이유 — `bss`라는 이상한 이름

#idx("bss")실무에서는 정적 구역이 다시 둘로 갈린다. 이름이 낯설지만 사연은
단순하다.

- *초깃값이 있는 것* — 예컨대 "이 값은 처음에 7이다"라고 정해 둔 것. 그 7이
  실행 파일 안에 실제로 들어 있어야 한다.
- *초깃값이 0인 것* — 처음 값이 0이라면, 파일에 0을 잔뜩 적어 둘 이유가 없다.
  *"이만큼을 0으로 채워 달라"는 숫자 하나*만 적어 두고, 실제로 채우는 일은
  프로그램이 시작될 때 한다.

두 번째 구역의 이름이 `bss`다. 1950년대 어셈블러 명령어의 약자에서 왔고,
뜻은 잊혔지만 이름만 반세기를 건너왔다. 이 구별 덕에 "백만 칸짜리 표를
전부 0으로 시작한다"는 프로그램의 실행 파일이 커지지 않는다.

#qa[
  그러면 "초기화하지 않은 값은 0"이라는 말은 어디서 나온 규칙인가?
][
  *정적 구역에 한해서* 그렇다 — 그리고 그 0은 공짜가 아니라 *누군가 채워 준
  것*이다. 운영체제가 0으로 채워진 자리를 주거나, 운영체제가 없는 기계에서는
  프로그램의 시작 코드가 직접 루프를 돌아 0으로 채운다.

  반대로 *작업대(스택)의 임시 값은 0이 아니다.* 방금 다른 일이 쓰고 간 자리를
  그대로 물려받으므로, 무엇이 남아 있을지 알 수 없다. 이 차이가 C를 배우는
  사람이 가장 자주 넘어지는 자리 중 하나이고, 43장에서 정식으로 다룬다.
]

== 세 구역의 성격 대조

앞으로 계속 쓸 표다. 지금은 느낌만 잡아 두면 된다.

#dtable(
  columns: 4,
  [], [*정적*], [*스택*], [*힙*],
  [크기 정하는 때], [번역할 때], [번역할 때(대개)], [*실행 중에*],
  [수명], [프로그램 내내], [그 일이 끝날 때까지], [돌려줄 때까지],
  [정리], [자동], [자동], [*사람이 직접*],
  [속도], [빠름], [가장 빠름], [상대적으로 느림],
  [크기 여유], [고정], [*좁다*(대개 몇 MiB)], [넓다],
  [흔한 사고], [아무 데서나 바뀜], [넘침·죽은 자리 참조], [누수·이중 반납],
)

이 표의 마지막 줄이 이 책의 뒷부분 상당 분량을 차지한다. C가 강력하면서
위험하다는 평판은 대부분 이 세 구역의 규칙을 어겼을 때 벌어지는 일에서
나온다.

#realcase[
  작업대가 좁다는 것의 실감
][
  창고(힙)가 수십 기가바이트인 오늘의 컴퓨터에서도 작업대(스택)는 좁다.
  흔한 기본값이 리눅스에서 8 MiB, 윈도우에서 1 MiB다 — 전체 기억의
  수천분의 일이다.

  그래서 초보자가 겪는 첫 붕괴가 대개 여기서 난다. 큰 표를 작업대에 통째로
  펼치려 하거나, 자기 자신을 끝없이 부르는 프로그램을 짜면 작업대가 넘치고
  프로그램이 그 자리에서 죽는다 — 이름도 그대로 *스택 오버플로*다(세계에서
  가장 유명한 프로그래밍 질문 사이트의 이름이 여기서 왔다). 자세한 숫자와
  대처는 43장과 81장에서 다룬다.
]

#misconception[
  "메모리를 많이 쓰면 프로그램이 느려진다"
][
  절반만 맞다. 문제는 양이 아니라 *어느 구역을 어떻게 쓰느냐*다. 작업대에서
  잠깐 쓰고 치우는 값은 아무리 많아도 거의 공짜이고, 창고에서 빌리고
  돌려주기를 반복하면 양이 적어도 눈에 띄게 느려진다(44장에서 실측한다).
  그리고 11장에서 볼 이유로, *같은 양이라도 흩어 놓느냐 모아 놓느냐*가 몇
  배의 차이를 만든다. 기억을 다루는 감각은 "얼마나"가 아니라 "어디에,
  어떤 순서로"에서 자란다.
]

== 이 지도가 앞으로 쓰이는 곳

지금은 이름만 알아 두면 된다. 이 네 구역은 책 전체에서 계속 돌아온다.

#dtable(
  columns: 2,
  [*어디서*], [*무엇을*],
  [5·6장], [주소와 정렬 — 구역들이 놓이는 복도의 지리],
  [11장], [기억의 사다리 — 같은 구역이라도 캐시에 따라 속도가 다르다],
  [43장], [수명과 저장 기간 — C의 문법이 이 구역들을 어떻게 부르는가],
  [44장], [동적 메모리 — 창고를 빌리고 돌려주는 규율],
  [81장], [운영체제와 임베디드에서의 실제 지도],
  [82장], [창고를 관리하는 할당자의 속],
)

프로그램이 무엇을 어디에 두는지 보았다. 다음 장은 그 프로그램이 *실행될 때*
운영체제가 무엇을 해 주는지 — 프로그램과 프로세스의 차이, 그리고 프로세스가
어떻게 태어나는지 — 를 짧게 본다.
