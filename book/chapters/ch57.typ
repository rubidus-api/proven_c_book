#import "../lib.typ": *

= 실전의 C — 도구, 프로젝트, 그리고 영역

#organizer[
  책을 닫기 전에 지면 밖의 세계를 둘러본다. 실무의 두 필수 도구(make와
  git), 오늘도 순수 C로 도는 대표 프로젝트들과 그들이 C를 고른 이유,
  임베디드에서 C가 하는 일, 그리고 C의 한계를 실전에서 어떻게 넘는지
  까지. 이 책이 가르친 언어가 실제로 어디에 서 있는지의 지도다.
]

#deepqa[
  43장에서 여러 파일 프로그램은 각 파일을 목적 파일로 만들어 두었다가
  링크만 다시 하는 것이 관행이라 했다. 파일이 수백 개인 프로젝트에서
  그 일을 사람이 손으로 할 수는 없을 텐데 — 무엇이 대신하는가?
][
  *빌드 도구*다. 파일 사이의 의존 관계를 적어 두면, 무엇이 바뀌었는지
  보고 다시 만들 것만 골라 만들어 준다. C 세계의 고전이자 여전한 표준이
  `make`이고, 이 장이 그 도구부터 시작한다.
]

== make — 무엇을 다시 만들 것인가

`make`의 착상은 한 줄이다 — *결과물이 재료보다 오래됐으면 다시 만든다.*
규칙을 적는 문법도 그 착상 그대로다:

```text
hello: hello.c greet.c greet.h      # 목표: 재료들
	cc -Wall -Wextra -o hello hello.c greet.c    # 만드는 법(탭으로 시작)
```

`make hello`라고 부르면 파일들의 시각을 비교해, 필요할 때만 명령을
실행한다. 파일 하나만 고쳤을 때 전체를 다시 컴파일하지 않는 것 —
14장의 릴레이와 43장의 분할이 실무에서 시간을 아끼는 방식이 이것이다.

make는 1976년에 만들어져 지금도 쓰인다. 문법의 괴벽(명령 줄은 반드시
탭으로 시작해야 한다 — 만든 사람 스스로 "그때는 사용자가 열둘뿐이었다"고
회고한 실수다)까지 그대로 남았지만, "의존 관계를 적으면 필요한 것만
다시 만든다"는 착상은 이후 모든 빌드 도구의 뿌리가 됐다. 오늘의 큰
프로젝트는 CMake·Meson·Ninja 같은 도구를 얹어 쓰지만, 그 도구들이
결국 만들어 내는 것도 대개 make류의 규칙이다.

== git — 무엇이 언제 바뀌었나

두 번째 필수 도구는 *버전 관리*다. 코드의 모든 변경을 기록해 두고,
언제든 되돌리고, 여럿이 같은 코드를 고쳐도 합칠 수 있게 하는 도구 —
오늘의 사실상 표준이 `git`이다.

일상의 명령은 다섯이면 시작할 수 있다.

```text
$ git init                 # 이 폴더를 저장소로
$ git add hello.c          # 기록할 변경을 고른다
$ git commit -m "첫 프로그램"   # 이유와 함께 기록한다
$ git log                  # 지나온 기록을 본다
$ git diff                 # 지금 무엇이 바뀌었나
```

git이 이 책의 주제와 이어지는 대목이 하나 있다 — *git 자신이 순수
C로 쓰인 프로그램이다.* 2005년 리누스 토르발스가 리눅스 커널 개발을
위해 며칠 만에 초기판을 만들었고, 지금은 세계에서 가장 널리 쓰이는
개발 도구가 됐다. 8장에서 본 CRLF 경고를 내는 그 도구가, 이 책이
가르친 언어로 만들어져 있는 것이다.

== 오늘도 C로 도는 것들 — 그리고 왜

1장에서 "C는 어디에나 있다"고 했다. 이제 구체적인 이름과 이유를 적을
수 있다.

#align(center, table(
  columns: 3,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left, left),
  [*프로젝트*], [*무엇*], [*왜 C인가*],
  [Linux 커널], [운영체제의 심장], [하드웨어를 직접 다뤄야 하고, 런타임 의존이 없어야 하며, 모든 아키텍처에 컴파일러가 있다],
  [SQLite], [세계에서 가장 많이 배포된 데이터베이스], [어디에나 이식되어야 하고(휴대폰·항공기·브라우저), 단일 소스 파일로 배포하는 극단적 단순함이 필요했다],
  [FFmpeg], [영상·음성 처리의 표준 도구], [성능이 전부인 코덱 작업, 하드웨어 가속 API와의 직접 연동],
  [Redis], [메모리 기반 데이터 저장소], [예측 가능한 지연시간과 메모리 제어 — 9장의 사다리를 직접 관리한다],
  [curl], [인터넷 전송의 만능 도구], [모든 플랫폼·모든 기기에 올라가야 한다(자동차·TV·우주선까지)],
  [CPython·Ruby 등], [다른 언어의 구현체], [언어 런타임은 결국 가장 낮은 층이 필요하고, C 인터페이스가 생태계의 공용어다],
  [OpenSSL·zlib], [암호화·압축 라이브러리], [모든 언어에서 불려야 하므로 — C ABI가 사실상의 만국 공통 인터페이스],
))

공통점을 뽑으면 C를 고르는 이유가 네 가지로 정리된다.

- *가장 낮은 층에 서야 할 때* — 운영체제, 드라이버, 런타임.
- *어디에나 옮겨야 할 때* — 새 칩이 나오면 C 컴파일러부터 만들어진다
  (2장의 이식성 혁명이 반세기째 유효하다).
- *성능과 자원을 손으로 통제해야 할 때* — 메모리 배치, 지연시간, 실행
  파일 크기.
- *다른 모든 언어와 대화해야 할 때* — 파이썬·자바·러스트 어느 쪽에서든
  C 함수는 부를 수 있다. C ABI가 언어들 사이의 국제 공용어다.

== 임베디드 — C의 본진

일상에서 눈에 안 띄지만 물량으로는 가장 큰 C의 영역이 임베디드다.
세탁기와 에어컨의 제어판, 자동차의 수십 개 제어 장치, 의료 기기,
드론과 로봇의 저수준 제어, 통신 모듈의 펌웨어 — 대부분이 C로 쓰인다.

이유는 그 환경의 제약에 있다. 메모리가 킬로바이트 단위이고, 운영체제가
없을 수도 있고(4장의 0번지가 인터럽트 벡터인 그 세계), 실행 시간이
예측 가능해야 하며(가비지 컬렉션 같은 예측 불가한 멈춤이 허용되지
않는다), 전력이 제한된다. C는 이런 환경에서 *실행되지 않는 것에는
비용을 물리지 않는* 언어라 자연스럽게 맞는다 — 표준 라이브러리 없이
쓸 수 있고(46장의 프리스탠딩), 생성되는 코드가 예측 가능하다.

임베디드의 C에는 이 책의 지식이 유난히 직접적으로 쓰인다 — 특정 주소를
장치 레지스터로 다루기(4장), `volatile`로 최적화를 막기(11장), 비트
연산으로 하드웨어 플래그를 조작하기(5·24장), 동적 할당을 아예 쓰지
않기(37장), 정렬과 표현을 의식하며 통신 프로토콜을 다루기(3·39장).

== 한계를 넘는 법 — 실전의 요령

C의 결핍은 분명하다 — 이름 공간이 없고, 제네릭이 없고, 자원 정리가
자동이 아니고, 안전이 강제되지 않는다. 실전은 이것을 언어 밖의 규율과
도구로 메운다. 이 책이 지나온 것들을 실무 지침으로 모으면 이렇다.

- *이름 공간이 없다* → 접두어 규약(35장의 `proven_`)과 파일 수준
  `static`(43장의 내부 연결).
- *제네릭이 없다* → 매크로로 코드 생성(44장의 `##`, X 매크로), 또는
  `void *`와 크기·비교 함수를 받는 일반화(표준의 `qsort`가 그 방식이다),
  또는 C11의 `_Generic` 선택.
- *자원 정리가 자동이 아니다* → 소유 규약을 이름과 문서로 명시하고
  (37장), 한 함수의 정리 지점을 한 곳으로 모으는 관용구(정리 라벨로
  `goto`하는 패턴은 커널에서 표준 관행이다).
- *안전이 강제되지 않는다* → 그물을 겹친다(15장): 경고 최대, 새니타이저,
  두 컴파일러 교차, 정적 분석 도구, 그리고 검사가 내장된 부품(제11부).
- *표준 라이브러리가 얇다* → 필요한 층을 고르거나 만든다. vendor로
  들여와 빌드를 단순하게 유지하는 것이 작은 프로젝트의 정석이다.
- *큰 프로젝트의 복잡도* → 컴파일 단위를 작게 나누고 헤더로 계약을
  드러내며(43장), 테스트와 빌드를 자동화한다(이 장의 make·git).

#realcase[
  섞어 쓰는 현실 — C는 사라지지 않고 층이 된다
][
  오늘의 큰 시스템은 대개 한 언어로만 쓰이지 않는다. 성능이 필요한
  핵심부는 C(또는 C++·Rust)로, 그 위의 논리는 파이썬·자바스크립트로
  쓰는 층 구조가 일반적이다. 파이썬의 수치 계산 라이브러리가 빠른
  이유는 그 밑이 C·포트란이기 때문이고, 브라우저가 영상을 재생하는
  것도 밑에 C 코덱이 있기 때문이다. 1장에서 Rust와 Zig가 C의 영역을
  잠식한다고 했지만, 실제로 벌어지는 일은 대체보다 *공존*에 가깝다 —
  새 언어들도 기존 C 자산과 대화해야 하므로 C 인터페이스를 갖추고
  태어난다. C를 읽고 쓸 줄 안다는 것은, 그래서 어느 층에서 일하든
  아래층을 들여다볼 수 있다는 뜻이다.
]

#qa[
  이 책을 다 읽은 사람이 다음으로 만들어 볼 만한 것은 무엇인가?
][
  작고 끝이 있는 것이 좋다. 텍스트 파일을 읽어 통계를 내는 도구, 간단한
  설정 파일 파서(53장의 스캐너가 그대로 쓰인다), 자료구조 하나를
  직접 구현해 보기(38장의 연결 구조), 좋아하는 작은 C 프로젝트의 소스를
  읽고 한 곳 고쳐 보기. 마지막 것이 특히 권할 만하다 — 남의 코드를
  읽는 능력이 실무에서 쓰는 시간의 대부분을 차지하고, 이 책은 그 읽기를
  위한 눈을 기르는 데 상당 부분을 썼다.
]

지면 밖의 지형까지 둘러보았다. 다음 장에서 이 책이 지나온 길을 한 번
되짚고 책을 닫는다.
