#import "../lib.typ": *

= 널 — 삼형제의 정식 취급

#prereq(
  ([6장 주소의 특수 지식], [0번지의 특별함]),
  ([35장 객체, 주소, 포인터], [포인터는 무엇을 가리키는 값이다]),
)

#deepqa[
  6장에서 "널 포인터의 내부 표현은 0이 아닐 수 있다"는 역사(Prime 50,
  CDC Cyber 180)를 보았다. 그러면 소스 코드에서 `p == 0`이라는 비교는 —
  그런 기계에서 깨지는가?
][
  깨지지 않는다 — 그것이 표준의 약속이다. 소스의 정수 상수 0은 포인터
  문맥에서 *널 포인터 상수*로 읽히고, 컴파일러가 그 기계의 실제 널
  표현과 비교하도록 번역한다. 기호(소스의 0)와 표현(기계 속 비트)의
  분리를 컴파일러가 지켜 주는 것이다. 깨지는 것은 다른 종류의 코드다 —
  "표현이 곧 0"이라고 *비트 차원에서* 가정하는 코드. 이 함정을 뒤에서
  본다.
]

#organizer[
  6장에서 얼굴만 익힌 널 삼형제 — 널 포인터, `NULL`/`nullptr`, NUL
  문자 — 를 문법과 실무 규칙으로 정식 취급한다. "비어 있음"을 다루는
  법은 포인터 안전 수칙의 첫 장이다.
]

#chapter-questions()

== nullptr — 비어 있음의 이름

포인터 변수는 선언 즉시 무언가를 가리키게 하거나, 아직 대상이 없다면
*비어 있음*을 명시해야 한다(23장의 초기화 규칙이 포인터에서는 더욱
사활적이다 — 쓰레기 주소를 따라가는 것이 최악의 사고이므로). 비어
있음의 표기는 두 가지가 통용된다 — 전통의 `NULL` 매크로와, C23이
들여온 키워드 `nullptr`. 이 책은 `nullptr`를 쓴다: `NULL`은 역사적으로
정의가 구현마다 달라 미묘한 함정(가변 인자 등)이 있었고, `nullptr`는
타입까지 명확한 현대적 표기다.

#demo("examples/ch36/null.c")

수칙도 이 시연 그대로다 — *포인터를 쓰기 전에 비어 있는지 확인한다.*
비어 있는 포인터를 역참조하는 것(`*p`)은 계약 밖(정의되지 않은 동작)
이고, 6장에서 배운 대로 보호 모드 환경에서는 대개 그 자리에서 프로그램이
붕괴한다 — 시끄럽게 죽는 것이 그나마 친절이라는 것도 6장의 이야기
그대로다.

#realcase[
  십억 달러의 실수 — 널의 발명자가 남긴 사과
][
  "비어 있음"이라는 값 자체가 논쟁의 역사를 가졌다. 널 참조를 1965년
  ALGOL 계열 언어에 처음 도입한 토니 호어(Tony Hoare — 31장 퀵소트
  계열 알고리즘의 그 호어다)는 2009년의 강연에서 그것을 *"나의 십억
  달러짜리 실수"*라 불렀다 — "구현하기 쉽다는 유혹을 참지 못했고,
  그 뒤 40년간 널 역참조가 일으킨 붕괴와 취약점의 비용이 십억 달러를
  넘을 것"이라는 자기 고발이다. 현대 언어들(Rust, Swift, Kotlin)이
  "비어 있을 수 있음"을 타입으로 강제 검사하게 진화한 것은 이 반성의
  직계 후손이다. C에는 그 강제가 없다 — 검사는 문화와 규율의 몫이고,
  그래서 이 책은 검사를 관용구로 못박는다.
]

== 어디까지 0을 써도 되는가 — 널 포인터 상수의 정확한 정의

6장에서 「소스의 0은 기호」라고 했다. 그 기호의 자격을 표준이 정확히 정해
두었다. *널 포인터 상수*(null pointer constant)는 다음 둘 중 하나다.

- 값이 0인 *정수 상수식*
- 그런 식을 `void *`로 캐스트한 것

여기에 C23이 키워드 `nullptr`를 더했다. 그래서 아래 다섯 가지는 모두
같은 뜻이다.

#dtable(
  columns: 3,
  [*적는 법*], [*널 포인터 상수인가*], [*비고*],
  [`0`], [그렇다], [가장 오래된 표기],
  [`0L`, `0u`, `'\0'`], [그렇다], [값이 0인 정수 상수식이면 된다],
  [`(void *)0`], [그렇다], [`NULL`이 흔히 이렇게 정의된다],
  [`NULL`], [그렇다], [구현이 위 둘 중 하나로 정의한 매크로],
  [`nullptr`], [그렇다], [C23. 타입이 `nullptr_t`로 분명하다],
  [`const int zero = 0;` 의 `zero`], [*아니다*], [상수식이 아니라 변수 — 컴파일 오류],
  [실행 중에 0이 된 `int` 변수], [*아니다*], [정수를 포인터에 넣는 것은 별개의 이야기],
)

마지막 두 줄이 요점이다. 자격은 *값이 0인지*가 아니라 *상수식인지*에
달렸다. 그래서 `int *p = zero;`처럼 값이 0인 변수를 넘기는 것은 널 대입이
아니라 「정수를 포인터로」라는 전혀 다른 일이 되고, 오늘날의 컴파일러는
이것을 오류로 잡는다.

=== 담긴 뒤의 비트는 별개다

표기가 같다고 표현까지 같은 것은 아니다. 여기서 6장의 복선이 회수된다.

#demo("examples/ch36/nullrep.c")

시연이 두 층을 나란히 보인다. 다섯 가지 표기가 전부 같은 값이 되고
(`a == b && b == c`), 비교도 전부 참이다 — *표기의 층은 표준이 약속한다.*
반면 인쇄된 바이트는 *이 구현이 그렇게 정했을 뿐*이다. 6장의 Prime 50이나
CDC Cyber 180에서 같은 프로그램을 돌렸다면 비교는 그대로 참이지만 바이트는
0이 아니었을 것이다.

#dtable(
  columns: 3,
  [*하는 일*], [*어느 층인가*], [*널을 약속하는가*],
  [`p = nullptr;` / `p = 0;`], [표기], [★ 약속한다],
  [`p == nullptr` / `!p`], [표기], [★ 약속한다],
  [`struct s x = { 0 };`], [값], [★ 약속한다 — 포인터 멤버는 널, 실수 멤버는 0.0],
  [`memset(&x, 0, sizeof x)`], [표현], [약속하지 않는다],
  [`calloc(n, size)`], [표현], [약속하지 않는다],
  [`memcmp(&p, zeros, sizeof p)`], [표현], [널인지 묻는 방법이 아니다],
)

시연의 셋째·넷째 부분이 아래 세 줄을 실물로 보인다. `{ 0 }`으로 만든
구조체는 *표준이* 포인터 멤버를 널로, 실수 멤버를 0.0으로 약속하고,
`memset`과 `calloc`은 *이 기계에서 우연히* 같은 결과를 낸다.

#platform[
  C++의 사정 — 같은 0, 다른 규칙
][
  이 자리는 C와 C++이 갈리는 유명한 지점이다. 뿌리는 하나다 — *C++에는
  `void *`에서 다른 포인터로의 암묵적 변환이 없다.*

  그래서 C++의 `NULL`은 `((void *)0)`으로 정의될 수 없다. 정의하면
  `int *p = NULL;`이 곧바로 오류가 되기 때문이다. 실제로 이 기계에서
  확인해 보면 C의 `NULL`은 `((void *)0)`이고 g++의 `NULL`은 컴파일러
  확장인 `__null`이다. 전통적인 C++ 구현은 그냥 `0`이나 `0L`로 정의했다.

  `0`을 널로 쓰는 데서 C++만의 사고가 생긴다. *오버로딩*이다.

  ```cpp
  void f(int);
  void f(char *);
  f(NULL);      // NULL 이 0 이면 → f(int) 가 조용히 뽑힌다
  ```

  직접 돌려 보면 `NULL`을 `0`으로 정의했을 때 `f(int)`가 뽑히고, g++의
  `__null`에서는 「모호하다」는 오류가 난다. 어느 쪽이든 원하던 `f(char *)`는
  아니다.

  #dtable(
    columns: 3,
    [*무엇*], [*C*], [*C++*],
    [`(void *)0`을 다른 포인터에], [된다], [오류 — 캐스트가 필요하다],
    [`NULL`의 정의], [대개 `((void *)0)`], [`0`·`0L`·`__null`],
    [값이 0인 `const int` 변수], [널 아님(상수식이 아니다)], [C++03은 널, *C++11부터 오류*],
    [전용 키워드], [C23의 `nullptr`], [C++11의 `nullptr`(타입 `std::nullptr_t`)],
  )

  셋째 줄의 변화가 흥미롭다. C++03의 정의는 「0으로 평가되는 정수 상수식」
  이어서 `const int zero = 0;`의 `zero`도 널 포인터 상수였는데, C++11이
  「0인 정수 리터럴 또는 `std::nullptr_t` 값」으로 좁혔다. 같은 코드가
  `-std=c++03`에서는 컴파일되고 `-std=c++11`에서는 오류가 되는 것을 이 책도
  확인했다.

  결론은 두 언어가 같다 — *`nullptr`를 쓴다.* C++11이 먼저 들여왔고 C23이
  뒤따랐으며, 오버로딩 사고도 「0인가 포인터인가」의 모호함도 이 한 낱말로
  사라진다.
]

== 함정 — 표현을 0으로 가정하는 코드

deepqa의 복선을 회수한다. 다음 두 코드는 뜻이 다르다:

```c
int *arr[8];
for (int i = 0; i < 8; i += 1) { arr[i] = nullptr; }  /* 올바르다 */
/* memset(arr, 0, sizeof arr);     — 비트를 0으로 채운다: 다른 뜻! */
```

첫 줄은 "널 포인터를 담아라"(표기의 세계 — 어떤 표현이든 컴파일러가
알아서). 주석의 memset은 "모든 비트를 0으로 채워라"(표현의 세계).
널 표현이 전부 0인 오늘의 주류 기계에서는 결과가 우연히 같지만, 6장의
역사가 보여 주듯 그 우연은 계약이 아니다. 실무에서 memset 방식을 흔히
보게 되는데(그리고 주류 플랫폼에서 실제로 동작하는데), *이식성의 눈*
으로는 회색 지대임을 알고 쓰는 것과 모르고 쓰는 것이 다르다 — 이
책의 선택은 명시적 `nullptr` 대입이다.

#qa[
  NUL 문자(`'\0'`) 쪽은 정식 취급이 언제인가 — 삼형제 중 하나가 아직
  남았다.
][
  세 장 뒤다. NUL 문자는 포인터의 세계가 아니라 *문자열의 세계*에서
  일하는 물건이라(끝 표시), 문자열을 정식으로 다루는 41장이 그 무대다.
  기다리는 동안 구별만 다시 — `nullptr`는 "가리킬 곳 없음"(포인터 값),
  `'\0'`은 "여기가 글의 끝"(문자 값, 크기 1바이트). 사는 세계가 다른
  남남이다.
]

비어 있음을 다루는 법을 갖췄다. 다음 장은 포인터에 걸린 나머지 규칙들
— 6장의 정렬이 포인터 캐스트에 거는 제약과, 14장에서 예고한 프로버넌스
(출처 딱지)의 실무 감각이다.
