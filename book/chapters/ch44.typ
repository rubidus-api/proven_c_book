#import "../lib.typ": *

= 실수 — 근사의 수학

#organizer[
  8장에서 지면으로 배운 근사의 세계가 드디어 C 코드로 내려온다. `float`
#idx("실수 비교")  와 `double`의 선택, 비교의 올바른 방법(엡실론 — 절대와 상대), 그리고
  특수값(무한대·NaN)까지. 8장의 세 사건이 실행 결과로 확인된다.
]

#deepqa[
  8장에서 `0.1 + 0.2`와 `0.3`은 마지막 비트 하나(1 ulp) 차이의 이웃이라
  했고, 비교는 "충분히 가까운가"로 바꿔야 한다고 했다. 그러면 그
  "충분히"의 기준 — 엡실론 — 은 어떻게 정하는가?
][
  값의 크기에 따라 달라져야 한다는 것이 8장 셋째 사건의 교훈이다.
  0 근처에서는 눈금이 촘촘하니 작은 고정값이면 되지만, $10^16$쯤에서는
  눈금 간격 자체가 1을 넘으므로 같은 기준이 무의미해진다. 그래서 실무는
  두 가지를 갖춘다 — *절대 오차*(0 근처용)와 *상대 오차*(크기에 비례).
  이 장의 시연이 둘을 나란히 보인다.
]

== 타입 선택과 비교

#demo("examples/ch44/eps.c")

첫 세 줄이 8장 첫 사건의 실행 확인이다 — `==`는 거짓이고, 20자리까지
찍어 보면 두 수가 마지막에서 갈린다. `near_abs`(절대 오차)를 쓰면 참이
된다.

뒷부분이 셋째 사건(흡수)의 확인이다 — $10^16$에 1을 더해도 값이 그대로
다. 그 크기에서는 표현 가능한 이웃 사이의 간격이 이미 1보다 넓기
때문이다(8장의 눈금 계산). 여기서는 절대 오차가 아니라 `near_rel`(상대
오차)이 옳은 도구다.

실무 수칙으로 줄이면 셋이다. *기본은 `double`* — 8장에서 본 대로
정밀도의 여유가 다르고, `float`는 메모리·대역폭이 아쉬운 곳에서만
고른다. *`==`는 쓰지 않는다* — 두 실수가 같은지 묻는 코드는 거의 언제나
의심 대상이다(정수와 비교할 때, 0과 비교할 때 등 예외는 있지만 의식
적으로 판단해야 한다). *허용치는 문제에서 나온다* — 계산의 성격과
값의 크기를 보고 정하는 것이지 마법 상수가 있는 것이 아니다.

== 특수값 — 무한대와 NaN

IEEE 754(8장)는 평범한 수 말고도 특별한 값을 정의해 두었다. *무한대*
(양·음)는 넘침이나 0으로 나누기의 결과로 나오고, *NaN*(Not a Number)은
"수가 아님" — $0/0$이나 음수의 제곱근 같은 정의 불가 연산의 결과다.
정수의 0 나누기가 계약 밖(27장)인 것과 대조적으로, *부동소수점의 0
나누기는 정의된 동작*(무한대)이라는 점이 흥미롭다 — IEEE 754가 계산을
멈추는 대신 특별한 값으로 흘려보내는 설계를 택했기 때문이다.

NaN에는 유명한 성질이 하나 있다 — *자기 자신과도 같지 않다.*
`x != x`가 참이면 x는 NaN이라는 뜻이고, 이것이 NaN을 판별하는 고전적
관용구다(오늘은 `isnan()`을 쓴다). "같음"이라는 관계의 기본 성질이
깨지는 값이라, 정렬이나 검색 알고리즘에 NaN이 섞여 들면 기묘한 일이
벌어진다 — 실수 데이터를 다룰 때 NaN 검사를 경계에 두는 것이 관행인
이유다.

#realcase[
  0.1초의 누적 — 패트리어트 미사일 사고
][
  8장에서 배운 "작은 어긋남이 쌓인다"가 실제로 사람의 목숨과 이어진
  사건이 있다. 1991년 걸프전에서 패트리어트 방공 시스템이 날아오는
  미사일을 요격하지 못해 28명이 사망한 사고인데, 원인 분석의 핵심이
  부동소수점 오차였다. 시스템은 시간을 0.1초 단위로 세었는데 — 8장에서
  본 대로 0.1은 2진법으로 무한소수라 담을 때마다 미세한 오차가 생긴다.
  그 시스템이 24비트 그릇을 쓴 탓에 오차가 상대적으로 컸고, 재부팅
  없이 100시간을 연속 가동하자 누적 오차가 약 0.34초에 이르렀다. 그
  0.34초 동안 목표는 500미터 넘게 이동하고, 추적 창은 엉뚱한 하늘을
  보게 된 것이다. "근사는 성실하지만 무해하지는 않다" — 8장의 교훈이
  가장 무겁게 확인된 사례다.
]

#qa[
  그러면 돈 계산 같은 곳에는 실수를 쓰면 안 되는가?
][
  쓰지 않는 것이 정석이다 — 8장에서 배운 고정소수점의 자리가 바로
  여기다. 금액을 원 단위 실수로 다루면 0.1원 단위의 어긋남이 쌓여
  장부가 맞지 않게 되므로, *전 단위 정수*로 계산하고 표시할 때만 소수점을
  찍는 것이 금융 소프트웨어의 관행이다. 규칙으로 줄이면 — *정확한 십진
  값이 중요한 곳에는 정수(고정소수점), 물리량과 과학 계산에는 부동
  소수점.* 도구를 문제에 맞추는 것이고, 그 판단의 근거가 8장과 이
  장에서 배운 표현의 성격이다.
]

근사의 세계를 C에서 다루는 법을 익혔다. 다음 장은 이 부의 중심 주제 —
계산이 실패할 수 있다는 사실을 프로그램이 어떻게 다루는가, 오류와
계약의 이야기다.
