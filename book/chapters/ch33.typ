#import "../lib.typ": *

= 객체, 주소, 포인터

#prereq(
  ([5장 워드와 주소], [주소는 값이다]),
  ([32장 함수의 의미], [값 복사만으로는 원본을 못 바꾼다]),
)

#deepqa[
  32장에서 "함수가 밖의 변수를 바꾸는 정공법은 변수의 *주소*를 값으로
  복사해 넘기는 것"이라 했다. 5장의 지식으로 풀어 보면 — 주소를 "값으로
  복사"할 수 있는 근거는 무엇이었나?
][
  주소가 *번호처럼 다룰 수 있는 값*이라는 것(5장) — 사물함 번호는 적어
  둘 수도, 복사해 건넬 수도 있다. C에서 그 "주소를 담는 변수"의 타입이
  포인터다. 오늘 배우는 것은 새 개념이 아니라, 5장의 착상에 문법 옷을
  입히는 일이다.

  한 가지만 미리 못박아 둔다. 복사하고 비교할 수 있다고 해서 *포인터가
  정수인 것은 아니다.* C는 포인터를 정수와 다른 종류의 값으로 두고, 거기에
  가리키는 타입과 출처(어느 객체에서 나왔는가)라는 계약을 붙인다. 6장에서
  본 "번호가 같아도 같은 포인터가 아닐 수 있다"는 이야기가 여기서 시작되고,
  35장의 프로버넌스에서 정식으로 마무리된다.
]

#organizer[
  제7부는 이 책의 두 번째 산 — 기억이다. 5장의 사물함 복도를 이번에는
#idx("포인터")  C 문법으로 걷는다. 첫 장에서 주소를 값으로 다루는 타입, *포인터*를
  정면으로 배운다 — `&`와 `*`, 그리고 `sizeof`까지, 25장부터 미뤄 온
  외상이 모두 청산된다.
]

#chapter-questions()

== 세 가지 표기 — 선언, &, \*

포인터 문법은 세 조각이 전부다.

*선언* — `int *p;`는 "int의 주소를 담는 변수 p"를 선언한다. 읽는
요령은 뒤에서부터다: "p는 포인터인데(`*p`), 가리키는 곳에 int가 있다".

*주소 얻기* — `&n`은 "n의 주소"라는 값이다. 25장의 `sscanf(..., &n)`
에서 스쳐 본 그 `&`가 드디어 정식 이름을 얻었다 — n이라는 사물함의
번호표를 떼어 주는 연산자다.

#idx("역참조")*역참조* — `*p`는 "p에 적힌 주소로 *찾아가서* 그 칸"이라는 뜻이다.
읽는 자리에 있으면 그 칸의 값을 읽고, 대입의 왼쪽에 있으면 그 칸에
쓴다. 선언의 `*`와 수식의 `*`(역참조), 그리고 곱셈의 `*`가 같은 글자를
쓰는 것은 C의 악명 높은 절약 정신이다 — 자리로 구별하는 수밖에 없다.

시연으로 세 조각을 한 번에 본다. 32장의 숙제 — 함수가 원본을 바꾸는
정공법 — 까지 포함이다.

#demo("examples/ch33/ptr.c")

`set_to(&n, 99)`의 동선을 정확히 읽어 두면 포인터의 절반이 끝난다 —
① `&n`(주소라는 값)이 매개변수 `target`에 *복사*되고(32장의 값 복사
규칙은 그대로다!), ② 함수 안에서 `*target = value`가 그 주소로 찾아가
원본 칸에 쓴다. 값 복사의 세계는 하나도 깨지지 않았다 — *복사된 것이
주소였을 뿐이다.*

== sizeof — 그릇의 크기를 묻다

미뤄 둔 마지막 외상 — `sizeof`는 타입이나 대상의 *바이트 크기*를
내놓는 연산자다(값은 `size_t` 타입 — 크기 전용 부호 없는 정수이고,
서식은 `%zu`다). 25장의 `fgets(line, sizeof line, stdin)`이 이제 완전히
읽힌다: "line이라는 그릇의 바이트 수를 fgets에게 알려 준다". 함수가
아니라 연산자라서 대부분 컴파일 시점에 값이 정해지고, `sizeof(int)`처럼
타입에도, `sizeof line`처럼 대상에도 쓸 수 있다.

#qa[
  포인터 변수 자체의 크기는 얼마인가 — `sizeof p`는?
][
  이 책의 검증 환경(x86-64 리눅스)에서는 *8바이트*이고, 오늘의 주류
  64비트 환경 대부분이 그렇다. 그리고 그 환경에서는 가리키는 대상이
  무엇이든(`int*`든 `double*`이든) 객체 포인터의 크기가 같다.

  다만 *그것은 표준의 보장이 아니다.* 표준은 포인터의 크기도, 표현도
  정해 주지 않는다. 서로 다른 객체 포인터 타입이 같은 크기와 표현을
  가져야 한다는 규칙도 없고(`char*`는 다른 포인터보다 큰 기계가 실제로
  있었다), 함수 포인터는 객체 포인터와 같은 표현을 요구받지도 않는다.
  "워드 크기"라는 말도 C 표준의 낱말이 아니라 기계 쪽 사정이다. 그래서
  크기를 알아야 하는 코드는 숫자를 적지 않고 언제나 `sizeof p`로 묻는다.

  그러면 타입은 왜 구별하는가 — 역참조할 때 "그 주소에서 몇 바이트를
  어떤 눈으로 읽을 것인가"를 타입이 정하기 때문이다. 5장의 후렴구
  (덩어리를 아는 것은 읽는 쪽)가 포인터 타입의 존재 이유다.
]

#misconception[
  "포인터는 어렵고 위험한 고급 기능이다"
][
  악명은 사실이지만 절반은 오해다. 개념 자체는 방금 본 대로 "번호를
  적어 두고, 번호로 찾아간다" — 사물함 복도를 아는 사람에게는 평범한
  이야기다. 악명의 진짜 출처는 개념이 아니라 *규칙 위반의 대가*다:
  비어 있는 포인터를 따라가거나(다음 장), 남의 땅 번호를 만들거나
  (35–36장), 사라진 칸의 번호를 간직하는(39–40장) 코드는 조용히,
  늦게, 크게 사고를 낸다. 그래서 이 부의 남은 장들이 전부 "규칙"의
  장이다 — 개념은 오늘로 끝났고, 이제 안전 수칙을 배운다.
]

#qa[
  변수의 주소를 직접 찍어 보면 재미있을 것 같은데 — 왜 시연에서 주소
  값을 인쇄하지 않았는가?
][
  찍을 수 있다 — `printf("%p", (void *)&n)`이 그 서식이다. 시연에서
  뺀 이유는 정직함 때문이다: 주소 값은 *실행할 때마다 다르다*. 현대
  운영체제는 보안을 위해 프로그램을 매번 다른 자리에 배치한다(주소
  공간 무작위화, ASLR — 공격자가 주소를 미리 알 수 없게 하는 방어다).
  이 책의 실행 결과는 전부 실제 캡처인데, 매번 달라지는 값을 실으면
  "같은 결과"를 보장할 수 없다. 주소의 *값*은 중요하지 않고, 주소들
  사이의 *관계*(같다/다르다/이웃이다)가 중요하다 — 시연이 `p == &n`을
  찍은 이유다.
]

== 포인터의 크기는 하나가 아니다

앞의 문답에서 "이 기계에서는 8바이트"라고 조심스럽게 말한 이유를 여기서
푼다. 널리 퍼진 통념은 이렇다 — *"포인터는 그냥 주소니까, 한 기계 안에서는
전부 같은 크기다."* 오늘 우리가 쓰는 x86-64 리눅스에서는 맞는 말이고, 그래서
이 통념은 좀처럼 반박당하지 않는다. 그러나 C 표준은 그렇게 약속한 적이
없고, 현실의 기계들도 그 통념을 여러 번 배신했다.

표준이 실제로 약속하는 것은 훨씬 좁다.

- *`void *`와 문자 타입 포인터(`char *`·`signed char *`·`unsigned char *`)는
  서로 같은 표현과 정렬 요구를 가진다.* 이 넷은 한 묶음이다.
- 구조체 포인터끼리는 서로 같은 표현을 가진다. 공용체 포인터끼리도 그렇다.
- *그 밖에는 아무 약속도 없다.* `int *`와 `double *`이 같은 크기여야 한다는
  규칙은 없고, 객체 포인터와 함수 포인터가 같은 표현이어야 한다는 규칙도
  없다.

왜 이렇게 헐겁게 정했는가 — 실제로 다른 기계들이 있었기 때문이다.

#realcase("한 기계 안에서 포인터 크기가 갈렸던 사례들")[
  *워드 주소 기계.* Cray, Data General Nova, Prime 50, Honeywell 같은
  기계는 주소가 *바이트*가 아니라 *워드*를 가리켰다. 워드 안의 몇 번째
  바이트인지를 따로 담아야 했으므로 `char *`는 `int *`보다 컸거나, 같은
  크기라도 비트 배치가 달랐다. C가 "`void *`와 `char *`만 표현을 공유한다"고
  좁게 정한 것은 이 세계를 수용하기 위해서였다.

  *세그먼트 x86(16비트 DOS·윈도).* 같은 프로그램 안에 `near` 포인터(16비트
  오프셋)와 `far`·`huge` 포인터(32비트 세그먼트:오프셋)가 공존했다. 어떤
  포인터가 어느 쪽인지는 *메모리 모델*이 정했다 — medium 모델은 코드
  포인터가 far, 데이터 포인터가 near였고 compact 모델은 그 반대였다. 즉
  `sizeof(void (*)())`와 `sizeof(void *)`가 한 프로그램 안에서 달랐다.

  *하버드 구조의 마이크로컨트롤러.* AVR, PIC, 일부 DSP는 코드와 데이터가
  아예 다른 주소 공간에 있다. 함수 포인터의 폭이 데이터 포인터와 다르고,
  플래시에 있는 상수를 읽으려면 별도의 명령과 별도의 포인터 종류(AVR의
  `__flash`, `PROGMEM` 관용구)가 필요하다. 80장의 임베디드 이야기가 이
  땅 위에 있다.

  *IBM AS/400(OS/400).* 포인터가 128비트였고, 그 안에 태그가 붙어 하드웨어가
  위조를 막았다. 주소를 정수로 바꿔 조작하는 코드는 여기서 통하지 않는다.
]

과거형으로만 말할 이야기도 아니다.

#realcase("지금과 앞으로")[
  (여기서도 이름을 외울 필요는 없다. *"포인터의 크기와 표현은 기계와 빌드가
  정한다"*만 남기면 된다.)

  *CHERI·Arm Morello.* 포인터를 주소 + 범위 + 권한의 묶음(capability)으로
  만든 실제 하드웨어다. 주소는 64비트지만 포인터는 *128비트*이고,
  `sizeof(void *)`가 16이 된다.

  여기서 두 가지를 구별해야 한다. *공식 통로인 `uintptr_t`를 거친 왕복*은
  지원된다 — CHERI C 의 `uintptr_t`는 주소만이 아니라 출처(권한과 범위)까지
  담도록 설계돼 있어서, 포인터를 그 타입으로 바꿨다가 되돌리면 쓸 수 있는
  포인터가 나온다. CHERI 이식 안내가 "`long` 대신 `uintptr_t`를 쓰라"고
  말하는 이유가 이것이다. 반면 *출처가 없는 평범한 정수에서 포인터를 만들거나,
  비트를 임의로 주물러 조립한 값*은 유효한 태그를 얻지 못해 쓸 수 없다.

  즉 CHERI 가 막는 것은 "정수로 바꾸는 일" 자체가 아니라 *출처 없는 주소를
  포인터로 위조하는 일*이다 — 35장의 프로버넌스가 표준의 궤변이 아니라
  하드웨어의 규칙이 되는 자리이고, 표준이 `uintptr_t` 왕복만을 보장하는
  이유이기도 하다.

  *주소의 상위 비트를 쓰는 기계.* AArch64 의 TBI(Top Byte Ignore)와 MTE,
  x86-64 의 LAM 은 포인터 *크기*는 그대로 두되 상위 바이트에 태그를 싣는다.
  크기가 같다고 해서 "포인터 = 주소값" 이 아니라는 사실을 보여 주는 현대적
  사례다(6장의 태그 포인터).

  *같은 CPU, 다른 ABI.* x86-64 의 x32 ABI 는 64비트 명령을 쓰면서 포인터만
  32비트로 둔다. WebAssembly 도 wasm32 와 wasm64 가 따로 있다. 기계가
  같아도 *빌드 대상*이 크기를 바꾼다.
]

#misconception[
  "어차피 요즘 기계는 다 64비트니 `sizeof(void *)`는 8이다"
][
  지금 이 책의 예제를 돌리는 환경에서는 8이 맞다. 그러나 그 8을 *코드에
  적는* 순간 그 코드는 위의 어느 세계로도 옮겨 갈 수 없게 된다. 흔한 사고는
  두 가지다 — 구조체에 포인터를 담을 자리를 `long`이나 `int`로 잡는 것,
  그리고 포인터 배열의 크기를 `n * 8`로 계산하는 것. 둘 다 x86-64 에서는
  조용히 잘 돌다가 32비트 빌드나 CHERI 에서 무너진다.

  규칙은 간단하다. *크기는 언제나 `sizeof`에게 묻고*, 포인터를 정수로
  담아야 하면 `uintptr_t`를 쓰고(35장), 함수 포인터를 `void *`에 담는
  일은 하지 않는다. C 표준은 그 변환을 보장하지 않는다 — POSIX 가 `dlsym`
  때문에 따로 요구할 뿐이다.
]

#qa[
  그러면 `void *`는 왜 "아무 포인터나 담는 그릇"으로 쓸 수 있는가?
][
  표준이 그 하나만은 특별히 약속했기 때문이다. *어떤 객체 포인터든
  `void *`로 바꿨다가 원래 타입으로 되돌리면 원래 값과 같다.* 그래서
  `malloc`이 `void *`를 돌려주고, `qsort`가 `void *`로 비교자를 받는다.
  주의할 것은 두 가지다 — 이 약속은 *객체* 포인터에만 걸려 있고(함수
  포인터는 빠져 있다), `void *`는 가리키는 대상의 크기를 모르므로 산술도
  역참조도 할 수 없다(더하기를 허용하는 컴파일러가 있지만 그것은 확장이다).

  `char *`는 또 다른 특권을 가진다 — 어떤 객체든 그 표현을 바이트 단위로
  들여다볼 수 있다. 두 특권을 나란히 두면 이렇게 된다: *`void *`는 "무엇을
  가리키는지 잊는" 통로이고, `char *`는 "무엇이든 바이트로 읽는" 통로다.*
  이 둘이 같은 표현을 갖도록 표준이 못박아 둔 것도 우연이 아니다 — 워드
  주소 기계에서 가장 넓은 주소를 담아야 하는 포인터가 바로 이 둘이었다.
  자세한 규칙과 그 대가는 35장에서 이어 간다.
]

포인터의 개념이 갖춰졌다. 다음 장은 이 부의 첫 안전 수칙 — "아무 데도
가리키지 않음"을 다루는 법이다. 6장에서 얼굴만 익힌 널 삼형제를,
이번에는 문법과 실무 규칙으로 정식 취급한다.
