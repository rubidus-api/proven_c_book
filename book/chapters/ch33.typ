#import "../lib.typ": *

= 배열

#organizer[
  연속된 기억 — 배열이다. 선언과 순회, 배열과 포인터의 진짜 관계
  (악명 높은 "무너짐"), 그리고 경계라는 생사의 규칙까지. 22장의
  `char line[100]` 외상이 여기서 청산된다.
]

#deepqa[
  9장에서 캐시 라인은 "이웃 예순네 칸을 상자째" 나른다고 했고, 배열을
  차례로 훑는 코드가 빠른 이유가 그것이라 했다. 그러면 "배열"이란
  기억의 눈으로 정확히 무엇인가?
][
  같은 타입의 칸들이 *빈틈없이 이웃하여* 늘어선 것 — 그것이 전부다.
  `int a[5]`는 int 다섯 개가 연속된 주소에 나란히 놓인 것이고, 그래서
  첫 칸의 주소와 타입만 알면 나머지 위치가 전부 계산된다($i$번 원소 =
  시작 주소 + $i times$ 칸 크기). 이 "계산 가능한 이웃"이라는 성질이
  배열의 힘(즉시 접근, 캐시 친화)이자 위험(경계 계산 착오)의 뿌리다.
]

== 선언, 접근, 순회

#demo("examples/ch33/arr.c")

`int a[5] = {3, 1, 4, 1, 5};` — 타입, 이름, 칸 수, 그리고 중괄호
초기화. 접근은 `a[i]`이고 *번호는 0부터*다(첫 원소가 `a[0]`, 마지막이
`a[4]`). 0부터 세는 이유는 위의 deepqa가 이미 답했다 — `a[i]`의 정체가
"시작에서 $i$칸 *떨어진* 곳"이라서, 첫 원소는 떨어진 거리가 0이다.

== 배열과 포인터 — 무너짐의 진실

시연의 뒷부분이 이 장의 심장이다. C에서 가장 많은 혼란을 낳은 관계 —
배열과 포인터 — 를 정확히 정리한다.

*규칙: 배열의 이름은 대부분의 문맥에서 "첫 원소의 주소"로 무너진다
(decay).* `int *p = a;`가 합법인 이유다 — `a`가 `&a[0]`이라는 포인터
값으로 읽힌 것이다. 그리고 `a[i]`라는 표기 자체가 사실 `*(a + i)`의
당의정이다 — 포인터에 정수를 더하면 "그 타입 크기만큼 $i$칸 옆"의
주소가 되고(포인터 산술 — 32장의 규칙들이 여기 걸린다), 그것을
역참조한 것이 인덱스 접근이다. 시연의 `a[2] == *(a + 2)`가 그 확인이다.

#misconception[
  "배열은 포인터다"
][
  이 유명한 문장은 틀렸다 — 무너짐이 하도 자주 일어나서 생긴 착시다.
  배열은 칸 다섯 개짜리 *기억 그 자체*이고, 포인터는 주소 하나를 담는
  *다른 변수*다. 시연의 마지막 줄이 결정적 증거다: `sizeof a`는 20
  (int 5칸의 총 크기), `sizeof p`는 8(주소 하나의 크기, 30장). 무너짐이
  일어나지 않는 대표 문맥이 바로 `sizeof`라서 이 차이가 보인다 — 22장의
  `fgets(line, sizeof line, stdin)`이 그릇 크기를 옳게 잰 것도 이 덕분
  이다. 한 가지 파급이 중요하다: 배열을 함수에 "넘기면" 무너진 *주소만*
  복사되므로(29장), 함수 쪽에서는 `sizeof`로 크기를 알 수 없다 — 그래서
  C 함수는 배열과 *길이를 별도 인자로* 함께 받는 것이 관행이다. 7장에서
  본 "길이를 데이터 곁에 두는" 표현 방식들의 문제의식이, C 함수 경계에서
  이렇게 재현된다.
]

== 경계 — 생사의 규칙

배열의 안전 수칙은 단 하나이고, 타협이 없다 — *유효한 번호는 0부터
칸수−1까지다.* `a[5]`(다섯 칸짜리에서)를 읽거나 쓰는 것은 계약 밖이고,
그 칸은 *남의 기억*이다 — 이웃 변수일 수도, 35장에서 볼 함수 호출의
장부일 수도 있다. 읽으면 쓰레기이고, 쓰면 남의 데이터가 조용히 부서진다
— 배열 경계 침범(buffer overrun)은 C 역사상 가장 많은 사고와 보안
취약점을 낳은 단일 원인이다(다음 두 장이 그 실화들이다).

경계에 관해 표준이 특별히 허락한 자리가 딱 하나 있다 — 32장에서 예고한
*한 칸 지난 자리*(`a + 5`)의 "주소를 만드는 것"까지는 합법이다(따라
가지만 않으면). 순회 관용구가 그 위에 서 있다:

```c
for (int *it = a; it != a + 5; it += 1) { /* *it 사용 */ }
```

#qa[
  경계를 어기면 컴파일러가 잡아 주지 않는가?
][
  일부만이다. `a[7]`처럼 상수로 뻔한 위반은 요즘 컴파일러 경고가 잘
  잡지만, 번호가 계산 결과일 때는 컴파일 시점에 알 수 없다 — 경계
  검사를 실행 중에 상시로 하는 것은 C가 성능을 위해 *하지 않기로 한*
  일이기 때문이다(이 선택의 대가와 보완이 이 부의 남은 주제다).
  실행 중 그물은 15장의 ASan이다 — 경계 침범을 일으키는 코드를 ASan
  빌드로 돌리면 침범의 순간 파일·줄 번호와 함께 잡아 준다. 그리고
  애초에 경계 검사가 내장된 부품을 쓰는 길이 있다 — 35장의 proven이
  그 길이다.
]

연속된 기억을 다루게 됐다. 다음 장은 배열의 가장 유명한 사용처 —
문자열이다. 6·7장의 배경지식(문자=번호, NUL 종단이라는 선택)이 드디어
C 문법과 완전히 만난다.
