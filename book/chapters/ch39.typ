#import "../lib.typ": *

= 다차원 배열

#prereq(
  ([38장 배열], [무너짐과 포인터 산술의 계약]),
  ([37장 포인터의 규칙], [정렬과 프로버넌스]),
  ([11장 기억의 분화], [캐시 라인과 지역성]),
)

#deepqa[
  38장에서 `a[i]`가 `*(a + i)`의 당의정이고, 포인터에 정수를 더하면 *가리키는
  타입의 크기만큼* 움직인다고 했다. 그러면 `int a[3][4]`에서 `a + 1`은 몇
  바이트를 움직이는가?
][
  *16바이트* — `int` 하나(4)가 아니라 `int` 넷짜리 행 하나다. 열쇠는 `a`의
  원소가 무엇인가에 있다. `int a[3][4]`는 "int 4개짜리 배열이 3개"이고, 따라서
  `a`의 원소 하나는 *행 전체*(`int[4]`)다. 38장의 규칙은 하나도 바뀌지 않았다 —
  "원소 하나만큼 움직인다"가 그대로 적용됐을 뿐인데, 그 원소가 배열인 것이다.

  이 장은 그 한 문장에서 자라 나오는 모든 것이다.
]

#organizer[
#idx("다차원 배열")  다차원 배열 — 배열의 배열이다. 기억에 어떻게 놓이는지(행 우선), 첨자가
  어떻게 풀리는지, 왜 `int *`가 아닌지, 같은 주소를 다른 타입으로 보는 것이
  어디까지 허용되는지, 그리고 실무의 무늬들(선행 차원, 행 포인터, 스트라이드)
  과 행렬 계산에서의 순회 순서까지 본다.
]

#chapter-questions()

== 배열의 배열 — 실체부터

`int a[3][4]`를 읽는 법은 하나뿐이다. *`int` 4개짜리 배열이 3개.* "3행 4열"은
사람의 해석이고, 타입이 말하는 것은 "원소가 `int[4]`인 배열"이다.

#demo("examples/ch39/md_layout.c")

시연의 첫 묶음이 그 확인이다. `sizeof a`는 48(전체), `sizeof a[0]`은 16(행
하나), `sizeof a[0][0]`은 4(원소 하나)다. 즉 `a`의 원소는 행이고, 행의 원소가
정수다.

*기억은 한 덩어리다.* 두 번째 묶음의 오프셋을 보면 `a[0][0]`부터 `a[2][3]`까지
0, 4, 8 … 44로 빈틈없이 이어진다. 표준은 이 배치를 못박아 둔다 — *마지막
첨자가 가장 빨리 변한다*(row-major, 행 우선). 다차원 배열이라고 해서 기억이
격자로 접히는 것이 아니라, 행을 차례로 이어 붙인 *한 줄*이다.

#qa[
#idx("그러면 열 우선")  그러면 열 우선(column-major)으로 두는 언어도 있는가?
][
  있다. 포트란이 대표이고, MATLAB·R·줄리아, 그리고 OpenGL의 행렬 관례가 열
  우선이다. 어느 쪽이 옳은 것이 아니라 *약속*이며, C는 행 우선을 골랐다.

  이 차이가 실무에서 문제가 되는 자리는 분명하다 — 포트란으로 쓰인 수치
  라이브러리(BLAS·LAPACK)를 C에서 부를 때다. 같은 기억을 서로 다른 순서로
  읽으므로, 그대로 넘기면 *전치된 행렬*을 넘기는 셈이 된다. 그래서 그 계열의
  API에는 "이 행렬은 전치해서 보라"는 인자(`CblasRowMajor` / `CblasColMajor`,
  또는 `trans` 플래그)가 거의 언제나 붙어 있다.
]

== 첨자식이 풀리는 과정

`a[i][j]`는 마법이 아니다. 38장의 규칙 두 개를 두 번 적용한 것뿐이다.

$ a[i][j] arrow.r.double *(a + i)[j] arrow.r.double *(*(a + i) + j) $

한 단계씩, 타입과 함께 따라가면 이렇다.

#dtable(
  columns: 3,
  [*수식*], [*타입*], [*한 걸음의 크기*],
  [`a`], [`int[3][4]` → 무너져 `int (*)[4]`], [—],
  [`a + i`], [`int (*)[4]`], [`sizeof(int[4])` = 16바이트],
  [`*(a + i)`], [`int[4]` → 무너져 `int *`], [—],
  [`*(a + i) + j`], [`int *`], [`sizeof(int)` = 4바이트],
  [`*(*(a + i) + j)`], [`int` (좌변값)], [—],
)

시연의 세 번째 묶음이 이 표를 실제 수로 보여 준다. `a + 2`가 오프셋 +32(=2 ×
16), `*(a+2) + 1`이 +36(=32 + 1 × 4). 그래서 `a[2][1]`의 값이 나온다.

*여기에 새 규칙은 없다.* 38장에서 "포인터 + 정수는 원소 크기만큼 움직인다"고
했고, 그 원소가 이번에는 `int[4]`였을 뿐이다. 다차원 배열이 어려워 보이는
이유의 절반은 이 사실을 놓치기 때문이다.

#figure-svg("mdarray", caption: [`a[2][1]`을 찾아가는 두 번의 점프 — 행 크기로 한 번, 원소 크기로 한 번.])

#qa[
  그러면 `a[i][j]`와 `a[j][i]`는 왜 다른 자리인가? 둘 다 결국 덧셈 아닌가?
][
  덧셈이지만 *곱해지는 값이 다르다.* 오프셋을 바이트로 풀어 쓰면
  `i × 16 + j × 4`다. `i`에 붙는 계수(행의 크기)와 `j`에 붙는 계수(원소의 크기)가
  다르므로, 둘을 바꾸면 다른 자리가 된다 — `a[1][2]`는 +24, `a[2][1]`은 +36이다.

  일반화하면 $n$차원 배열 `T a[d_1][d_2]...[d_n]`에서 `a[i_1]...[i_n]`의 오프셋은
  $ (((i_1 times d_2 + i_2) times d_3 + i_3) dots times d_n + i_n) times "sizeof"(T) $
  이다. 안쪽 차원의 크기들이 전부 계수로 들어오므로, *가장 바깥 차원의 크기만은
  계산에 쓰이지 않는다* — 그래서 매개변수에서 첫 차원만 생략할 수 있다(다음 절).
]

== 매개변수 — 왜 `int *`가 아닌가

38장에서 "매개변수의 배열은 포인터로 무너지되 *가장 바깥 차원만* 벗겨진다"고
했다. 다차원에서 그 규칙이 실제로 무엇을 뜻하는지 보자.

```c
void f(int m[3][4]);   /* 셋은 컴파일러에게 완전히 같은 선언이다 */
void f(int m[][4]);
void f(int (*m)[4]);
```

바깥 차원 `3`은 사라지고 `4`는 남는다. 앞 절의 오프셋 식이 그 이유다 — 자리를
계산하는 데 필요한 것은 *안쪽 차원들*이고, 바깥 차원은 쓰이지 않는다. 그래서
안쪽 차원은 반드시 적어야 하고(`int m[][]`는 컴파일되지 않는다), 바깥은 적어도
검사되지 않는다.

#demo("examples/ch39/md_param.c")

#misconception[
  "2차원 배열은 `int *`로 받으면 된다"
][
  가장 흔하고 가장 비싼 오해다. `int a[3][4]`가 무너지면 `int (*)[4]`이지
  `int *`가 아니다. 둘은 *기억 배치가 아예 다르다.*

  `int (*)[4]`는 "정수가 12개 줄지어 있는 곳"의 첫 행을 가리킨다. 주소 하나로
  모든 칸을 계산해 낸다. 반면 `int *`는 "정수를 가리키는 포인터들이 줄지어
  있는 곳"을 가리킨다 — 자리를 알려면 주소를 *두 번* 따라가야 하고, 그 포인터
  배열이 실제로 있어야 한다.

  그래서 진짜 2차원 배열을 `int *`를 받는 함수에 넘기면 컴파일러가 거절한다.
  거절을 캐스트로 눌러 넘기면 정수 몇 개를 *주소로 착각해* 따라가는, 진단도
  나오지 않는 붕괴가 된다. 시연의 세 번째 묶음이 두 배치의 크기와 간접 참조
  횟수를 나란히 보여 준다.
]

*VLA 매개변수*가 이 자리에서 값을 한다(38장). 폭이 실행 중에 정해져도
`a[i][j]` 표기를 그대로 쓸 수 있다.

```c
void sum(size_t rows, size_t cols, const int a[rows][cols]);
```

크기를 앞에 받는 것이 규칙이다 — 뒤에 오는 차원 이름이 이미 선언되어 있어야
하기 때문이다. 이것이 오늘날 수치 코드에서 가장 읽기 좋은 형태이고, 이 책이
권하는 기본값이다.

== 같은 주소, 다른 눈 — 평탄화의 계약

시연의 마지막 묶음이 이 절의 출발점이다. `a`, `a[0]`, `&a[0][0]`, `&a`는 *모두
같은 주소*다. 그러나 타입이 다르므로 *한 걸음의 크기가 다르다* — 각각 16, 4,
4, 48바이트다. 같은 번호를 들고 있어도 그 번호로 할 수 있는 일이 다르다(35장의
"주소는 그냥 정수가 아니다"가 여기서 실물이 된다).

그렇다면 이런 질문이 자연스럽게 나온다.

#qa[
  `int *p = &a[0][0];` 로 받아 `p[7]`처럼 평탄하게 훑어도 되는가? 어차피 같은
  기억이고, 오프셋도 맞는데.
][
  거의 모든 컴파일러에서 *돌아가지만*, 표준의 글자로는 계약 밖이다. 이유를
  정확히 나누어 보아야 한다 — 두 규칙이 얽혀 있는데 걸리는 것은 하나뿐이다.

  *엄격한 앨리어싱(§6.5)은 문제가 아니다.* 이 규칙은 "객체의 유효 타입과
  맞지 않는 타입의 좌변값으로 읽지 말라"는 것인데, 여기서 읽는 대상인
  `a[i][j]`의 유효 타입은 어느 쪽으로 접근하든 `int`다. `int` 좌변값으로
  읽으니 규칙을 어기지 않는다.

  *걸리는 것은 포인터 산술의 범위다(§6.5.6, 그리고 37장의 프로버넌스).*
  `&a[0][0]`은 *`a[0]`이라는 원소 4개짜리 배열*의 첫 원소를 가리킨다. 그
  포인터로 갈 수 있는 곳은 `a[0]`의 안쪽과 마지막 다음 자리까지다. `p + 4`는
  마지막 다음이라 만들기까지는 되지만 *역참조는 금지*이고, `p + 5`는 만드는
  것부터 계약 밖이다. `a[1][0]`이 바로 그 주소에 있다는 사실은 규칙과 무관하다 —
  규칙은 주소가 아니라 *출처*로 따진다.
]

이 회색지대는 오래된 것이고, 위원회도 실무의 관행을 알고 있다. 그럼에도 규칙이
그대로인 이유는 최적화기가 이 약속에 기대기 때문이다 — "이 포인터는 저 행
안에서만 움직인다"를 믿을 수 있어야 루프를 다시 짤 수 있다(38장의 실제 사례와
같은 논리다).

#realcase("반대 방향은 튼튼하다 — 평탄하게 잡고 2차원으로 보기")[
  실무의 해답은 방향을 뒤집는 것이다. *선언된 2차원 배열을 평탄하게 훑는* 대신,
  *평탄하게 할당한 기억을 2차원으로 보는* 것이다.

  ```c
  double *m = malloc(rows * cols * sizeof *m);
  double (*view)[cols] = (double (*)[cols])m;   /* 2차원 뷰 */
  view[i][j] = ...;
  ```

  이쪽이 튼튼한 이유가 있다. `malloc`이 준 기억에는 *선언된 타입이 없고*,
  표준은 그런 객체의 유효 타입을 "그 자리에 값을 쓰는 데 쓰인 좌변값의 타입"으로
  정한다(§6.5). 즉 2차원으로 쓰는 순간 그 모양이 유효 타입이 된다. 그리고 포인터
  산술의 범위도 *할당된 블록 전체*이므로, 어느 방향으로 훑든 출처를 벗어나지
  않는다.

  그래서 실무의 규칙은 이렇게 정리된다 — *평탄하게 다루고 싶으면 처음부터
  평탄하게 잡는다.* 굳이 선언된 2차원 배열을 평탄하게 넘겨야 한다면, 행 단위로
  다루거나 `memcpy`로 옮긴다.
]

== 실무의 무늬들

다차원 자료를 다루는 방식은 넷으로 정리된다. 각각이 어디서 쓰이는지 실제
사례와 함께 본다.

#dtable(
  columns: 4,
  [*무늬*], [*모양*], [*기억 배치*], [*대표 사례*],
  [고정 폭 2차원], [`int a[R][C]`, `int (*)[C]`], [한 덩어리], [화면 버퍼, 게임 판, 임베디드 표],
  [VLA 매개변수], [`a[rows][cols]`], [한 덩어리], [수치 코드, C99 이후의 기본값],
  [평탄 + 선행 차원], [`a[i * lda + j]`], [한 덩어리], [BLAS·LAPACK, 부분행렬],
  [행 포인터 배열], [`int *rows[R]`, `int *`], [행마다 따로], [`argv`, 이미지 라이브러리, 들쭉날쭉한 행],
)

=== 평탄 + 선행 차원 — 수치 라이브러리의 공용어

#demo("examples/ch39/md_flat.c")

수치 계산 세계에서 가장 널리 쓰이는 무늬다. 핵심은 *열 수와 행 간격을 분리*
하는 것이다.

- *열 수*(`cols`)는 이 행렬이 실제로 쓰는 열의 개수다.
- *선행 차원*(leading dimension, BLAS에서 `lda`)은 한 행에서 다음 행까지의
  간격이다.

둘이 같을 필요가 없다는 데서 힘이 나온다. `lda`를 원본 그대로 두고 시작점만
옮기면, *부분행렬이 복사 없이 만들어진다.* 시연에서 `sub`가 원본의 (1,1)을
가리키고 간격은 5 그대로다 — 그래서 `sub`에 쓰면 원본이 바뀐다. 뷰이기
때문이다.

이 설계가 BLAS·LAPACK의 API가 반세기 가까이 유지되는 이유이기도 하다. 행렬
곱 함수 하나가 부분행렬·전치·패딩된 버퍼를 전부 받아 낼 수 있는 것은, 자료의
모양을 *시작 주소 + 간격*이라는 두 수로 환원했기 때문이다.

#qa[
  간격을 따로 두면 좋은 점이 부분행렬뿐인가?
][
  셋 더 있다.

  *정렬을 맞출 수 있다.* SIMD 명령은 행의 시작 주소가 16·32·64바이트 경계에
  놓이기를 원한다. 열 수가 어중간하면 행 끝에 패딩을 넣고 `lda`를 늘려 맞춘다 —
  자료의 논리적 모양은 그대로 두고 물리적 간격만 바꾸는 것이다.

  *전치가 공짜가 된다.* 읽는 순서만 바꾸면 되므로(시연의 마지막 묶음), 전치된
  행렬을 따로 만들 필요가 없다. BLAS의 `trans` 플래그가 하는 일이 정확히 이것이다.

  *더 일반화하면 스트라이드가 된다.* 행 간격만이 아니라 *축마다* 간격을 두면,
  전치·부분 뷰·역순 보기가 전부 간격의 조작이 된다. NumPy의 `strides`, OpenCV의
  `Mat::step`이 그 일반형이고, C23의 이웃 세계인 C++에서는 `std::mdspan`이 같은
  발상을 타입으로 굳혔다.
]

=== 행 포인터 배열 — 흩어진 행을 하나로 묶기

`int *rows[R]`은 배치가 아예 다르다. 행들이 기억의 서로 다른 자리에 있어도
되고, 행마다 길이가 달라도 된다.

시연의 뒷부분이 두 성질을 보여 준다. *행 교환이 포인터 교환*이 되므로 자료를
옮기지 않고 순서를 바꿀 수 있고(정렬·피벗팅에서 값을 한다), *들쭉날쭉한 행*을
담을 수 있다.

대표 사례가 셋이다.

- `int main(int argc, char *argv[])` — 길이가 제각각인 문자열들의 배열이다.
  52장에서 볼 `argv`가 바로 이 무늬다.
- *이미지 라이브러리* — libjpeg의 `JSAMPARRAY`가 행 포인터 배열이다. 이미지를
  한 줄씩 처리하는 API에서, 줄 단위로 버퍼를 갈아 끼울 수 있어야 하기 때문이다.
- *들쭉날쭉한 자료* — 문장마다 길이가 다른 텍스트, 노드마다 자식 수가 다른 그래프.

대가도 분명하다. 간접 참조가 한 겹 더 있고, 행들이 흩어지면 11장의 지역성이
깨진다. 그리고 할당과 해제가 행 수만큼 늘어난다.

== 순회 순서와 캐시 — 왜 같은 계산이 몇 배씩 갈리는가

같은 자료를 같은 횟수만큼 읽는데도 순서에 따라 속도가 갈린다. 이 사실이
다차원 배열에서 가장 실용적인 지식이다.

#demo("examples/ch39/md_stride.c")

시연은 시간을 재지 않는다(기계마다 다르므로). 대신 *접근의 간격*을 센다.
64×64 `double` 행렬을 두 순서로 4096번 읽었을 때, 만지는 캐시 라인의 총수는
512개로 같다. 갈리는 것은 *연속성*이다 — 행 우선은 3584번을 직전과 같은 라인
안에서 해결하고, 열 우선은 한 번도 그러지 못한다.

이유는 11장의 사다리 그대로다. 캐시는 바이트가 아니라 *라인*(대개 64바이트)
단위로 기억을 실어 온다. `double`이라면 한 라인에 8개가 실린다.

- *행 우선(`for i { for j { a[i][j] } }`)* — 안쪽 걸음이 8바이트다. 한 번
  실어 온 라인에서 여덟 번을 쓰고 다음 라인으로 간다. 게다가 주소가 규칙적으로
  증가하므로 *하드웨어 프리페처*가 다음 라인을 미리 실어 온다.
- *열 우선(`for j { for i { a[i][j] } }`)* — 안쪽 걸음이 한 행의 크기(여기서는
  512바이트)다. 매번 다른 라인을 건드리고, 실어 온 라인의 나머지 7개는 쓰지도
  못한 채 밀려난다.

행렬이 캐시보다 커지면 차이가 눈에 띄게 벌어진다. 흔히 인용되는 수치가 *몇
배에서 열 배 이상*인데, 정확한 값은 기계와 크기에 따라 다르므로 이 책은 수를
못박지 않는다. 기억할 것은 원리다 — *안쪽 루프가 기억을 이어서 훑게 만든다.*

#realcase("행렬 곱에서 루프 순서를 바꾸는 것만으로")[
  고전적인 예가 행렬 곱 `C = A × B`다. 교과서대로 쓰면 안쪽 루프가 `k`이고,
  그 안에서 `B[k][j]`가 *열 방향*으로 읽힌다 — 최악의 순서다.

  ```c
  for (i) for (j) for (k) C[i][j] += A[i][k] * B[k][j];   /* ijk */
  for (i) for (k) for (j) C[i][j] += A[i][k] * B[k][j];   /* ikj */
  ```

  두 번째(`ikj`)는 계산량이 똑같은데 안쪽 루프에서 `B[k][j]`와 `C[i][j]`가
  둘 다 행 방향으로 흐른다. 그것만으로 큰 행렬에서 몇 배가 빨라진다.

  실전의 수치 라이브러리는 여기서 한 걸음 더 간다 — 행렬을 캐시에 들어가는
  크기의 *타일*(블록)로 잘라, 한 번 실어 온 조각을 최대한 여러 번 쓰고 버린다.
  BLAS 구현들이 단순한 삼중 루프보다 열 배 이상 빠른 이유의 상당 부분이 이
  타일링과 SIMD에 있다. 알고리즘이 아니라 *기억을 다루는 순서*가 성능을 갈랐다.
]

#platform("배경 지식 — 이터레이터라는 관념")[
  지금까지 한 이야기를 다른 언어의 낱말로 옮기면 *이터레이터*(iterator)다.
  "자료 구조를 어떻게 훑을 것인가"를 자료 구조 자체와 분리해 하나의 값으로
  만든 것인데, C에는 그 이름이 없을 뿐 물건은 있다 — *포인터가 곧 이터레이터*다.

  ```c
  for (int *p = a[0]; p != a[0] + 12; ++p) ...   /* 시작·끝·전진 */
  ```

  시작점, 끝점, 그리고 "다음으로 가는 법"만 있으면 훑기가 성립한다는 발상은
  같다. C++의 `begin()`/`end()`, 파이썬의 `__iter__`, 자바의 `Iterator`가 이
  셋을 이름 붙여 규격으로 만든 것이다.

  다차원에서는 이 관념이 특히 쓸모 있다. 앞 절의 *선행 차원*과 *스트라이드*는
  결국 "다음으로 가는 법"을 축마다 따로 적어 둔 것이고, NumPy의 `nditer`나
  C++의 `mdspan`은 그 규칙을 값으로 만들어 넘긴다. C에서는 그 값이 없으니
  `(시작 주소, 간격)` 두 수를 손으로 들고 다니는 셈이다 — 무늬는 같고, 이름만
  없다.
]

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [실체], [`int a[3][4]`는 "`int[4]`가 3개" — 원소가 행이다],
    [배치], [행 우선. 마지막 첨자가 가장 빨리 변한다],
    [첨자], [`a[i][j]` = `*(*(a+i)+j)`, 오프셋 = `i×행크기 + j×원소크기`],
    [매개변수], [바깥 차원만 벗겨진다. `int (*)[4]`이지 `int *`가 아니다],
    [평탄화], [선언된 2차원을 평탄하게 훑는 것은 회색지대(출처)],
    [반대 방향], [평탄하게 잡고 2차원 뷰를 씌우는 것이 정공법],
    [선행 차원], [시작 주소 + 간격 = 부분행렬·전치·정렬이 공짜],
    [순회], [안쪽 루프가 기억을 이어서 훑게 한다],
  )
]

배열이 겹쳐 쌓이는 자리를 보았다. 다음 장은 그 배열 중에서도 가장 많이 쓰이고
가장 많이 다치는 것 — 문자의 배열, 곧 문자열이다.

행과 열을 얻었으니, 그 위를 *어떤 순서로 걷는가*가 다음 질문이다 --- 다음 장이
중첩 루프의 기법이다.
