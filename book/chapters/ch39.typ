#import "../lib.typ": *

= 구조체를 부리는 법 — 임시 값, 이름 붙은 인자, 배치

#organizer[
  38장이 구조체의 문법이었다면 이 장은 *쓰는 법*이다. 구조체 안의 구조체를
#idx("복합 리터럴")  참조하는 표기, 그 자리에서 만들어 넘기는 임시 구조체(복합 리터럴),
  그것으로 만드는 "순서 없는 이름 붙은 인자" 관용구, 그리고 멤버 사이에
#idx("패딩")  끼는 패딩과 그것을 없애거나 강제하는 방법까지. 마지막으로 배열을
  값으로 넘기는 유일한 길과 그 대가를 잰다.
]

#deepqa[
  38장에서 구조체는 값이라 대입하면 통째로 복사되고 함수에 넘기면 사본이
  건너간다고 했다. 그런데 33장에서는 배열을 함수에 넘기면 포인터로 무너져
  *원본이 건드려진다*고 했다. 두 말이 부딪히지 않는가 — 구조체 안에 배열이
  있으면 어느 쪽인가?
][
  구조체가 이긴다. 배열이 포인터로 무너지는 것은 *배열 그 자체를 인자로
  적었을 때*의 규칙이고, 구조체 멤버로 들어간 배열은 구조체라는 값의 일부라
  복사 대상이다. 그래서 C에서 배열을 진짜로 *값처럼* 넘기는 유일한 방법이
  "구조체로 감싸기"다. 이 장의 두 번째 예제가 그 대비를 실측으로 보인다 —
  값으로 넘긴 쪽은 원본이 그대로이고, 포인터로 무너진 쪽은 원본이 바뀐다.
]

== 중첩과 접근 — 점과 화살표를 섞어 읽기

구조체의 멤버가 또 구조체일 수 있다. 이때 표기는 겹쳐 적을 뿐이다.

```c
struct point { int x, y; };
struct rect  { struct point origin; struct point size; };
struct scene { struct rect *frame; const char *name; };

struct rect  r  = { .origin = { .x = 1, .y = 2 }, .size = { .x = 30, .y = 40 } };
struct scene s  = { .frame = &r, .name = "main" };

r.origin.x        /* 값 안의 값        : 점 + 점 */
s.frame->size.y   /* 값 안의 포인터    : 점 + 화살표 + 점 */
(&r)->origin.y    /* 화살표는 (*p).의 줄임일 뿐이다 */
```

규칙은 하나뿐이다. *왼쪽이 값이면 점, 포인터면 화살표.* `p->x`는
`(*p).x`의 줄임이고(38장), 줄임이 생긴 이유는 포인터로 구조체를 다루는 일이
압도적으로 흔해서다. 실제로 `(*p).x`라고 적힌 코드를 보면 대개 옛 코드이거나
연산자 우선순위를 설명하는 자리다.

#qa[
  `s.frame->size.y`처럼 길게 이어진 표기는 어디서 끊어 읽는가?
][
  왼쪽에서 오른쪽으로, 한 단계씩 내려간다. `s`(장면) → `.frame`(그 안의
  포인터) → `->size`(가리키는 사각형의 크기) → `.y`(그 점의 y). 각 화살표는
  *역참조가 한 번 일어난다*는 표시이므로, 화살표의 개수만큼 널 검사가 필요한
  포인터가 있다는 뜻이기도 하다. `s.frame`이 널이면 이 표기는 그 자리에서
  무너진다 — 긴 사슬은 읽기 편한 만큼 검사를 감추기도 한다.
]

== 임시 구조체 — 복합 리터럴

이름 붙은 변수를 만들지 않고 *그 자리에서* 구조체 값을 하나 만드는 표기가
복합 리터럴이다(C99).

```c
draw_((struct draw_opts){ .width = 40, .title = "차트" });   /* 인자로 바로 */
return (struct point){ .x = a.x + dx, .y = a.y + dy };       /* 반환값으로 */
```

성질 세 가지를 챙긴다.

*첫째, 이것은 lvalue다.* 이름이 없을 뿐 진짜 객체라서 주소를 얻을 수 있고
멤버에 대입할 수도 있다. 예제의 `&(struct draw_opts){ … }`가 그 확인이다.
"임시값(rvalue)이라 주소를 못 얻는다"고 생각하기 쉬운데, C의 복합 리터럴은
그렇지 않다 — C++의 임시 객체와 다른 점이다.

*둘째, 수명은 문장이 아니라 블록 끝까지다.* 블록 안에 적은 복합 리터럴은
그 블록이 끝날 때까지 산다(자동 저장 기간). 그래서 같은 블록 안에서는
주소를 들고 다녀도 안전하다.

*셋째, 그러므로 함수 밖으로 주소를 내보내면 매달린 포인터다.*

#antipattern[
  복합 리터럴의 주소를 반환하기
][
  ```c
  struct point *make(int x, int y)
  {
      return &(struct point){ .x = x, .y = y };   /* 함수가 끝나면 사라진다 */
  }
  ```
  32장의 지역 변수 주소 반환과 정확히 같은 사고다. 값을 돌려주려면 *값으로*
  돌려주거나(`struct point make(...)`), 호출자가 준 자리에 채우거나,
  동적 할당(37장)을 쓴다. 파일 유효 범위에 적은 복합 리터럴은 정적 저장
  기간이라 이 문제가 없지만, 그 자리에는 대개 이름을 붙이는 편이 낫다.
]

== 이름 붙은 인자 — 구조체 하나로 넘기기

#demo("examples/ch39/opts.c")

여기서부터가 실무에서 코드를 가장 많이 바꾸는 관용구다. 인자가 대여섯 개인
함수를 생각해 보자.

```c
draw(40, 20, false, true, 3, "차트");    /* 세 번째 참은 무엇인가? */
```

C에는 다른 언어의 이름 붙은 인자(named argument)나 기본값이 없다. 그러나
*지정 초기화 + 복합 리터럴*을 겹치면 사실상 같은 것을 얻는다.

```c
struct draw_opts { int width; int height; bool grid; const char *title; };
static void draw_(struct draw_opts o);
#define draw(...) draw_((struct draw_opts){ __VA_ARGS__ })

draw(.title = "차트", .height = 20, .width = 40);   /* 순서 무관 */
draw(.grid = true);                                  /* 나머지는 기본값 */
draw();                                              /* 전부 기본값 */
```

얻는 것이 네 가지다.

+ *순서에서 자유롭다.* 지정 초기화는 이름으로 자리를 정하므로 호출자가
  편한 순서로 적는다.
+ *빠뜨린 것은 0이다.* 적지 않은 멤버는 0(포인터는 널)으로 채워진다는
  표준의 약속이 곧 "기본값"이 된다. 그래서 *0이 뜻이 통하는 기본값이
  되도록* 필드를 설계하는 것이 요령이다 — 예제가 `width == 0`을 "기본 80"
  으로 읽는 것이 그 예다.
+ *호출부가 자기 설명적이다.* 위 `false, true, 3`이 무엇인지 묻지 않아도
  된다.
+ *나중에 필드를 늘려도 기존 호출이 깨지지 않는다.* 인자 목록을 바꾸면 모든
  호출부를 고쳐야 하지만, 구조체에 멤버를 하나 더하는 것은 기존 호출부에
  아무 영향이 없다(그 멤버는 0이 된다). 오래 유지되는 API에서 이 성질이
  특히 값지다.

#qa[
  이 관용구에 함정은 없는가?
][
  셋을 조심한다.

  첫째, *초기화 항목들 사이의 평가 순서는 정해져 있지 않다.*
  `draw(.width = i++, .height = i)`처럼 부수 효과를 섞으면 결과를 예측할 수
  없다(17장). 인자에는 값만 적는다.

  둘째, *0이 유효한 값인 필드*가 있으면 "빠뜨림"과 "0을 지정함"을 구별할 수
  없다. 그런 필드는 의미를 뒤집어 설계하거나(`no_grid` 대신 `grid`),
  별도의 존재 표시 필드를 둔다.

  셋째, *큰 구조체를 매번 만들어 넘기는 비용*이다. 옵션 구조체는 대개
  작아서 문제가 없지만, 커지면 `const struct opts *`로 받고 호출부에서
  `&(struct opts){ … }`를 넘기는 변형을 쓴다 — 주소를 얻을 수 있다는 위
  성질이 여기서 일한다.
]

#realcase[
  실무에서 만나는 이름 붙은 인자
][
  이 무늬는 널리 쓰인다. 리눅스 커널의 여러 초기화 함수, `struct
  sigaction`(57장)이나 `struct timespec`처럼 표준·POSIX API가 옵션을 구조체로
  받는 방식, 그래픽 라이브러리들의 `..._desc` 구조체(예: 여러 GPU API의
  `..._DESC`)가 모두 같은 착상이다. "인자가 많아지면 구조체로 묶는다"는
  것은 C에서 거의 관용이라 할 만하고, C99의 지정 초기화가 그것을 읽기 좋게
  만들어 주었다.
]

== 패딩 — 멤버 사이의 빈자리

#demo("examples/ch39/layout.c")

`char`, `int`, `char` 세 멤버의 크기 합은 6인데 구조체는 12바이트다. 6바이트가
*패딩*으로 들어간 것이다. 이유는 4장의 정렬이다 — `int`는 4의 배수 주소에
놓여야 하므로, 첫 `char` 뒤에 3바이트를 비워 `b`를 4번지로 민다. 그리고
마지막 `char` 뒤에도 3바이트가 붙는다(*꼬리 패딩*): 이 구조체를 배열로
늘어놓았을 때 다음 원소의 `int`도 정렬을 지켜야 하기 때문이다.

규칙 셋으로 정리된다.

+ 각 멤버는 *자기 정렬의 배수 오프셋*에 놓인다.
+ 구조체 전체의 정렬은 *멤버 정렬의 최댓값*이다.
+ 구조체 크기는 그 정렬의 *배수로 올림*된다(꼬리 패딩).

그래서 *멤버 순서만 바꿔도 크기가 준다.* 예제의 `tight`은 큰 것부터 늘어놓아
12바이트를 8바이트로 줄였다. 구조체를 수백만 개 늘어놓는 프로그램에서는 이
한 줄의 재배열이 메모리와 캐시 적중률을 함께 바꾼다(9장).

#misconception[
  "패딩 바이트에는 0이 들어 있다"
][
  아니다. 패딩의 값은 *정해져 있지 않다*. 초기화로 0이 들어갈 수도, 이전에
  그 자리를 쓰던 값이 남아 있을 수도 있다. 여기서 세 가지 실무 함정이 나온다.

  - *`memcmp`로 구조체를 비교하면 안 된다*(52장) — 값이 같아도 패딩이 달라
    "다르다"가 나올 수 있다. 멤버별로 비교한다.
  - *구조체를 통째로 해시하면 안 된다* — 같은 이유로 같은 값이 다른 해시를
    낸다.
  - *구조체를 그대로 파일이나 네트워크로 내보내면 안 된다* — 패딩의 쓰레기
    값이 함께 나가고(정보 누출이 되기도 한다), 받는 쪽의 배치가 다르면
    해석도 어긋난다.

  구조체 대입(`b = a;`)이 패딩까지 복사하는지도 표준이 약속하지 않는다.
  *의미 있는 것은 멤버뿐*이라고 기억하면 전부 설명된다.
]

== 패딩을 없애는 법, 정렬을 강제하는 법

패딩이 곤란한 자리가 분명히 있다 — 파일 형식이나 통신 규약처럼 *바이트 배치가
바깥에서 정해진* 경우다. 그래서 구현들은 패딩을 끄는 장치를 제공한다.

#platform[
  packed 와 pragma pack — 표준이 아니다
][
  ```c
  #pragma pack(push, 1)          /* MSVC·GCC·Clang 공통으로 널리 쓰인다 */
  struct header { char kind; int length; };
  #pragma pack(pop)              /* 반드시 되돌린다 */

  struct header2 { char kind; int length; } __attribute__((packed));  /* GCC·Clang */
  ```
  둘 다 *표준 C가 아니다.* 예제에서 `#pragma pack(1)`은 6바이트, 정렬 1의
  구조체를 만들었다. `push`/`pop`을 짝지어 쓰지 않으면 그 뒤에 포함되는
  헤더의 구조체 배치까지 바뀌어, 라이브러리와 배치가 어긋나는 고약한 버그가
  난다 — 헤더 안에서 pack 을 열어 둔 채 끝내지 않는 것이 대표적 사고다.
]

#antipattern[
  packed 구조체의 멤버 주소를 넘기기
][
  ```c
  struct __attribute__((packed)) h { char k; int len; };
  void take(int *p);
  take(&s.len);          /* 정렬되지 않은 주소 — 계약 밖이다 */
  ```
  packed 구조체의 멤버는 정렬이 어긋난 자리에 있을 수 있다. 그 주소를 평범한
  `int *`로 넘기면, 받는 쪽은 정렬을 가정하고 접근한다 — 관대한 기계(x86)
  에서는 느려질 뿐이지만 엄격한 기계에서는 그 자리에서 죽는다(4장).
  GCC와 Clang은 이 자리에 `-Waddress-of-packed-member` 경고를 낸다.

  값을 읽는 것(`int n = s.len;`)은 컴파일러가 바이트를 모아 처리해 주므로
  안전하다. *주소를 새는 것*이 문제다.
]

*정렬을 강제하는* 반대 방향의 도구도 있고, 이쪽은 C23의 표준 낱말이다(61장).

```c
struct cacheline { alignas(64) int counter; };   /* 64바이트 경계에 */
```

예제에서 이 구조체는 크기 64, 정렬 64가 됐다. 쓰는 자리가 분명하다 —
9장에서 본 *거짓 공유*를 피하려고 갈래마다 다른 캐시 라인에 카운터를 두거나,
DMA·SIMD처럼 하드웨어가 정렬을 요구하는 경우다. 다만 공짜가 아니다: 위
구조체는 `int` 하나를 담으려고 64바이트를 쓴다.

#qa[
  그러면 파일 형식이나 통신 규약을 다룰 때는 packed 구조체를 쓰는 것이
  정답인가?
][
  더 안전한 정답이 따로 있다. *바이트 열과 구조체 사이를 손으로 옮기는 것*
  이다.

  ```c
  /* 읽기: 버퍼에서 필드를 하나씩 꺼낸다 */
  uint32_t len;
  memcpy(&len, buf + 1, sizeof len);
  len = le32toh(len);            /* 엔디안까지 명시한다 (3장) */
  ```

  packed 구조체를 버퍼에 겹쳐 놓는 방식(`struct h *p = (struct h *)buf;`)은
  세 가지를 한꺼번에 가정한다 — 패딩 없음, 정렬 맞음, 엔디안 같음. 그중
  하나만 어긋나도 조용히 깨지고, 게다가 타입을 갈아 끼우는 접근은 별칭 규칙
  (43장)에도 걸린다. 필드별 `memcpy`는 길지만 세 가정을 전부 코드에 드러낸다.
  제12부의 라이브러리가 하는 일도 바로 이 지루한 작업을 한 자리에 모으는
  것이다.
]

== 배열을 값으로 넘기기 — 되는가, 그리고 해야 하는가

예제 후반이 그 대비다. `total(struct row r)`는 사본을 받아 고쳤고 원본은
그대로였다(`cell[0] = 1`). `total_raw(int cell[8])`은 포인터로 무너져 원본을
고쳤다(`cell[0] = 999`). 대입 `struct row copy = r;` 역시 배열 멤버까지
통째로 복사한다.

그러므로 이렇게 정리된다. *C에서 배열을 값 의미론으로 다루고 싶으면 구조체로
감싼다.* 이것이 유일한 방법이고, 실제로 쓰이는 자리도 있다.

#dtable(
  columns: 2,
  [*쓸 만한 자리*], [*이유*],
  [작은 고정 크기 벡터·행렬(`struct vec3`, `struct mat4`)], [값처럼 계산하는 것이 자연스럽다],
  [고정 크기 식별자·키(`struct uuid { unsigned char b[16]; }`)], [복사가 싸고 실수 여지가 없다],
  [배열을 *반환*해야 할 때], [배열은 반환할 수 없지만 구조체는 된다],
  [불변으로 넘기고 싶을 때], [사본이라 호출된 쪽이 원본을 못 건드린다],
)

대가도 분명하다.

*스택 사용량.* 사본은 대개 호출된 함수의 스택 프레임에 놓인다. 16KiB짜리
구조체를 값으로 넘기면 호출 한 번에 그만큼이 더 쌓인다 — 실제로 재어 보면
호출 전후의 스택 위치가 구조체 크기만큼 벌어진다. 스택은 대개 8MiB 안팎이고
(32장), 재귀나 갈래마다 따로 잡히는 스택(58장)에서는 훨씬 작을 수 있다.
큰 구조체를 값으로 넘기는 재귀는 스택 넘침의 지름길이다.

*복사 비용.* 다만 "복사가 반드시 일어난다"고 말하면 부정확하다. 호출 규약이
정한다 — 작은 구조체(대개 두 워드 이하)는 *레지스터로* 건너가 복사랄 것이
없고, 큰 구조체는 호출자가 메모리에 만들어 그 주소를 숨겨 넘기는 방식이
흔하다. 게다가 함수가 인라인되면 컴파일러가 사본 자체를 없애 버릴 수도 있다.
그래서 정확한 문장은 이렇다: *의미는 언제나 복사이고, 실제 비용은 크기와
호출 규약과 최적화가 정한다.*

#misconception[
  "구조체를 값으로 넘기면 항상 느리다"
][
  크기에 달렸다. `struct point { int x, y; }`처럼 작은 것은 값으로 넘기는
  편이 오히려 빠르고 읽기도 좋다 — 포인터를 넘기면 역참조가 생기고, 컴파일러가
  "이 포인터로 누가 값을 바꿀 수 있다"고 의심해야 해서 최적화가 줄어든다.

  실무의 어림 규칙은 이렇다. *두어 워드까지는 값으로, 그보다 크면
  `const 포인터`로.* 그리고 이 선택은 성능만의 문제가 아니라 계약의 문제이기도
  하다 — 값으로 받으면 "원본을 건드리지 않는다"가 문법으로 보장되고, 포인터로
  받으면 `const`로 약속할 뿐이다.
]

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요지*],
    [접근], [왼쪽이 값이면 `.`, 포인터면 `->`(=`(*p).`)],
    [복합 리터럴], [`(struct T){…}` — lvalue이고 수명은 *블록 끝*],
    [주소 반환], [복합 리터럴의 주소를 함수 밖으로 내보내지 않는다],
    [이름 붙은 인자], [옵션 구조체 + 지정 초기화. 빠뜨린 멤버는 0],
    [기본값 설계], [*0이 뜻이 통하는 기본값*이 되게 필드를 정한다],
    [패딩], [정렬 때문에 생긴다. 값은 정해져 있지 않다],
    [멤버 순서], [큰 것부터 늘어놓으면 크기가 준다],
    [`memcmp`·해시·직렬화], [구조체를 통째로 다루지 않는다],
    [`pack`], [표준 아님. `push`/`pop` 짝, 멤버 주소를 새지 않는다],
    [`alignas`], [표준. 거짓 공유 회피·하드웨어 요구],
    [배열을 값으로], [구조체로 감싸면 된다. 스택과 크기를 함께 본다],
  )
]

구조체를 부리는 법을 익혔다. 다음 장은 같은 기억을 *다른 눈으로* 보는
장치 — 공용체와 표현의 세계다. 3장에서 예약해 둔 엔디안 관찰 시연이
드디어 열린다.
