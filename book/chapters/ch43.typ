#import "../lib.typ": *

= 정의되지 않은 동작

#organizer[
  이 책이 곳곳에서 "계약 밖"이라 부르며 미뤄 온 세계를 정면으로 다룬다.
  UB란 정확히 무엇이고 왜 존재하며, 왜 "조금 위험함"이 아니라 "무엇이든
  가능함"인지 — 그리고 실무에서 어떻게 피하고 잡아내는지까지. 제2부의
  계약 서사가 여기서 완결된다.
]

#deepqa[
  11장에서 엄격한 앨리어싱 위반이 "컴파일러에게 사실이 아닌 것을 사실
  이라고 알리는 행위"라 했다. 그러면 표준이 어떤 동작을 "정의되지 않음"
  으로 두었다는 것은 — 구현에게 무엇을 허락한 것인가?
][
  *아무것도 요구하지 않기로* 한 것이다. 표준의 문장은 냉정하다 — 정의
  되지 않은 동작에 대해 표준은 "어떤 요구도 부과하지 않는다". 즉 그런
  프로그램에는 옳은 실행 결과라는 것이 아예 없다. 흔한 오해가 "위험한
  동작이지만 대개 예상대로 돌아간다"인데, 계약의 눈으로는 그 프로그램
  전체가 *의미를 잃는다*. 이 차이가 이 장의 전부다.
]

== 세 가지 회색지대 — UB, 미지정, 구현 정의

#idx("구현 정의")#idx("미지정")#idx("정의되지 않은 동작(UB)")혼동되는 세 낱말을 먼저 갈라 둔다. 이 책이 여기까지 만난 사례들로
정리하면 이렇다.

#dtable(
  columns: 3,
  [*종류*], [*표준의 태도*], [*이 책에서 만난 예*],
  [구현 정의
  (implementation-defined)], [구현이 정하고 *문서화한다*], [`int`의 크기(23장), `char`의 부호],
  [미지정(unspecified)], [정해진 선택지 중 하나이나 문서화 의무 없음], [한 수식 안 부분식의 평가 순서(29장)],
  [정의되지 않음
  (undefined behavior, UB)], [아무 요구도 하지 않는다], [부호 있는 오버플로(5장), 경계 침범(33장), 널 역참조(31장)],
)

앞의 둘은 "답이 여럿이지만 어쨌든 답은 있는" 세계다. UB만이 답 자체가
없는 세계다.

== 왜 존재하는가

계약 밖을 남겨 둔 이유는 두 가지다. 첫째, *기계들이 서로 다르기
때문이다* — 5장의 폭 이상 시프트가 대표다. x86과 옛 ARM이 다르게
대응하는데 표준이 한쪽을 택하면 다른 쪽 기계는 매번 보정 코드를 넣어야
한다. 어느 편도 들지 않는 대신 "그 코드를 쓰지 말라"고 한 것이다.
둘째, *최적화의 전제를 얻기 위해서다* — 5장에서 본 대로 "부호 있는
정수는 넘치지 않는다"는 전제가 있어야 컴파일러가 루프를 분석하고
재배열할 수 있다(11장). C가 반세기 동안 속도의 언어로 남은 대가가
이 조항들이다.

== "무엇이든 가능함"의 실제 얼굴

UB의 결과는 붕괴만이 아니다. 11장에서 본 무늬가 더 무섭다 — *컴파일러가
UB를 "일어날 리 없는 일"로 읽고 코드를 지워 버리는* 것이다. 널 검사가
통째로 사라지거나(포인터를 이미 역참조한 뒤라면 "널일 리 없다"고
추론한다), 오버플로 검사가 사라지거나(부호 있는 넘침은 없다고 전제
하므로), 루프가 무한이 되거나 아예 없어진다. 그래서 UB의 대표적 증상은
"그 자리에서 죽는 것"이 아니라 *엉뚱한 곳에서, 최적화 수준을 바꾸면
사라지는, 재현이 어려운 버그*다.

#realcase[
  사라진 널 검사 — 리눅스 커널 CVE-2009-1897
][
  이 무늬가 커널에서 실제로 터진 사건이 있다. 코드는 대략 이랬다 —
  포인터 `tun`을 먼저 역참조해 값을 꺼내고, 그 아래에서 `if (!tun)
  return ...;`으로 널을 검사했다. 순서가 뒤집힌 실수이지만, 사람 눈에는
  "그래도 검사는 있으니 널이면 걸리겠지"로 보인다. 컴파일러의 추론은
  달랐다: *이미 역참조했다 → 널이었다면 그 순간 UB → UB는 일어나지
  않는다고 전제 → 따라서 tun은 널이 아니다 → 아래의 널 검사는 죽은
  코드다.* 검사가 통째로 최적화로 제거됐고, 널 페이지를 매핑할 수 있던
  당시 환경과 결합해 권한 상승 취약점이 됐다. 컴파일러는 규칙대로
  일했고, 무너진 것은 계약이었다.
]

== 피하는 법 — 규율, 도구, 그리고 부품

실무의 방어는 세 겹이다.

*규율* — 이 책이 지나온 수칙들이 그 목록이다: 초기화하고 쓴다(20장),
경계를 지킨다(33장), 널을 확인한다(31장), 한 문장에서 한 변수는 한 번만
바꾼다(29장), 계약 밖의 지름길(포인터 캐스트, 표현 가정)을 쓰지
않는다(11·32장).

*도구* — 15장에서 장만한 그물이다. 컴파일러 경고는 컴파일 시점에,
UBSan·ASan은 실행 시점에 잡는다. C23이 들여온 *검사 산술*도 이 층의
도구다 — 넘침을 UB로 만들지 않고 값으로 알려 주는 함수들이다:

#demo("examples/ch43/checked.c")

`ckd_add`는 "넘쳤는가"를 반환값으로 돌려준다 — 5장에서 배운 "부호
있는 넘침은 계약 밖"이라는 함정을, 42장에서 배운 "오류는 값이다"라는
규율로 다스리는 표준의 답이다.

*부품* — 애초에 계약 위반이 어려운 API를 쓰는 것이다(35장의 proven이
그 층이다). 도구가 그물이라면 좋은 부품은 발판이라는 15장의 비유가
여기서 완성된다.

#qa[
  UB를 전부 외워야 하는가? 표준에 수백 가지가 있다고 들었는데.
][
  외우는 것이 목표가 아니다 — 목록은 방대하고 계속 다듬어진다. 실무에서
  통하는 것은 *감각*이다: "이 코드가 옳다고 말하는 근거가 계약서에
  있는가, 아니면 '내 컴퓨터에서 돌아서'인가"를 묻는 습관. 그리고 그
  감각을 도구로 뒷받침하는 것 — 경고를 켜고, 새니타이저로 시험 실행
  하고, 두 컴파일러로 교차 검증하는 것(15장). 이 책이 제2부부터 "추상
  기계 위에서 옳은가"를 되풀이한 이유가 바로 이 감각을 심기 위해서다.
]

== 제9부를 닫으며

정밀의 부가 끝났다 — 근사를 다루는 법(41장), 실패를 다루는 법(42장),
그리고 계약 밖의 세계를 아는 법(43장). 세 장의 공통 주제는 하나다:
*C는 프로그래머에게 많은 것을 맡기는 언어이고, 맡겨진 것을 아는 사람이
안전한 코드를 쓴다.*

마지막 부가 남았다. 지금까지의 프로그램은 파일 하나짜리였다 — 이제
여러 파일로 자라고(44장), 전처리와 번역의 층을 정면으로 보고(45장),
표준 라이브러리의 지형을 익히고(48장), proven을 정면으로 다루며(제12부),
모던 C의 관행으로 책을 닫는다(73장).
