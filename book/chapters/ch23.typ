#import "../lib.typ": *

= 정수 — 유한한 수의 세계

#organizer[
  제6부는 값을 벼리고 흐름을 다스리는 부다. 첫 장은 C의 정수를 정면으로
  다룬다 — 5장에서 배운 표현의 세계가 C의 타입 가족으로 어떻게
  나타나는지, 오버플로가 실무에서 어떤 얼굴인지, 그리고 현대 C의 관행
  (`<stdint.h>`)까지.
]

#deepqa[
  5장에서 부호 없는 정수의 넘침은 "정의된 감아 돌기"이고 부호 있는
  넘침은 계약 밖이라 했다. 그런데 20장에서 만든 `int`는 부호가 있는가
  없는가 — 우리가 여태 쓴 변수들은 어느 세계에 있었던 것인가?
][
  `int`는 부호 *있는* 정수다 — 즉 여태 우리의 변수들은 "넘치면 계약
  밖"의 세계에 있었다. 다행히 지금까지의 예제는 ±21억의 그릇을 넘볼
  일이 없었지만, 이제 그 경계선을 정확히 그어 둘 때가 됐다. 그릇의
  종류와 크기, 경계의 위치, 넘었을 때의 규칙 — 이 장의 내용이다.
]

== 정수 타입 가족

C의 정수 타입은 `int` 하나가 아니라 가족이다. 부호 있는 쪽의 기본
구성원은 크기 순으로 `char`(1바이트 — 6장의 그 "바이트=문자" 사연이
이름에 남았다), `short`, `int`, `long`, `long long`이고, 각각의 앞에
`unsigned`를 붙이면 부호 없는 짝이 된다(`unsigned int` 등). 표준은 각
타입의 *최소* 크기만 정한다 — `int`는 최소 16비트, `long`은 최소
32비트라는 식이라, 정확한 크기는 플랫폼의 몫이다(주류 환경에서 `int`는
32비트).

"기계마다 다르다"가 불안하게 들린다면 정확한 직감이다 — 그래서 현대
C의 관행은 크기가 이름에 박힌 타입을 쓰는 것이다. `<stdint.h>` 도구
상자의 `int32_t`(정확히 32비트, 부호 있음), `uint8_t`, `int64_t`
같은 타입들이다. 크기가 계약의 일부인 자리 — 파일 형식, 통신, 3장의
엔디안이 문제 되는 모든 곳 — 에서는 이쪽이 표준 관행이다. C23은 여기에
`_BitInt(N)` — 원하는 비트 폭을 직접 적는 정수 — 까지 보탰다. 이
책의 예제는 지면의 단순함을 위해 `int`를 기본으로 쓰되, 크기가 중요한
장면에서는 `<stdint.h>` 가족으로 갈아탄다.

== 경계를 눈으로 본다

그릇의 가장자리는 `<limits.h>` 도구 상자가 알려 준다. 그리고 부호 없는
세계의 감아 돌기는 — 5장에서 지면으로 배운 그것 — 이제 직접 실행해
보일 수 있다.

#demo("examples/ch23/wrap.c")

서식 하나가 새로 합류했다 — 부호 없는 정수는 `%d`가 아니라 `%u`로
찍는다(19장의 서식 계약이 타입 가족을 따라 늘어나는 것이다). 그리고
셋째 줄이 5장의 약속 그대로다: 최댓값에 1을 더하니 0 — 시계가 한 바퀴
돈 것이고, 이것은 *정의된* 동작이다.

부호 있는 쪽은 사정이 다르다. `INT_MAX + 1`을 계산하는 코드는 계약
밖(정의되지 않은 동작)이라서, *이 책의 검증 파이프라인에 넣을 수조차
없다* — 15장에서 장만한 UBSan이 정확히 이런 코드를 실행 중에 잡아
낸다. 같은 "+1"인데 한쪽은 시연 가능하고 한쪽은 불가능하다는 것 자체가,
두 세계의 차이를 웅변한다.

#misconception[
  "오버플로는 큰 수를 다루는 특수한 프로그램의 문제다"
][
  그럴듯하지만, 실제 사고 목록은 반대를 말한다. 가장 흔한 오버플로는
  평범한 곳에서 난다 — 두 인덱스의 평균을 `(a + b) / 2`로 구하다가
  *더하는 중간 결과*가 넘치고(유명한 이진 탐색 버그: 표준 라이브러리
  들에 20년 가까이 숨어 있었다), 밀리초 타이머가 49일 만에 감아 돌고
  (Windows 95의 49.7일 멈춤 사고), 크기 계산 `개수 * 크기`가 넘쳐
  터무니없이 작은 메모리를 잡는다(보안 사고의 고전). 공통점은 *최종
  값이 아니라 중간 계산*이 넘친다는 것이다 — 그릇 걱정은 결과가 아니라
  수식의 모든 중간 단계에 대해 하는 것이고, 이것이 proven류의 검사
  산술이 존재하는 이유다(35장).
]

#qa[
  `char`가 정수 타입 가족이라는 것이 이상하다 — 문자 아닌가?
][
  C에서 문자와 작은 정수는 같은 것이다 — 6장에서 배운 대로 문자는
  번호이고, `char`는 그 번호를 담는 1바이트 정수 그릇일 뿐이다.
  `'A'`라는 문자 리터럴의 값은 그냥 65다. 한 가지 함정만 미리 —
  `char`가 부호 있는지 없는지는 *플랫폼마다 다르다*(둘 다 아닌 제3의
  타입이다). 그래서 수치 계산에는 `char`를 쓰지 않고, 바이트가 필요하면
  `uint8_t`를 쓰는 것이 현대 관행이다. 문자열과 `char`의 본격적인
  이야기는 34장에 있다.
]

정수의 그릇들을 정리했다. 다음 장은 그 그릇 위의 연산 — 17장에서
미뤄 둔 나눗셈의 진실과, 5장에서 배운 비트 세계의 연산자들이 C 문법으로
합류한다.
