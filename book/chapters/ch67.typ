#import "../lib.typ": *

= 비지역 점프 — `<setjmp.h>`

#prereq(
  ([66장 신호], [흐름을 끊고 들어오는 것]),
  ([39장 스택과 호출], [호출 프레임이 쌓이고 걷히는 방식]),
  ([48장 오류를 알리는 법], [실패를 값으로 돌려주기]),
)

#deepqa[
  39장에서 함수를 부르면 프레임이 쌓이고, `return`하면 그 프레임이 걷히면서
  *한 칸씩* 돌아간다고 했다. 그러면 열 겹 깊이에서 맨 위로 *한 번에* 돌아가는
  일은 C에서 어떻게 하는가?
][
  `<setjmp.h>`의 두 낱말이 그 일을 한다. `setjmp`가 "지금 여기"를 기록해 두고,
  `longjmp`가 그 기록을 되살려 *스택을 통째로 되감는다.* 열 겹이든 백 겹이든
  한 번에 돌아간다.

  다만 되감기는 것은 *스택 포인터와 몇 개의 레지스터*뿐이다. 그 사이에 열어 둔
  파일도, 잡아 둔 기억도, 걸어 둔 잠금도 아무도 정리해 주지 않는다 — 이 장의
  절반은 그 대가에 관한 이야기다.
]

#organizer[
#idx("비지역 점프")  `<setjmp.h>` 하나를 정독한다. 왜 생겼는지(예외가 없는 언어의 궁여지책),
  두 낱말의 정확한 모양과 반환값, `jmp_buf`의 정체, 표준이 정한 네 가지 문맥
  제약과 `volatile` 규칙, 그리고 libjpeg·Lua 같은 실물이 이것을 어떻게 쓰는지와
  오늘날의 대안까지.
]

#chapter-questions()

== 왜 있는가 — 예외가 없는 언어의 궁여지책

깊은 곳에서 문제가 생겼을 때, 언어에 예외가 있으면 `throw` 한 줄로 위층까지
빠져나간다. C에는 그것이 없다. 그래서 오류는 *한 겹씩 반환값으로 올려야* 하고,
중간의 모든 함수가 그 값을 받아 다시 올려 주어야 한다(48장).

`setjmp`/`longjmp`는 그 사슬을 건너뛰는 장치다. 1970년대 유닉스 V7에 이미
있었고, 등장 이유도 지금과 같았다 — *깊은 재귀에서 한 번에 빠져나오기*.

표준의 각주는 이 함수들의 용도를 이렇게만 적어 둔다: "프로그램의 낮은 층에서
만나는 이례적인 상황을 다루는 데 쓸모가 있다." 즉 *예외의 대체물이 아니라
이례 상황용 탈출구*라는 것이 원래의 자리다.

#qa[
  그러면 이것이 C의 예외 처리인가?
][
  아니다. 결정적인 차이가 셋이다.

  *첫째, 정리해 주는 것이 없다.* C++의 예외는 스택을 되감으며 소멸자를 부른다
  (스택 되감기, stack unwinding). `longjmp`는 그냥 뛴다 — 열린 파일, 잡은 기억,
  잠근 뮤텍스가 그대로 남는다.

  *둘째, 타입이 없다.* 던지는 것은 `int` 하나뿐이다. 무엇이 잘못됐는지 담으려면
  그 값을 색인 삼아 다른 곳에 적어 두어야 한다.

  *셋째, 지역이 제한된다.* `setjmp`를 부른 함수가 *아직 살아 있어야* 한다. 이미
  `return`한 함수의 자리로는 뛸 수 없다 — 뛰면 정의되지 않은 동작이다.

  그래서 이 장의 결론을 미리 적으면 이렇다. *새 코드에서는 대개 쓰지 않는다.
  다만 읽을 줄은 알아야 한다* — 널리 쓰이는 라이브러리들이 이것으로 지어졌기
  때문이다.
]

== 두 낱말 — 정확한 모양

#dtable(
  columns: 2,
  keycol: false,
  [*선언*], [*하는 일*],
  [`int setjmp(jmp_buf env);`], [지금의 호출 환경을 `env`에 저장한다. *매크로다*],
  [`[[noreturn]] void longjmp(jmp_buf env, int val);`], [`env`에 저장된 자리로 돌아간다. 돌아오지 않는다],
)

=== `setjmp` — 두 번 돌아오는 것처럼 보이는 매크로

`setjmp`는 함수가 아니라 *매크로*다(표준은 함수로도 제공될 수 있다고 하지만,
매크로를 지우고 함수를 직접 부르는 것은 정의되지 않은 동작이다).

반환값의 규칙은 단순하고, 이 헤더의 전부라 할 만하다.

#dtable(
  columns: 2,
  [*어떻게 도달했는가*], [*`setjmp`가 돌려주는 값*],
  [직접 불렸을 때(자리를 저장하는 순간)], [0],
  [`longjmp(env, val)`로 돌아왔을 때], [`val` — 단, `val`이 0이면 1],
)

`longjmp(env, 0)`이 0을 만들지 못하는 이유가 여기 있다. 0은 *"저장하는 중"*을
뜻하는 값으로 이미 쓰이고 있으므로, 표준이 1로 바꿔 준다.

#demo("examples/ch67/jmp_basic.c")

시연이 그 흐름 그대로다. 처음에는 0이 나와 `deep`으로 내려가고, 깊이 3에서
`longjmp(env, 42)`를 하자 *`main`의 `setjmp` 자리로 곧바로* 돌아와 42가 나온다.
중간의 `deep(2)`·`deep(1)`은 "정상으로 빠져나온다"는 줄을 인쇄할 기회를 얻지
못한다 — 그 프레임들은 그냥 사라졌다.

마지막 시험이 `longjmp(env, 0)`이다. 0을 주었는데 1이 돌아왔다.

=== `setjmp`를 쓸 수 있는 자리 — 네 가지뿐

표준(§7.13.1.1)이 문맥을 *열거로* 제한한다. 이 넷 밖에서 쓰면 정의되지 않은
동작이다.

#dtable(
  columns: 2,
  keycol: false,
  [*허용되는 문맥*], [*보기*],
  [`if`·`switch`·`while` 등의 제어식 *전체*], [`if (setjmp(env)) { ... }`],
  [정수 상수식과의 비교의 한쪽(그리고 그 전체가 제어식)], [`if (setjmp(env) == 0)`],
  [`!` 하나를 붙인 것(그리고 그 전체가 제어식)], [`if (!setjmp(env))`],
  [수식문 전체(`void`로 캐스트해도 된다)], [`setjmp(env);`],
)

즉 `int rc = setjmp(env);`처럼 *값을 변수에 담는 것은 표준의 문맥이 아니다.*
많은 구현에서 실제로 동작하고 이 책의 시연도 그렇게 썼지만, 이식성을 따진다면
`switch (setjmp(env))`나 `if (setjmp(env) == 0)` 꼴로 적는 편이 안전하다.

#qa[
  왜 이런 이상한 제한이 있는가?
][
  `setjmp`가 *한 번 불려 두 번 돌아오는* 물건이기 때문이다. 컴파일러 입장에서
  이것은 보통의 함수 호출이 아니다 — 반환값이 복잡한 수식의 중간에 끼어 있으면,
  그 수식을 계산하다 만 상태(레지스터에 걸쳐 있는 임시값들)를 어떻게 되살릴지
  정할 수 없다.

  그래서 표준은 아예 *임시값이 살아 있을 수 없는 자리*로 문맥을 좁혔다. 제어식
  하나, 상수와의 비교 하나, 그리고 값을 버리는 수식문. 제한이 이상해 보이지만
  구현 가능성에서 나온 것이다.
]

=== `longjmp` — 뛰기 전에 확인할 세 가지

`longjmp(env, val)`은 돌아오지 않는다(C23에서 `[[noreturn]]`이 붙었다). 뛰기
전에 세 조건이 성립해야 하고, 하나라도 어기면 정의되지 않은 동작이다.

#dtable(
  columns: 2,
  [*조건*], [*어기면*],
  [그 `env`에 `setjmp`가 실제로 있었다], [저장된 적 없는 자리로 뛴다],
  [`setjmp`를 부른 함수가 *아직 살아 있다*], [사라진 프레임으로 뛴다 — 대개 즉사],
  [같은 스레드다], [다른 스레드의 스택으로 뛴다],
)

두 번째가 가장 자주 나는 사고다. "오류 처리 함수를 만들어 두고 아무 데서나
부르면 되겠지"라고 생각하고 `setjmp`를 부른 함수가 이미 반환한 뒤에 `longjmp`
하면, 그 자리에는 이미 다른 함수의 프레임이 들어와 있다.

== `jmp_buf`의 정체

`jmp_buf`는 *배열 타입*이다. 표준이 그렇게 못박아 둔 데는 실용적인 이유가 있다 —
배열이므로 함수에 넘기면 자동으로 첫 원소의 주소로 무너져(37장), `setjmp(env)`가
`&env`를 쓰지 않고도 *원본을 고칠* 수 있다.

안에 무엇이 들었는가. 표준은 "`longjmp`가 올바른 블록으로 돌아가기에 충분한
정보"라고만 말한다. 실제로는 대개 이런 것들이다.

#dtable(
  columns: 2,
  [*저장되는 것*], [*왜*],
  [스택 포인터], [되감을 자리를 알아야 한다],
  [프로그램 카운터(복귀 주소)], [어디로 돌아갈지],
  [피호출자 보존 레지스터], [호출 규약상 함수가 지켜 주기로 한 값들],
  [(구현에 따라) 신호 마스크], [POSIX `sigsetjmp`가 선택적으로],
)

시연이 인쇄한 크기(이 구현에서 200바이트)가 그 내용물의 총합이다. 다만 *속을
들여다보거나 손으로 고치는 것은 계약 밖*이다 — 불투명한 자료로 다뤄야 한다.

표준은 저장되지 *않는* 것도 분명히 적어 둔다. *부동소수점 환경의 상태, 열린
파일, 그리고 추상 기계의 다른 어떤 구성 요소도 포함되지 않는다.* 이 한 문장이
다음 절의 모든 함정을 낳는다.

== 돌아왔을 때 값은 어떻게 되는가 — `volatile` 규칙

이 장에서 가장 실질적인 규칙이다. 표준(§7.13.2.1)은 이렇게 정한다.

*`longjmp`를 부른 시점의 값이 모든 객체에 그대로 남는다. 단 하나 예외가 있다 —
`setjmp`를 부른 함수의 지역 자동 변수 중, `volatile`이 아니고, `setjmp` 이후에
값이 바뀐 것은 그 표현이 정해지지 않는다.*

#demo("examples/ch67/jmp_volatile.c")

세 변수가 정확히 그 규칙을 갈라 보인다.

#dtable(
  columns: 3,
  [*변수*], [*저장 기간·한정자*], [`longjmp` 뒤],
  [`plain`], [자동, 비 `volatile`, 중간에 바뀜], [*보장 없음*],
  [`guarded`], [자동, `volatile`], [보장된다],
  [`statik`], [정적], [보장된다],
)

*이 규칙은 실제로 물린다.* 최적화를 끄고 빌드하면 셋 다 2로 보이지만, 같은
코드를 `-O2`로 빌드하면 `plain`이 *1로 되돌아간다* — 컴파일러가 그 변수를
레지스터에 두었고, `longjmp`가 레지스터를 `setjmp` 시점 값으로 되살렸기
때문이다. 이 책은 두 빌드를 모두 돌려 그 차이를 확인했고, 예제가
`__OPTIMIZE__`를 보고 어느 쪽인지 밝히도록 해 두었다.

#misconception[
  "`volatile`은 하드웨어 레지스터에나 쓰는 것이다"
][
  이 자리가 `volatile`의 두 번째 정당한 용도다(첫 번째는 66장의
  `volatile sig_atomic_t`). 셋을 나란히 두면 쓰임이 분명해진다.

  #dtable(
    columns: 2,
    [*자리*], [*무엇을 막는가*],
    [MMIO·하드웨어 레지스터], [읽기·쓰기를 지우거나 합치는 최적화],
    [신호 처리기와 주고받는 깃발], [루프에서 읽기를 생략하는 최적화],
    [`longjmp`를 건너는 지역 변수], [값을 레지스터에만 두는 최적화],
  )

  셋 다 "컴파일러가 기억을 건너뛰는 것"을 막는다는 점에서 같다. 반대로
  *스레드 사이의 동기화에는 `volatile`이 쓸모없다* — 그 자리는 `<stdatomic.h>`
  다(69장). 이 구별을 놓치면 `volatile`을 만능 부적으로 오해하게 된다.
]

== 자원은 아무도 정리해 주지 않는다

`longjmp`는 스택 포인터를 되돌릴 뿐이다. 그 사이에 무엇을 잡았든 그대로 남는다.

#antipattern("뛰어넘은 자리에 자원이 남는 코드")[
  ```c
  void work(void) {
      FILE *f = fopen("data", "r");     /* 열었다 */
      char *buf = malloc(1024);          /* 잡았다 */
      parse(f, buf);                     /* 여기서 longjmp 가 일어나면… */
      free(buf);                         /* 실행되지 않는다 — 누수 */
      fclose(f);                         /* 실행되지 않는다 — 파일도 샌다 */
  }
  ```
  `parse` 안에서 `longjmp`가 일어나면 `work`의 프레임은 그냥 사라진다.
  `free`도 `fclose`도 실행되지 않는다. 오래 도는 프로그램에서 이 무늬는
  요청마다 조금씩 새는 형태로 나타난다.
]

그래서 이 장치를 쓰는 코드는 예외 없이 하나의 규율을 지킨다 — *자원을 잡고
푸는 자리를 `setjmp`를 부른 함수 안에 모은다.* 뛰어 돌아온 뒤 그 함수가 직접
정리하면 되기 때문이다.

#demo("examples/ch67/jmp_error.c")

시연의 `load_image`가 그 구조다. `malloc`은 `setjmp` *앞*에서 하고, `free`는
성공·실패 두 경로가 모두 지나는 자리에 한 번만 둔다. 깊은 곳의
`parse_header`·`parse_size`는 자원을 잡지 않고 오직 뛰기만 한다.

== 실제로 무엇이 이것으로 지어졌는가

이 장치는 오늘날 새 코드에서 권하지 않지만, *이미 지어진 것*은 아주 많다.

#dtable(
  columns: 3,
  [*무엇*], [*어떻게 쓰는가*], [*왜 그렇게 했는가*],
  [libjpeg], [`struct jpeg_error_mgr`에 `jmp_buf`를 넣고, 오류 시 `error_exit`에서 뛴다], [디코딩이 깊은 재귀라 반환값 사슬이 너무 길다],
  [libpng], [`png_jmpbuf(png_ptr)` 매크로로 같은 무늬], [같은 이유. C API 로 예외 비슷한 것을 흉내],
  [Lua 인터프리터], [`LUAI_THROW`/`LUAI_TRY`가 C 빌드에서 `longjmp`로 구현된다], [스크립트의 `error()`를 호스트 언어로 옮겨야 한다],
  [일부 코루틴 구현], [`setjmp`로 문맥을 저장하고 스택을 바꿔치기], [표준만으로 문맥 전환을 흉내 내려면 이 길뿐],
  [테스트 프레임워크], [단언 실패 시 그 시험만 중단하고 다음으로], [한 시험의 실패가 전체를 멈추면 안 된다],
)

#realcase("libjpeg 의 오류 관리자 — 왜 이 모양이 되었나")[
  libjpeg 를 쓰는 코드는 거의 언제나 이렇게 시작한다.

  ```c
  struct my_error_mgr { struct jpeg_error_mgr pub; jmp_buf setjmp_buffer; };

  static void my_error_exit(j_common_ptr cinfo) {
      struct my_error_mgr *err = (struct my_error_mgr *)cinfo->err;
      longjmp(err->setjmp_buffer, 1);
  }
  ```

  라이브러리의 기본 동작은 *오류가 나면 `exit`* 이었다. 라이브러리가 응용
  프로그램을 죽이는 셈이라, 이미지 하나가 깨졌다고 편집기 전체가 종료되는 일이
  벌어진다. 그래서 libjpeg 은 "오류가 났을 때 부를 함수"를 사용자가 갈아 끼울 수
  있게 열어 두었고, 그 자리에서 되돌아올 유일한 방법이 `longjmp` 였다.

  이 사례가 보여 주는 것은 설계의 순서다. *깊은 호출 사슬 + C API + 라이브러리가
  프로세스를 죽이면 안 됨* — 이 셋이 겹치면 `setjmp`/`longjmp` 말고는 선택지가
  거의 없었다. 오늘날 새로 설계한다면 48장의 "실패는 값" 쪽을 택하겠지만,
  1990년대의 제약 안에서는 합리적인 결정이었다.
]

== POSIX의 `sigsetjmp`/`siglongjmp`

유닉스 계열에는 짝이 하나 더 있다.

```c
int  sigsetjmp(sigjmp_buf env, int savemask);
[[noreturn]] void siglongjmp(sigjmp_buf env, int val);
```

차이는 *신호 마스크*(66장)를 함께 저장할지다. `savemask`가 0이 아니면 지금의
마스크를 함께 저장하고, `siglongjmp`가 그것을 복원한다.

왜 필요한가. 신호 처리기 안에서 `longjmp`로 빠져나오는 코드를 생각해 보자.
처리기가 도는 동안에는 그 신호가 막혀 있는데, 표준 `longjmp`로 뛰어나오면
*막힌 채로 남을 수 있다* — 그 뒤로 같은 신호가 영영 오지 않는다. `sigsetjmp`는
그 구멍을 막는다.

#misconception[
  "신호 처리기에서 `longjmp`로 빠져나오면 된다"
][
  널리 쓰이지만 위험한 무늬다. 두 가지가 겹친다.

  *첫째, 마스크 문제* — 위에서 본 그대로다. 유닉스에서는 `sigsetjmp` 짝을 써야
  한다.

  *둘째, 그리고 더 근본적으로, 어디서 끊겼는지 모른다.* 신호는 `malloc`이나
  `printf`의 중간에서도 온다(66장). 그 상태에서 뛰어나오면 그 자료구조는 반쯤
  고쳐진 채로 남는다. 뛰어나온 뒤에 다시 `malloc`을 부르면 그때 무너진다.

  그래서 이 무늬가 그나마 정당한 자리는 좁다 — *뛴 다음 곧바로 끝내거나*
  (`_Exit`), 표준 라이브러리를 더는 쓰지 않는 경로로 가는 경우다. 오래 도는
  프로그램에서 "타임아웃을 신호로 걸고 `longjmp`로 빠져나오는" 무늬는, 겉보기에
  잘 돌다가 드물게 무너진다.
]

== 오늘날의 자리 — 언제 쓰고, 무엇으로 대신하는가

#dtable(
  columns: 2,
  [*하고 싶은 일*], [*권하는 방법*],
  [깊은 곳의 실패를 위로 올리기], [반환값으로 올린다(48장). 중간 층이 지루해도 이 쪽이 안전하다],
  [함수 하나 안의 여러 실패 경로 정리], [`goto cleanup` 하나로 모은다 — 리눅스 커널의 관행],
  [자원 정리를 잊지 않기], [C23의 `[[maybe_unused]]`가 아니라 *구조*로 — 잡는 자리와 푸는 자리를 한 함수에],
  [파서·인터프리터의 깊은 탈출], [`setjmp`가 정당한 몇 안 되는 자리. 단 자원 규율을 지킨다],
  [스레드를 넘나드는 탈출], [불가능하다 — `longjmp`는 같은 스레드 안에서만],
)

`goto cleanup` 무늬를 굳이 다시 적어 두는 이유는, 실무에서 `setjmp`로 하려던 일의
대부분이 사실 이것으로 충분하기 때문이다.

```c
int work(void) {
    int rc = -1;
    FILE *f = fopen("data", "r");
    if (!f) goto out;
    char *buf = malloc(1024);
    if (!buf) goto close;
    if (parse(f, buf) != 0) goto free_buf;
    rc = 0;
free_buf: free(buf);
close:    fclose(f);
out:      return rc;
}
```

*한 함수 안*이라면 이 편이 낫다. 흐름이 눈에 보이고, 정리 순서가 코드에 적혀
있고, 컴파일러가 검사해 준다. `setjmp`가 필요한 것은 *여러 함수를 건너뛸 때*
뿐이다.

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [정체], [스택을 통째로 되감는 탈출구 — 예외가 아니다],
    [`setjmp`], [매크로. 직접 부르면 0, 뛰어 돌아오면 `val`(0이면 1)],
    [문맥 제약], [제어식·상수 비교·`!`·수식문 넷뿐],
    [`longjmp`의 전제], [저장한 함수가 아직 살아 있고, 같은 스레드],
    [`jmp_buf`], [배열 타입. 스택·복귀 주소·보존 레지스터 정도가 들어간다],
    [`volatile` 규칙], [자동·비 `volatile`·중간에 바뀐 변수는 보장 없음(실측으로 확인)],
    [자원], [아무도 정리하지 않는다 — 잡고 푸는 자리를 한 함수에 모은다],
    [오늘의 선택], [대개 반환값과 `goto cleanup`. 깊은 파서에서만 정당하다],
  )
]

흐름을 끊고 뛰는 두 장치를 보았다 — 밖에서 들어오는 신호와, 안에서 뛰는 비지역
점프다. 다음 장은 표준이 새로 보탠 것들과 "안전한 함수"를 둘러싼 오래된
논쟁이다.
