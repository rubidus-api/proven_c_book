#import "../lib.typ": *

= 수 — `<math.h>`·`<fenv.h>`·`<tgmath.h>`

#organizer[
  실수 계산이 실패를 알리는 방식을 본다. 6장과 41장에서 배운 근사의 수학이
  여기서 함수의 계약이 된다 — 정의역 밖 호출, 범위를 넘는 결과, NaN과
  무한대의 성질, 그리고 반올림 모드라는 숨은 전역 상태까지.
]

#deepqa[
  41장에서 실수를 `==`로 비교하지 말라고 했고, 6장에서는 0.1이 정확히
  표현되지 않는다고 했다. 그러면 수학 함수가 "계산할 수 없는 입력"을
  받으면 어떻게 알려 주는가?
][
  두 경로가 있다. *반환값*으로 NaN이나 무한대를 주고, 동시에 *`errno`*에
  이유를 남긴다 — 정의역 밖이면 `EDOM`, 결과가 표현 범위를 넘으면
  `ERANGE`. 다만 구현이 `errno` 대신 부동소수점 예외 플래그(`<fenv.h>`)로
  알리도록 선택할 수도 있어서, 이식성 있게 확인하려면 둘 다 볼 준비를 해야
  한다. 실무에서는 대개 반환값을 `isnan`·`isinf`로 검사하는 쪽이 간단하다.
]

== NaN과 무한대의 성질

#demo("examples/ch55/math.c")

출력에서 네 가지를 짚는다.

*① NaN은 자기 자신과 같지 않다.* IEEE 754가 그렇게 정했다. 그래서
`x != x`가 참이면 `x`는 NaN이라는 오래된 관용구가 성립하고, 표준 함수로는
`isnan(x)`를 쓴다. 이 성질 때문에 NaN이 든 배열을 `qsort`로 정렬하면
비교자의 전순서가 깨져 결과가 무너진다(53장).

*② 실수의 0 나누기는 계약 밖이 아니다.* 정수 나눗셈과 달리(24장) IEEE 754
환경에서는 무한대나 NaN을 낳는다. 다만 *0으로 나눈다는 사실 자체가 대개
버그*라는 점은 같다.

*③ `sqrt(-1)`은 `EDOM`, `exp(1000)`은 `ERANGE`.* 전자는 정의역 밖,
후자는 결과가 표현 범위를 넘은 경우다. `errno`를 볼 생각이라면 호출 직전에
0으로 놓아야 한다(57장).

*④ 0.0과 −0.0은 `==`로 같다.* 그러나 부호 비트는 다르고, `1/0.0`과
`1/-0.0`은 각각 +∞와 −∞다. 부호를 구별해야 하면 `signbit`을 쓴다.

#misconception[
  "실수 비교는 엡실론을 쓰면 안전하다"
][
  41장에서 배운 엡실론 비교는 만능이 아니다. 절대 오차(`fabs(a-b) < eps`)는
  값이 클 때 무의미해지고 — 1e9 근처에서 1e-9는 표현조차 안 된다 —
  상대 오차는 0 근처에서 무너진다. 실무의 처방은 *상황에 맞는 허용 오차를
  정하는 것*이지 보편 상수를 쓰는 것이 아니다. 그리고 애초에 비교가 필요
  없도록 정수나 고정소수점으로 다룰 수 있는지 먼저 묻는 것이 낫다(6장의
  금액 계산 이야기).
]

== 자주 틀리는 함수들

#dtable(
  columns: 3,
  [*함수*], [*하는 일*], [*함정*],
  [`pow(x, y)`], [거듭제곱], [정수 거듭제곱에 쓰면 느리고 부정확할 수 있다],
  [`round`·`nearbyint`], [반올림], [`round`는 0에서 먼 쪽, `nearbyint`는 현재 모드],
  [`floor`·`ceil`·`trunc`], [정수로 깎기], [음수에서 방향이 다르다],
  [`fmod`·`remainder`], [나머지], [부호 규칙이 서로 다르다],
  [`abs`·`fabs`], [절댓값], [★ `abs`는 정수용. 실수에 쓰면 잘린다],
  [`atan2(y, x)`], [각도], [인자 순서가 `y, x`다],
  [`isnan`·`isinf`], [분류], [매크로다 — 함수 포인터로 쓸 수 없다],
)

`pow(x, 2)`는 널리 쓰이지만, 정수 제곱이라면 `x * x`가 빠르고 정확하다.
컴파일러가 최적화해 주는 경우도 많지만 언제나 그런 것은 아니다.

`abs`를 실수에 쓰는 실수는 특히 조용하다. `<stdlib.h>`의 `abs`는 `int`를
받으므로 `abs(-1.5)`는 −1.5를 1로 만들어 버린다. 요즘 컴파일러는 경고하지만
`<math.h>`를 포함하지 않은 파일에서는 놓치기 쉽다.

== 반올림 모드와 부동소수점 예외 — `<fenv.h>`

부동소수점 연산에는 *숨은 전역 상태*가 둘 있다.

*반올림 모드* — 기본은 "가장 가까운 값, 동점이면 짝수"다. `fesetround`로
바꿀 수 있고, 바꾸면 그 뒤의 모든 실수 연산이 영향을 받는다.

*예외 플래그* — 나눗셈에서 0, 넘침, 부정확 등이 일어나면 플래그가 켜진다.
`fetestexcept`로 읽고 `feclearexcept`로 지운다. `errno`보다 정밀하지만,
이 기능을 쓰려면 `#pragma STDC FENV_ACCESS ON`을 켜야 하고 — 그러면
컴파일러가 실수 연산 재배열을 자제한다 — 최적화가 줄어든다.

#antipattern[
  `-ffast-math`를 켜고 NaN을 검사하기
][
  ```sh
  cc -O2 -ffast-math app.c        # 컴파일러에게 "NaN·무한대는 없다고 쳐라"
  ```
  ```c
  if (isnan(x)) { /* 이 분기가 통째로 사라질 수 있다 */ }
  ```
  `-ffast-math` 계열 선택지는 결합 법칙을 가정하고 NaN·−0.0의 존재를
  무시해도 좋다고 컴파일러에 알린다. 속도는 얻지만 *검사 코드가 최적화로
  사라질 수 있다* — 15장에서 본 "릴리스에서만 나는 버그"의 실수 판이다.
  수치의 정확성이 중요한 프로그램에서는 켜지 않는 것이 기본이다.
]

== 타입 제네릭 — `<tgmath.h>`

`sqrt`는 `double`용, `sqrtf`는 `float`용, `sqrtl`은 `long double`용이다.
`<tgmath.h>`를 포함하면 `sqrt(x)` 하나로 인자의 타입에 맞는 판이 골라진다 —
46장에서 본 `_Generic`이 표준 라이브러리에 쓰인 대표 사례다.

편하지만 대가가 있다. 매크로이므로 함수 포인터로 넘길 수 없고, 인자를
두 번 평가하는 구현이 있을 수 있어 부수효과가 있는 표현식을 넣으면 위험하다.

#realcase[
  같은 계산, 다른 답 — 초과 정밀도의 역사
][
  x86의 옛 부동소수점 장치(x87)는 내부적으로 80비트로 계산했다. 그래서
  같은 `double` 연산이 레지스터에 남아 있을 때와 메모리에 저장된 뒤가
  달라지는 일이 생겼다 — 최적화 수준을 바꾸면 결과가 미세하게 달라지고,
  `x == y`가 참이었다가 거짓이 되기도 했다.

  C99가 `FLT_EVAL_METHOD`로 이 사정을 명시하게 만들었고, 오늘날 64비트
  x86은 SSE를 쓰므로 문제가 크게 줄었다. 그러나 "같은 코드, 다른 답"의
  가능성은 여전히 컴파일 선택지와 대상 기계에 남아 있다 — 41장에서
  "실수 계산은 재현 가능성을 따로 챙겨야 한다"고 한 이유다.
]

#recap[
  수 요약.

  #dtable(
    columns: 3,
    [*상황*], [*쓸 것*], [*조심할 것*],
    [NaN 검사], [`isnan`], [`x == NaN`은 언제나 거짓],
    [무한대 검사], [`isinf`], [실수의 0 나누기는 UB가 아니다],
    [값의 종류], [`fpclassify`], [비정규 수의 존재],
    [정의역·범위 오류], [반환값 + `errno`], [호출 직전 `errno = 0`],
    [정수 제곱], [`x * x`], [`pow(x, 2)`],
    [실수 절댓값], [`fabs`], [`abs`(정수용)],
    [타입별 함수], [`<tgmath.h>`], [매크로 — 부수효과 인자 금지],
    [빠른 수학 옵션], [끄는 것이 기본], [검사 코드가 사라진다],
  )
]

수를 지났다. 다음 장은 시간 — 표준이 정해 주지 않은 것이 유난히 많은
자리다.
