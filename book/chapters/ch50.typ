#import "../lib.typ": *

= 기반 — 바이트, 뷰, 그리고 넘치지 않는 산술

#organizer[
  라이브러리 전체가 딛고 선 네 가지 기본 어휘를 본다 — 원시 바이트를
#idx("뷰(view)")  가리키는 타입, 포인터와 길이를 하나로 묶은 *뷰*(view), 감기지 않는 크기
  계산, 그리고 정렬. 47장의 여섯째 버그(바이트의 숨은 타입)와 33장의 경계
  문제가 여기서 타입 차원의 답을 얻는다.
]

#deepqa[
  32장에서 `char*`(그리고 `unsigned char*`)만은 어떤 객체든 바이트 단위로
  들여다볼 수 있는 특권을 가진다고 했고, 11장에서는 그 규칙을 어긴 코드가
  최적화에서 조용히 무너지는 것을 보았다. 그러면 "안전하게 바이트를
  다룬다"는 것은 구체적으로 무엇을 지키는 일인가?
][
  두 가지다. 첫째, *들여다보는 타입을 규칙이 면제한 것으로 고정한다* —
  `unsigned char`다. 둘째, *들여다보는 범위를 잃어버리지 않는다* — 포인터
  하나만 들고 다니면 어디까지가 내 땅인지 잊게 되고, 그것이 33장의 경계
  침범이다. proven의 기본 어휘는 이 두 가지를 각각 타입으로 만든 것이다.
]

== 바이트에는 이름이 있다

`proven_byte_t`는 `unsigned char`의 별칭이다. 별것 아닌 것 같지만
선언 하나가 계약을 적는다 — "이 포인터는 *표현*(representation)을 보는
눈이지, 어떤 타입의 값을 가리키는 것이 아니다".

이 구분이 왜 중요한지는 11장에서 이미 보았다. 같은 기억을 `uint32_t*`와
`uint16_t*`로 번갈아 보는 코드는 앨리어싱 규칙 위반이고, 컴파일러는 그
전제를 최적화에 쓴다. 반면 `unsigned char`로 보는 것은 표준이 명시적으로
허용한다. 라이브러리가 원시 메모리를 다룰 때 언제나 이 타입을 거치는
이유이고, 그래서 라이브러리를 통해서는 그 버그를 쓸 수가 없다.

== 뷰 — 포인터와 길이를 하나로

읽기 전용 뷰는 이렇게 생겼다. 구조체 두 칸이 전부다.

```c
typedef struct {
    const proven_byte_t *ptr;
    proven_size_t        size;
} proven_mem_view_t;
```

쓰기가 가능한 판(`proven_mem_mut_t`)은 `const`만 빠진 쌍둥이다. 이 작은
구조체가 하는 일은 하나다 — *포인터와 길이가 절대로 헤어지지 않게 하는
것*. 34장에서 "문자열의 진짜 문제는 길이를 따로 들고 다니는 것"이라 했던
그 문제에 대한 답이다.

#demo("examples/ch50/view.c")

`slice 6+4`가 이 절의 핵심이다. 8바이트짜리 뷰에서 6번째부터 4바이트를
달라고 했으니 두 바이트가 모자란다. 라이브러리는 *있는 만큼 잘라 주지
않는다* — 에러를 돌려준다(2번은 `PROVEN_ERR_OUT_OF_BOUNDS`다).
있는 만큼 주는 편이 친절해 보이지만, 그러면 호출자는 자기가 받은 것이
요청한 것인지 알 수 없게 된다. 47장의 잘림 문제가 여기서 되풀이되지
않도록 막은 것이다.

#qa[
  자르기 함수가 `_checked`와 `_unchecked` 두 판으로 있던데, 후자는 왜
  존재하는가?
][
  경계를 *이미 확인한* 자리를 위해서다. 반복문 안에서 매 회 같은 검사를
  다시 하는 것은 낭비이므로, 루프 앞에서 한 번 확인하고 안쪽에서는
  검사 없는 판을 쓰는 식이다. 이름에 `_unchecked`가 붙어 있다는 점이
  중요하다 — 위험한 선택은 *눈에 보이는 이름*으로만 할 수 있고, 기본값은
  언제나 안전한 쪽이다. 41장에서 말한 "계약을 코드로 적는다"의 한 형태다.
]

#antipattern[
  뷰를 소유자보다 오래 들고 있기
][
  ```c
  proven_mem_view_t get_view(void) {
      proven_byte_t local[16] = {0};
      return (proven_mem_view_t){ .ptr = local, .size = sizeof local };
  }   /* local 은 여기서 죽는다 — 돌려준 뷰는 이미 유효하지 않다 */
  ```
  뷰는 *빌린 것*이다. 소유자가 사라지면 그 순간 유효하지 않게 되고, 그
  뒤의 사용은 36장에서 배운 죽은 자동 변수 접근이다. 뷰가 포인터보다
  안전한 것은 *경계*에 대해서이지 *수명*에 대해서가 아니다 — 수명은
  여전히 사람이 지켜야 한다. 그래서 다음 장의 할당자 이야기가 필요해진다.
]

== 넘치지 않는 크기 산술

23장에서 무부호 정수의 감김을 배웠고, 5장에서 그것이 왜 정의된 동작인지
보았다. 감김이 조용하다는 사실은 한 자리에서 특히 위험해진다 — *할당할
바이트 수를 계산할 때*다.

```c
void *p = malloc(count * sizeof(item_t));   /* count 가 크면 감긴다 */
```

`count`가 충분히 크면 곱이 감겨서 아주 작은 수가 되고, `malloc`은 그
작은 크기로 성공한다. 그 뒤 프로그램은 원래 의도한 개수만큼 쓰기 시작한다
— 전형적인 힙 넘침이다. 23장에서 "크기 계산"을 오버플로의 실제 사고
사례로 든 것이 이 무늬였다.

#idx("검사 산술")라이브러리는 크기 계산에 C23의 검사 산술을 쓴다. 예제의
`PROVEN_CKD_MUL`이 그것으로, 곱이 넘치면 *참*을 돌려준다(계산 결과가
아니라 넘침 여부가 반환값이다). 넘치면 할당을 아예 시도하지 않는다.

#misconception[
  "`size_t`는 64비트나 되니 넘칠 리 없다"
][
  넘칠 수 있고, 실제로 넘긴다. 곱셈은 값을 *제곱 규모로* 키우기 때문이다
  — 40억 × 40억은 이미 64비트를 넘는다. 게다가 32비트 기계에서
  `size_t`는 여전히 32비트이고, 파일 형식에서 읽어 온 32비트 필드 둘을
  곱하는 코드는 그 자리에서 감긴다. 무엇보다 중요한 것은 *누가 그 수를
  정하는가*다. 크기가 프로그램 안의 상수라면 안심해도 좋지만, 파일이나
  네트워크에서 온 수라면 그것은 *공격자가 고른 피연산자*다.
]

#realcase[
  곱셈 하나가 만든 취약점들
][
  이 무늬는 CVE 목록의 단골이다. 이미지 디코더가 `width * height *
  bytes_per_pixel`을 32비트로 계산하다 감긴 사례, 폰트 파서가 글리프 개수를
  곱하다 감긴 사례, 압축 해제 코드가 원본 크기를 곱하다 감긴 사례가
  반복해서 보고됐다. 공통점은 모두 *입력 파일이 크기를 정한다*는 것 —
  즉 공격자가 곱셈의 피연산자를 고를 수 있다는 것이다. 그래서 요즘 언어와
  라이브러리는 크기 계산을 검사 산술로 하거나 아예 넘칠 수 없는 타입으로
  다룬다.
]

== 정렬 — 다음 경계로 밀어 올리기

4장에서 배운 정렬(alignment)이 여기서 실용적인 도구가 된다.
`proven_mem_align_up(13, 8)`은 16을 돌려준다 — 13번지에서 시작하는
객체를 8바이트 경계에 맞추려면 16으로 밀어야 하기 때문이다. 다음 장의
아레나가 객체를 줄지어 놓을 때 매번 하는 계산이 바로 이것이다.

`PROVEN_MAX_ALIGN`은 "어떤 타입이든 담을 수 있는 가장 엄격한 정렬"이고,
`proven_is_pow2`는 정렬 값이 2의 거듭제곱인지 확인한다(정렬은 언제나
2의 거듭제곱이어야 한다 — 그래야 비트 연산으로 올림을 계산할 수 있다).

#recap[
  이 장의 어휘.

  #dtable(
  columns: 3,
    [*이름*], [*무엇*], [*계약*],
    [`proven_byte_t`], [`unsigned char` 별칭], [표현을 보는 유일한 합법 창],
    [`proven_mem_view_t`], [읽기 전용 뷰(ptr+size)], [빌린 것 — 소유자보다 오래 살 수 없다],
    [`proven_mem_mut_t`], [쓰기 가능 뷰], [같음],
    [`..._slice_checked`], [부분 뷰 만들기], [범위를 넘으면 에러, 자르지 않음],
    [`PROVEN_CKD_MUL/ADD`], [검사 산술], [넘치면 참 — 계산은 버린다],
    [`proven_mem_align_up`], [정렬 올림], [정렬은 2의 거듭제곱],
)
]

기억을 *보는* 어휘를 갖췄다. 다음은 기억을 *얻는* 이야기다 — 그리고 그
자리에서 이 라이브러리의 가장 특징적인 결정을 만나게 된다.
