#import "../lib.typ": *

= 전처리기와 번역 단계

#organizer[
  16장에서 "C를 모르는 텍스트 도구"라 부르고 지나친 층을 정면으로 연다.
  매크로의 정체, `#`과 `##`이라는 두 연산자의 원리와 관용구, 그 함정들,
#idx("번역 단계")  그리고 소스가 프로그램이 되기까지 표준이 못박은 *번역 단계*의 전모
  까지. C 소스 파일 안에 사는 "두 번째 언어"의 문법서다.
]

#deepqa[
  20장에서 `#`으로 시작하는 줄은 문장이 아니고 세미콜론도 없다고 했고,
#idx("전처리기")  그 이유가 "전처리기는 C 문법이 아니라 줄 단위로 일하는 텍스트 도구라
  서"라고 했다. 그러면 전처리기는 C의 무엇을 아는가?
][
  거의 아무것도 모른다 — 타입도, 스코프도, 함수도 모른다. 아는 것은
  *토큰*(더 이상 쪼갤 수 없는 낱말 조각: 이름, 숫자, 연산자, 문자열)
  뿐이고, 하는 일은 토큰을 지우고 끼우고 이어 붙이는 것이다. 이 무지가
  전처리기의 힘이자(어떤 코드 조각이든 만들어 낼 수 있다) 위험이다
  (문법과 의미를 지켜 주지 않는다). 이 장은 그 힘과 위험을 함께 본다.
]

== 매크로 — 이름을 토큰 열로 바꾸기

`#define NAME 내용`은 "이후 NAME이라는 토큰을 만나면 내용으로 바꿔
치우라"는 지시다. 인자를 받는 꼴(`#define MAX(a,b) ...`)도 있는데,
함수처럼 보이지만 함수가 아니다 — *호출이 아니라 치환*이고, 타입 검사도
평가 순서 보장도 없다.

두 가지 관용구가 이 차이에서 나온다. 첫째, *인자와 전체를 괄호로 감싼다*
— `#define SQ(x) ((x) * (x))`처럼. 괄호가 없으면 `SQ(1+2)`가
`1+2*1+2`로 펼쳐져 5가 된다(21장의 우선순위가 치환된 토큰 열에 그대로
적용되기 때문이다). 둘째, *인자를 두 번 쓰는 매크로를 조심한다* —
`SQ(i++)`는 `i`를 두 번 증가시킨다. 33장에서 배운 "한 문장에서 한
변수는 한 번만"이 매크로 때문에 몰래 깨지는 것이다.

== `#` — 토큰을 문자열로 (stringize)

매크로 인자 앞에 `#`을 붙이면, 그 인자가 *글자 그대로 문자열 리터럴이*
된다. 값이 아니라 *적힌 모양*이 문자열이 되는 것이 핵심이다.

#demo("examples/ch50/macro.c")

시연의 `SHOW(2 + 3 * 4)`가 그 실물이다 — `#expr`이 `"2 + 3 * 4"`라는
문자열이 되고, 옆의 `(expr)`은 계산되어 14가 된다. 하나의 인자가 *글자
로도, 값으로도* 쓰인 것이다. 이 무늬가 디버깅·검사 매크로의 기본형이다:
`assert(x > 0)`가 실패했을 때 "x > 0"이라는 조건을 그대로 찍어 주는
것이 바로 이 원리다(46장에서 만난 그 assert의 속살이다).

== `##` — 토큰 두 개를 하나로 (token paste)

`##`은 양옆의 토큰을 *붙여서 하나의 새 토큰으로* 만든다. 시연의
`MAKE_NAME(value_, 1)`이 `value_1`이라는 *식별자*가 되는 것이 그
예다 — 문자열이 아니라 진짜 이름이라, 변수 선언에 쓸 수 있다.

용도는 *이름을 조립하는* 코드 생성이다. 같은 모양의 함수·구조체를
타입마다 찍어 내야 할 때(C에는 제네릭이 없으므로), 접두어와 타입 이름을
붙여 `stack_int_push`, `stack_double_push` 같은 이름들을 매크로 하나로
만들어 내는 것이 고전적 기법이다.

== 두 겹 우회 — 왜 한 겹으로는 안 풀리는가

`#`과 `##`을 쓸 때 반드시 만나는 규칙이 하나 있다. *`#`이나 `##`의
피연산자가 되는 매크로 인자는 먼저 펼쳐지지 않는다.* 시연의 마지막
두 줄이 그 대조다 — `STR_RAW(WIDTH)`는 `"WIDTH"`가 되고,
`STR(WIDTH)`는 `"80"`이 된다.

이유는 치환 순서에 있다. 매크로 인자는 보통 *먼저 완전히 펼쳐진 뒤*
본문에 끼워지지만, `#`·`##`의 대상이면 *펼치지 않은 원래 토큰*이
쓰인다. 그래서 값을 원할 때는 한 겹을 더 두른다 — 바깥 매크로(`STR`)가
인자를 평범하게 받아 *펼친 뒤*, 안쪽 매크로(`STR_RAW`)에 넘겨 거기서
문자열로 만드는 것이다. `##`도 같은 사정이라 `PASTE`/`PASTE_RAW` 짝을
두는 것이 관용구다. 전처리기의 함정 중 가장 자주 사람을 붙잡는 규칙
이고, 알고 나면 두 줄로 해결되는 문제이기도 하다.

#realcase[
  매크로 하나가 만든 언어 — X 매크로
][
  `#`과 `##`의 힘을 보여 주는 고전 기법이 있다. 목록을 한 곳에만 적어
  두고 여러 형태로 펼치는 *X 매크로*다. 이름은 관례적으로 쓰이는
  매개변수 `X`에서 왔다. 다음 절에서 실물을 본다.
]

== X 매크로 — 목록 하나에서 코드 여러 벌을

C에는 코드 생성 기능이 없다. 그래서 "같은 목록을 여러 형태로 되풀이해
적어야 하는" 문제가 생긴다 — 오류 코드를 예로 들면, 열거형에 한 번,
이름 문자열 표에 한 번, 메시지를 돌려주는 `switch`에 한 번. 항목을
추가할 때 한 곳을 빠뜨리면 그때부터 표가 어긋난다.

X 매크로는 이 문제를 *목록을 한 번만 적는 것*으로 푼다. 요령은 두
단계다.

+ 목록을 "아직 정의되지 않은 매크로 `X`를 호출하는 형태"로 적어 둔다.
+ `X`를 그때그때 다르게 정의한 뒤 목록을 펼친다. 같은 목록이 매번 다른
  코드가 되어 나온다.

#demo("examples/ch50/xmacro.c")

네 벌이 한 목록에서 나왔다. 하나씩 짚는다.

*① 열거형* — `AS_ENUM`은 `ERR_##name = code,`로 펼쳐진다. `##`가
`ERR_`와 이름을 이어 붙여 `ERR_NOT_FOUND = 2,`를 만든다.

*② 이름 문자열 표* — `#name`이 이름을 그대로 문자열로 바꾼다. 게다가
`[code] = #name`이라는 *지정 초기화*(42장)를 써서, 코드 값이 곧 배열의
인덱스가 되게 했다. 값이 듬성듬성해도(0·2·5·9) 표가 알아서 맞는다.

*③ `switch` 생성* — `case ERR_##name: return text;`가 항목 수만큼
펼쳐진다. 함수 본문 전체가 목록에서 만들어진 셈이다.

*④ 개수 세기* — `0 + 1 + 1 + 1 + 1`로 펼쳐지도록 만든 요령이다. 목록이
늘어도 `ERROR_COUNT`가 저절로 따라온다.

*⑤ 그리고 본문에서* — `main` 안에서도 같은 목록을 한 번 더 펼쳐 표를
찍었다. 매크로를 쓰고 나서 `#undef`로 지우는 것이 관례다. 이름이
남아 있으면 다음 사용과 충돌하기 때문이다.

#qa[
  목록 파일을 따로 두는 판도 있다고 들었다. 무엇이 다른가?
][
  같은 착상의 다른 포장이다. 목록을 `errors.def` 같은 파일에 적어 두고,
  쓰는 쪽에서 `X`를 정의한 뒤 `#include "errors.def"`로 펼친다 —
  `#include`가 텍스트 붙여넣기라는 사실(49장)을 그대로 이용하는 것이다.
  매크로 줄바꿈(`\\`)이 없어져 목록이 읽기 좋아지는 대신, 파일이 하나
  늘고 IDE가 그 파일을 코드로 인식하지 못하는 불편이 있다. 리눅스 커널의
  시스템 호출 표가 이 방식을 오래 써 왔다.
]

#antipattern[
  X 매크로를 쓰지 않는 편이 나은 자리
][
  ```c
  /* 항목이 셋뿐이고 파생물도 하나뿐인 경우 */
  #define COLOR_LIST(X) X(RED) X(GREEN) X(BLUE)
  ```
  이 정도면 그냥 열거형과 문자열 배열을 나란히 적는 편이 읽기 쉽다.
  X 매크로의 값은 *목록이 길고, 목록에서 파생되는 코드가 여러 벌이며,
  항목이 계속 늘어날 때* 나온다. 짧은 목록에 쓰면 얻는 것(중복 제거)보다
  잃는 것(펼쳐진 코드가 디버거·오류 메시지에서 보이지 않음, 처음 보는
  사람이 읽지 못함)이 크다.
]

#realcase[
  실무에서 만나는 X 매크로
][
  이 기법은 이름만 낯설 뿐 곳곳에 있다. 리눅스 커널의 시스템 호출 표와
  오류 코드 정의, 여러 임베디드 SDK의 레지스터·핀 정의, 게임 엔진의
  자산 종류 목록, 그리고 상태 기계의 상태 목록이 대표적이다. 공통점은
  같다 — *한 목록이 열거형·문자열·검사 코드·직렬화 코드로 동시에
  쓰이는* 자리다. 도구가 없는 언어에서 중복을 없애려는 압력이 만들어 낸
  발명품이고, 그래서 C의 성격을 잘 보여 주는 기법이기도 하다.
]

#misconception[
  "매크로는 함수의 빠른 버전이다"
][
  옛 시절에는 반쯤 사실이었다 — 함수 호출 비용을 아끼려고 짧은 함수를
  매크로로 적곤 했다. 그러나 오늘의 컴파일러는 작은 함수를 알아서
  인라인으로 펼치므로(14장의 편집자), 속도를 위해 매크로를 쓸 이유는
  거의 사라졌다. 남은 것은 대가뿐이다 — 타입 검사 없음, 인자 다중 평가,
  디버거에서 안 보임, 오류 메시지가 펼쳐진 코드 기준으로 나와 읽기 어려움.
  현대적 지침은 분명하다: *값 계산은 함수(필요하면 `static inline`),
  상수는 `enum`이나 C23의 `constexpr`, 매크로는 함수로 안 되는 일 —
  조건부 컴파일, 토큰 조립, 소스 위치 포착 — 에만.*
]

== 번역 단계 — 표준이 못박은 여덟 걸음

전처리가 "먼저" 일어난다는 것은 알았는데, 정확히 무엇이 어떤 순서로
일어나는가. 표준은 이것을 *번역 단계*(translation phases)라는 이름으로
여덟 걸음으로 못박아 두었다. 실제 컴파일러가 이 단계를 물리적으로 나눠
수행할 필요는 없지만, *마치 이 순서로 한 것처럼* 동작해야 한다(14장의
"겉보기만 같으면"이 여기에도 적용된다).

#dtable(
  columns: 2,
  [*단계*], [*하는 일*],
  [1], [소스 문자를 읽어 내부 문자 집합으로 옮긴다(9장의 인코딩 세계). 옛날에는 삼중자 치환도 여기였다 — C23이 제거했다],
  [2], [줄 끝의 역슬래시로 이어진 줄들을 한 줄로 합친다],
  [3], [주석을 공백 하나로 바꾸고, 소스를 *토큰*으로 쪼갠다],
  [4], [전처리 지시를 실행한다 — `#include`(재귀적으로 1\~4단계 반복), `#define` 치환, 조건부 컴파일],
  [5], [문자·문자열 리터럴의 문자를 실행 문자 집합으로 변환한다],
  [6], [이웃한 문자열 리터럴들을 하나로 잇는다(`"가" "나"` → `"가나"`)],
  [7], [토큰을 C 문법으로 해석하고 번역한다 — 여기가 "컴파일"이다],
  [8], [필요한 정의들을 이어 실행 이미지를 만든다 — 링크],
)

이 표가 여러 수수께끼를 한꺼번에 푼다. *주석이 3단계에서 공백이 되므로*
`a/**/b`는 `ab`가 아니라 `a b`다. *`#include`가 4단계에서 재귀적으로
처리되므로* 헤더 안의 헤더가 자연스럽게 펼쳐진다. *문자열 이어 붙이기가
6단계에 있으므로* 긴 문자열을 여러 줄로 나눠 적는 관용구가 성립하고,
매크로가 만든 문자열 조각(`#` 연산자의 결과)도 옆 리터럴과 붙는다.
그리고 *문법 해석이 7단계*라는 사실이 이 장의 핵심을 다시 말해 준다 —
전처리기가 일할 때 C 문법은 아직 존재하지 않는다.

#qa[
  그러면 매크로 안에서 문법 오류를 내면 어디서 잡히는가?
][
  7단계에서, *펼쳐진 뒤의 코드*를 기준으로 잡힌다 — 그래서 오류 메시지가
  내가 적은 줄과 동떨어져 보이는 것이다. 진단의 정석은 16장에서 배운
  `-E`(전처리만 하기)다: 문제의 파일을 전처리해 실제로 펼쳐진 모습을
  눈으로 보면, 수수께끼 같던 오류가 대개 즉시 풀린다. 현대 컴파일러는
  "매크로 NAME에서 펼쳐짐"이라는 추적 정보를 함께 보여 주어 이 일을
  거들기도 한다.
]

소스가 프로그램이 되는 전 과정을 이제 층별로 알게 됐다. 다음 장은
`printf`가 인자를 몇 개든 받는 비밀 — 가변 인자 함수다.
