#import "../lib.typ": *

= 함수를 값으로 — 함수 포인터

#prereq(
  ([24장 함수 선언과 정의], [함수의 이름]),
  ([38장 배열], [이름이 값으로 무너진다]),
)

#deepqa[
  38장에서 배열 이름이 첫 원소의 주소로 무너진다고 했고, 57장에서는
  가변 인자 함수가 타입 정보를 잃는다고 했다. 그러면 *함수 이름*은
  수식에서 무엇으로 평가되는가?
][
  *함수를 가리키는 포인터*로 평가된다 — 배열과 같은 무너짐(decay)이
  함수에도 있다. 예외는 `sizeof`와 `&`의 피연산자일 때뿐인데, 함수에
  `sizeof`를 쓰는 것은 애초에 금지이므로 사실상 `&`만 예외다. 그리고
  `&함수`도 결국 같은 포인터를 준다. 그래서 `f`, `&f`, `*f`가 전부 같은
  값이 되는 기묘한 상황이 벌어진다 — 이 장의 첫 예제가 그것이다.
]

#organizer[
  지금까지 함수는 "부르는 것"이었다. 이 장에서는 함수를 *값으로* 다룬다 —
  변수에 담고, 인자로 넘기고, 배열에 늘어놓는다. 그 과정에서 C의 오래된
  규칙 하나(*함수 이름은 포인터로 무너진다*)와, 그 규칙이 만드는 기묘한
  문법, 그리고 이 도구로 객체지향을 손수 짓는 방법까지 본다.
]

#chapter-questions()

== 이름은 포인터로 무너진다

#demo("examples/ch58/funcptr.c")

출력의 첫 두 줄이 이 장의 핵심이다.

*`add == &add`* — 함수 이름은 포인터로 무너지고, `&`를 붙여도 같은 포인터를
얻는다. 그래서 둘은 같다.

*`(*******p)(2,3)`이 컴파일된다* — 별표를 아무리 붙여도 결과가 같은 이유는
이렇다. `*p`는 포인터를 역참조해 *함수 지시자*를 얻는데, 그 지시자가 값으로
쓰이는 순간 *다시 포인터로 무너진다.* 즉 `*` 하나가 붙을 때마다 "포인터 →
함수 → 포인터"를 한 바퀴 돌 뿐, 제자리다.

반대 방향은 성립하지 않는다. `&&add`는 문법 오류다 — `&add`는 이미 *값*
(포인터)이고, C에서 값의 주소는 얻을 수 없기 때문이다(주소를 얻으려면
이름이 붙은 자리, 즉 lvalue여야 한다). `**add`는 되는데 `&&add`는 안 되는
비대칭이 여기서 나온다.

#misconception[
  "함수 포인터를 호출하려면 `(*p)(x)`처럼 역참조해야 한다"
][
  옛 관행이다. `p(x)`와 `(*p)(x)`는 완전히 같고, 표준은 둘 다 허용한다.
  `(*p)(x)`를 선호하는 코드베이스가 있는 이유는 *읽는 사람에게 이것이
  함수 포인터임을 알리려는* 문서 목적이지 문법 요구가 아니다. 어느 쪽이든
  일관되게 쓰면 된다.
]

== 타입이 곧 계약이다

함수 포인터의 타입은 반환형과 매개변수 목록으로 정해진다.

```c
int  (*p)(int, int);       /* int 둘을 받아 int 를 주는 함수의 포인터 */
void (*q)(void);           /* 인자도 반환도 없는 함수의 포인터 */
int  (*r[4])(int);         /* 그런 포인터 네 개의 배열 */
int  (*(*s)(void))(int);   /* 읽기 어렵다 — typedef 를 쓴다 */
```

마지막 줄이 보여 주듯 선언이 금세 험해진다. 실무의 관행은 `typedef`다.

```c
typedef int (*binop_fn)(int, int);
binop_fn table[] = { add, mul };
```

*다른 타입의 함수 포인터로 캐스트해 호출하는 것은 계약 밖이다.* 예를 들어
`void (*)(void)`로 캐스트해 둔 것을 `int (*)(int)`로 되돌려 부르면 안 된다.
저장했다가 *원래 타입으로 되돌려* 부르는 것만 보장된다.

#antipattern[
  비교자 시그니처를 캐스트로 맞추기
][
  ```c
  int cmp_int(const int *a, const int *b);            /* 편해 보이지만 */
  qsort(v, n, sizeof v[0], (int (*)(const void *, const void *))cmp_int);
  ```
  `qsort`는 `const void *` 두 개를 넘기는데, 실제 함수는 `const int *`를
  기대한다. 두 타입이 같은 표현을 갖는 것이 *보장되지 않으므로* 이 호출은
  계약 밖이다. 옳은 방법은 시그니처를 그대로 맞추고 안에서 캐스트하는
  것이다 — 예제의 `cmp`가 그렇게 했다.
]

== `void *`와 함수 포인터는 다른 세계다

35장에서 `void *`가 "어떤 데이터 포인터든 담는 그릇"이라고 배웠다. 그런데
*함수 포인터는 그 그릇에 담기지 않는다.* 표준은 데이터 포인터와 함수
포인터 사이의 변환을 정의하지 않는다.

이유는 역사와 하드웨어에 있다. *하버드 구조*(Harvard architecture)를 쓰는
기계에서는 코드와 데이터가 서로 다른 주소 공간에 있다 — AVR 마이크로
컨트롤러가 대표적이고, 여기서는 "0번지"가 코드 영역에도 데이터 영역에도
따로 존재한다. 주소의 폭조차 다를 수 있다. 그런 기계에서 두 포인터를
같은 그릇에 담는 것은 애초에 불가능하다.

그래서 예제가 두 크기를 함께 찍은 것이다. 이 기계에서는 우연히 같았지만,
*같다는 보장은 어디에도 없다*.

#realcase[
  POSIX가 표준과 어긋난 자리 — `dlsym`
][
  동적 라이브러리에서 함수를 찾아오는 POSIX 함수 `dlsym`은 `void *`를
  돌려준다. 그런데 우리가 원하는 것은 함수 포인터다. 즉 이 API는 *표준 C가
  정의하지 않은 변환*을 요구한다.

  POSIX는 이 모순을 인정하고, 2008년 판에서 "구현은 이 변환을 지원해야
  한다"고 따로 못박았다. 그래도 컴파일러는 경고를 내므로, 실무에서는
  다음 관용구가 굳었다.

  ```c
  void (*fn)(void);
  *(void **)&fn = dlsym(handle, "do_work");   /* 표준의 틈을 우회하는 관용구 */
  ```

  "표준이 금지한 것을 플랫폼이 요구하는" 드문 사례이고, C의 이식성이
  어디서 갈라지는지 보여 주는 좋은 표본이다.
]

== 함수 포인터를 인쇄하는 법

앞 절이 「`void *`와 함수 포인터는 다른 세계」라고 했다. 그 사실이 가장 자주
발목을 잡는 자리가 *로그*다 — 「지금 어느 콜백이 불렸나」를 남기고 싶은데,
35장에서 배운 `printf("%p", (void *)p)`가 여기서는 통하지 않는다.

#demo("examples/ch58/print_funcptr.c")

=== 왜 `%p`에 넘길 수 없는가

이유가 두 겹이다.

*첫째, `%p`가 받는 것은 `void *`(또는 문자 포인터)뿐이다*(35장). 함수 포인터는
그 목록에 없다.

*둘째, 함수 포인터를 `void *`로 바꾸는 것 자체를 표준이 정의하지 않는다.* 앞
절에서 본 그대로다. 그래서 아래 줄은 컴파일러가 받아 주더라도 표준의 문장으로는
근거가 없다.

```c
printf("%p", (void *)f);      /* ISO C 가 정의하지 않는 변환 */
```

이 책이 직접 확인한 GCC의 반응이 그것을 그대로 말해 준다 — `-Wpedantic`을 켜면
*"ISO C forbids conversion of function pointer to object pointer type"*라는 경고가
나온다.

#antipattern[
  세 가지 흔한 오답
][
  ```c
  printf("%p", f);              /* ① 함수 포인터를 그대로 — 계약 밖 */
  printf("%p", (void *)f);      /* ② 표준 밖의 변환(POSIX 에서는 통한다) */
  printf("%p", (void *)&f);     /* ③ 컴파일도 되고 경고도 없는데… */
  ```
  셋째가 특히 고약하다. `&f`는 *함수가 아니라 함수 포인터 변수의 주소*다.
  컴파일러는 아무 말도 하지 않고, 출력은 그럴듯한 16진수이며, 값은 완전히
  엉뚱하다. 「경고가 없으니 맞겠지」가 통하지 않는 자리다.
]

#platform[
  POSIX 는 이 구멍을 메운다
][
  유닉스 계열에서는 사정이 다르다. POSIX가 `dlsym()`이 *함수의 주소를 `void *`로
  돌려주도록* 정해 두었기 때문에, 그 시스템에서는 함수 포인터와 `void *` 사이의
  변환이 동작해야 한다.

  그래서 리눅스·macOS·BSD 만 겨냥하는 코드에서 `(void *)f`는 관행이 아니라
  규격에 가깝다. 다만 그것은 *C 표준이 아니라 POSIX의 약속*이다 — 12장에서 말한
  회색지대의 전형이고, 그 규율도 그대로다: *쓰기로 했다면 그 자리에 왜 괜찮은지를
  한 줄로 적어 두고, 이식이 필요해질 때 갈아탈 길을 알아 둔다.*
]

=== 이식성 있는 길 — 바이트로 떠서 찍기

표준만으로 해결하려면 *포인터를 값으로 읽지 말고 바이트로 옮긴다.* `memcpy`는
어떤 타입에서든 계약 안이다.

```c
unsigned char raw[sizeof f];
memcpy(raw, &f, sizeof raw);
for (size_t i = sizeof raw; i-- > 0; ) printf("%02X", raw[i]);
```

시연의 `fmt_funcptr`가 이 형태다. 얻는 것과 잃는 것이 분명하다 — *어디서나
컴파일되고 경고도 없다.* 대신 *사람에게는 수수께끼다.* 어느 플랫폼에서는 그
바이트가 함수의 진짜 진입점이 아니라 *디스크립터의 주소*이기도 하다(뒤의
플랫폼 노트).

=== 가장 좋은 답 — 주소 대신 이름

실무의 답은 사실 셋째 길이다. *주소를 찍지 말고 이름을 찍는다.*

시연의 `struct named_op`가 그 무늬다. 함수 표에 이름 문자열을 함께 담아 두면,
로그에는 `mul(7, 3) = 21`처럼 사람이 읽는 줄이 남는다. 함수 포인터끼리의 *비교*는
표준이 보장하므로(같은 함수를 가리키면 같다), 표를 훑어 이름을 찾는 것도 계약
안이다.

#dtable(
  columns: 3,
  [*방법*], [*이식성*], [*로그로서의 값*],
  [이름표를 함께 들고 다니기], [★ 어디서나], [★ 사람이 바로 읽는다],
  [`memcpy` 로 바이트 인쇄], [★ 어디서나], [수수께끼 — 심볼과 대조해야 한다],
  [`(void *)f` 로 `%p`], [POSIX 에서만], [수수께끼 — 위와 같다],
  [`dladdr`·`SymFromAddr` 로 이름 찾기], [플랫폼별], [★ 이름이 나오면 최고],
)

#realcase[
  주소에서 이름을 되찾는 길, 그리고 그 한계
][
  이미 돌고 있는 프로그램에서 주소만 들고 이름을 알아내는 방법이 있다. 유닉스는
  `dladdr()`, Windows 는 DbgHelp 의 `SymFromAddr()`, 리눅스 커널은 앞서 본
  `%pS` 지정자다.

  이 책이 `dladdr`을 직접 돌려 본 결과가 그 한계를 그대로 보여 준다.

  #dtable(
    columns: 2,
    [*대상*], [*결과*],
    [`static` 함수], [이름을 찾지 못했다 — 심볼 표에 없다],
    [보통의 전역 함수], [`-rdynamic` 없이는 못 찾고, 붙이면 찾았다],
    [`printf`], [찾았지만 내부 별칭인 `_IO_printf` 로 나왔다],
  )

  즉 *이름이 나오면 운이 좋은 것*이다. 심볼을 남기지 않는 빌드(릴리스 빌드가
  대개 그렇다)에서는 아무것도 나오지 않고, 나오더라도 소스에 적은 이름과 다를 수
  있다. 그래서 「주소를 이름으로 되돌리는 것」은 *디버깅 도구의 일*이고, 프로그램이
  스스로 남기는 로그는 *처음부터 이름을 들고 있는 편*이 낫다.
]

#platform[
  함수 포인터가 주소 하나가 아닌 기계들
][
  이 절이 표준을 유난히 조심스럽게 따르는 데는 이유가 있다. *함수 포인터가 단순한
  주소가 아닌 플랫폼이 실제로 있었다.*

  - 세그먼트가 있던 시절의 x86 에서는 `far` 함수 포인터가 세그먼트와 오프셋의
    쌍이었고, 데이터 포인터와 크기부터 달랐다.
  - IBM AIX 나 옛 Itanium ABI 에서는 함수 포인터가 *디스크립터*를 가리켰다 —
    진입점 주소와 전역 데이터 포인터가 함께 든 구조체다. 두 함수 포인터의 「주소」를
    찍으면 진입점이 아니라 그 구조체의 주소가 나온다.
  - 하버드 구조의 마이크로컨트롤러에서는 코드와 데이터의 주소 공간이 아예 다르다.

  그래서 표준은 「함수 포인터를 `void *`로 바꿀 수 있다」고 말하지 않았고, 앞으로도
  말하지 않을 것이다. 오늘날의 평범한 데스크톱만 보면 과한 조심으로 보이지만,
  *임베디드로 내려가면 지금도 살아 있는 이야기*다.
]

== 디스패치 표 — `switch` 대신 배열

예제의 ④가 그것이다. 이름과 함수를 짝지어 배열에 늘어놓으면, 분기 대신
*자료*로 고르게 된다. 항목을 늘릴 때 코드가 아니라 표만 고치면 되고,
56장의 X 매크로와 결합하면 목록 하나에서 표까지 만들어 낼 수 있다.

이 무늬가 커지면 상태 기계, 명령 해석기, 플러그인 구조가 된다. 그리고
더 커지면 — 다음 절의 이야기다.

== 가상 함수 표 — C로 짓는 객체지향

C에는 클래스가 없다. 그러나 *구조체의 첫 멤버에 함수 포인터들의 표를
가리키는 포인터를 두면* 객체지향의 핵심인 다형성을 얻을 수 있다.

#demo("examples/ch58/vtable.c")

설계의 뼈대는 넷이다.

+ *표는 타입마다 하나*다(`static const`). 인스턴스마다 함수 포인터를
  복사해 두면 객체가 커지고 캐시가 나빠진다 — 그래서 표는 분리해서 한 벌만
  두고, 객체는 그 표를 가리키는 포인터 하나만 갖는다.
+ *기반 구조체를 첫 멤버로 둔다.* 표준이 "구조체의 첫 멤버는 구조체 자신과
  같은 주소에서 시작한다"고 보장하므로, `struct circle *`와
  `struct shape *`를 안전하게 오갈 수 있다(45장).
+ *호출은 표를 통해* 한다 — `s->vt->area(s)`. 이 한 줄이 C++의 가상 함수
  호출이 하는 일과 정확히 같다.
+ *자기 자신을 첫 인자로* 넘긴다. C++이 감춰 주는 `this`를 손으로 적는
  것이다.

출력의 마지막 두 줄이 비용을 보여 준다. 객체마다 늘어나는 것은 포인터
하나(8바이트)뿐이고, 표는 타입당 한 번만 존재한다.

#realcase[
  GTK의 GObject — 실제로 쓰이는 손수 지은 객체 체계
][
  이 방식이 장난감이 아니라는 증거가 GTK다. 리눅스 데스크톱의 주요 툴킷인
  GTK는 순수 C로 쓰였고, 그 밑에 *GObject*라는 객체 체계를 두고 있다.
  구조는 방금 본 것과 같은 뼈대 위에 서 있다.

  - 인스턴스 구조체의 첫 멤버가 *클래스 구조체*(우리의 vtable)를 가리킨다.
  - 클래스 구조체에는 함수 포인터들이 늘어서 있고, 파생 클래스는 그중
    일부를 자기 함수로 덮어써서 "재정의"한다.
  - 상속은 기반 구조체를 첫 멤버로 두는 것으로 표현하고, 타입 변환은
    검사를 곁들인 매크로(`GTK_WIDGET(x)` 같은)로 감싼다.
  - 여기에 참조 계수, 시그널(관찰자 패턴), 속성 체계가 얹힌다.

  같은 설계를 다른 곳에서도 볼 수 있다. 리눅스 커널의 `struct file_operations`
  —파일 시스템마다 다른 `read`·`write` 함수를 담은 표— 가 정확히 vtable이고,
  Windows의 COM은 아예 이 규약을 ABI 수준에서 못박은 것이다.
]

#qa[
  그러면 C++의 가상 함수와 무엇이 다른가?
][
  개념은 같고 *자동화의 정도*가 다르다. C++은 컴파일러가 표를 만들고,
  포인터를 심고, 생성자에서 연결하고, 타입 변환을 검사해 준다. C에서는
  그 전부가 손일이고, 그래서 실수할 자리도 많다 — 표를 연결하지 않은 객체,
  첫 멤버 규칙을 어긴 구조체, 파생 타입을 잘못 캐스트하는 코드.

  대신 얻는 것도 분명하다. *무엇이 어디에 있는지 전부 보인다.* 표가
  몇 개인지, 호출 한 번에 포인터를 몇 번 따라가는지, 객체가 몇 바이트인지
  세어 볼 수 있다. 이 책이 내내 말해 온 "비용이 보이는 언어"의 성격이
  객체지향에서도 그대로 나타나는 자리다.

  덧붙여, C++의 가상 함수 표 배치는 *표준이 정하지 않는다*(ABI가 정한다).
  그래서 C와 C++을 섞을 때 클래스 객체를 경계 너머로 넘기지 않고
  `extern "C"` 함수와 단순 구조체만 주고받는 것이다(94장).
]

#recap[
  함수 포인터 요약.

  #dtable(
    columns: 2,
    [*규칙*], [*내용*],
    [무너짐], [함수 이름은 값으로 쓰이면 포인터가 된다],
    [`f`·`&f`·`*f`], [모두 같은 포인터. `&&f`는 문법 오류],
    [호출 표기], [`p(x)`와 `(*p)(x)`는 동일],
    [타입], [반환형+매개변수. 다른 타입으로 캐스트해 호출하면 계약 밖],
    [`void *`], [함수 포인터와 서로 변환된다는 보장이 없다(하버드 구조)],
    [`dlsym`], [POSIX가 따로 보장. `*(void **)&fn` 관용구],
    [디스패치 표], [분기 대신 자료로 고르기],
    [vtable], [표는 타입당 하나, 객체는 포인터 하나. 첫 멤버 규칙이 근거],
    [실물], [GObject(GTK), 커널의 `file_operations`, COM],
  )
]

함수를 값으로 다루는 법까지 갖췄다. 그런데 이 장에서 몇 번, 선언 자체가
읽기 어려워 `typedef`로 도망친 자리가 있었다 — `int (*(*s)(void))(int);`
같은 것들이다. 다음 장이 그 빚을 갚는다: C에서 가장 악명 높은 자리인
*선언 읽기*를, 두 가지 독법과 `typedef`로 정면 돌파한다.
