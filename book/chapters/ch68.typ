#import "../lib.typ": *

= 로케일 ② — 숫자·통화·시간·정렬

#prereq(
  ([67장 로케일 ①], [범주와 `setlocale`]),
  ([62장 스트림의 실제], [`printf` 서식]),
  ([73장 시간], [`strftime`]),
)

#deepqa[
  67장에서 로케일이 관습을 담은 데이터라고 했다. 그러면 그 데이터는 정확히
  무엇으로 이루어져 있고, 프로그램은 그것을 어떻게 꺼내 쓰는가?
][
  꺼내는 창구는 놀랄 만큼 좁다 — 함수 하나(`localeconv`)와 구조체
  하나(`struct lconv`)다. 숫자와 통화에 관한 모든 관습이 그 구조체의 스물네
  멤버에 들어 있다.

  나머지는 *창구가 없다.* 날짜 표기는 `strftime`이 알아서 쓰고, 정렬 순서는
  `strcoll`이 알아서 견준다 — 프로그램이 그 데이터를 직접 볼 방법이 표준에는
  없다. 이 비대칭이 이 장의 지도다: *꺼내 쓰는 것*(숫자·통화)과 *맡기는
  것*(시간·정렬).
]

#organizer[
#idx("localeconv")  로케일이 실제로 바꾸는 것들을 하나씩 뜯어본다. `struct lconv`의 스물네
  멤버 전부, 자리 묶음 문자열의 인코딩, 통화 표기가 조립되는 규칙,
  `LC_NUMERIC`이 데이터를 망가뜨리는 무늬, `strftime`의 로케일 의존 서식과
  불변 서식, `strcoll`과 정렬 키, 그리고 스레드와 로케일.
]

#chapter-questions()

== 창구 하나 — `localeconv`

```c
struct lconv *localeconv(void);
```

돌려주는 것은 *지금 로케일의 숫자·통화 관습*이 채워진 구조체의 주소다. 규칙이
둘 있다. 프로그램이 그 내용을 *고쳐서는 안 되고*, 다음 `localeconv` 호출이나
`setlocale(LC_ALL/LC_MONETARY/LC_NUMERIC, …)` 호출이 그 내용을 *덮어쓸 수
있다*. 즉 오래 들고 있을 값이 아니라 그 자리에서 읽고 마는 값이다.

#demo("examples/ch68/lconv.c")

=== 스물네 멤버의 지도

표준은 구조체가 *적어도* 다음 멤버를 갖되 순서는 정하지 않는다고 말한다. 그래서
지정 초기화나 순서 가정 없이 이름으로만 접근해야 한다.

#dtable(
  columns: 3,
  [*멤버*], [*무엇*], [*"C" 로케일*],
  [`decimal_point`], [비통화 소수점], [`"."`],
  [`thousands_sep`], [비통화 자리 구분], [`""`],
  [`grouping`], [비통화 자리 묶음 규칙], [`""`],
  [`mon_decimal_point`], [통화 소수점], [`""`],
  [`mon_thousands_sep`], [통화 자리 구분], [`""`],
  [`mon_grouping`], [통화 자리 묶음], [`""`],
  [`positive_sign`], [양수 부호 문자열], [`""`],
  [`negative_sign`], [음수 부호 문자열], [`""`],
  [`currency_symbol`], [지역 통화 기호], [`""`],
  [`frac_digits`], [소수 자릿수], [`CHAR_MAX`],
  [`p_cs_precedes`·`n_cs_precedes`], [기호가 값 앞인가(1/0)], [`CHAR_MAX`],
  [`p_sep_by_space`·`n_sep_by_space`], [기호와 값 사이 칸(0/1/2)], [`CHAR_MAX`],
  [`p_sign_posn`·`n_sign_posn`], [부호의 자리(0\~4)], [`CHAR_MAX`],
  [`int_curr_symbol`], [국제 통화 기호 + 구분 문자], [`""`],
  [`int_frac_digits` 및 `int_*` 여섯], [국제 표기용 같은 항목들], [`CHAR_MAX`],
)

두 가지 관례를 읽어야 한다. 문자열 멤버가 `""`이면 *이 로케일이 그 값을 정하지
않았다*는 뜻이고(`decimal_point`만은 예외로 언제나 값이 있다), `char` 멤버가
`CHAR_MAX`이면 역시 *정해지지 않았다*는 뜻이다. 시연이 `"C"` 로케일에서 인쇄한
`CHAR_MAX`들이 그것이다 — C 로케일은 통화에 대해 아무것도 정하지 않는다.

#idx("ISO 4217")`int_curr_symbol`에는 표준이 다른 국제 규격을 직접 끌어온다. *첫 세 글자는
ISO 4217이 정한 국제 통화 기호*이고, 네 번째 글자는 그 기호와 금액을 가르는
문자다. 시연에서 `"KRW "`·`"EUR "`처럼 뒤에 칸이 붙어 나온 이유가 이것이다.

=== 자리 묶음 문자열의 인코딩

`grouping`은 사람이 읽는 문자열이 아니라 *숫자들의 배열*이다. 표준이 그 해석을
이렇게 정한다.

#dtable(
  columns: 2,
  [*원소의 값*], [*뜻*],
  [`CHAR_MAX`], [여기서부터는 더 묶지 않는다],
  [`0`], [직전 원소를 남은 자릿수 전체에 되풀이한다],
  [그 밖의 값], [이 자리의 그룹이 그만큼의 자릿수. 다음 원소가 그 앞 그룹을 정한다],
)

그래서 `"\3"`은 "세 자리 묶음을 끝까지 되풀이"가 아니라 *"첫 그룹이 3, 그다음은
원소가 없으므로 더 묶지 않음"*으로 읽힐 것 같지만, 실제 로케일들은 `"\3"` 하나로
`1,234,567`을 만든다. glibc가 마지막 원소를 반복으로 취급하기 때문이다.
분명하게 적으려면 `"\3\0"`처럼 0을 덧붙인다.

시연이 실측으로 보인 것은 en_IN의 `3 2`다. 오른쪽부터 세 자리, 그다음부터는 두
자리씩 — `12345678`이 `1,23,45,678`이 된다. 인도의 라크·크로르 셈법이고,
*자리 묶음이 3자리 고정이라는 가정이 틀렸다*는 살아 있는 증거다.

=== 통화 표기는 세 값으로 조립된다

통화 하나를 찍는 데 왜 여섯 개나 되는 멤버가 필요한가. 실제 표기가 그만큼
갈리기 때문이다. `p_cs_precedes`(기호가 앞인가), `p_sep_by_space`(칸을 넣는가),
`p_sign_posn`(부호를 어디에)의 조합이 표기를 결정한다.

#dtable(
  columns: 2,
  [`p_sign_posn`], [*뜻*],
  [0], [금액과 통화 기호를 괄호로 감싼다],
  [1], [부호 문자열이 금액과 기호 앞에 온다],
  [2], [부호 문자열이 금액과 기호 뒤에 온다],
  [3], [부호 문자열이 통화 기호 바로 앞에 붙는다],
  [4], [부호 문자열이 통화 기호 바로 뒤에 붙는다],
)

표준이 이 조합의 결과를 직접 표로 실어 두었다. `$`를 기호로, `+`를 부호로 삼아
`1.25`를 찍으면 이렇게 갈린다(일부).

#dtable(
  columns: 4,
  [`p_cs_precedes`], [`p_sign_posn`], [`p_sep_by_space`=0], [`p_sep_by_space`=1],
  [0], [0], [`(1.25$)`], [`(1.25 $)`],
  [0], [1], [`+1.25$`], [`+1.25 $`],
  [0], [3], [`1.25+$`], [`1.25 +$`],
  [1], [0], [`($1.25)`], [`($ 1.25)`],
  [1], [1], [`+$1.25`], [`+$ 1.25`],
  [1], [4], [`$+1.25`], [`$+ 1.25`],
)

*회계 표기의 괄호*(`p_sign_posn`이 0)가 표준에 들어 있다는 것도 눈여겨볼
만하다. 음수를 `-1,234`가 아니라 `(1,234)`로 적는 그 관습이다.

#qa[
  그러면 통화를 찍어 주는 표준 함수는 무엇인가?
][
  *표준 C에는 없다.* `localeconv`가 재료를 줄 뿐, 조립은 프로그램의 몫이다.
  위의 표대로 여섯 멤버를 보고 문자열을 만들어야 한다.

  POSIX에는 `strfmon`이 있어 그 일을 대신해 준다(`strfmon(buf, n, "%n", 1234.5)`).
  Windows에는 `GetCurrencyFormat`이 있다. 어느 쪽도 표준 C가 아니므로,
  이식성이 필요하면 직접 조립하거나 라이브러리를 쓴다.

  덧붙이면 *금액을 `double`로 다루지 않는 것*이 더 중요한 규율이다(49장).
  1원 단위 정수로 다루고 표시할 때만 소수점을 넣는 쪽이 안전하다.
]

== `LC_NUMERIC` — 조용히 데이터를 망가뜨리는 자리

이 장에서 가장 실무적인 경고다. `LC_NUMERIC`은 `localeconv`의 값만 바꾸는 것이
아니라 *`printf`·`scanf`·`strtod`가 쓰는 소수점 문자 자체*를 바꾼다.

시연의 마지막 부분이 그것이다. 같은 `printf("%.2f", 1234.5)`가 `1234.50`이 되기도
하고 `1234,50`이 되기도 한다. 그리고 `strtod("3.14", …)`는 소수점이 쉼표인
로케일에서 `3`까지만 읽고 멈춘다.

#realcase[
  소수점 하나가 서버를 멈춘 무늬
][
  같은 프로그램이 개발자의 기계(영어 로케일)에서는 `3.14`를 쓰고, 사용자의
  기계(독일어 로케일)에서는 `3,14`를 쓰는 일이 실제로 반복해서 일어났다.

  - *설정 파일을 저장한 뒤 다시 읽지 못한다* — 쓸 때는 `3,14`로 쓰고 읽을 때는
    `3.14`를 기대한다.
  - *CSV 가 열을 잃는다* — 값 안의 쉼표가 열 구분자와 겹친다.
  - *두 서버가 주고받는 JSON이 어긋난다* — JSON 표준은 소수점을 `.`으로
    못박았는데, `printf`가 `,`를 찍는다.
  - *로그의 숫자를 집계하지 못한다*.

  공통점이 있다. 전부 *기계가 읽을 숫자*를 사람용 경로로 내보낸 것이다. 처방은
  하나다 — 67장의 관용구로 `LC_NUMERIC`을 `"C"`에 묶어 두고, 사람에게 보일
  때만 따로 서식을 만든다.
]

#misconception[
  "우리 서비스는 한국 안에서만 쓰니 로케일 문제는 없다"
][
  두 자리에서 걸린다. 첫째, *로케일은 사용자의 기계가 정한다.* 같은 프로그램이
  다른 사람의 컴퓨터에서는 다른 로케일로 돈다 — 데스크톱 프로그램이라면 그
  기계의 설정이, 서버라면 컨테이너 이미지의 환경 변수가 정한다.

  둘째, *한국어 로케일 자체도 `"C"`가 아니다.* `ko_KR.UTF-8`은 소수점이 `.`이라
  운이 좋았을 뿐, 자리 구분·날짜·정렬은 모두 다르다. `%c`로 찍은 날짜를 나중에
  파싱하려 들면 그때 걸린다.
]

== `LC_TIME` — 날짜와 시각의 표기

시간 계산 자체는 로케일과 무관하다(73장). 로케일이 바꾸는 것은 *표기*이고,
창구는 `strftime`의 서식 문자다.

#demo("examples/ch68/time_locale.c")

시연이 같은 한 시각을 여섯 로케일로 찍는다. 서식 문자를 두 부류로 갈라 두는
것이 요령이다.

#dtable(
  columns: 3,
  [*부류*], [*서식*], [*성질*],
  [로케일이 정한다], [`%c` `%x` `%X` `%A` `%a` `%B` `%b` `%p` `%r`], [나라마다 다르다 — *사람에게 보여 줄 때만*],
  [로케일과 무관], [`%Y` `%m` `%d` `%H` `%M` `%S` `%j` `%F` `%T`], [어디서나 같다 — *기록·전송·파싱에*],
)

`%F %T`가 ISO 8601 표기(`2026-08-06 15:04:05`)를 그대로 만든다. 로그·파일
이름·API 응답에는 이 서식만 쓰는 것이 규율이다. 시연에서 이 두 줄만 여섯
로케일에서 모두 같은 결과를 낸 것을 볼 수 있다.

#platform[
  시간대는 로케일이 아니다
][
  흔한 혼동이다. `%Z`(시간대 이름)와 `%z`(오프셋)가 보이는 값은 `LC_TIME`이
  아니라 *`TZ` 환경 변수*와 `tzset`이 정한다. 「로케일을 한국으로 바꿨는데 시각이
  안 맞는다」는 대개 `TZ`를 안 바꾼 것이다.

  로케일과 시간대는 축이 다르다 — *로케일은 어떻게 적을지, 시간대는 언제인지*를
  정한다. 독일어 로케일로 서울 시각을 찍는 조합도 완전히 정상이다.
]

== `LC_COLLATE` — `strcmp`는 사전순이 아니다

`strcmp`는 *바이트 값*을 견준다. 그래서 `"Zebra"`가 `"apfel"`보다 앞서고
(`Z`=0x5A \< `a`=0x61), 한글은 유니코드 코드포인트 순서로 늘어선다. 사람이
기대하는 순서가 아니다.

#demo("examples/ch68/collate.c")

시연의 결과가 그 차이를 그대로 보인다. `"C"` 로케일에서는 `strcoll`과 `strcmp`가
같은 답을 내지만, 다른 로케일에서는 갈린다 — 독일어에서 `Äpfel`이 `apfel`
바로 뒤에 오고, 대소문자가 뒤섞여 정렬된다.

=== `strxfrm` — 왜 이런 함수가 있는가

`strxfrm`은 문자열을 *정렬 키*로 바꾼다. 키끼리는 평범한 `strcmp`로 견주어도
`strcoll`과 같은 순서가 나온다. 시연의 마지막 부분이 그것을 확인한다.

왜 필요한가. `strcoll` 한 번은 싸지 않다 — 로케일 규칙을 매번 적용해야 하기
때문이다. `n`개를 정렬하면 비교가 대략 $n log n$번이므로, *변환을 `n`번 하고
비교는 싸게* 하는 편이 낫다.

```c
/* 정렬 전에 키를 만들어 둔다 */
size_t need = strxfrm(NULL, s, 0);   /* 필요한 크기를 먼저 묻고 */
char *key = malloc(need + 1);        /* 그만큼 잡아 */
strxfrm(key, s, need + 1);           /* 채운다 */
```

시연이 인쇄한 키의 크기를 보면 이 함수의 성격이 드러난다. `"C"` 로케일에서는
키가 원본과 같은 6바이트지만, 독일어 로케일에서는 43바이트다 — 로케일 정렬은
*여러 단계의 가중치*(기본 글자 → 발음 기호 → 대소문자)를 쌓아 두기 때문이다.

#qa[
  그러면 한국어 정렬은 `strcoll`로 충분한가?
][
  간단한 목록이라면 충분하다. 한글 음절은 유니코드에서 이미 가나다순으로
  배열되어 있어서, `ko_KR.UTF-8`의 `strcoll`이 기대에 맞는 순서를 준다.

  그러나 실무의 정렬에는 규칙이 더 붙는다 — 숫자를 자연수 순으로("파일2"가
  "파일10"보다 앞), 초성만 뽑아 묶기, 한자를 음으로 읽어 섞기, 대소문자·공백
  무시. 이런 것은 `strcoll`의 범위 밖이고, 유니코드의 정렬 알고리즘(UTS \#10)과
  그 구현인 ICU가 맡는 자리다.

  경계는 이렇게 그으면 된다 — *로케일 정렬로 될 일이면 `strcoll`, 그 이상이면
  전용 라이브러리.* 다만 `strcmp`로 사람에게 보일 목록을 정렬하는 것만은
  피한다.
]

== 스레드와 로케일

67장에서 로케일이 프로세스 전역이라고 했다. 여러 갈래로 도는 프로그램에서는
그것이 문제가 된다 — 한 스레드가 사용자에게 보일 날짜를 찍으려고 로케일을
바꾸면, 다른 스레드의 `printf("%f")`가 함께 흔들린다.

표준 C에는 처방이 없다. 있는 것은 확장이다.

#dtable(
  columns: 3,
  [*체계*], [*수단*], [*모양*],
  [POSIX], [스레드 지역 로케일], [`newlocale`/`uselocale`/`freelocale`],
  [POSIX], [로케일을 인자로 받는 함수], [`strtod_l`, `strcoll_l`, `strftime_l` 등],
  [Windows], [스레드별 로케일 모드], [`_configthreadlocale`, `_locale_t`와 `_l` 함수들],
)

`_l` 계열이 근본 처방이다 — 전역 상태를 건드리지 않고 *이 호출에만* 로케일을
지정한다. 이식성이 필요하면 이 두 갈래를 감싸는 얇은 층을 직접 두게 된다.

#antipattern[
  라이브러리 안에서 `setlocale` 부르기
][
  ```c
  /* 라이브러리의 함수 */
  double parse_number(const char *s) {
      setlocale(LC_NUMERIC, "C");     /* 남의 프로그램 상태를 바꾼다 */
      return strtod(s, NULL);
  }
  ```
  이 세 줄이 응용 프로그램의 날짜 표기와 통화 표기를 조용히 망가뜨린다. 게다가
  스레드 사이에서는 데이터 경쟁이다.

  라이브러리의 규율은 하나다 — *로케일을 읽기만 하고 바꾸지 않는다.* 로케일에
  흔들리지 않는 파싱이 필요하면 `strtod_l`을 쓰거나, 직접 자리 표기법을 다룬다.
]

== 실무의 처방

#dtable(
  columns: 2,
  [*하고 싶은 일*], [*방법*],
  [사용자에게 날짜·정렬·통화를 그 나라 방식으로], [`setlocale(LC_ALL, "")`],
  [파일·프로토콜에 숫자 적기], [`LC_NUMERIC`을 `"C"`로 묶는다],
  [로그의 시각], [`strftime`의 `%F %T`(ISO 8601)],
  [사람에게 보일 목록 정렬], [`strcoll`(또는 키를 만들 때 `strxfrm`)],
  [기계가 견줄 문자열], [`strcmp` — 로케일에 흔들리지 않아야 한다],
  [스레드마다 다른 로케일], [`uselocale`/`_l` 계열(표준 밖)],
  [라이브러리를 만들 때], [`setlocale`을 부르지 않는다],
)

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [창구], [`localeconv` 하나. 고치지 말고, 오래 들고 있지도 말 것],
    [`struct lconv`], [스물네 멤버. `""`와 `CHAR_MAX`는 "정해지지 않음"],
    [`int_curr_symbol`], [앞 세 글자가 ISO 4217 코드],
    [`grouping`], [숫자들의 배열. `CHAR_MAX`=끝, `0`=반복. 3자리 고정이 아니다],
    [통화 조립], [`cs_precedes`·`sep_by_space`·`sign_posn` 조합. 표준 함수는 없다],
    [`LC_NUMERIC`], [★ `printf`·`strtod`의 소수점을 바꾼다 — 데이터 파손의 주범],
    [`LC_TIME`], [`%c %x %X %A %B %p`는 로케일, `%F %T %Y-%m-%d`는 불변],
    [시간대], [로케일이 아니라 `TZ`],
    [`LC_COLLATE`], [`strcmp`≠사전순. 반복 비교는 `strxfrm` 키로],
    [스레드], [표준에는 없다. `uselocale`·`_l` 계열],
  )
]

로케일이 무엇을 바꾸는지 끝까지 보았다. 남은 축이 하나 있다 — `LC_CTYPE`이
지배하는 *여러 바이트 문자와 와이드 문자*다. 다음 장에서 `wchar_t`의 정체와
바이트열이 글자로 풀리는 과정을 한 단계씩 본다.
