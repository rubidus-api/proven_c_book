#import "../lib.typ": *

= 입력

#organizer[
  19장의 예고를 완결한다 — 저장할 곳(변수)이 생겼으니 입력을 받는다.
  이 책이 처음부터 가르치는 방식은 현대적 관행 그대로다: *한 줄을
  통째로 읽고, 그 줄을 해석한다.* 읽기와 해석의 분리 — 이 두 단계
  구조가 편리함과 안전함의 원천이다. 제5부가 여기서 완성된다.
]

#deepqa[
  8장에서 입력도 출력처럼 "문자가 한 줄씩 흐르는 띠"라 했고, 사람의
  입력은 엔터로 끝나는 줄 단위가 자연스럽다고 했다(행 버퍼링). 그러면
  프로그램이 입력을 받는 자연스러운 단위도 — 줄인가?
][
  바로 그렇다 — 그리고 그것이 이 장의 설계 원리다. 사람은 한 줄을
  적고 엔터를 친다. 그러니 프로그램도 *한 줄을 통째로* 받아 온 다음,
  그 안에서 필요한 값을 찾아 읽는 것이 흐름의 결을 따르는 방식이다.
  글자의 띠에서 줄을 뜨는 일과, 뜬 줄을 해석하는 일 — 이 둘을 나누면
  각각이 단순해진다.
]

== 두 단계 — 읽고, 해석한다

시연부터 본다. 표준 입력에서 정수 하나를 받아 제곱을 알려 주는
프로그램이다. (이 실행에서 표준 입력으로 준 내용을 가운데 상자에
보였다.)

#demo("examples/ch22/read.c", stdin: true)

새 얼굴이 둘 있다 — 한 단계에 하나씩이다.

*1단계, `fgets` — 한 줄을 통째로 읽는다.* `fgets(line, sizeof line,
stdin)`은 표준 입력(`stdin`이 그 이름이다)에서 한 줄을 읽어 `line`
이라는 공간에 담는다. 첫 줄의 `char line[100];`은 "글자 100칸짜리
공간을 잡고 line이라 부른다"는 선언인데 — 여러 칸짜리 공간(배열)의
정식 문법은 32장의 것이라, 지금은 "줄을 담는 그릇"이라고만 알아 두면
된다(의도된 외상이다). 둘째 재료 `sizeof line`은 "그릇의 칸 수" —
fgets에게 그릇 크기를 알려서 *넘치게 담는 일이 없게* 하는 안전장치다
(`sizeof` 연산자의 정식 취급은 29장).

*2단계, `sscanf` — 담아 온 줄을 해석한다.* `sscanf(line, "%d", &n)`은
19장 `printf`의 짝이다 — 같은 서식 언어를 *반대 방향*으로 쓴다.
`printf`가 값을 글자로 바꿔 내보냈다면, `sscanf`는 글자(`line` 속
`"7"`)에서 서식(`%d`)에 맞는 값을 찾아 *변수에 담는다*. 이름 앞의
`&`는 "값을 담을 변수의 *자리*를 알려 주는 표시"다 — 3장에서 배운
주소가 문법에 처음 얼굴을 내민 순간인데, 정식 취급은 29장에 있다
(이것이 제5부의 마지막 의도된 외상이다).

#qa[
  왜 두 단계로 나누는가? 입력에서 바로 `%d`를 읽어 주는 함수(`scanf`)도
  있다고 들었는데.
][
  있고, 많은 입문서가 그것부터 가르친다 — 이 책이 다른 길을 택한
  이유는 두 가지다. 첫째, *실패의 뒤처리가 깨끗하다.* 해석에 실패해도
  (숫자가 아닌 것이 들어와도) 줄은 이미 내 그릇에 있으므로, 입력의
  띠가 어중간한 자리에서 멈춰 꼬이는 일이 없다 — 입력 띠에서 직접
  읽다 실패하면 남은 글자들이 띠에 걸린 채 다음 읽기를 오염시키는데,
  이것이 `scanf` 직접 사용의 고전적 골칫거리다. 둘째, *안전의 결이
  같다.* 줄 읽기는 그릇 크기를 알려 주며 읽는 구조라 넘침이 원천
  차단된다 — 그릇 크기를 말하지 않는 옛 방식들이 어떤 사고를 냈는지,
  그리고 이 두 단계 관행이 어떻게 그 사고를 봉쇄하는지가 33장의
  이야기다. 처음부터 안전한 결로 손에 익히는 것 — 그것이 이 책의
  선택이다.
]

== 해석은 실패할 수 있다

출력과 달리, 입력에는 본질적인 새 문제가 하나 있다 — *상대가 무엇을
보낼지 모른다.* `7`을 기대했는데 `일곱`이 올 수 있다. 그래서 `sscanf`의
반환값은 *해석에 성공한 값의 개수*다 — 위 예제라면 성공 시 1, 실패
시 0이다.

지금의 우리는 이 반환값을 확인하고도 "성공이면 이 길, 실패면 저 길"로
갈라설 문법 — 분기 — 이 없다. 그래서 위 예제는 반환값을 버렸고,
`n`을 0으로 초기화해 두는 것(20장의 습관)으로 실패 시의 바닥을 깔아
두었다. 갈림길은 다음 부(25장)에서 생기고, "실패를 값으로 알리고
반드시 확인한다"는 규율은 40장에서 정식으로 세운다 — 입력 검사는 그
규율의 첫 실전이 될 것이다.

#misconception[
  "입력은 키보드에서 온다"
][
  대개 그렇지만, 본질은 아니다 — 입력은 *표준 입력 스트림*에서 온다
  (8장). 사실 방금의 시연이 그 증거다: 이 책의 예제 검증 기계에는
  키보드를 두드리는 사람이 없다 — 위 실행의 입력 `7`은 파일에 담겨
  표준 입력의 띠로 흘러 들어간 것이다(리다이렉션). 19장의 출력이
  화면행이 아니듯 입력도 키보드행이 아니라서, 같은 프로그램이 사람과의
  대화에도, 파일 처리에도, 프로그램들의 연결에도 고치지 않고 쓰인다.
  스트림 설계의 힘이 입력 쪽에서도 반복되는 것이다.
]

== 제5부를 닫으며

이름을 만드는 법을 다 배웠다 — 값의 이름(20장), 일의 이름(21장),
그리고 바깥세상에서 값을 받아 이름에 담는 법(22장)까지. 13장의 외상
장부는 청산되었고, 새로 진 외상은 정확히 둘 — 줄 그릇(`char line[100]`,
32장에서)과 자리 표시(`&`, 29장에서) — 뿐이며, 둘 다 청산 기일이
적혀 있다.

무엇보다, 이제 프로그램이 *대화*할 수 있게 됐다. 받아서, 계산하고,
답한다 — 다음 부에서는 그 계산 자체를 벼린다: 정수의 세계를 정면으로
다루고(23·24장), 비교하고 판단하고(25·26장), 반복하고(27장), 함수의
의미를 완성한다(28장). 값과 흐름의 부다.
