#import "../lib.typ": *

= 실무 — 유니코드와 멀티바이트를 다루는 법

#prereq(
  ([69장 와이드 문자 ②], [UTF-16과 플랫폼의 갈라짐]),
  ([68장 와이드 문자 ①], [변환 함수와 `mbstate_t`]),
  ([9장 문자와 텍스트], [코드포인트와 인코딩]),
)

#deepqa[
  68·69장을 지나며 「UTF-8 바이트열을 그대로 다루라」는 권고가 여러 번 나왔다.
  그런데 바이트열을 그대로 다루면 「세 번째 글자를 지워라」 같은 일은 어떻게
  하는가?
][
  *대부분의 프로그램은 그런 일을 하지 않기 때문*이다. 실제로 하는 일을 세어
  보면 이렇다 — 읽고, 저장하고, 비교하고, 이어 붙이고, 찾고, 그대로 내보낸다.
  이 여섯 가지는 UTF-8 바이트열에서 *아무 해독 없이* 된다.

  글자 단위로 만져야 하는 일 — 커서 옮기기, 줄 바꿈 자리 찾기, 글자 수 세기 —
  은 편집기나 렌더러의 몫이고, 그 층에서는 코드포인트도 모자라 *그래핌
  클러스터*가 필요하다. 이 장은 그 경계를 그어 주는 장이다: 어디까지 바이트로
  가고, 어디서부터 해독하며, 그때 무엇을 조심하는가.
]

#organizer[
#idx("UTF-8")  실무의 처방을 모은다. UTF-8 우선 설계가 왜 이기는지, 「글자 수」의 세
  층위와 그래핌 클러스터, 정규화, 언어에 달린 대소문자, 직접 만든 UTF-8
  검증기, 그리고 아직 살아 있는 레거시 2바이트 인코딩의 함정까지.
]

#chapter-questions()

== 원칙 — UTF-8을 안에, 변환은 경계에만

먼저 결론이다.

#dtable(
  columns: 2,
  [*자리*], [*무엇을 쓰는가*],
  [프로그램 내부], [UTF-8 바이트열(`char *`, 길이는 바이트 수)],
  [파일·네트워크·데이터베이스], [UTF-8],
  [Windows API 를 부를 때], [경계에서만 UTF-16 으로 변환],
  [글자 단위 편집·렌더링], [필요한 자리에서만 코드포인트·그래핌으로 해독],
)

이 설계가 이기는 이유는 UTF-8의 성질에서 그대로 나온다.

#dtable(
  columns: 2,
  [*UTF-8의 성질*], [*그래서 얻는 것*],
  [아스키와 완전히 호환된다], [기존 코드·프로토콜·파일 형식이 그대로 돈다],
  [후행 바이트가 언제나 0x80 이상], [`'/'`·`'\\'`·`','` 같은 구분자와 절대 겹치지 않는다],
  [자기동기화된다], [아무 위치에서나 글자 경계를 찾을 수 있다],
  [바이트 순서가 코드포인트 순서와 같다], [`strcmp`가 코드포인트 순 정렬을 준다],
  [엔디안이 없다], [바이트 순서 표시(BOM)가 필요 없다],
)

#demo("examples/ch70/utf8_scan.c")

시연의 마지막 부분이 자기동기화를 보인다. 아무 바이트나 짚어도
`(b & 0xC0) == 0x80`인지만 보면 그것이 글자 중간인지 시작인지 알 수 있다 —
이 성질 덕분에
버퍼 가운데를 잘라도 복구할 수 있고, 손상된 데이터에서도 다음 글자로 넘어갈 수
있다.

== 검증 — 문법이 맞아도 거절해야 하는 것들

UTF-8을 읽는 코드는 *반드시 검증해야 한다.* 규칙은 RFC 3629(STD 63)가 정하고,
문법만으로는 걸러지지 않는 것이 셋 있다.

#dtable(
  columns: 3,
  [*거절할 것*], [*보기*], [*왜*],
  [과잉 인코딩(overlong)], [`C0 80`], [U+0000을 두 바이트로 적었다 — 검사를 우회하는 고전적 공격],
  [서러게이트 인코딩], [`ED A0 80`], [U+D800은 문자가 아니다(69장)],
  [범위 초과], [`F5 80 80 80`], [U+10FFFF를 넘는다],
)

시연이 이 셋을 각각 거절하는 것을 보인다. 특히 첫 줄이 중요하다 —
*과잉 인코딩은 보안 문제다.* `..`나 `/`를 여러 바이트로 적어 경로 검사를
통과시킨 뒤, 뒤쪽 층에서 다시 해석되게 만드는 공격이 실제로 있었다. 그래서
"가장 짧은 표현만 유효하다"가 규격의 요구 사항이 되었다.

#misconception[
  "UTF-8은 바이트열이니 검증 없이 넘겨도 된다"
][
  중간에 그냥 흘려보내는 것(pass-through)은 대개 안전하다. 문제는 *해석하는
  순간*이다.

  검증하지 않은 바이트열은 층마다 다르게 해석될 수 있고, 그 어긋남이 보안
  구멍이 된다 — 앞 층은 "이상한 바이트"로 보고 통과시키고, 뒤 층은 그것을
  `/`로 해석하는 식이다. 웹 서버·파일 시스템·데이터베이스가 걸쳐 있는 곳에서
  특히 그렇다.

  규율은 하나다 — *입력 경계에서 한 번 검증하고, 그 뒤로는 유효하다고 믿는다.*
  검증한 자리를 코드에 분명히 표시해 두는 것까지가 규율이다. 제12부 proven 의
  `u8` 계열이 이 규율을 타입으로 강제하는 예다.
]

== 「글자 수」의 세 층위, 그리고 넷째

69장에서 길이가 셋이라고 했다. 사람이 보는 글자까지 넣으면 넷이다.

#demo("examples/ch70/graphemes.c")

#dtable(
  columns: 3,
  [*층*], [*단위*], [*쓰는 자리*],
  [바이트], [`char`], [저장·전송·버퍼 크기],
  [코드 단위], [UTF-16 단위 등], [자바·자바스크립트의 `length`],
  [코드포인트], [유니코드 하나], [정규화·분류·변환],
  [그래핌 클러스터], [사람이 보는 글자], [커서 이동·글자 수·자르기],
)

시연이 그 차이를 실측으로 보인다. 이모지 가족 하나가 *18바이트, 코드포인트
5개, 보이는 글자 1개*다. ZWJ(U+200D)라는 보이지 않는 문자가 사람 셋을 하나로
묶은 것이다. 태극기는 지역 표시자 두 개가 모여 하나가 된다.

한글에서도 같은 일이 일어난다. "가"는 완성형 U+AC00 하나로 적을 수도 있고,
조합형 U+1100(ㄱ) + U+1161(ㅏ) 둘로 적을 수도 있다 — 화면에 보이는 것은 같은
글자다.

#qa[
  그래핌 클러스터는 어떻게 세는가?
][
  규칙은 유니코드 부속서 UAX \#29가 정한다. 「어디서 글자가 끊기는가」를
  문자 속성의 조합으로 정의한 표이고, 제대로 구현하려면 유니코드 데이터베이스가
  필요하다.

  시연의 셈은 자주 만나는 네 가지만 추린 *간이 판*이다 — 결합 표시, 한글 조합
  자모, ZWJ 연결, 지역 표시자 쌍. 그것만으로도 이모지와 한글의 대부분을 맞게
  세지만, 완전하지는 않다.

  실무의 결론은 이렇다 — *직접 구현하지 않는다.* 그래핌 단위가 정말 필요한
  자리(편집기의 커서, 화면 폭 계산)라면 ICU나 그에 준하는 라이브러리를 쓴다.
  대신 *언제 그것이 필요한지*를 아는 것이 이 절의 목적이다: 저장·비교·전송에는
  필요 없고, 사람이 보는 것을 다룰 때만 필요하다.
]

== 정규화 — 같은 글자, 다른 바이트

앞에서 본 "가"의 두 가지 표기는 실무에서 곧바로 문제가 된다. *바이트가 다르면
`strcmp`가 다르다고 답하기 때문*이다. 사용자에게는 같은 글자인데 검색이 안 되고,
파일 이름이 겹치고, 로그인 아이디가 다르게 취급된다.

유니코드는 이 문제를 *정규화*(normalization, UAX \#15)로 다룬다. 형태가 넷이다.

#dtable(
  columns: 3,
  [*형태*], [*무엇*], [*보기*],
  [NFC], [최대한 합친다(기본 권장)], [`ㄱ`+`ㅏ` → `가`],
  [NFD], [최대한 쪼갠다], [`가` → `ㄱ`+`ㅏ`],
  [NFKC·NFKD], [모양이 다른 호환 문자까지 통일], [`㈜` → `(주)`, 전각 `Ａ` → `A`],
)

#realcase[
  macOS 의 파일 이름과 NFD
][
  macOS 의 HFS+ 파일 시스템은 파일 이름을 *NFD 에 가까운 형태로 정규화해서*
  저장했다. 그래서 한국어 파일 이름을 macOS 에서 만들어 리눅스로 옮기면 이름이
  "ㄱㅏㄴㅏㄷㅏ"처럼 자모가 풀린 채로 보이는 일이 흔했다.

  같은 이유로 압축 파일이 깨지고, 웹 서버가 404를 내고, `git status`가 멀쩡한
  파일을 「수정됨」으로 표시했다. Git 은 결국 `core.precomposeunicode` 설정을
  두어 macOS 에서 이름을 NFC 로 되돌리도록 했다.

  교훈은 둘이다. *하나, 문자열을 견주기 전에 정규화한다.* 특히 파일 이름·
  사용자 이름·검색어처럼 사람이 입력한 것을 키로 쓸 때 그렇다. *둘, 어떤 형태로
  정규화할지를 시스템 전체에서 하나로 정한다* — 대개 NFC 다.

  표준 C 에는 정규화 함수가 없다. ICU 나 그에 준하는 라이브러리가 필요하다.
]

== 대소문자와 정렬은 언어에 달렸다

`toupper`가 한 글자를 한 글자로 바꾼다는 가정도 유니코드에서는 무너진다.

#dtable(
  columns: 3,
  [*보기*], [*무슨 일이*], [*그래서*],
  [터키어 `i`], [대문자가 `İ`(점 있는 I)], [로케일 없이는 옳게 바꿀 수 없다],
  [터키어 `I`], [소문자가 `ı`(점 없는 i)], [영어와 정반대],
  [독일어 `ß`], [대문자가 `SS` 두 글자], [1:1 대응이 아니다],
  [그리스어 `Σ`], [낱말 끝에서는 `ς`], [자리에 따라 다르다],
)

#realcase[
  터키어 `i` 문제 — 실제로 무너진 코드들
][
  「대소문자를 무시하고 견주려면 둘 다 대문자로 바꿔서 견준다」는 흔한 관용구가
  터키어 로케일에서 깨진다. `"file"`을 대문자로 바꾸면 `"FİLE"`이 되어
  `"FILE"`과 다르기 때문이다.

  이 때문에 확장자 검사가 실패하고, HTTP 헤더 이름이 매칭되지 않고, 설정 키가
  인식되지 않는 사고가 실제로 여러 소프트웨어에서 났다. 자바에는
  `toUpperCase(Locale.ROOT)`라는 별도 호출이 이 문제 때문에 생겼다.

  처방은 층을 나누는 것이다. *프로토콜·식별자·파일 확장자처럼 기계가 견줄
  것은 로케일과 무관하게 아스키 규칙으로만 접는다.* 사람에게 보여 줄 이름을
  대문자로 만드는 일에만 로케일을 쓴다.

  ```c
  /* 기계용 — 아스키만, 로케일과 무관하게 */
  static char ascii_lower(char c)
  { return (c >= 'A' && c <= 'Z') ? (char)(c - 'A' + 'a') : c; }
  ```
]

== 아직 살아 있는 레거시 — 2바이트 인코딩

세상이 전부 UTF-8이 된 것은 아니다. 옛 파일, 옛 데이터베이스, 옛 장비의
프로토콜에는 지역별 2바이트 인코딩이 그대로 남아 있다.

#dtable(
  columns: 3,
  [*인코딩*], [*지역*], [*배경 규격*],
  [EUC-KR / CP949(UHC)], [한국], [KS X 1001 (옛 KS C 5601)],
  [Shift_JIS / CP932], [일본], [JIS X 0208],
  [Big5], [대만·홍콩], [—(사실상 표준)],
  [GBK / GB 18030], [중국], [GB 18030-2022(중국 국가표준, 필수 준수)],
)

이들의 공통 구조는 「선행 바이트 + 후행 바이트」다. 그리고 *어떤 인코딩은 후행
바이트가 아스키 영역과 겹친다* — 여기서 유명한 사고가 난다.

#demo("examples/ch70/legacy_lead.c")

시연이 그 사고를 그대로 재현한다. Shift_JIS에서 「表」는 `95 5C`, 「ソ」는
`83 5C`인데, 그 뒤 바이트 `0x5C`가 아스키로는 역슬래시(`\`)다. 그래서
`strrchr(path, '\\')`이 *문자의 후행 바이트를 경로 구분자로 잘못 짚는다.*
시연에서 올바른 위치 5 대신 7을 짚고, 그 자리에서 자르면 파일 이름이 깨진다.

일본에서는 이 글자들을 「다메모지」(ダメ文字)라 부른다. 表·ソ·十·ダ 같은 글자가
경로·이스케이프·SQL 처리에서 반복해서 사고를 냈다.

#dtable(
  columns: 3,
  [*인코딩*], [*후행 바이트 범위*], [`0x5C`가 후행에?],
  [EUC-KR], [A1\~FE], [아니다 — 안전],
  [CP949], [41\~5A, 61\~7A, 81\~FE], [아니다 — 안전],
  [Shift_JIS], [40\~7E, 80\~FC], [★ 그렇다],
  [Big5], [40\~7E, A1\~FE], [★ 그렇다],
  [GBK], [40\~FE(7F 제외)], [★ 그렇다],
  [UTF-8], [80\~BF], [아니다 — 구조적으로 불가능],
)

*한국어 인코딩이 이 사고에서 자유로웠던 것은 운이 좋아서다.* EUC-KR의 후행
바이트가 모두 0xA1 이상이라 아스키와 겹치지 않았기 때문이다. 반대로 일본어·
중국어 코드를 다루는 프로그램은 반드시 선행 바이트를 아는 훑기를 써야 한다.

#antipattern[
  레거시 인코딩 문자열을 바이트로 훑기
][
  ```c
  char *ext = strrchr(filename, '.');    /* Shift_JIS 에서 오작동 가능 */
  for (char *p = s; *p; p++)             /* 후행 바이트를 문자로 오인 */
      if (*p == ',') split(p);
  ```
  선행 바이트를 만나면 두 칸을 건너뛰어야 한다. 시연의 `safe_last_sep`이 그
  형태다. 표준 함수로 하려면 `mblen`이나 `mbrtowc`로 한 글자씩 나아간다 — 단,
  그러려면 로케일이 그 인코딩이어야 한다(68장).

  더 나은 처방은 애초에 이 문제를 만들지 않는 것이다 — *입력 경계에서 UTF-8로
  바꾸고, 내부에서는 UTF-8만 다룬다.*
]

== 변환은 무엇으로 하는가

#dtable(
  columns: 3,
  [*수단*], [*어디*], [*성격*],
  [`iconv`], [POSIX], [인코딩 이름으로 지정. 표준 C 밖이지만 유닉스 어디에나 있다],
  [`MultiByteToWideChar` 계열], [Windows], [코드 페이지 번호로 지정],
  [ICU], [이식], [가장 완전하다. 정규화·정렬·그래핌까지],
  [`mbrtowc` 계열], [표준 C], [*로케일의 인코딩만* 다룰 수 있다는 제약(68장)],
  [직접 구현], [—], [UTF-8↔UTF-16/32 정도는 짧다. 시연이 그 예],
)

표준 C만으로 임의의 인코딩을 다룰 수 없다는 점을 다시 확인해 두자.
`mbrtowc`는 「지금 로케일의 인코딩」만 안다. 그래서 「EUC-KR 파일을 읽어 UTF-8로
저장한다」 같은 일은 표준 C의 범위 밖이고, `iconv`나 직접 만든 변환표가 필요하다.

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [설계], [안은 UTF-8 바이트열, 변환은 경계에서만],
    [검증], [입력 경계에서 한 번. 과잉 인코딩·서러게이트·범위 초과를 거절],
    [길이], [바이트·코드 단위·코드포인트·그래핌 — 어느 층인지 먼저 정한다],
    [정규화], [비교 전에 NFC 로. 표준 C 에는 없다],
    [대소문자], [기계용은 아스키 규칙으로. 터키어 `i` 를 기억],
    [레거시], [Shift_JIS·Big5·GBK 는 후행 바이트가 아스키와 겹친다],
    [훑기], [레거시 인코딩은 선행 바이트를 알고 건너뛴다],
    [변환], [`iconv`·ICU·플랫폼 API. 표준 C 는 로케일 인코딩만],
  )
]

65장에서 시작한 문자 이야기를 여섯 장에 걸쳐 마쳤다 — 한 바이트의 판정에서
로케일, `wchar_t`, 그리고 실무의 처방까지. 다음 장은 표준이 최근에 보탠 것들과
「안전한 함수」를 둘러싼 오래된 논쟁이다.
