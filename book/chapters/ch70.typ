#import "../lib.typ": *

= 와이드 문자 ② — 플랫폼의 현실과 와이드 입출력

#prereq(
  ([69장 와이드 문자 ①], [`wchar_t`의 계약과 변환]),
  ([62장 스트림의 실제], [스트림과 버퍼]),
)

#deepqa[
  69장에서 `wchar_t`의 크기와 인코딩을 표준이 정하지 않았다고 했다. 정하지
  않으면 구현이 알아서 고를 텐데, 그게 그렇게 큰 문제인가?
][
  *두 진영으로 갈렸다는 것*이 문제다. 리눅스·macOS는 4바이트를 골랐고
  Windows는 2바이트를 골랐다. 그리고 그 선택이 1990년대의 사실 하나 —
  「유니코드는 16비트면 충분하다」 — 위에 서 있었는데, 그 사실이 1996년에
  깨졌다.

  유니코드가 U+FFFF를 넘어가면서 2바이트 `wchar_t`는 *한 글자를 한 단위로
  담는다*는 원래의 약속을 지킬 수 없게 되었다. 그 뒷수습이 서러게이트 쌍이고,
  오늘날 Windows 프로그래밍의 문자열 함정 대부분이 여기서 나온다.

  이 장은 그 갈라짐의 정확한 모습과, 각 플랫폼·툴킷이 그 위에서 어떤 선택을
  했는지를 본다.
]

#organizer[
#idx("UTF-16")  같은 `wchar_t`가 플랫폼마다 무엇이 되었는지 본다. UTF-16의 한계와 서러게이트
  쌍의 계산, Windows의 `W` 함수군과 정확한 인코딩, glibc의 선택, GTK와 Qt가
  문자열을 담는 방식, 그리고 스트림의 방향이라는 잘 알려지지 않은 규칙까지.
]

#chapter-questions()

== 두 진영으로 갈린 `wchar_t`

#dtable(
  columns: 4,
  [*플랫폼*], [`sizeof(wchar_t)`], [*사실상의 인코딩*], [`__STDC_ISO_10646__`],
  [리눅스(glibc)·macOS], [4], [UTF-32(UCS-4)], [정의된다],
  [Windows(MSVC·MinGW)], [2], [UTF-16], [정의되지 않는다],
  [AIX 등 일부 유닉스], [2 또는 4], [구현마다], [구현마다],
)

69장 시연이 이 기계의 답을 보였다 — 4바이트, `__STDC_ISO_10646__` 정의됨. 즉
`wchar_t` 하나가 코드포인트 하나다. Windows에서는 그 등식이 성립하지 않는다.

#qa[
  Windows는 왜 2바이트를 골랐는가?
][
  시대의 사정이다. Windows NT의 설계가 한창이던 1990년대 초, 유니코드는
  *16비트 고정 폭 문자집합*이었다. 세상의 모든 문자를 65,536개 안에 담는다는
  것이 당시의 설계였고, 그 전제 위에서 「2바이트면 한 글자」는 합리적인
  선택이었다. 자바의 `char`, 자바스크립트의 문자열, Qt의 `QChar`가 모두 같은
  시기에 같은 선택을 했다.

  1996년 유니코드 2.0이 그 전제를 깼다. 한자만으로도 65,536개가 모자랐고,
  범위를 U+10FFFF까지 열면서 *16비트로는 담을 수 없는 문자*가 생겼다. 이미
  16비트로 지어 놓은 시스템들은 타입을 바꿀 수 없었으므로, 대신 인코딩을
  바꿨다 — 두 단위로 한 글자를 적는 UTF-16이다.

  그래서 오늘날 Windows의 `wchar_t`는 *"한 글자"가 아니라 "UTF-16 코드 단위
  하나"*다. 이름과 실체가 어긋난 채로 30년이 지난 셈이다.
]

== UTF-16의 한계와 서러게이트 쌍

유니코드의 코드포인트 공간은 U+0000부터 U+10FFFF까지다. 이 중 앞의
U+0000\~U+FFFF를 *기본 다국어 평면*(BMP)이라 부르고, 16비트에 담기는 것은
여기까지다.

그 위의 문자를 16비트 단위로 적기 위해 유니코드는 BMP 안에 *쓰지 않는 구역*을
따로 비워 두었다.

#dtable(
  columns: 3,
  [*구역*], [*범위*], [*무엇*],
  [고위 서러게이트], [U+D800\~U+DBFF], [쌍의 앞쪽 단위(1024개)],
  [저위 서러게이트], [U+DC00\~U+DFFF], [쌍의 뒤쪽 단위(1024개)],
  [(합쳐서 표현하는 범위)], [U+10000\~U+10FFFF], [1024 × 1024 = 1,048,576자],
)

#demo("examples/ch70/surrogate.c")

시연이 계산을 그대로 보인다. U+1F600을 예로 들면 이렇다.

+ 코드포인트에서 0x10000을 뺀다 → 0x0F600 (20비트에 들어간다)
+ 위쪽 10비트를 0xD800에 더한다 → 0xD83D (고위)
+ 아래쪽 10비트를 0xDC00에 더한다 → 0xDE00 (저위)

*서러게이트 값 자체는 문자가 아니다.* U+D800\~U+DFFF는 코드포인트로서는 어떤
글자에도 배정되지 않으며, UTF-8이나 UTF-32에 단독으로 나타나면 부정한
데이터다(71장의 검증에서 다시 만난다). 시연에서 U+D800을 UTF-16으로 담으려 할
때 "담을 수 없다"가 나온 이유다.

#misconception[
  "문자열의 길이는 문자 수다"
][
  이 문장이 어느 층의 이야기인지에 따라 답이 셋으로 갈린다. 시연의 뒷부분이
  그것을 실측으로 보인다 — 이모지 하나가 *UTF-8 4바이트, 코드포인트 1개,
  UTF-16 2단위*다.

  그래서 언어마다 「길이」의 뜻이 다르다. C의 `strlen`은 바이트를 세고,
  자바·자바스크립트·C\#의 `length`는 UTF-16 단위를 세며, 파이썬 3의 `len`은
  코드포인트를 센다. 「이모지 하나를 넣었더니 길이가 2로 나온다」는 자바스크립트의
  유명한 놀라움이 정확히 이 자리다.

  그리고 셋 중 어느 것도 *사람이 보는 글자 수*는 아니다. 그 넷째 층은 71장에서
  본다.
]

== Windows — `W` 함수군과 그 정확한 인코딩

Windows API는 문자열을 받는 거의 모든 함수를 두 벌로 제공한다.

#dtable(
  columns: 3,
  [*접미사*], [*문자열 타입*], [*인코딩*],
  [`A`(ANSI)], [`char *`], [프로세스의 *활성 코드 페이지*(한국어 판이면 대개 CP949)],
  [`W`(Wide)], [`wchar_t *`(`WCHAR`)], [UTF-16LE],
)

`MessageBoxA`와 `MessageBoxW`가 그 예이고, `MessageBox`라는 이름은 매크로다 —
`UNICODE` 매크로가 정의되어 있으면 `W`로, 아니면 `A`로 펼쳐진다. `TCHAR`·`_T()`
같은 이름도 같은 시대의 유산이다.

#platform[
  Windows에서 문자열을 다루는 세 가지 길
][
  *① `W` 함수를 쓰고 경계에서 변환한다(고전적 정공법).* 프로그램 내부는
  UTF-8로 두고, API를 부를 때만 `MultiByteToWideChar(CP_UTF8, …)`로 UTF-16으로
  바꾼다. 돌아올 때는 `WideCharToMultiByte`. 변환 함수는 필요한 크기를 먼저
  묻고(길이 인자에 0을 주면 필요한 문자 수를 돌려준다) 그다음 채우는 두 단계
  호출이다.

  *② 프로그램 전체를 UTF-16으로 짠다.* Windows 전용 프로그램의 오래된 방식이다.
  문자열 상수마다 `L`을 붙이고, `wcslen`·`wprintf` 계열을 쓴다. 이식성은
  포기한다.

  *③ 활성 코드 페이지를 UTF-8로 만든다(현대적 처방).* Windows 10 1903부터
  응용 프로그램 매니페스트에 `<activeCodePage>UTF-8</activeCodePage>`를 넣으면
  `A` 함수들이 UTF-8을 받는다. 그러면 리눅스용으로 짠 UTF-8 코드가 거의
  그대로 돈다. 새 프로그램이라면 이 길이 가장 짧다.

  콘솔은 또 별개다. 유니코드를 콘솔에 제대로 내보내려면
  `_setmode(_fileno(stdout), _O_U16TEXT)`로 와이드 모드를 켜거나, 코드 페이지를
  `SetConsoleOutputCP(CP_UTF8)`로 바꿔야 한다. 「한글이 물음표로 나온다」는
  대개 이 자리의 문제다.
]

#realcase[
  짝이 맞지 않는 서러게이트 — Windows 파일 이름의 그늘
][
  Windows의 파일 이름은 「UTF-16 단위의 배열」이지 「올바른 유니코드 문자열」이
  아니다. 검사하지 않기 때문에, *짝이 맞지 않는 고위 서러게이트 하나만 든 이름*
  도 파일 시스템에 존재할 수 있다.

  그런 이름을 UTF-8로 바꾸려 하면 문제가 생긴다 — 올바른 UTF-8로는 표현할 수
  없는 값이기 때문이다. `WideCharToMultiByte`는 실패하거나 U+FFFD로 바꿔
  버리고, 그러면 *그 파일을 다시 열 수 없다.*

  이 문제 때문에 몇몇 프로그램은 「WTF-8」이라는 확장(짝 없는 서러게이트도
  UTF-8식으로 적는 비표준 인코딩)을 내부적으로 쓴다. 러스트의 `OsString`이
  Windows에서 그렇게 구현되어 있다.

  교훈은 이렇다 — *플랫폼이 주는 문자열이 언제나 올바른 유니코드라고 가정하지
  않는다.* 파일 이름·명령줄 인자·환경 변수는 특히 그렇다.
]

== 리눅스와 glibc — 크지만 쓰이지 않는다

glibc의 `wchar_t`는 4바이트 UCS-4이고 `__STDC_ISO_10646__`도 정의된다. 즉 표준이
그리던 그림에 가장 가까운 구현이다. 그런데 실무의 리눅스 코드는 `wchar_t`를
거의 쓰지 않는다. 이유가 넷이다.

#dtable(
  columns: 2,
  [*이유*], [*설명*],
  [기억을 네 배 쓴다], [아스키 위주 텍스트에서 4배는 큰 낭비다],
  [경계에서 언제나 변환해야 한다], [파일·소켓·API는 모두 바이트다],
  [로케일에 매여 있다], [`LC_CTYPE`이 UTF-8이 아니면 변환이 실패한다(69장)],
  [UTF-8이 대부분의 일을 해 준다], [`strlen`·`strcmp`·`strstr`이 그대로 동작한다],
)

마지막 줄이 결정적이다. UTF-8은 *아스키와 호환되고, 자기동기화되며, 바이트
순서가 코드포인트 순서와 일치*하므로 기존 바이트 함수들이 대부분 그대로 쓸모가
있다(71장). 그래서 리눅스 진영은 「내부도 UTF-8」이라는 길을 갔다.

== 툴킷의 선택 — GTK와 Qt

응용 프로그램 층에서도 같은 갈림이 있다.

#dtable(
  columns: 4,
  [*툴킷*], [*문자열 타입*], [*인코딩*], [*코드포인트 타입*],
  [GTK / GLib], [`gchar *`(= `char *`)], [UTF-8], [`gunichar`(32비트), `gunichar2`(16비트)],
  [Qt], [`QString`], [UTF-16], [`QChar`(16비트 단위)],
  [Windows API], [`WCHAR *`], [UTF-16], [—],
)

GLib은 「모든 문자열은 UTF-8」이라는 규칙을 세우고, 그 위에 `g_utf8_strlen`(글자
수)·`g_utf8_next_char`(다음 글자로)·`g_utf8_validate`(검증) 같은 함수를 얹었다.
`wchar_t`는 아예 쓰지 않는다 — 플랫폼마다 달라지는 타입을 API에 넣지 않겠다는
결정이다.

Qt는 반대로 갔다. 내부를 UTF-16으로 고정하고, 바깥과는 `QString::fromUtf8`·
`toUtf8`로 오간다. Windows API와 붙일 때 변환이 없다는 이점이 있고, 대신
`QString::size()`가 UTF-16 단위를 센다는 함정을 안는다.

#qa[
  그러면 내 프로그램은 무엇을 골라야 하는가?
][
  *새 코드라면 UTF-8 바이트열*이다. 이유는 위의 표가 그대로 말한다 — 크기가
  작고, 기존 C 함수와 어울리고, 파일·네트워크의 표현과 같고, 로케일에 덜
  매인다.

  `wchar_t`를 써야 하는 자리는 좁다. *Windows API를 직접 부를 때*(그것도 경계
  에서만), 그리고 *와이드 문자만 받는 기존 라이브러리에 붙일 때*다.

  고정 폭 코드포인트가 정말로 필요하면 `wchar_t`가 아니라 `char32_t`를 쓴다.
  크기가 어디서나 4바이트이고 UTF-32임이 매크로로 보장된다 — `wchar_t`가 주지
  못하는 두 가지다.
]

== 스트림에는 방향이 있다

와이드 문자에는 입출력 함수도 딸려 있다 — `wprintf`·`fputws`·`getwc`·`fgetws`,
그리고 `EOF` 대신 `WEOF`. 그런데 잘 알려지지 않은 규칙이 하나 있다.

*스트림 하나에는 방향(orientation)이 있고, 한 번 정해지면 바꿀 수 없다.*

#dtable(
  columns: 2,
  [*상태*], [*뜻*],
  [방향 없음], [갓 열린 스트림. 아직 아무 방향도 아니다],
  [바이트 방향], [`fputs`·`fprintf` 같은 바이트 함수를 처음 쓰면 이쪽으로 정해진다],
  [와이드 방향], [`fputws`·`fwprintf` 같은 와이드 함수를 처음 쓰면 이쪽으로 정해진다],
)

정해진 뒤에 반대 계열 함수를 쓰면 *정의되지 않은 동작*이다. `fwide` 함수가
방향을 묻거나(0을 주면 질의) 아직 없을 때 미리 정한다.

#demo("examples/ch70/wide_io.c")

시연이 그 규칙을 그대로 보인다. 갓 연 스트림은 방향이 없고, `fwide(f, 1)`로
와이드로 정할 수 있으며, 그 뒤 `fwide(f, -1)`로 되돌리려 해도 *바뀌지 않는다.*
그리고 이 프로그램의 `stdout`은 `printf`로 시작했기 때문에 이미 바이트
방향이다 — 여기서 `wprintf`를 부르면 계약 밖이다.

#antipattern[
  `printf`와 `wprintf`를 섞어 쓰기
][
  ```c
  printf("이름: ");
  wprintf(L"%ls\n", name);      /* 이미 바이트 방향인 stdout 에 와이드 출력 */
  ```
  운이 좋으면 출력 순서가 뒤섞이는 정도이고, 나쁘면 아무것도 나오지 않는다.
  Windows에서 `_O_U16TEXT`를 켠 뒤 `printf`를 부르면 프로그램이 곧바로
  죽기도 한다.

  규율은 하나다 — *스트림 하나에는 한 계열만.* 와이드 출력을 쓰기로 했다면
  프로그램 전체에서 그렇게 하고, 아니면 아예 쓰지 않는다. 이 책의 권고는
  후자다: 와이드 함수로 얻는 것보다 잃는 이식성이 크다.
]

#recap[
  #dtable(
    columns: 2,
    [*기억할 것*], [*요점*],
    [갈라짐], [리눅스 4바이트 UTF-32, Windows 2바이트 UTF-16],
    [원인], [1990년대의 "유니코드=16비트" 전제가 1996년에 깨졌다],
    [서러게이트], [U+10000 이상을 D800\~DBFF + DC00\~DFFF 쌍으로. 그 값 자체는 문자가 아니다],
    [길이], [바이트·코드포인트·UTF-16 단위가 모두 다르다],
    [Windows], [`W` 함수는 UTF-16LE. 새 코드는 매니페스트로 UTF-8 코드 페이지],
    [파일 이름], [올바른 유니코드가 아닐 수 있다(짝 없는 서러게이트)],
    [툴킷], [GTK=UTF-8 `char*`, Qt=UTF-16 `QString`],
    [고정 폭이 필요하면], [`wchar_t`가 아니라 `char32_t`],
    [스트림 방향], [한 번 정해지면 못 바꾼다. 섞으면 정의되지 않은 동작],
  )
]

플랫폼의 현실까지 보았다. 다음 장은 결론이다 — 그래서 실무에서는 유니코드와
멀티바이트 인코딩을 *어떻게 다루는가*. 세 층위의 길이, 정규화, UTF-8 검증,
그리고 아직도 살아 있는 레거시 2바이트 인코딩의 함정을 본다.
