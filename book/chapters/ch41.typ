#import "../lib.typ": *

= 구조체

#prereq(
  ([23장 변수 선언], [변수 선언]),
  ([36장 배열], [여러 값을 한 이름으로]),
)

#organizer[
  제5부에서 "타입을 만드는 선언은 메모리 모델을 갖춘 뒤"라며 미뤄 둔
#idx("구조체")  약속을 지킨다. 여러 값을 하나로 묶는 타입 — 구조체다. 선언과 초기화,
  두 가지 접근 표기(`.`와 `->`), 그리고 구조체가 값으로 오가는 방식까지.
]

#deepqa[
  23장에서 "타입은 값의 집합 + 연산의 약속"이라 했고, 지금까지 쓴 타입은
  전부 미리 있는 것들(int, double, char, 포인터)이었다. 그러면 프로그래머가
  *새 타입을 만든다*는 것은 무슨 뜻인가?
][
  기억의 모양을 새로 정하고 이름을 붙이는 일이다. "정수 둘이 나란히
  있는 덩어리를 점(point)이라 부르겠다"고 선언하면, 그 순간부터 point는
  변수를 만들고 함수에 넘기고 배열로 늘어놓을 수 있는 어엿한 타입이
  된다. 36장의 배열이 *같은 타입의 반복*이었다면, 구조체는 *다른 타입의
  묶음*이다 — 그리고 그 묶음에 이름이 붙는 순간, 프로그램의 어휘가
  늘어난다.
]

#chapter-questions()

== 선언, 초기화, 접근

#demo("examples/ch41/point.c")

*선언*은 `struct point { int x; int y; };` — 중괄호 안의 각 항목을
*멤버*(member)라 부른다. 이 선언 자체는 기억을 잡지 않는다. "이런
모양의 타입이 있다"는 정의일 뿐이고, `struct point a;`처럼 써야 변수가
생긴다.

*초기화*는 시연처럼 멤버 이름을 적는 *지정 초기화*(designated
initializer, C99)를 권한다 — `{ .x = 3, .y = 4 }`. 순서에 기대는
`{3, 4}`보다 읽기 좋고, 멤버가 늘거나 순서가 바뀌어도 안전하다.
적지 않은 멤버는 0으로 채워진다.

*접근*은 두 표기다 — 값에는 점(`a.x`), 포인터에는 화살표(`p->x`).
화살표는 사실 `(*p).x`의 줄임이다(33장의 역참조 + 점). 포인터로
구조체를 다루는 일이 압도적으로 흔해서 전용 표기를 둔 것이다.

*복합 리터럴* — 시연의 `(struct point){ .x = ..., .y = ... }`는 "그
자리에서 이름 없는 구조체 값을 하나 만드는" 표기다(C99). 반환값이나
인자로 구조체를 즉석에서 넘길 때 요긴하다.

== 구조체는 값이다

C에서 구조체는 *값처럼* 다뤄진다 — 대입하면 통째로 복사되고, 함수에
넘기면 32장의 규칙 그대로 복사되어 건너가며, `return`으로 통째로 돌려
줄 수도 있다. 시연의 `moved(a, 10, -1)`이 그 확인이다: a는 그대로이고
새 값 b가 나왔다.

이 복사는 멤버를 그대로 옮겨 적는 *얕은 복사*(shallow copy)다. 멤버가
전부 수라면 문제가 없지만, 멤버 중에 포인터가 있으면 주소가 그대로
복제되어 원본과 사본이 같은 곳을 가리키게 된다 — 이 사실이 뒤에서
자기를 가리키는 자료를 다룰 때 결정적인 함정이 된다.

다만 실무에서는 큰 구조체를 값으로 주고받는 대신 *포인터로 넘기는*
관행이 흔하다 — 복사 비용을 아끼기 위해서다(11장의 기억 사다리를 떠올리면
큰 덩어리의 복사가 공짜가 아님이 보인다). 읽기만 할 때는 `const struct
point *p`처럼 const 포인터로 받는 것이 관행이다 — 23장의 `const`가
"이 함수는 원본을 건드리지 않는다"는 계약 표시로 일하는 것이다.

#qa[
  `struct point`라고 매번 `struct`를 붙이는 것이 번거롭다 — 줄일 수
  없는가?
][
  전통적으로는 `typedef`로 별명을 만들어 왔다 — `typedef struct point
  point_t;`처럼. 다만 취향과 유파가 갈리는 지점이라(별명이 "이것이
  구조체"라는 정보를 감춘다는 반론이 있다), 이 책은 지면에서 정체가
  보이도록 `struct`를 붙여 쓴다. 어느 쪽이든 일관성이 중요하다.
]

#qa[
  구조체 안에 자기 자신을 멤버로 넣을 수 있는가 — 리스트 같은 것을
  만들려면 필요할 것 같은데.
][
  자기 자신을 *값으로* 담을 수는 없다(무한한 크기가 되므로). 그러나
  *자신을 가리키는 포인터*는 담을 수 있고, 바로 그것이 연결 자료구조의
  씨앗이다: `struct node { int value; struct node *next; };` 이 한 줄에
  33장의 포인터와 40장의 동적 메모리가 만나면 연결 리스트·트리 같은
  구조가 열린다 — 이 책의 범위를 넘는 자료구조의 세계이지만, 문을 여는
  열쇠가 여기 있다는 것은 알아 둘 만하다.
]

값들을 묶는 법을 얻었다. 다음 장은 그것을 *부리는* 법이다 — 그 자리에서
만들어 넘기는 임시 구조체, 그것으로 얻는 순서 없는 이름 붙은 인자, 그리고
멤버 사이에 끼는 빈자리(패딩)를 다루는 법.
