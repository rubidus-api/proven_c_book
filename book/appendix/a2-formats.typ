#import "../lib.typ": *

= 부록 B — printf·scanf 서식 요약

19장에서 최소 집합(`%d`, `%s`, `%%`)만 익혔다. 자주 쓰는 나머지를
모아 둔다. 서식과 인자의 타입이 어긋나면 계약 밖이라는 것(19·42장)을
잊지 않는다.

== 출력 변환

#align(center, table(
  columns: 3,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left, left),
  [*서식*], [*인자 타입*], [*뜻*],
  [`%d` `%i`], [int], [십진 정수],
  [`%u`], [unsigned int], [부호 없는 십진],
  [`%x` `%X`], [unsigned int], [16진(소문자·대문자)],
  [`%o`], [unsigned int], [8진],
  [`%f` `%F`], [double], [고정 소수 표기],
  [`%e` `%E`], [double], [지수 표기],
  [`%g` `%G`], [double], [짧은 쪽 자동 선택],
  [`%c`], [int(문자로 진급)], [문자 하나],
  [`%s`], [`char *`], [NUL 종단 문자열],
  [`%p`], [`void *`], [포인터(형식은 구현 정의)],
  [`%zu`], [`size_t`], [크기 값],
  [`%lld` `%llu`], [long long 계열], [긴 정수],
  [`%%`], [—], [퍼센트 글자],
))

폭과 정밀도는 `%8.3f`(폭 8, 소수 3자리), `%-10s`(왼쪽 정렬), `%.*s`
(길이를 인자로 — 8장의 뷰를 찍을 때 요긴하다)처럼 적는다.

== 입력 해석

`sscanf` 계열의 서식은 출력과 대체로 같되, 세 가지가 다르다.

- 인자는 *담을 곳의 주소*여야 한다(`&n`, 배열은 그대로).
- `%s`는 공백까지 읽고 멈추며, *반드시 최대 폭을 준다* — `%99s`처럼.
  폭 없는 `%s`는 35장의 `gets`와 같은 위험이다.
- 반환값은 *해석에 성공한 항목 수*다. 확인하지 않으면 실패를 놓친다
  (22·41장).

긴 정수·크기 타입에는 `%lld`, `%zu` 대신 입력 쪽 지정자를 정확히 맞춰야
하고, 정밀한 해석이 필요하면 53장의 proven 스캐너처럼 실패가 값으로
드러나는 도구를 쓰는 편이 안전하다.
