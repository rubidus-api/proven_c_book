#import "../lib.typ": *

= 부록 A — 연산자 우선순위와 결합

본문(17장)은 "표를 외우지 말고 괄호를 쓰라"고 권했다. 이 표는 외우기
위한 것이 아니라, 남의 코드를 읽다 막혔을 때 찾아보기 위한 것이다.
위쪽이 더 강하게 묶는다.

#align(center, table(
  columns: 3,
  stroke: 0.5pt + rgb("#cccccc"),
  inset: 5pt,
  align: (left, left, left),
  [*묶음*], [*연산자*], [*결합*],
  [후위], [`() [] . -> ++(후위) --(후위)`, 복합 리터럴], [왼→오],
  [단항], [`++(전위) --(전위) + - ! ~ (타입) * & sizeof alignof`], [오→왼],
  [곱셈류], [`* / %`], [왼→오],
  [덧셈류], [`+ -`], [왼→오],
  [시프트], [`<< >>`], [왼→오],
  [관계], [`< <= > >=`], [왼→오],
  [상등], [`== !=`], [왼→오],
  [비트 AND], [`&`], [왼→오],
  [비트 XOR], [`^`], [왼→오],
  [비트 OR], [`|`], [왼→오],
  [논리 AND], [`&&`], [왼→오],
  [논리 OR], [`||`], [왼→오],
  [조건], [`?:`], [오→왼],
  [대입], [`= += -= *= /= %= &= ^= |= <<= >>=`], [오→왼],
  [쉼표], [`,`], [왼→오],
))

특히 헷갈리는 세 자리를 적어 둔다.

- *비트 연산이 비교보다 약하다* — `a & b == c`는 `a & (b == c)`다.
  비트 검사에는 괄호가 사실상 필수다.
- *시프트가 덧셈보다 약하다* — `a << 1 + 2`는 `a << 3`이다.
- *대입은 오른쪽 결합* — `a = b = 0`은 `a = (b = 0)`으로 읽힌다.

우선순위는 *묶는 규칙*일 뿐이고 평가의 시간 순서가 아니라는 것(17·29장)
을 함께 기억하면 된다.
