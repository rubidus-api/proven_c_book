# Plans

Temporary task plans.

- `active/`: current non-trivial work plans and user confirmation gate.
- `archive/`: completed, cancelled, or superseded plans. Do not read by default.

For non-trivial requests, create a concise active plan, ask the user for needed confirmation, then move the plan to `archive/` when the task is complete, cancelled, or superseded.
