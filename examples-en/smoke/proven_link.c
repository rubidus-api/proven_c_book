/* A smoke test of the vendor/proven link — not a body example but a standing check of the pipeline. */
#include <proven.h>
#include <stdio.h>

int main(void)
{
    printf("proven vendor link: ok\n");
    return 0;
}
