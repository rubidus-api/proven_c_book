#ifndef GREET_H          /* an include guard — safe even if it is included twice */
#define GREET_H

void greet(const char *who);   /* a declaration: the signature of the contract only */
void printf_count(void);

#endif
