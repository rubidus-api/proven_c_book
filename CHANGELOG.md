# Changelog

All notable changes to this project will be documented in this file.

This project follows Keep a Changelog.

## [Unreleased]

## [v0.59.1] - 2026-08-11

### Changed
- 머리말 「이 책의 의도와 이용 방법」의 **둘째 문단**에 두 가지를 넣고, 앞뒤가
  이어지도록 다듬었다(저자 지시).
  - **분량** --- 연습과 과제를 넣지 않은 첫째 이유는 *자리가 없었다*는 것이다.
    설명만으로 이미 800쪽이 넘는다. 머리말에서 이미 분량을 두고 사과했으므로,
    그 사과가 여기서 불리도록 두 자리를 이어 두었다(「다음 절에서 말할 이 책의
    한계도 절반은 거기서 온다」).
  - ★ **그런데도 이 책이 값하는 이유**를 새 문단으로 세웠다. **C 에는
    안전장치가 거의 없다** --- 다른 언어라면 잘못 짚은 생각을 컴파일러나
    실행기가 그 자리에서 막아 주지만, C 는 대개 그냥 받아들이고 그럴듯한 답을
    내놓는다. 어긋난 이해가 *틀렸다는 신호 없이* 살아남는다는 뜻이다. 그래서
    기초가 얕은 채로 손부터 움직이면 훈련의 값이 비싸진다 --- 더 많은 시간과
    시행착오가 들고, 그 사이 굳은 오개념은 나중에 버그가 되고 더러는 보안
    취약점이 된다. 든든한 기초는 그 훈련을 *대신하지는 못해도 훨씬 싸게*
    만들어 준다.
  - 두 판 모두 같은 뜻·같은 무게로 썼다.

## [v0.59.0] - 2026-08-11

### Changed
- ★ 머리말 「이 책의 의도와 이용 방법」의 셋째 문단을 다시 썼다(저자 지시).
  헬로 월드가 제3부에서야 나오는 것을 *변명하지 않고 이유를 밝힌다*:
  제1부와 제2부는 곁들인 배경이 아니라 그만한 무게를 지닌 자리이며, 저자가
  보기에 이 책에서 가장 값진 부분이다 --- C 의 생김새가 대부분 거기서 다루는
  사정(기억을 다루는 방식·표현의 한계·번역이라는 과정)에서 비롯되기 때문이다.
  그래서 당부를 하나 넣었다: **시간이 넉넉하지 않다면 다른 곳을 건너뛰더라도
  제1부와 제2부만은 읽어 두시라.** 그리고 그 둘을 읽고 나면 뒤에서 마주칠
  물음들(정수는 왜 감아 도는가 · 문자열의 끝은 왜 눈에 보이지 않는가 ·
  컴파일러는 어째서 내가 쓴 대로 하지 않는가) 가운데 적지 않은 것이 따로
  배우지 않아도 풀린다는 것도 함께 적었다. 두 판 모두.

## [v0.58.2] - 2026-08-11

### Added
- **웹판 표지(index)의 맨 아래에 판권**(저작권과 연락처)을 넣었다(저자 지시).
  텍스트 판은 2쪽 아래에 두는데 웹판에는 그런 「쪽」이 없으므로 표지 페이지의
  끝에 둔다 --- 어느 판이든 *맨 처음 자리에서 한 번* 만나게 된다. 두 언어 모두.
- ★ `scripts/check-colophon.py` --- 판권 문구가 이제 **네 자리**에 있다
  (텍스트 두 판 · 웹 두 판). 조판 도구가 달라 한 곳에서 만들 수 없으니, 남는
  길은 *어긋나면 알아채는 것*이다. 라이선스 이름과 URL 이 네 자리에 모두
  있는지 대조한다(문장 전체는 대조하지 않는다 --- 언어와 매체가 다르면 문장은
  달라도 된다). `check-counts` 와 함께 릴리스 게이트에 걸었다.

## [v0.58.1] - 2026-08-11

### Fixed
- ★ **소스 코드가 상자를 뚫고 나가던 것**(저자 지시). 두 가지가 겹쳐 있었다.
  - 줄바꿈기가 글자를 **한 칸으로 세고 있었다.** 고정폭 글꼴에서 한글·한중일
    글자는 라틴의 *두 배*를 차지한다 --- 한글 주석이 든 줄이 그래서 넘쳤다.
    이제 폭으로 센다. 실측: 88칸을 넘는 예제 줄이 **245줄** 있었다.
  - 그 줄바꿈을 **실행 결과에만** 걸고 소스에는 걸지 않았다(「낱말 한복판에서
    잘리면 읽기 나쁘다」는 이유였는데, 상자를 뚫고 나가는 편이 더 나쁘다).
    이제 소스에도 건다.
  - 고친 뒤 150ppi 렌더에서 **판면 밖 잉크 0픽셀**을 확인했다.
- 웹판에서도 코드·실행 결과를 **글자 단위로 접는다**. 가로로 넘치게 두면 긴
  줄이 가로 스크롤 안에 숨는다 --- 종이와 같은 약속을 화면에서도 지킨다.

### Changed
- 「읽기 설정」·「Reading settings」를 **「설정」·「Settings」**로 줄였다(저자
  지시). 판 번호는 그 바로 왼쪽이다.

## [v0.58.0] - 2026-08-11

### Changed
- ★ **앞부속의 순서를 좌철 책의 관행에 맞췄다**(저자 지시).
  1쪽 표지 → **2쪽 아래 판권(저작권과 연락처)** → **3쪽부터 차례** → 머리말과
  「이 책의 의도와 이용 방법」. 판권은 *찾아보는 것*이지 읽는 것이 아니라,
  머리말 뒤에 있으면 읽는 흐름을 한 번 끊는다. 두 판 모두 같게 고쳤다.
- 웹판 펼침 패널 **맨 왼쪽에 판 번호**를 표시한다(저자 지시). 「지금 보는 것이
  어느 판인가」는 오류를 신고할 때 가장 먼저 필요한 사실이다. 도구가 아니라
  표시라 누를 것은 없다.

## [v0.57.2] - 2026-08-11

### Fixed
- ★★ **테마 단추가 정말로 아무 일도 하지 않던 진짜 까닭**(저자 재지적).
  앞 판에서 「직접 고른 색이 테마를 덮는다」를 고쳤지만 그것은 원인이 아니었다.
  **`[data-theme]` 에 대응하는 색 정의가 아예 없었다** --- 어두운 팔레트가
  `@media (prefers-color-scheme: dark)` *안에만* 있어서, 단추는 속성을 붙이는데
  CSS 에 응답할 규칙이 없었다. 시스템이 어두울 때 「밝게」도 마찬가지였다.
  이제 색을 **세 자리**에 적는다.
  - `:root` --- 밝은 팔레트
  - `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) }`
    --- 시스템이 어둡고 「밝게」를 고르지 않았을 때
  - `:root[data-theme="dark"]` --- 「어둡게」를 골랐을 때
  ★ 이 셋을 다 적지 않으면 토글은 반쪽만 돈다. 값을 고칠 때는 둘을 함께 고쳐야
  한다(CSS 에는 변수 묶음에 이름을 붙일 방법이 없다).
- 선·제목 밑줄·시연 테두리에 박혀 있던 고정색을 **팔레트 변수**(`--box-rule`)로
  바꿨다. 고정색이면 어두운 테마에서 그 선만 홀로 밝다.

## [v0.57.1] - 2026-08-11

### Fixed
- ★ **테마 단추가 색을 바꾸지 못하던 것**(저자 지적). 직접 고른 색은 인라인
  스타일로 남아 테마의 색을 *덮는다* --- 그래서 「어둡게」를 눌러도 글자·배경·
  링크가 그대로였다. 이제 테마를 고르는 것은 색을 **놓아 주는** 일이기도 하다:
  저장된 개별 색을 지우고, 색 입력칸도 그 테마의 값으로 다시 채운다.
- **되돌리기 단추를 없앴다**(저자 지적). 테마 단추가 이미 그 일을 한다 ---
  같은 일을 하는 단추가 둘일 까닭이 없다.

## [v0.57.0] - 2026-08-11

### Added
- ★ **읽기 설정**(웹판, 저자 지시). 상단 바 → 메뉴 → ⚙ 에서 연다.
  - **가로폭 제한** 켜고 끄기. *기본은 제한 없음* --- 폭을 열어 두는 것은
    저자의 설계 의도이므로 기본값을 바꾸지 않고, 원하는 독자가 켠다(46rem).
  - **테마** 시스템 / 밝게 / 어둡게.
  - **색** 글자·배경·링크를 직접 고른다. 되돌리기 하나로 전부 원래대로.
  - 값은 **이 브라우저에만** 남는다(`localStorage`). 서버도 계정도 없다.
  - ★ 적용 스크립트는 `<head>` 에서 먼저 돈다 --- 본문이 그려진 뒤에 색을
    바꾸면 흰 화면이 한 번 번쩍인다.
- **상단 바를 넷으로 줄이고 나머지를 접었다**(저자 지시). 늘 보이는 것은
  책 이름 · 지금 어디인가(단추) · 이전/목차/다음뿐이다. 단추를 누르면 그
  아래로 펼쳐지며 **한 줄로 죽 늘어서는 도구**(설정 · 상세 차례 · 다른 판 ·
  PDF · Git)가 나오고, 좁으면 다음 줄로 넘어간다. 그 아래가 이 장의 차례다.
  - 다른 판 링크는 **보고 있던 장의 그 언어판**으로 간다.
  - PDF 는 **그 판의 최신 PDF 를 바로 내려받는** 주소다.
- **누를 수 있음을 바탕색으로 말한다.** 단추·바 링크·차례 항목에 손이 닿으면
  옅은 바탕이 깔린다. 본문 링크는 평소 밑줄로 있음을 알리고, 닿으면 바탕과
  진한 밑줄로 답한다 --- 글자색만 바뀌면 닿기 전에는 알 수 없다.

## [v0.56.0] - 2026-08-11

디자인 검증에서 나온 네 가지를 모두 고쳤다. 판정은 실측이었고, 고친 뒤에도
다시 쟀다.

### Fixed
- ★ **표지 줄이 쪽 맨 아래에 홀로 남던 곳 24군데 → 0**. 「흔한 오해.」만 찍히고
  내용은 다음 쪽으로 넘어가면, *어디에 무엇이 있는지 기대할 수 있게 한다*는
  이 책의 설계가 그 자리에서 무너진다. 표지 줄 블록 여섯 곳에 `sticky: true`
  를 주어 조판기가 내용과 함께 넘기게 했다.
- ★ **웹판에서만 선이 너무 옅던 것.** `#d5d5d5` 는 흰 배경 대비 **1.47:1** ---
  화면 밝기를 낮추면 사실상 사라져 「영역을 한눈에」가 화면에서만 무너졌다.
  종이는 잉크가 실제로 얹히므로 그대로 두고, **웹만 `#c4c4c4`(1.75:1)** 로
  한 단계 진하게 했다. 어두운 모드도 함께 맞췄다.
- **키보드 초점이 보이지 않던 것** --- `:focus-visible` 윤곽선을 넣었다.
- **고정 바 아래로 제목이 숨던 것** --- `scroll-margin-top` 으로 비켜 세운다.
  차례에서 절을 누르면 그 제목이 바에 가려 있었다.
- **본문 건너뛰기 링크**를 두 판에 넣었다(`Tab` 한 번에 상단 바를 지나친다).
  ★ 함정 하나: 자리표(`#content`)가 쪽마다 있는데 앵커 재작성기가 그것을
  *한 쪽*으로 이어 버려 건너뛰기가 1장으로 갔다. 그 id 만 예외로 두었다.

### Changed
- ★ **문단 들여쓰기와 문단 사이 여백을 둘 다 쓰던 것을 하나로**. 첫 줄
  들여쓰기가 이미 문단의 시작을 말하므로 여백은 중복이었고, 재 보니 그 중복이
  **30쪽 남짓**을 먹고 있었다. 조판의 관례대로 들여쓰기만 남겼다.
- ★ **제본 여백(gutter)**을 넣었다. 830쪽을 묶으면 안쪽이 접혀 들어간다.
  좌우를 비대칭(**안쪽 2.7cm · 바깥 1.7cm**)으로 두되 합계는 예전(4.4cm)과
  같게 유지해 **판면 폭과 밀도는 그대로**다.
- 쪽수: 873 → **830쪽**(한국어), 929 → **891쪽**(영어). 표지 줄 높이 조정
  (v0.55.1)과 문단 여백 정리가 합쳐진 결과다.

## [v0.55.1] - 2026-08-11

### Changed
- **웹판 장 서두 칸의 목록과 문단 간격**을 줄였다(저자 지적). 항목마다 여백을
  또 주면 *행간이 두 번 붙은 것처럼* 보인다 --- 짧은 줄이 여럿 늘어서는
  자리라 그 겹침이 특히 커 보였다. 항목 사이는 **행간이 대신하게** 두고
  (`li { margin:0 }`), 여백은 문단 사이에만 최소로 준다. 행간 자체는 본문에서
  물려받는다 --- 값을 따로 두지 않는다.
- ★ **텍스트 판 장치 표지 줄의 높이**를 글자의 1.5배로 맞췄다(저자 지시).
  안쪽 여백 13pt 는 과했다 --- 실측 12.3mm(35pt)였다. 3.5pt 로 낮춰
  **5.6mm(15.8pt)**, 글자 10.3pt 의 **1.53배**가 됐다. 재서 맞춘 값이다.

## [v0.55.0] - 2026-08-11

### Changed
- ★ **리듬을 하나로**(저자 지시: 「글 전체의 통일성이 중요합니다」).
  - **장 안의 차례**가 본문보다 넓었다. 200ppi 렌더에서 재 보니 차례 줄은
    **18.7pt**, 본문 줄은 **17.6pt** --- 그 1.1pt 가 어색함의 정체였다.
    `chtoc-row-gap` 을 0.95 → **0.84em** 으로 낮춰 본문과 같은 17.6pt 가 됐다.
    (짐작으로 고치지 않고 렌더를 재서 맞췄다.)
  - **웹판의 장치 안 글**이 본문과 같은 크기·행간·문단 간격을 쓰도록 못박았다.
    상자를 지날 때마다 리듬이 흔들리던 것을 없앴다. 표지 줄만은 제 크기를
    지킨다 --- 그것은 이름이지 본문이 아니다.
- **제목과 밑줄 사이를 넉넉히** 했다(저자 지시). 텍스트 판은 0.22~0.30 →
  **0.40~0.55em**, 밑줄 아래 여백도 0.75~1.25 → **0.95~1.55em**.
  웹판도 같은 비율로 늘렸다.
- 3장 흔한 오해의 제목을 「이런 것도 C 표준에 들어 있겠지」에서
  **「프로세스를 다루는 함수도 C 표준에 들어 있겠지」**로 --- 「이런 것」이
  무엇인지 제목만 보고 알 수 있어야 한다(저자 지적).

## [v0.54.2] - 2026-08-11

### Added
- ★ **「입력 버퍼를 비운다」가 왜 뜻이 서지 않는 말인지**를 세 자리에 나누어
  적었다(저자 지시). 지금까지는 「계약 밖이다」라고만 했는데, 그보다 앞서는
  물음이 있다 --- *무엇을 버리라는 말인가.*
  - **10장** --- 띠의 반대편에 무엇이 있는지 프로그램은 모른다. 사람이 한 줄씩
    치는 터미널일 수도, 이미 다 와 있는 파일일 수도, 아직 오는 중인 파이프일
    수도, 띄엄띄엄 오는 네트워크일 수도 있다. 그러니 「지금 비워라」는
    *마침 그 순간까지 도착한 만큼*이 되고, 그 양은 내 논리가 아니라 **상대의
    속도**가 정한다. 버릴 것은 시점이 아니라 **경계**로 말해야 한다.
  - **25장** --- 문답 하나로 뒤집어 물었다: 표준이 그런 함수를 넣어 주었다면
    그것은 무엇을 하는 함수여야 하는가. 네 경우(터미널·리다이렉션·파이프·
    네트워크)를 같은 한 문장으로 정의할 수 없음을 보이고, 표준의 침묵이
    빠뜨림이 아니라는 결론에 이른다.
  - **63장** --- `fflush(stdin)` 반례에 같은 물음을 얹어, 미정의라는 사실보다
    *같은 프로그램이 터미널에서는 멀쩡하다가 파이프로 이으면 어긋난다*는
    사실을 앞세웠다.

## [v0.54.1] - 2026-08-11

### Added
- ★ **버퍼를 이야기하는 자리를 만들었다**(저자 지시). 스트림을 「띠」로만 그려
  두었더니, 정작 초보자가 겪는 입력 사고를 설명할 자리가 없었다. 둘로 나눠 넣었다.
  - **10장 「띠의 중간에는 통이 있다 --- 버퍼」** --- 프로그램과 장치 사이에
    입력·출력 버퍼가 있다고 *상상하게* 한다. 두는 까닭 셋(한꺼번에 보내려고 ·
    처리 전까지 안전히 두려고 · 속도), 그래서 「내보내라」는 동작(flush)이 따로
    있다는 것, 그리고 ★ *비우기는 출력 쪽 동작이라 입력에는 없다*는 것 ---
    들어온 글자는 누군가 읽어 가야 없어진다. 마지막으로 들어오는 쪽에만 있는
    물음(어디까지 가져가고 무엇을 남기나)을 25장으로 넘긴다.
  - **25장 「통에 무엇이 남는가 --- 예상하고, 가져가고, 남긴다」** --- 서식 읽기의
    세 걸음을 표준 조항으로 적었다. 입력 항목의 정의(§7.23.6.2p9)와 ★ *「입력
    항목 다음의 첫 글자는 읽히지 않은 채로 남는다」*, 공백 건너뛰기의 예외
    `[`·`c`·`n`(p8), 서식 속 공백은 결코 실패하지 않음(p5). `%d` 뒤에 개행이
    남아 다음 읽기를 먹어 버리는 고전적 사고를 그 규칙으로 설명하고, 두 단계
    읽기가 *예상을 통에서 그릇으로 옮기는 일*이라 그 사고가 사라짐을 보였다.
    `fflush(stdin)` 이 왜 안 되는지도 여기서 짚고 63장으로 잇는다.

## [v0.54.0] - 2026-08-10

### Fixed
- ★ **`strlcpy`·`strlcat` 이 「C23 부속서」에 있다는 서술**(65·62장). **틀렸다.**
  C23(N3220) 전문에 두 이름은 **한 번도 나오지 않는다** --- 본문에도, 경계 검사
  부속서 K 에도 없다. 부속서 K 가 담은 것은 `_s` 로 끝나는 다른 갈래이고 그마저
  선택 사항(`__STDC_LIB_EXT1__`)이며, C23 이 `<string.h>` 본문에 새로 넣은 것은
  `strdup`·`strndup`·`memccpy`·`memset_explicit` 넷으로 **잘림과 무관하다.**
  지위를 「BSD 계열 --- 표준 아님」으로 고치고 왜 헷갈리는지를 적었다.
- ★ **자유 사용(freestanding) 구현의 필수 헤더가 열뿐이라는 서술**(62장).
  C23 §4p7 은 거기에 `<string.h>`(여섯 함수 제외)와 `<stdlib.h>` 의
  `memalignment` 를 더 요구하고, §4p8 은 IEC 60559 매크로가 정의되면
  `<fenv.h>`·`<math.h>`·`strto*` 까지 얹는다. 예전 문장은 정반대로
  「`<string.h>` 는 자유 사용 필수가 아니다」였다. 빠진 여섯의 공통점(로캘·
  할당기·숨은 상태)까지 짚어 목록의 성격을 드러냈다.
- **README 의 규모 표기**가 실제와 달랐다(「13부 97장 + 부록 A~E」 → 실제
  **98장 + 부록 A~F**). `scripts/check-counts.py` 를 새로 두어 `main.typ` 과
  `appendix/` 를 정본으로 대조한다 --- 사람이 세는 한 또 어긋난다.
- **「모든 서지가 ACM 서식을 따른다」는 선언**이 사실이 아니었다. 그 서식을
  지키는 것은 「인용한 사건들」이고 앞의 두 목록은 식별자 목록이다. 선언을 그
  범위로 좁히고, 없는 판·연도를 지어내는 대신 「인용할 때 판까지 적으라」는
  규칙을 적었다.

### Changed
- ★ **34.2 「대입이 하는 일은 둘이다」 → 「셋이다」**(저자 지적). 표준의 어휘로
  가르면 *왼쪽의 값 계산 · 오른쪽의 값 계산 · 저장이라는 부수효과* 셋이다.
  그 사이의 순서까지 적었다 --- 두 값 계산은 **순서 없음**(§6.5.17.1p3),
  저장은 그 둘보다 **뒤**. 「순서 없음」이 「어느 쪽이 먼저인지 모른다」보다
  세다는 것(뒤섞일 수 있다)과, 그에 해당하는 말은 **부정 순서**이며 함수 호출이
  그 관계라는 것(§6.5.3.3p8)을 갈라 놓았다. 미정의 판정은 §6.5.1p2 와 각주 81
  (`i = ++i + 1;`·`a[i++] = i;` 는 미정의, `i = i + 1;`·`a[i] = i;` 는 허용)로
  근거를 댔다.
  - **C++ 와의 차이**를 플랫폼 노트로 새로 넣었다. C++11 은 대입의 저장을 두
    피연산자의 값 계산 뒤로 못박아 `i = ++i;` 를 정의했고, C++17 은 P0145R3 으로
    「오른쪽이 왼쪽보다 먼저」를 규칙으로 삼아 `i = i++;` 까지 정의했다.
    **C 는 따라가지 않았다** --- C23 은 여전히 「순서 없음」이라 둘 다 계약 밖이다.
- ★ **제12부의 proven 소개를 장 하나 분량으로 채웠다**(저자 지적: 「이게 뭐하는
  라이브러리인지 설명을 제대로 해야죠」). 86장을 「proven이란 무엇인가 --- 그리고
  시작하기」로 바꾸고 다섯 절을 새로 썼다: 왜 만들었는가 / 이름이 말하는 것
  (유래·provenance 와 37장의 연결) / 무엇을 대신하려는가(libc 를 몰아내지 않는다,
  전역 상태·스레드·`atexit` 없음) / 설계를 결정한 약속 여섯(각각이 이 부의 한 장) /
  무엇이 아닌가(암호 라이브러리 아님·플랫폼 층의 경계·메모리 안전 언어 아님).
  실제 사용 사례(터미널 편집기)의 **얻은 것과 치른 것**을 함께 적었다 ---
  성능은 얻지 못했다는 사실을 포함해서. 근거는 저자 자신의 `proven_c_lib`
  README·매뉴얼이다.
- **검증이 뜻하는 범위**를 두 판 README 에 명시했다. 예제 수를 실제(160개)로
  고치고, 검증이 표준 적합성 감사·보안 감사·대규모 실사용 검증이 *아님*을 밝혔다.
- 장 서두 칸의 글이 **본문과 완전히 같은 크기·행간**을 쓴다(저자 지적).
  값을 따로 두지 않고 `body-leading`·`par-spacing` 을 그대로 가리킨다.
- 웹판 상단 바의 「상세 차례」를 그림글자 **▤** 로(뜻은 `title`/`aria-label`).
  표지(index)에서는 서식 예시 줄을 뺐다 --- README 링크가 그 자리를 대신한다.

## [v0.53.0] - 2026-08-10

### Changed
- ★ **견본에서 시험하던 조판을 책에 들였다**(저자 승인, RFC-0027 §5.3). 저자가
  견본만 보고 열 몇 차례 고친 값들이며, 787쪽을 다시 찍은 것은 *이번 한 번*이다.
  - **여백** --- 일반적인 책의 숨에 맞췄다. 본문 행간 0.78 → 0.94em, 문단 사이
    1.35em, 장치 표지 줄 위아래 4~5 → 13pt, 내용 칸 2 → 7pt, 서두 칸의 줄
    사이 0.85 → 1.02em·항목 사이 0.55 → 1.20em.
  - **선의 색을 하나로** --- 왼쪽 세로선(표지·내용), 제목 밑줄, 시연 상자
    테두리가 모두 `#d5d5d5` 다. 전에는 검정·`#777`·`#111` 로 갈려 있었다.
    굵기가 층위를 말하므로 색까지 세게 말할 까닭이 없다. **표만 예외** ---
    격자는 옅으면 칸이 안 읽힌다(바깥 0.6pt `#8a8a8a` · 안쪽 0.4pt `#c4c4c4`).
  - **쪽 머리말**도 흐린 회색(`#9a9a9a`)으로. 쪽마다 되풀이되는 글이다.
  - **「이 장의 차례」를 다른 장치와 같은 꼴로** --- 굵은 세로선 위의 표지 줄과
    가는 세로선 위의 내용. 항목은 본문과 같은 크기·색이고 줄 사이는 0.95em.
  - ★ **대가**: 한국어판 792 → **859쪽**, 영어판 → **913쪽**(+8%). 여백을
    넓히면 쪽이 는다 --- 저자가 알고 고른 값이다.
- 조판 값을 시험하는 장치(`_pick` · `styles/book-trial.css`)는 그대로 둔다.
  다음 시험을 위해 비워 두었다.

## [v0.52.3] - 2026-08-10

### Fixed
- 웹판 **반례 상자의 왼쪽 세로선이 점선이던 것**을 문답과 같은 실선으로
  바꿨다(저자 지시). 점선은 상자 시절 「갈래를 테두리 모양으로 구별한다」던
  규칙의 잔재다 --- 왼쪽 세로선 체계에서는 그것만 따로 놀았고, 갈래는 표지 글
  (「반례.」)이 이미 말한다.
- 서식 예시에서 **장 서두 다섯 칸(먼저 알아야 할 것·돌아보기·이 장의 필요성과
  맥락·이 장이 끝나면·이 장에서 답할 질문)에 이름표가 붙지 않던 것**(저자 지적).
  이 라벨들은 본문이 아니라 `lib.typ` 이 만들어서, 견본이 이름을 적을 자리가
  없었다. 서두 칸에 `name:` 선택 인자를 두어 라벨 옆에 붙게 했다 --- 책에서는
  넘기지 않으므로 아무것도 달라지지 않는다.
  - ★ 이때 `stylename` 정의가 `_open-block` 보다 *아래*에 있어 「unknown
    variable」로 멈췄다. Typst 의 최상위 바인딩은 위에서 아래로 평가된다.
- 견본의 답 칸에 **「답.」이 두 번** 나오던 것(저자 지적). 표지 글은 장치가
  스스로 붙이는데 견본이 또 적었다.

### Added
- ★ **「견본만 고치기」 절차**(저자 지시, RFC-0027 §5). 값을 한곳에 모으니 이번엔
  반대 문제가 생겼다 --- 값 하나를 만지면 책도 함께 바뀌어, 「조금 넓혀 보고
  판단하겠다」는 시도조차 787쪽 재빌드를 끌고 왔다.
  - 디자인 값에 자리를 둘 둔다: `_pick(지금, 시험)`. `--input trial=1` 을 주는
    것은 견본 스크립트뿐이라 **책 빌드는 한 톨도 달라지지 않는다.**
  - 웹도 같다 --- `styles/book-trial.css` 는 견본에만 얹힌다.
  - `scripts/publish-specimen.sh` --- 견본만 만들어 `docs/style-specimen.html`
    만 밀어 올린다(릴리스도, 787쪽도 없다).
  - 채택은 값을 왼쪽으로 옮기는 일이며, 그때 한 번만 전체를 찍는다.
- 지금 시험 중인 값(견본에서만 보인다): 서두 다섯 칸의 표지·내용 여백,
  본문 행간 0.78 → 0.94em, 문단 사이 1.0 → 1.35em. 저자 지적 --- 일반적인 책에
  견주어 우리 쪽이 빽빽했다.

## [v0.52.2] - 2026-08-10

### Changed
- ★ **장치 제목의 왼쪽 줄 굵기를 하나로**(저자 지적: 「수학 상자의 제목 줄이 다른
  것보다 얇다」). 원인은 옛 상자 시절의 잔재였다 --- `_device` 가 *호출자가 넘긴
  테두리 굵기*를 제목 줄에 그대로 썼던 탓에 수학 1pt · 실제 사례 2.2pt ·
  문답 3.2pt 로 갈렸다. 이제 굵기는 `style.typ` 한 곳에서 온다.
- 텍스트 판 **앞차례(2~15쪽)의 행간**을 조금 넓혔다(저자 지적). 절까지 펴 놓아
  빽빽했다. 장 안의 차례는 지금이 알맞다고 하여 그대로 두었다 --- 두 차례는
  이제 값을 따로 쓴다.

### Fixed
- 웹판 **index.html 의 표지와 차례에서 가로·세로줄을 걷어 냈다**(저자 지시).
  제목 밑의 옅은 줄은 *일반 제목의 밑줄 규칙*이 표지 제목까지 잡은 것이었고,
  소개글 밑의 줄은 표지 블록의 아래 테두리였다. 차례의 왼쪽 세로줄과 장마다의
  밑줄도 없앴다 --- 거기는 장치가 아니라 목록이라 선이 뜻하는 바가 없다.
- ★ **서식 예시의 웹판이 통째로 밋밋하게 나오던 것**(저자 지적). 견본을 HTML 로
  뽑을 때 `--input mode=html` 을 빠뜨려, 장치들이 웹 분기가 아니라 *조판 분기*를
  타고 클래스 없는 맨 `<div>` 로 나왔다. 눈으로 보는 견본이라 아무도 오류라고
  일러 주지 않는다 --- 스크립트 두 곳에 인자를 넣고 까닭을 적어 두었다.
- 장치 안에 시연 상자를 넣어도 웹판에서 상자가 유지된다. 문단 간격 규칙의
  `> div` 가 시연까지 잡아 여백을 0 으로 만들던 것을 `:not(.demo)` 로 갈랐다.

### Added
- ★ **서식 예시를 책 한 권의 축소판으로 넓혔다**(저자 지시). 표지 · 차례 ·
  머리말 · 저작권 · 각주 · 참고 문헌 · 찾아보기까지 담고, **각 요소 옆에 그
  이름표**를 붙였다 --- 웹은 CSS 선택자(`.cover-sub`, `.dev.realcase` …),
  텍스트 판은 `lib.typ` 의 함수 이름(`toc-row`, `mathbox` …). 어디를 고쳐야
  하는지 견본이 스스로 일러 준다.
  - 이를 위해 표제면과 차례 줄을 `lib.typ` 으로 옮겼다(`cover-body`,
    `toc-row`/`toc-subrow`/`toc-part`/`toc-group`/`toc-block`). 두 판의
    `main.typ` 이 그것을 부른다 --- 견본이 따로 그리면 언젠가 반드시 갈린다.
  - 이름표는 견본 전용 장치(`stylename` / `code.style-name`)다. 본문에서는
    쓰지 않는다.
- 서식 예시의 1.1.1 각 장치 본문에 **소스와 실행 결과를 중첩**해 두었다(저자 지시).
  바깥 세로선이 안쪽 상자의 여백을 빼앗지 않는지 보는 자리다. 반례도 다른 장치와
  같은 꼴(제목 + 산문 + 시연)로 맞췄다 --- 견본만 고쳤고 본문은 두었다.

## [v0.52.1] - 2026-08-10

### Fixed
- ★ **웹 판에서 실제 사례·반례·흔한 오해·수학·플랫폼·복습 상자의 글이 왼쪽 선에
  바짝 붙던 것**(저자 지적 세 건, 원인은 하나였다).
  - 문단 간격을 좁히려고 둔 옛 규칙 `.dev.realcase > div { padding:0 }` 이
    **본문 칸(`.dev-body`) 자체를 잡아** 방금 준 패딩을 덮고 있었다.
  - 선택자를 `.dev > .dev-body > p` 처럼 *안쪽 문단*만 가리키도록 좁혔다.
    여섯 상자가 한꺼번에 풀렸다.
- 텍스트 판에서 **제목과 그 밑줄이 글자에 바짝 붙던 것**(저자 지적). 수준별로
  0.22~0.30em 의 숨을 주었다. ★ Typst 는 이웃한 블록 사이를
  `max(뒤의 above, 앞의 below)` 로 잡으므로, 이 값은 *제목 블록의 below* 다.
- 장 서두 칸(「이 장의 필요성과 맥락」·「이 장이 끝나면」·「이 장에서 답할 질문」)의
  표지 줄 위아래에 숨을 주어 넉넉하게 했다(저자 지적).

### Added
- ★ **서식 예시**를 판마다 함께 낸다(저자 지시). 책에 쓰이는 장치를 한 번씩 모아 둔
  두 쪽이다 --- `proven_c_book-<판>-style-specimen.pdf` 와 글꼴까지 심어
  혼자 도는 `…-style-specimen.html`. 웹판 옆에도 `docs/style-specimen.html`
  로 두고, 표지에서 잇는다.
  - **왜**: 디자인은 게이트가 판정할 수 없다. 그 자리를 견본이 대신하는데,
    `build/` 에만 있으면 고친 사람만 본다. 판마다 내보내면 *어느 판의 디자인이
    어땠는지*가 나중에도 두 쪽으로 확인된다.
  - 작업 순서도 이것을 따른다: `scripts/style-preview.sh` 로 0.25초 만에 견본만
    돌려 보고, 마음에 들 때에야 787쪽을 찍는다. `scripts/make-specimen.sh` 는
    `release.sh` 가 부른다.

## [v0.52.0] - 2026-08-10

### Added
- ★ **조판을 본문에서 떼어 냈다**(저자 지시, RFC-0027). "디자인 바꿀때는 본문을
  테스트할 필요가 없고 반대로 본문만 바뀌었을 때는 디자인을 테스트할 필요가 없지요."
  - **`book/style.typ`** --- PDF 조판 값의 단일 출처. 세로선 굵기·틈·여백, 제목
    밑줄 굵기와 간격, 본문 행간, 차례 간격, 시연 상자, 쪽 머리말. `lib.typ` 과
    `main.typ` 은 이제 숫자를 적지 않고 여기서 가져온다.
  - **`styles/book.css`** --- 웹 조판. 전에는 `wrap-html.py` 안의 문자열 363줄
    이었다. 파이썬 파일에는 *생성 논리*만 남았다.
  - **`book/style-specimen.typ`** + **`scripts/style-preview.sh`** --- 책의 모든
    장치를 한 번씩 담은 두 쪽짜리 견본. PDF 와 HTML 을 함께 낸다.
  - ★ **실측 248ms.** 전체 빌드 25초의 100분의 1이고, 확인할 분량은 787쪽에서
    두 쪽으로 줄었다. 디자인 반복에서 본문 게이트 열둘을 돌릴 까닭도 없어졌다.

### Changed
- `check-editions` 가 조판 견본을 세지 않는다 --- 견본은 책이 아니다.

## [v0.51.0] - 2026-08-10

### Fixed
- ★ **제목과 밑줄 사이가 벌어지고 밑줄 아래가 좁던 것을 바로잡았다**(저자 지적).
  원인은 Typst 의 간격 규칙이었다 --- 이웃한 두 블록 사이는 *`max(뒤의 above,
  앞의 below)`* 로 잡히는데, 제목 블록의 `below`(장 1.35em)가 제목과 밑줄
  *사이*로 들어가고 있었다. 밑줄의 `above` 를 아무리 줄여도 소용이 없던 까닭이다.
  - 제목 블록의 `below` 를 0.1em 으로 낮추고, 필요한 숨을 *밑줄 아래*로 옮겼다
    (장 1.25em · 절 1.00em · 항 0.85em · 소항 0.75em). 머리말·부록·장 제목 모두.
- **PDF 플랫폼 노트를 다른 장치와 같은 꼴로** 바꾸었다(저자 지시). 점선 한 줄로
  따로 놀던 것을 굵은 표지선 + 가는 본문선으로 통일했다.

### Changed
- **PDF 앞차례에 부록 A\~F 의 소제목까지 실었다**(저자 지시). 머리말 쪽 항목도
  같다.
- **차례의 행간과 글자 크기를 다듬었다**(저자 지시). 절 항목을 본문과 같은 1em
  으로 키우고 흐린 회색을 걷어 **본문과 같은 검은색**으로 바꿨다. 장 항목 위
  0.95em·아래 0.42em, 절 항목 위아래 0.42em.
- **장 내부 차례도 본문과 같은 크기·색·행간**으로 맞췄다(저자 지시). 점선만 흐리다.

## [v0.50.1] - 2026-08-10

### Fixed
- ★ **웹 판 상자의 왼쪽 선을 바로잡았다**(저자 지적 여섯 건). 뿌리는 하나였다 ---
  선을 *자식마다* 그었기 때문이다. 본문을 한 겹으로 감싸고(`.dev-body`) 그 칸에만
  그으니 여섯 가지가 한꺼번에 풀렸다.
  - 문답의 **선이 이중으로** 그어지던 것 --- 바깥 칸과 안쪽 `<p>` 에 모두 그어졌다.
  - **흔한 오해**의 본문에 선이 *없던* 것 --- 한 문단짜리 본문은 맨 글(텍스트
    노드)이라 선을 받을 자식이 아예 없었다.
  - 문단이 여럿일 때 **선이 문단마다 끊기던** 것 --- 이제 제목과 본문 사이에서만
    떨어지고 본문 안은 하나로 이어진다.
  - 장 서두·실제 사례·플랫폼 노트·수학 상자에 **선 오른쪽 여백이 없던** 것 ---
    표지와 본문 모두 `.8rem` 을 준다.
  - **플랫폼 노트의 겹선**을 없애 다른 상자와 같은 꼴로 맞췄다(저자 확인).
- **색인의 「정확한 조판은 PDF 를 보라」 링크가 그 판의 PDF 를 바로 내려받도록**
  고쳤다(저자 지시). 전에는 `dist` 폴더를 가리켜 한 번 더 들어가야 했다.

## [v0.50.0] - 2026-08-10

### Changed
- **제목 밑줄을 글자에 붙였다**(저자 지시). 선 위 여백을 0.35em 에서 0.12em 으로
  줄여 밑줄이 글자 바로 아래에 오게 하고, 줄어든 만큼을 *선 아래* 여백(0.55em)으로
  옮겼다 --- 다음 글과의 거리는 그대로다.
- ★ **앞차례를 상세 차례로 폈다**(저자 지시). 장 제목만 싣던 것을 절까지 편다.
  양판. 1·2단계 제목을 한 번에 훑어 문서 순서로 묶는 방식으로 만든다 ---
  선택자 조합(`.after().before()`)은 판에 따라 빈 값을 돌려주어 버렸다.
- **쪽 머리말을 굵은 진한 글씨로** 바꾸고 아래 가로선을 없앴다(저자 지시).
- ★ **장 서두를 문답과 같은 꼴로** 바꾸었다(저자 지시). 굵은 왼쪽 선(표지) +
  가는 왼쪽 선(내용), 그 사이에 여백. **칸과 칸 사이의 가로선을 없앴고**,
  서두를 닫던 굵은 가로선도 걷었다 --- 이제 세로선이 영역을 말한다.
- **장 서두 차례의 글자를 본문 크기로** 키웠다(저자 지시). 표지만 1.06em 이고
  항목은 본문과 같은 1em 이다(전에는 0.94/0.88em).
- **웹 판에서 수학·흔한 오해·플랫폼·실제 사례·반례·복습의 네 변 상자를 걷고**
  왼쪽 세로선만 남겼다(저자 지시). 플랫폼은 겹선, 반례는 점선으로 갈래를 구별한다.

### Unchanged (일부러)
- ★ **소스 코드와 실행 결과는 그대로 상자다**(저자 지시). 파일 이름 칸이 코드
  칸에 붙어 있는 옛 모양도 그대로 --- 양판 모두 손대지 않았다.

## [v0.49.0] - 2026-08-10

### Changed
- ★ **상자를 걷고 왼쪽 세로선으로 바꾸었다**(저자 지시). 양판 모두.
  - 네 변 상자는 좌우 안쪽 여백이 *양쪽에서* 폭을 갉았다. 이제 왼쪽 세로선
    하나로 영역만 표시하고 오른쪽은 열어 둔다 --- 본문과 같은 폭을 쓴다.
  - ★ **제목 쪽 선이 내용 쪽보다 굵다**(PDF 3.2pt 대 1.2pt, 웹 4px 대 2px).
    문답의 「문」과 「답」이 굵기로 갈린다.
  - ★ **두 선은 서로 붙지 않는다** --- 사이에 여백을 두어 이어진 하나가 아니라
    떨어진 둘로 보이게 했다.
  - 대상: 문답·심화 문답·흔한 오해·실제 사례·반례·플랫폼 노트·복습 정리·
    장 서두 4종.
- ★ **제목은 전폭 밑줄로 표시한다**(저자 지시). 수준마다 두께가 다르다 ---
  장 2.0pt · 절 1.0pt · 항 0.5pt · 소항 0.4pt. 웹도 같은 규율(1.5px·1px).
  - 장치는 세로선으로 *영역*을 말하고 제목은 가로선으로 *경계*를 말한다 ---
    하는 일이 다르므로 표시도 갈랐다.
- **PDF 각 쪽에 머리말을 넣었다**(저자 지시). 왼쪽에 `Proven C Book`,
  오른쪽에 판 번호. ★ 표지와 부 제목 쪽에서는 뺐다 --- 그 쪽들은 그 자체가
  표제라 겹친다. 763/776쪽에 들어간다.

### Reverted
- **웹 본문 폭 고정(v0.48.0 의 `--measure: 40rem`)을 되돌렸다**(저자 지시:
  "본문 폭 고정 때문에 모양 깨졌음"). 화면 가로를 그대로 쓴다.
  줄 길이 문제는 RFC-0026 D4 의 *독자가 고르는 폭*으로 다시 다룬다.

## [v0.48.1] - 2026-08-10

### Changed
- **웹 판 상단 바를 손봤다**(저자 지시). 항목이 왼쪽부터 죽 이어지고, 좁아지면
  두세 줄로 개행한다 --- 전에는 가운데 여백이 뒤쪽 항목을 오른쪽 끝으로 밀어
  두 덩이로 갈라져 있었다.
  - 「이 장의 차례」 단추 글자를 *지금 있는 자리*로 바꿨다 --- 「59장 ▾」·
    「부록 A ▾」·「머리말 ▾」(영어판은 `ch. 59`·`app. A`). 짧아지고, 어디인지도
    함께 알려 준다.
  - `English`→`En`, `한국어`→`Ko`, `GitHub`→`Git`.
  - 바에서 「상세 차례」로 바로 갈 수 있다.
  - 절이 없는 자리(상세 차례 페이지)에서는 누를 것이 없으므로 단추 대신 자리
    이름만 표시한다.

## [v0.48.0] - 2026-08-10

### Fixed
- ★ **웹 판의 한 줄이 너무 길었다**(RFC-0026 D1). 독자 평 「가독성이 지나치게
  떨어집니다」를 받고 재 보니 1920px 창에서 한 줄이 *한글 115자*였다 --- 권장
  35\~45자의 두세 배다. 본문 폭을 `40rem` 으로 가뒀다.
  - 다만 *본문만* 가둔다. 코드 블록·표·도해·차례는 넓을수록 좋아 `72rem` 까지
    허용한다. 예전의 「폭을 가두지 않는다」가 틀린 것이 아니라, 그 하나를 전부에
    적용한 것이 틀렸다.
- ★ **웹 판이 글꼴을 싣지 않고 있었다**(RFC-0026 D2). `@font-face` 가 **0개**라
  CSS 가 적어 둔 이름이 방문자 기계에 없으면 대체 글꼴로 떨어졌다 --- 세리프 본문이
  대부분의 독자에게 고딕으로 보였을 것이다. *우리가 본 화면과 독자가 본 화면이
  달랐다.*
  - 이 책이 쓰는 갈래는 셋뿐이다(serif 본문 · sans-serif 제목 · monospace 코드).
    그 셋을 `woff2` 로 자체 호스팅한다. 쓰인 글자만 남겨 **합계 783KB** 다.
  - `scripts/make-webfonts.sh` 가 만들고 `build-html.sh` 가 이어서 부른다.
    Noto·D2Coding 모두 OFL 1.1 이라 재배포가 되고, 라이선스 안내를 함께 싣는다.
  - `font-family` 선언을 전수 감사했다 --- 모두 `serif`·`sans-serif`·`monospace`
    로 끝나 실린 글꼴이 없어도 브라우저가 알맞게 고른다.

### Changed
- **상세 차례에 부록의 절도 나온다**(저자 지시). 부록의 절 제목은 번호가 없어
  빠져 있었다. 번호 없는 제목도 받도록 고쳐 부록 절 **68개**가 들어왔다
  (상세 차례 전체 **766개**).

## [v0.47.0] - 2026-08-10

### Added
- **웹 판에 「상세 차례」 페이지를 만들었다**(저자 지시). 색인의 머리말 바로 다음
  줄에서 들어간다. 장 아래 *절과 항까지* 펼쳐 절 항목 **698개**를 링크로 건다.
  - 조판된 `h3`·`h4` 에는 번호가 있었으나 `id` 가 없어 링크할 수 없었다. 번호를
    열쇠로 `id` 를 달아(`s59-4-2` 꼴) 상세 차례와 아래의 패널이 함께 쓴다.
- **웹 판의 각 장 상단 바에 「이 장의 차례」 단추를 넣었다**(저자 지시). 누르면
  그 장의 절 목록이 패널로 열리고, 항목을 고르면 닫히며 그 자리로 간다.
  Esc 로도 닫힌다. 인쇄에는 나오지 않는다.
- **PDF 판에서 장 제목 바로 밑, 서두 상자 앞에 「이 장의 차례」를 넣었다**
  (저자 지시). 그 장의 절·항을 쪽 번호와 함께 늘어놓는다. 서두 정형이 여섯이
  되었고 게이트도 그에 맞췄다. ★ 웹 판에는 내지 않는다 --- 위의 두 장치와
  겹치기 때문이다.

### Changed
- ★ **인라인 코드의 크기를 재서 고쳤다**(저자 지적: "`<stdio.h>` 쓰는 부분이 다른
  부분 글꼴에 비해 조금 작은데").
  - *실제로 작았다.* `<stdio.h>` 를 D2Coding 0.96em 으로 찍으면 같은 문자열을
    본문 글꼴로 찍은 것보다 **10% 낮다**. 한국어판은 1.06em, 영어판은 1.00em 으로
    맞췄다(영어판은 Noto Serif 와 Noto Sans Mono 의 x높이가 정확히 같다).
  - ★ 글꼴을 바꿔서 될 일이 아니었다 --- D2Coding 이든 Noto Sans Mono 든 본문에
    맞추는 배율이 1.065 로 *같았다.* 크기의 문제였다.
  - ★ 그리고 한국어판이 D2Coding 을 계속 쓰는 이유가 측정으로 분명해졌다:
    **Noto Sans Mono 에는 한글이 한 글자도 없고**(cmap 을 읽어 확인), 예제 160개
    가운데 **153개에 한글 주석**이 있다. D2Coding 은 한글이 라틴의 *정확히 2.00배*
    라 칸이 맞는데, 대체 글꼴로 넘어가면 1.533배가 되어 코드의 열이 어긋난다.
  - 코드 *블록*은 0.96em 그대로 둔다 --- 블록은 본문과 나란히 놓이지 않고, 키우면
    80칸 줄이 38.4em 에서 42.6em 으로 넓어져 넘칠 위험이 있다.

## [v0.46.0] - 2026-08-10

### Added
- ★ **79장 「갈래를 나누어 돌리기 --- `<threads.h>`」를 신설했다**(RFC-0024).
  Modern C 와의 내용 비교에서 드러난 우리 책의 가장 큰 공백이었다 --- 세어 보니
  `thrd_create` 0건, `mtx_` 0건이었다. 양판.
  - 만들기·기다리기(`thrd_*`), 상호 배제(`mtx_*`), 조건 변수(`cnd_*`),
    한 번만(`call_once`), 갈래마다 따로(`thread_local`·`tss_*`).
  - ★ **경쟁을 실측으로 보인다** --- 갈래 넷이 20만 번씩 더해 80만이어야 하는데
    23만\~29만이 나왔다. 그리고 *처음에는 재현되지 않았다*: 컴파일러가 루프를
    레지스터 하나로 접어 경쟁할 틈을 없앤 것이다. 그 사정까지 실제 사례 상자로
    실었다(`volatile` 은 고치려고가 아니라 *틈을 만들려고* 붙였다).
  - ★ 논지: **왜 실무는 여전히 `pthread` 를 쓰는가.** ①표준이 「없어도 좋다」고
    적었고 ②glibc 는 2018년 2.28 에야 넣었으며(NEWS 인용) ③macOS 는 헤더도
    탐지 매크로도 주지 않고 ④기능이 얇다. 78장의 부속서 K 에 이은 「표준에
    들어갔다고 이기는 것은 아니다」의 두 번째 사례로 이었다.
  - C11·POSIX·Win32 대응표를 실어, 이 어휘를 알면 다른 API 가 *같은 것의 다른
    철자*로 읽히게 했다.
- **부록 F 「표준 라이브러리 요람」 착수**(RFC-0025 L0+L1). 모든 함수·매크로·타입에
  대해 인자·반환·하는 일·함정을 적는 전수 표다.
  - `scripts/lib-inventory.py` --- C23 부속서 B 에서 인벤토리를 뽑는다(헤더 29 ·
    함수 571행 · 매크로 312 · 타입 87). 표준 문서는 저장소 밖 경로로 받는다.
  - `scripts/check-library-tables.py` --- 빠진 이름을 잡는 완전성 게이트. 미착수
    헤더는 `docs/library-todo.tsv` 에 명시하고, 다 쓰면 그 파일이 빈다.
  - L1 로 `string.h`(36) · `ctype.h`(14) · `stdckdint.h`(3) 를 썼다. 양판.
  - 릴리스 게이트가 12개에서 **13개**가 되었다.

### Changed
- **79\~97장이 80\~98장으로 밀렸다**(새 79장 삽입). 장 파일·예제 디렉터리·본문
  상호 참조·예제 경로를 함께 옮겼다.

## [v0.45.0] - 2026-08-10

### Added
- **9장에 「알파벳이 연속이 아니다」가 코드에 하는 일을 넣었다**(저자 지시). 양판.
  - 누구나 쓰는 `c >= 'A' && c <= 'Z'` 가 EBCDIC 에서 무엇을 하는지 *실제 표
    (코드 페이지 037)를 재어* 보였다. 알파벳이 세 토막(0xC1\~0xC9, 0xD1\~0xD9,
    0xE2\~0xE9)이라, `'A'` \~ `'Z'` 범위는 41칸인데 글자는 26개뿐이다.
  - ★ 나머지 **15칸**에 무엇이 앉아 있는지 세었다 --- 악센트 글자들, 그리고
    **`}` 와 `\`**. 즉 저 한 줄은 EBCDIC 에서 *닫는 중괄호와 백슬래시를 대문자라고
    대답한다.* 경고도 오류도 없이 조용히 틀린다.
  - 순서까지 뒤집혀 있다 --- EBCDIC 에서는 `'a'` \< `'A'` 이고 `'0'` \> `'Z'` 다.
  - ★ **표준은 숫자 열 개의 연속만 보장한다**(§5.2.1). 알파벳에는 같은 문장이
    없다 --- 표준을 만든 사람들이 EBCDIC 을 알고 있었다는 증거다. 그래서
    `c - '0'` 은 어디서나 옳고 `c - 'A'` 는 옳지 않다.
  - 이어서 *왜 라이브러리와 추상이 필요한가*를 논했다. `isupper` 가 파는 것은
    기능이 아니라 **변하는 것을 한 자리에 가두는 일**이고, 같은 논리가 42·69·72장
    에서 되풀이된다. 「EBCDIC 은 박물관 물건 아닌가」라는 반문에도 답을 달았다.

## [v0.44.0] - 2026-08-10

### Added
- ★ **장 서두에 「이 장의 필요성과 맥락」 칸을 신설했다**(저자 지시). 97개 장 ×
  양판 = **194 편**을 모두 새로 썼다.
  - 서두 정형이 넷에서 **다섯**이 되었다 --- 기댄 것 → 인출 → **맥락** → 이 장이
    끝나면 → 질문. 새 칸과 「이 장이 끝나면」이 짝이다: *자리를 대고 나서 얻을
    것을 약속한다.*
  - 쓰는 방향은 저자가 정해 준 그대로다 --- 이 장이 왜 필요한지, 그리고 **왜 하필
    이 순서, 책의 이 자리에서** 설명되는지. 그래서 각 편이 앞 장이 남긴 빚, 이
    장이 없으면 못 하는 일, 더 일찍·더 늦게 놓을 수 없는 이유를 짚는다.
  - `check-chapter-openings.py` 를 다섯 장치 기준으로 고쳐 정형을 강제한다.
- **35장에 `size_t` 를 정면으로 다루는 절을 넣었다**(저자 지시). `sizeof` 의 결과
  타입이 계약이라는 것부터, 부호가 없어서 생기는 비교 사고(`-1 < sizeof arr` 가
  거짓임을 실측), `size_t` 와 포인터 크기가 같다는 오해, 그리고 *「몇 개인가」는
  `size_t`·「얼마나 떨어졌는가」는 `ptrdiff_t`* 라는 가름까지.
- **38장에 「허용되는 포인터 연산의 꼴」 표와 세 정수 타입 절을 넣었다**(저자 지시).
  - ★ 저자의 기억(포인터±정수, 포인터−포인터 세 가지)이 맞다 --- 다만 **넷**이다.
    `정수 + 포인터` 도 되기 때문이고, 그러면서 **`정수 − 포인터` 는 안 된다.**
    표준이 덧셈은 「한쪽이 포인터」로, 뺄셈은 「*왼쪽이* 포인터」로 적어 두었다(§6.5.7).
  - 금지된 네 꼴(`5 - p`·`p + q`·`void *` 산술·함수 포인터 산술)을 컴파일러에 물어
    진단 문구까지 실었다. 뒤의 둘은 조문의 「*완전 객체 타입*」 단서에서 나온다.
  - `ptrdiff_t`·`intptr_t`·`uintptr_t` 가 *서로 다른 질문에 답한다*는 것과,
    「주소를 정수로 바꿔 빼기」가 왜 컴파일러의 감시를 스스로 끄는 일인지.

### Changed
- **26장에 `char` 와 `int` 의 비대칭을 넣었다**(저자 제안, 검토 결과 옳음).
  `int` 와 `signed int` 는 *같은 타입*이지만 `char`·`signed char`·`unsigned char`
  는 *세 타입*이다. 근거는 표준의 타입 지정자 목록(§6.7.3.1) --- 한 항목에 함께
  적힌 철자는 같은 타입인데, 문자 타입만 세 항목으로 흩어져 있다. `_Generic` 에
  `int` 와 `signed int` 를 같이 적으면 「두 호환 타입」이라며 컴파일 오류가 나는
  것으로 확인했고, 포인터 호환성 차이도 실측했다.
  - ★ 예외가 하나 있다 --- **비트필드**. 맨 `int` 비트필드의 부호는 구현이 정한다.
- **26장의 갈래 용어에 영어 원문을 병기했다**(저자 지시, 한국어판만) --- 객체·함수
  타입, 완전·불완전 타입, 정수·실수·산술·스칼라·집합체 타입, 파생 타입, 파생
  선언자 타입. 새로 등록된 용어 6개에 색인 표시도 함께 달았다.

### Fixed
- ★ **26.7.1 의 질문과 답이 어긋나 있었다**(저자 지적). 질문은 「`uint32_t` 가 없는
  기계가 있는가」인데 답은 처음부터 끝까지 `uint8_t` 이야기였다. 질문을 `uint8_t`
  로 바로잡고, 겸해 「선택 사항」이라는 말이 절반만 맞다는 것을 표준 원문으로
  보였다 --- *그런 타입이 기계에 있으면 정의해야 한다*(§7.22.1.1). 폭이 넓을수록
  없을 위험이 작다는 것도 함께 적었다.

## [v0.43.0] - 2026-08-10

### Added
- **59장에 「범용 함수 포인터 하나로 두면 되지 않나」 문답을 넣었다**(저자 제안
  `typedef void *(*generic_func_ptr_t)(void);` 검토). 결론은 *착상이 옳다* 이고,
  그 근거와 단서를 표준 원문과 실측으로 적었다. 양판.
  - ★ 흔한 설명 「함수 포인터는 어차피 다 같은 크기」는 **표준에 없다.** §6.2.5 는
    같은 표현을 약속하는 대상으로 *구조체 포인터끼리*·*공용체 포인터끼리*만 들고,
    「그 밖의 타입에 대한 포인터는 같지 않을 수 있다」고 못박는다.
  - 진짜 근거는 §6.3.2.3 이고 이쪽이 더 세다 --- *표현*이 아니라 *값*의 약속이라,
    표현이 다른 하버드 구조에서도 왕복이 성립한다. 객체 포인터 왕복에 붙는
    「정렬」 단서조차 함수 포인터에는 없다.
  - 철자 선택을 셋 재어 표로 실었다. `void (*)(void)` 만 GCC 가 범용으로 인정해
    `-Wcast-function-type` 에서 빼 주고(제안하신 `void *(*)(void)` 는 캐스트마다
    경고), 그 둘은 **캐스트를 깜빡하고 그냥 호출해도 컴파일된다**(조용한 UB).
  - ★ FreeBSD 헤더의 실물이 답을 보여 준다 ---
    `typedef void (*dlfunc_t)(struct __dlfunc_arg);`. 매개변수가 사설 구조체라
    *깜빡 호출이 아예 컴파일되지 않는다.* 좋은 범용 타입의 조건은 하나 더 있다:
    **그 자체로는 호출할 수 없어야 한다.**

### Changed
- **59장의 겹치던 두 상자를 하나로 합쳤다**(저자 지적). 실제 사례
  「POSIX 가 표준과 어긋난 자리 — `dlsym`」과 플랫폼 노트 「POSIX 는 이 구멍을
  메운다」가 같은 이야기를 두 번 하고 있었다. 앞 절은 본문 두 문장으로 넘기고,
  상자 하나를 재설계 문답 바로 앞에 두어 `dlsym` 의 실타래를 한 줄로 이었다.
- **59장 「왜 `%p`에 넘길 수 없는가」를 두 겹에서 세 겹으로** 고쳤다(저자 지적).
  빠져 있던 고리가 *가변 인자는 변환을 대신해 주지 않는다*(기본 인자 승격은
  포인터에 없다)이고, 마지막에 **함수 포인터가 객체 포인터와 독립된 계열**이라
  `void *` 가 함수 주소를 온전히 담는다는 보장조차 없음을 명시했다.
- **85장 제목을 「시작하기 — 설치할 것이 없다」에서 「proven 시작하기」로**
  바꾸었다(저자 지시). 영어판도 `Getting started with proven` 으로 맞췄다.

### Fixed
- ★ **한국어판 59장의 `dlsym` 관용구가 컴파일되지 않는 코드였다** ---
  `*(void *)&fn = …` 은 `void *` 를 역참조하는 꼴이다. GCC 가
  *"dereferencing 'void *' pointer"* 로 경고한다. `*(void **)&fn` 으로 고쳤다.
  (영어판은 원래 맞게 적혀 있었다 --- 한쪽만 틀어져 있었다.)

## [v0.42.1] - 2026-08-10

### Changed
- **제12부의 이름을 「proven — 검증된 기본기」에서 「proven — 새로운 안정적인
  기반」으로 바꾸었다**(저자 지시).
  - 영어판 제목도 그 문구를 그대로 옮긴 것이었으므로 함께 맞췄다 ---
    `Part XII — proven — fundamentals, verified` → `a new, stable foundation`.
  - 목차·러닝헤드·README 의 부 목록·`docs/DEPENDENCIES{,-en}.md` 까지 따라간다.

## [v0.42.0] - 2026-08-10

### Added
- **59장에 「오늘 POSIX 를 다시 만든다면 `dlsym` 을 어떻게 설계할까」를 넣었다**
  (저자 요청). 「플랫폼 노트: POSIX 는 이 구멍을 메운다」 바로 다음 자리다. 양판.
  - POSIX 규격이 스스로 「ISO C 가 정의하지 않는 변환을 이 표준은 요구한다」고
    적어 둔 대목을 인용했다 — 규격이 **땜질임을 자백한** 드문 문장이다.
  - 답은 이미 구현되어 있다 — FreeBSD 의 `dlfunc()`. 반환 타입을 함수 포인터
    쪽에 두되 **그 타입이 무엇인지는 일부러 미명세**로 남긴다. 그래야 호출자가
    자기 서명으로 한 번만 캐스트하고, 그 캐스트는 함수 포인터끼리라 표준 안이다.
  - ★ 왜 표준이 되지 못했는지도 **1차 자료**로 적었다. 2002-05-29 의 FreeBSD 커밋
    두 개(`dc12134a8`, `fda230194`)가 남아 있고, 뒤엣것은 「내가 오스틴 그룹에
    제안한 것과 맞추려고」라고 적었다. 선례를 만들려던 시도가 퍼지지 않아 24년째
    규격 밖이다 — *더 나은 설계가 아니라 이미 퍼진 것이 남는다.*

### Changed
- **예제가 찍는 입출력 메시지를 전부 영어로 바꾸었다**(저자 지적: "소스 코드에서
  주석을 제외하면 모든 입출력 메시지는 영어여야 합니다").
  - ★ 지적하신 `examples/ch59/print_funcptr.c` 하나가 아니었다. 재 보니
    **예제 160개 중 96개, 문자열 1024건**이 한국어였다. 1001건을 영어로 옮겼다.
  - 주석은 한국어 그대로 둔다 — 한국어판 독자가 읽는 설명이기 때문이다.
  - 남긴 것은 **한글이 실험 대상 자체**인 39건뿐이다(문자 집합·와이드 문자·
    UTF-8 훑기·조합형 인코딩·바이트 폭 시연). 그 표본을 영어로 바꾸면 예제가
    증명하려던 것이 사라진다. 파일 단위로 `docs/example-hangul-allow.tsv` 에 적었다.
  - 예제 전부 다시 컴파일·실행해 초록을 확인했고, 캡처 출력에 한글은 0 이다.
- ★ **그러다 영어판 예제에서 번역이 빠진 한국어 메시지 한 건을 찾았다** —
  `examples-en/ch40/arrptr.c` 의 「원소 수: …」. 영어판은 `examples-en/` 이라는
  자체 트리를 쓰는데, 새 검사를 두 트리 모두에 걸었더니 그 하나가 드러났다.
  (그래서 이 판의 CHANGELOG 초고에 적었던 「두 판이 같은 예제를 함께 쓴다」는
  틀린 서술이다. 두 트리는 별개이고, 영어판은 원래도 영어였다.)

### Fixed
- ★ **굵게 하려던 글자 360군데가 실제로는 굵지 않았다.** 마크다운 습관으로
  `**굵게**` 라고 쓴 자리들이다. Typst 에서 `*` 는 강조를 여닫는 기호라 별 둘은
  *빈 강조*가 되고 글자는 그대로 남는다. 2·4·9·24·26·40·44·45·47·49·54·55·59·95장,
  양판 합쳐 360건.
  - **조판은 성공하고 있었다.** Typst 가 `no text within stars` 로 줄·칸까지 찍어
    계속 알려 주었는데, 게이트 열 개 중 **아무도 경고를 보지 않았다.**

### Added (gates)
- `scripts/check-typst-warnings.sh` — 양쪽 판의 조판 경고를 **0** 으로 못박는다.
  성공하는 빌드가 틀린 결과를 감추지 못하게 하는 최종 방어선이다.
- `scripts/check-example-messages.py` — 예제의 문자열 리터럴에 든 한글을 잡는다.
  표본 예외는 `docs/example-hangul-allow.tsv` 에 파일 단위로 올린다.
- 릴리스 게이트가 10개에서 **12개**가 되었다.

## [v0.41.1] - 2026-08-09

### Fixed
- **개념도에서 글자가 상자를 뚫고 나오던 것을 고쳤다**(저자 지적: "그림 26.1 에서
  박스에서 그 내부의 글이 삐져 나와요 … 특히 integer types 밑의 박스들").
  - ★ **눈으로 찾지 않고 재서 찾았다.** 조판에 쓰는 실제 글꼴(Noto Sans CJK KR)로
    개념도 28개의 글자 폭과 상자 폭을 전부 대조했더니 **9건**이 나왔다 —
    지적하신 26.1 의 정수 타입 상자 넷 말고도 **그림 8.1(IEEE 754)의 「부호」·`sign`
    라벨 4건**이 더 있었다. 양판 모두.
  - **원인은 하나다 — 상자 폭이 `150` 처럼 손으로 적힌 수였다.** 라벨이 길어지거나
    번역이 길어져도 아무도 알려 주지 않았다. 영어판 `signed integer types (short,
    int, long…)` 는 상자보다 **15.5px** 나 길었다.
  - 처방 ① `make-figures.py` 에 **`text_width()`·`fit_width()`** 를 넣어 **글자를
    재서 상자를 만든다.** 그림 26.1 의 정수·파생 상자가 이제 라벨에 맞춰 넓어진다.
    판마다 그림을 따로 만들므로 폭도 그 판의 라벨에 맞는다.
  - 처방 ② 그림 8.1 의 부호 비트는 32비트 중 1비트라 **칸이 이름보다 좁을 수밖에
    없다.** 그런 칸은 이름을 **칸 위로 빼고 지시선**을 달도록 했다. 두 줄 간격도
    함께 넓혔다.
  - ★ 글꼴이나 PIL 이 없는 환경에서는 어림값으로 물러서되 **그 사실을 인쇄한다** —
    조용히 통과하는 검사는 없느니만 못하다.

### Added
- **`scripts/check-figures.py` 신설 — 결과물을 다시 재는 최종 방어선.**
  만드는 쪽(`fit_width`)만 고치면 다음에 좌표를 손으로 적는 그림이 하나 생기는
  순간 되돌아간다. 그래서 **생성된 SVG 를 실제 글꼴로 다시 재어** 넘친 곳이 있으면
  배포를 막는다. `release.sh` 게이트에 등록했다.
  - 일부러 상자를 좁혀 넣어 **검사가 실제로 잡는지 확인**했다(고장 주입 1건 → 검출).

## [v0.41.0] - 2026-08-09

### Changed
- **「지도」라는 비유를 걷어내고 갈래마다 제 낱말을 주었다**(저자 지시: "'지도'라는
  낱말이 한국인한테 잘 먹히는 개념은 아니다 … 차후 버전에도 지속적으로 영향을
  끼치게").
  - ★ **조사해 보니 번역투보다 큰 문제가 따로 있었다.** 한국어판 명사 「지도」 76건,
    영어판 `map` 101건이 **서로 다른 네 가지**를 가리키고 있었다 — 주소 공간의
    *공간 배치*, 표준이 타입을 가른 *분류*, 컴파일러·라이브러리의 *판세*, 앞으로 갈
    길의 *길잡이*. 게다가 18·61장 제목은 「지형」인데 본문은 같은 것을 「지도」라 불러
    **인접한 장에서 두 비유가 뒤섞여** 있었다.
  - 그래서 하나로 바꾸지 않고 **네 갈래로 갈랐다.**

    | 무엇 | 한국어 | 영어 |
    |---|---|---|
    | 공간에 놓인 것 | **배치**(도) | `layout` |
    | 갈래로 나뉜 것 | **갈래** | `families` |
    | 판세를 훑는 것 | **지형** | `landscape` |
    | 먼저 그려 두는 큰 틀 | **밑그림**·**길잡이** | `sketch`, `guide` |
    | 늘어놓고 훑는 것 | **한눈에 보기** | `at a glance` |

  - 원칙 한 줄 — ★ **「지도」는 *지리*를 말할 때만 쓴다. C 에는 지리가 없다.**
  - 저자가 제안한 세 낱말을 평가해 **「밑그림」을 채택**했다(2장의 「이 밑그림을 먼저
    그려 두면 이후의 이야기가 전부 제자리를 찾는다」가 그 자리다). 「개요」는 이 책의
    문체에서 관공서 말투로 읽혀 표제어로만 아꼈고, 「개괄」은 `organizer` 장치와
    겹쳐 쓰지 않았다.
  - **장 제목 셋**을 바꾸고 그 참조를 전수 갱신했다(양판).
    - 26장 타입의 ~~지도~~ → **갈래** — 부제 「표준이 *가른* 것들」과 이제 한 말을 한다
    - 62장 표준 라이브러리 전체 ~~지도~~ → **한눈에 보기**
    - 82장 프로그램의 기억 ~~지도~~ → **배치**
  - 2장의 절 제목도 다듬었다 — 「이 지도는 「대개」의 지도다」 → **「이 밑그림이
    통하는 데까지」**.

### Added
- **`scripts/check-metaphor.py` 신설 — 이 규율을 게이트로 굳혔다.** 저자 지시의
  "차후 버전에도 지속적으로 영향"이 이 항목이다. 문서로만 남기면 다음 집필에서 잊힌다.
  - 예외는 `docs/metaphor-allow.tsv` 에만 올릴 수 있고, **왜 옳은지를 함께 적게** 했다.
  - `release.sh` 게이트에 넣었다. 이제 「지도」가 되살아나면 배포가 막힌다.
  - ★ 정규식에 함정 둘을 담았다(둘 다 실제로 밟았다). ① 한국어 **「-지도」는 조사**다
    — 문자열 118건 중 **65건이 「읽히지도」·「나머지도」 같은 조사**였다. 일괄 치환했다면
    문장이 망가졌다. ② **Typst 마크업이 낱말과 조사 사이에 낀다**(`*다른 지도*를`) —
    소박한 정규식이 한 건을 놓쳤고, 그래서 마크업을 허용하도록 고쳤다.
  - 정규식 단위 시험 7건으로 조사·마크업 처리를 확인했다.

### Fixed
- ★ **도해의 글자는 SVG 가 아니라 `scripts/make-figures.py` 가 원천**이라는 것을
  확인하고 생성기 쪽을 고쳤다. SVG 만 고쳤다면 다음 생성 때 「기억 지도」가
  되돌아왔을 것이다.
- 3장의 가상 메모리 설명에서 「프로세스마다 *다른 지도*를 보여 준다」를
  **「저마다 다른 주소 공간」**으로 바로잡았다 — 비유를 걷어내니 오히려 정확해졌다.

## [v0.40.0] - 2026-08-09

### Added
- **2장에 「이 지도는 「대개」의 지도다」 절 신설**(저자 지시: 스택-힙 구조는 OS·하드웨어
  에 따라 달라질 수 있고 실제로 다른 곳이 있으며, 다만 이 구성이 주류라는 것 —
  그리고 C 표준은 스택을 요구하지 않는다는 것).
  - ★★ **표준 원문을 기계로 훑어 확인했다 — C23 초안(N3220) 759쪽 전체에 `stack`
    이 0회, `heap` 도 0회 나온다.** 짐작이 아니라 전수 검색 결과다.
  - 표준이 대신 쓰는 낱말을 대조표로 실었다 — 스택 → *자동 저장 기간*, 정적 구역 →
    *정적 저장 기간*, 힙 → *할당된 저장 기간*, 스택 프레임 → **재귀 호출이 허용된다**.
  - ★ 결정적인 조항을 인용했다(§6.5.3.4). **「스택을 두라」가 아니라 「재귀가 되게
    하라」**다. 스택은 그 요구를 만족시키는 가장 흔한 방법일 뿐이고, 표준은 방법을
    구현에 맡기고 결과만 요구한다 — 13·14장의 추상 기계가 여기서도 작동한다.
  - ★ 실제 사례 상자 「스택이 있는데 쓸 수 없는 기계」 — 8비트 PIC. 하드웨어 스택이
    **깊이 고정**(중급 8단·개선 중급 16단)이고 **돌아갈 주소만 담아 값을 올릴 수
    없다.** 그래서 지역 변수는 컴파일러가 호출 관계를 미리 분석해 정적으로 배치한다
    (XC8 의 *컴파일된 스택*). 그 결과 함수가 **재진입하지 않으므로 자기 자신을 부를
    수 없고**, 표준 조항과 정면으로 부딪힌다. 제조사 문서를 1차 자료로 인용했다.
  - 역사 표 — 초기 메인프레임(고정 복귀 주소 칸 → 재귀 불가), 초기 포트란·코볼(정적
    지역 변수), 버로스 B5000(스택이 하드웨어), 하버드 구조, 오늘의 8비트 MCU.
  - ★ 문답으로 닫았다 — **「어떻게 생겼는가」는 대개 맞고, 「반드시 그래야 하는가」는
    아니다.** 그러므로 *지도는 이해를 위해 쓰고, 코드는 계약에 기대어 쓴다.* 스택이
    아래로 자란다고 가정하거나 두 지역 변수의 주소를 견주는 코드가 왜 안 되는지까지.
  - 참고문헌에 「기계 구조와 스택」 절을 신설해 출처를 실었다.

## [v0.39.1] - 2026-08-09

### Changed
- 인용한 문헌을 **위치가 아니라 개념으로** 가리키게 고쳤다 — 각주에서 상대 책의 장
  번호를 걷어내고 「무엇을 다룬 대목인지」로 바꿨다(양판 여덟 자리).

### Fixed
- ★ 참고문헌이 **존재하지 않는 부록 항목을 가리키고 있었다.** 거짓 참조를 걷어냈다(양판).
- ★ 참고문헌의 장 번호가 낡아 있었다 — 40장 삽입으로 59→**60장**이 되었는데
  참고문헌만 그대로였다(양판).
- 각주의 발행 연도 오타 하나.

### Added
- 참고문헌에 **한국어판 서지**를 보탰다(양판). 본문 저자명 표기도 번역서의 공식
  표기로 통일하고, 첫 각주에 한국어판 제목을 밝혔다 — 원서 제목만으로는 번역서를
  찾을 수 없기 때문이다.
- **제12부 도입부와 85장에 저장소 URL을 넣었다**(저자 지시).
  `github.com/rubidus-api/proven_c_book` — proven 소스는 그 안의 `vendor/proven/`,
  이 부의 예제는 `examples/` 에 있다.
  - ★ 넣으면서 실제 결함 하나가 드러났다. 85장이 "받아 둔 소스를 컴파일하면 그만"
    이라고만 하고 **어디서 받는지를 말하지 않고 있었다.** 이제 그 자리에서 밝힌다.

## [v0.39.0] - 2026-08-09

### Added
- **4장에 「기수의 경제성」과 3진 컴퓨터 이야기를 보탰다**(저자 제안: "가장 효율적인
  기수법은 e에 가장 가까운 자연수인 3 … 3진법 컴퓨터 연구 … 별로 맞는 말이 아니거나
  필요없으면 빼도 되요").
  - ★ **조사 결과: 맞는 말이되 전제가 붙는다.** 그리고 그 전제가 4장이 이미 하던 말
    (「2진법은 수학적 우아함이 아니라 *공학적 정직함* 때문에 이겼다」)을 훨씬 날카롭게
    만들어 주므로 빼지 않고 넣었다.
  - `mathbox` 「기수의 경제성」 — $E(b,N) = b log_b N = (b\/ln b) ln N$ 를 미분해
    $b = e approx 2.718$ 을 얻고, 정수 기수를 견준다. ★ 계산으로 확인한 것 둘:
    **2와 4가 정확히 같다**($4=2^2$ 이므로 $4\/ln4 = 2\/ln2$), 그리고 **3이 2보다
    나은 정도는 겨우 5.7%**.
  - ★ 그 5.7% 조차 늘 나타나지 않는다는 것을 표로 보였다 — 자릿수는 올림해야 하므로
    $10^3$ 에서는 2진법(20)이 3진법(21)을 **이긴다**.
  - `realcase` 「Setun — 3진법이 실제로 이겼던 잠깐」 — 1958년 모스크바 대학,
    소볼레프·브루센초프. **1959\~1965년 50대 생산, 30대가 대학에 설치.**
    ★ 핵심은 *왜 그때는 유리했는가*다: 트랜지스터가 아니라 **페라이트 자심**이라
    「음의 자화 / 자화 없음 / 양의 자화」 세 상태를 자연스럽게 가졌다. 6트릿 = 1트라이트
    ≈ 9.5비트. 끝은 기술이 아니라 행정이었다는 것(박물관 기록의 "대학의 별난 상상의
    산물", 2.5배 비싼 2진 기계로 대체)까지.
  - 균형 3진법의 아름다움 셋 — 부호 비트가 없다 / 부정 = 자리마다 부호 뒤집기
    ($13=111$, $-13="TTT"$) / 반올림 = 잘라내기. 크누스의 "아마도 모든 수 체계 중
    가장 예쁜 것" 인용.
  - ★★ **「그러면 왜 2진법이 이겼는가」 절** — 답은 *비용 모형이 틀렸다*는 것.
    「자리 하나의 비용 ∝ $b$」는 $b$ 갈래 스위치를 모형화한 것이고 페라이트 자심이
    거기 가까웠으나, 트랜지스터 회로의 비용은 부품 수가 아니라 **전압 준위를 얼마나
    확실히 구별하는가**다. 이웃 준위 간격이 $1\/(b-1)$ 로 줄어드는 것을 표로 보였다
    (2진법 100% / 3진법 50% / 10진법 11%).
  - 오개념 상자 「3진법이 최적이니 언젠가 바뀔 것이다」 — "최적"은 특정 비용 모형
    아래의 말이다. ★ 다만 연구가 죽지 않았다는 것도 정직하게: CNTFET 처럼 문턱
    전압을 소자마다 정할 수 있는 소자에서 재검토되고 있고 2024년 보고도 있다
    (**논문이지 제품이 아니라는 단서를 달았다**). 노리는 것은 자릿수가 아니라
    *배선과 전력*이다.
  - ★ 마무리에서 이 이야기를 왜 길게 했는지 밝혔다 — **깔끔한 이론이 가리키는 답과
    물건을 만들 때의 답이 다르다**는 것이 이 책에서 되풀이되는 무늬이고, C 의 여러
    결정도 그렇게 생겼다.
  - 참고문헌에 **「기수와 3진 컴퓨터」 절 신설** — 러시아 가상 컴퓨터 박물관의 Setun
    기록, Knuth TAOCP vol.2 §4.1, Science Advances 2024. 세 URL 모두 응답 확인,
    본문 각주와 짝을 맞췄다(ACM 서식).

## [v0.38.0] - 2026-08-09

### Added
- **선언 앞자리에 오는 낱말들을 갈래마다 「모아 두는 자리」를 만들었다**(저자 지시:
  "타입 퀄리파이어 였던가 const volatile register 같은거 한번에 모아서 보여주는 곳이
  있나요"). ★ 물음 자체가 이 책이 고쳐야 할 오해를 드러냈다 — **`register` 는 한정자가
  아니라 저장 클래스 지정자**다. 그래서 *하나로 뭉치지 않고*, 표준의 갈래대로 나누어
  각 갈래가 실제로 문제되는 장에 모았다(저자 결정).

  | 갈래 | 낱말 | 모아 둔 곳 |
  |---|---|---|
  | 타입 한정자 | `const`·`volatile`·`restrict`·`_Atomic` — **넷** | **26장** |
  | 저장 클래스 지정자 | `auto`·`constexpr`·`extern`·`register`·`static`·`thread_local`·`typedef` — **일곱** | **44장** |
  | 함수 지정자 | `inline`·`_Noreturn` | **24장** |
  | 정렬 지정자 | `alignas` | **47장** |

  네 자리가 서로를 가리키고, 26장의 문답이 **다섯 갈래 전체 지도**를 실어 어디로
  가야 하는지 알려 준다.

- **26장 「한정자」 절 전면 확장** — ★ **`_Atomic` 이 빠져 있던 것을 채웠다**(표가
  셋뿐이었다).
  - 반례 상자 「`register` 도 한정자 아닌가」 — 두 갈래를 표로 가르고, ★ 가르는 물음
    하나를 준다: **"`typedef` 로 뽑아낼 수 있는가."** `typedef const int ci;` 는 되고
    `typedef static int si;` 는 문법 오류다(실측).
  - ★ `_Atomic` 의 두 얼굴 — `_Atomic T`(한정자) vs `_Atomic(T)`(타입 지정자).
    그리고 **배열·함수에는 못 붙인다**(실측: `'_Atomic'-qualified array type`).
    `_Atomic int a[3]` 은 *원소마다* 원자적이라는 뜻이다.
  - 한정판은 크기·정렬을 안 바꾼다 — 다만 ★ **`_Atomic` 은 예외가 될 수 있다**
    (표준이 같은 크기를 요구하지 않는다). 실측값은 그대로였지만 계약이 아니라는 것을
    명시하고 `atomic_is_lock_free` 로 이었다.
  - 순서 자유·중복 허용(`-Wduplicate-decl-specifier`), 한정자 버리기 경고,
    `char **`→`const char **` 함정, 그리고 붙는 자리(60장 C항)로 연결.
  - 예제 `ch26/qualifiers.c` 신설(양판).

- **44장 「저장 클래스 지정자 일곱」 표로 승격** — 산문 목록이던 것을 *저장 기간·연결·
  하는 일* 네 칸 표로 다시 짰다.
  - ★ **한 선언에 이 자리는 하나뿐**이고 예외는 `thread_local` 하나임을 실측으로
    보였다(`static extern`·`typedef static`·`auto constexpr` 전부 오류).
  - `register` 는 **옮기지 않고 제자리(저장 클래스)에 제대로 놓았다**(저자 판단).
    오늘 남은 효력이 「주소를 못 얻는다」 하나뿐이라는 것과, 그것을 *일부러* 쓰는
    용법까지 적었다.
  - 문답 「그러면 `const static int x;` 는?」 — 갈래가 다르므로 합법. 이것이 두 갈래를
    가르는 시험지다.
  - 예제 `ch44/storage_class.c` 신설(양판). ★ 작성 중 `bump(), bump(), bump()` 가
    `3 2 1` 로 인쇄되는 것을 발견 — 인자 평가 순서가 미지정이라 이 책이 금지한
    무늬였다. 한 줄에 하나씩 부르도록 고쳤다.

- **24장 「선언에 붙는 낱말 하나 — `inline`」 신설**(저자 지시: "inline도 설명하는 곳이
  필요해요 함수 선언에서 처리해야 하지 않을까요 … 참조 가능 여부라던가 … 제한과,
  헤더파일 등 어디에 있어야 하나").
  - ★ **주소는 얻을 수 있다** — `inline` 은 타입의 일부가 아니다. 그러나 *얻는 순간
    외부 정의가 필요해진다.*
  - ★★ **`-O2` 에선 링크되고 `-O0` 에선 깨진다**(실측). 인라인 정의는 외부 정의를
    제공하지 않는데, 최적화기가 전부 펼쳐 버리면 실물을 찾을 일이 없기 때문이다.
    이 책이 여러 번 본 "릴리스에서만 나는 버그"의 **정반대 방향**이고, 디버그 빌드에서만
    링크가 깨지는 프로젝트의 흔한 원인이다. 네 경우를 `-O0`/`-O2` 표로 갈랐다.
  - 오해 상자 「`inline` 을 붙이면 호출이 사라진다」 — 표준은 무시해도 적합하다고
    못박는다. ★ 실제 값어치는 속도가 아니라 **「이 정의가 여러 번역 단위에 나타나도
    좋다」는 허가**이고, 그게 없으면 헤더에 몸통을 둘 수 없다.
  - **헤더 배치**를 형태별로 못박았다 — `static inline`(대부분) / `inline` 정의는
    헤더 + `extern` 선언은 소스 하나(주소가 하나여야 할 때).
  - 반례 상자 「헤더에 `inline` 정의만 두고 끝내기」 — ★ **코드를 안 고쳤는데 빌드
    설정만 바꾸면 드러나는** 결함.
  - 인라인 정의의 두 제한(수정 가능한 정적 객체 정의 금지·내부 연결 이름 참조 금지)을
    실측으로. ★ 기본이 경고이고 `-pedantic-errors` 에서 오류로 올라간다.
  - 문답 「전부 `static inline` 로 쓰면 안 되나」 — 사본 증가와 ★ **주소가 하나가
    아니라 함수 포인터 비교가 어긋난다**는 대가.
  - 예제 `ch24/inline_fn.c` 신설(양판, `-O0`/`-O2` 양쪽 검증).

- **47장 「`alignas` — 다섯째 갈래, 정렬 지정자」 신설**(저자 지시: "align 관련해서도
  한정자 같은거 뭐 있지 않나요"). 두 꼴(`alignas(상수)`·`alignas(타입)`), 짝인
  `alignof` 는 지정자가 아니라 *연산자*, C23 부터 둘 다 키워드.
  - ★ **정렬은 올릴 수만 있고 낮출 수 없다**(실측: `cannot reduce alignment`).
  - 붙일 수 없는 세 자리(매개변수·`typedef`·비트필드)를 실측 오류 메시지와 함께.
  - ★ **`typedef` 에 못 붙는다는 것이 뜻깊다** — 정렬은 *타입*이 아니라 *선언*의
    성질이라는 뜻이고, 타입의 일부인 `const` 와 정확히 대비된다.

### Changed
- 26장 요약표에 `_Atomic` 을 넣어 한정자를 넷으로 고치고, 저장 클래스·함수 지정자
  줄을 보태 「표준의 낱말 → 다루는 장」 대응을 닫았다.
- ★ 26장의 `volatile` 상세 참조가 낡아 있던 것을 바로잡았다(78·80장 → **76·77장**).

## [v0.37.2] - 2026-08-09

### Fixed
- **웹판의 출처가 문장 한가운데 끼어들던 것을 장 끝으로 모았다**(저자 지적: "각주는
  각 장 밑에 모여야 하지 않나요").
  - ★ **먼저 PDF 를 확인했다 — 그쪽은 정상이었다.** 각주 13개가 전부 해당 *쪽 하단*
    (y≈730\~765 / 842)에 자리 잡고, 참조와 본문이 같은 쪽에 있다. 상자 장치
    (`realcase`) 안에서 부른 각주도 상자에 갇히지 않고 쪽 하단으로 내려간다.
    각주(footnote)의 자리는 쪽 하단이 맞으므로 조판은 손대지 않았다.
  - **문제는 웹판이었다.** v0.37.1 에서 각주 본문이 사라지는 것을 막느라 본문 안에
    작은 글씨 괄호로 펴 두었는데, 그러면 *읽던 문장이 출처에 끊긴다.*
    웹 페이지에는 「쪽 하단」이 없으므로 옳은 자리는 **장 끝**이다.
  - 처방: `wrap-html.py` 에 **`collect_notes()`** 신설. 장 페이지를 쓸 때 본문의
    `src-note` 를 훑어 **위첨자 번호 `[1]`** 로 바꾸고, 본문은 페이지 맨 끝의
    **「주」 절**(`<section class="chapter-notes" role="doc-endnotes">`)로 옮긴다.
    번호는 *장마다 1 부터*, 앵커는 `ch40-fn-1` 처럼 페이지 이름으로 짓고,
    각 항목 끝에 본문으로 돌아가는 ↩ 링크를 단다. 영어판 표제는 "Notes".
  - ★ 여는 `<span>` 부터 짝이 맞는 `</span>` 까지 *중첩을 세어* 잘라 낸다 — 주석
    안에 `<a>`·`<code>`·`<span>` 이 들어 있어도 안전하다.
  - `check-editions.py` 의 대조 자국을 `class="src-note"` → **`class="fnref"`**
    로 바꿨다. 「본문에 남은 위첨자 참조」를 세므로, 옮기다 빠뜨리면 게이트가 잡는다.

## [v0.37.1] - 2026-08-09

### Fixed
- ★★ **웹판이 각주 본문을 통째로 잃고 있었다** — v0.37.0 을 올린 직후 라이브
  페이지를 확인하다 발견했다. PDF 에는 출처가 다 들어가 있는데 HTML 에는 없었다.
  - 원인: **Typst 0.15.1 의 HTML 내보내기가 각주 *참조*만 내보내고 *본문*을
    버린다.** 통짜 HTML 을 직접 뽑아 세어 확인했다 — `doc-noteref` 13개에
    `doc-endnote` **1개**. 장별 분할 이전 단계에서 이미 사라져 있었다.
    (Typst 스스로 "html export is under active development and incomplete" 라고
    경고하는 영역이다.)
  - 처방: `lib.typ` 에 **`note()` 장치 신설**. PDF 에서는 그대로 각주이고,
    HTML 에서는 `<span class="src-note">` 로 본문 안에 작은 글씨로 편다.
    v0.37.0 에서 넣은 출처 각주 **26곳(양판)을 전부 이 장치로 옮겼다.**
  - ★ **재발 방지**: `check-editions.py` 의 장치 대조 목록에 `note` 를 넣었다.
    앞으로 출처가 HTML 에서 사라지면 릴리스 게이트가 막는다. 아울러 이 검사가
    `#장치(` 형태만 세고 `#장치[` 형태를 놓치던 것도 함께 고쳤다.

## [v0.37.0] - 2026-08-09

### Added
- **본문의 일화에 1차 자료 URL을 달았다**(저자 지시: "일화 같은게 있다면 인터넷
  자료에서 그 URL을 찾아 참고문헌에 … 인용 형식 하나 정해서 그걸 그대로 엄격하게
  지키면서. 각주로도 넣고 참고문헌으로도").
  - ★ **인용 서식을 참고문헌 첫머리에 못박았다** — *ACM 참고문헌 서식*. 자료 종류
    여섯(책·학술 발표·기술 보고서·규격·보안 권고/CVE·온라인 글)마다 적는 순서를
    표로 고정했고, 세 규칙을 명시했다: *연도는 그 판의 연도 · URL 은 되도록 1차
    자료 · 본문 각주와 목록이 같은 자료를 가리킨다.* 항목 끝에 인용한 장을 적는다.
  - ★ **여덟 건 전부 URL 을 실제로 확인하고 실었다**(HTTP 200 확인). 검색 결과를
    그대로 믿지 않고 응답 코드를 확인했으며, **죽은 URL 하나는 교체했다** —
    `debian.org/security/2008/dsa-1571` 이 404 라 공식 발표 메일
    (`lists.debian.org/debian-security-announce/2008/msg00152.html`)로 바꾸고,
    추적 항목(`security-tracker.debian.org`)을 함께 달았다.
  - 실은 자료 목록:
    - **아리안 5** — ARIANE 5 Flight 501 Failure: Report by the Inquiry Board.
      1996. ESA/CNES. (29장)
    - **goto fail** — CVE-2014-1266. 2014. NVD, NIST. (31장)
    - **모리스 웜** — Eugene H. Spafford. 1988. *The Internet Worm Program: An
      Analysis*. CSD-TR-823, Purdue University. ★ 저자 본인이 올려 둔 사본. (42·64장)
    - **형식 문자열** — Tim Newsham. 2000. *Format String Attacks*. Guardent.
      + 분류는 CWE-134. (61·64장)
    - **알고리즘 복잡도 공격** — Crosby and Wallach. 2003. USENIX Security. (84·91장)
    - **데비안 OpenSSL 난수** — DSA-1571-1 (CVE-2008-0166). 2008. (92장)
    - **커널 VLA 제거** — Kees Cook. 2018. LKML + LWN 배경 기사. (38장)
    - **커널 `-fno-strict-aliasing`** — 커널 최상위 `Makefile` 자체를 1차 확인처로.
      ★ "리누스가 어디선가 말했다"가 아니라 *지금도 그렇게 빌드된다는 사실*을
      확인할 수 있는 자리를 가리켰다. (13·17장)
  - 본문 여덟 자리에 각주를 달았고(양판 16곳), 참고문헌의 「이 책이 인용한 사건들」
    절을 서식에 맞춰 전면 재작성해 각 항목에 *무슨 일이었는지* 한두 줄을 붙였다.

## [v0.36.0] - 2026-08-09

### Added
- **40장 「배열과 포인터 — 언제 같고 언제 다른가」 신설**(저자 지시: "배열과
  포인터의 올바른 관계 라는 장이 필요할 것 같아요 … 그 필요성과 함께 아주 길게").
  39장 뒤·41장(반복의 기법) 앞에 끼웠고, 41장 이후는 하나씩 밀렸다(96→97장).
  - ★ **새 문법은 하나도 없다.** 35·38·39·59장에 흩어져 있던 조각을 한자리에 모아
    *규칙으로 닫는* 종합 장이다. 저자 지시대로 「왜 이 장이 필요한가」를 §1 통째로
    적었다 — 학습 경로가 「교환되는 문맥(함수 인자)」을 가장 많이 연습시키기 때문에
    오해가 굳고, *파일 경계를 넘는 순간* 처음 깨진다.
  - **다섯 축의 대조** — 무엇을 담는가 / 접근에 몇 걸음 / 이름이 있는가 / 크기를
    아는가 / 어디에 쓰나.
  - ★ **접근의 걸음 수를 어셈블리로 보였다**(실측, `gcc -O2` + `objdump`).
    배열은 `lea` 뒤 `mov` 하나 = 메모리 접근 **1회**, 포인터는 앞에 `mov`가 하나 더
    붙어 **2회**. 「배열은 데이터를, 포인터는 데이터의 주소를 담는다」가 명령 하나로
    드러난다.
  - ★ **파일을 건너뛰는 사고를 실물로 재현**했다 — 정의 `char msg[16]` / 선언
    `extern char *msg`. **컴파일 무경고 → 링크 무경고 → Segmentation fault(139)**.
    문자 `'A'`\~`'H'` 여덟 바이트가 주소 `0x4847464544434241`로 읽힌 것이다.
    처방은 반례 상자로 — *선언은 헤더에 한 번, 정의하는 쪽도 그 헤더를 포함한다.*
  - **무너짐의 세 규칙**과 **무너지지 않는 자리**(`sizeof`·`&`·`alignof`·`typeof`·
    문자열 리터럴 초기화). `char a[]="…"`와 `char *p="…"`의 차이도 여기서 닫았다.
  - `6[a]`가 합법인 이유 — *첨자는 배열의 문법이 아니라 포인터의 문법*이라는 증명.
  - ★ **매개변수 변환은 한 겹만 일어난다** — 배열의 배열은 「포인터의 포인터」가
    아니라 **「배열에 대한 포인터」**가 된다. 그래서 `main`의 둘째 인자가
    `char **argv`인 것이다(`argv`가 *포인터의 배열*이므로).
  - ★ **C가 못 하던 것과 C99의 답** — 임의 크기 다차원 매개변수. 우회 셋(포인터
    배열·평탄화·고정)과 각각의 대가를 적고, `int m[rows][cols]`가 이것을 조건부로
    해결했음을 실측으로 보였다(같은 함수가 `[2][3]`과 `[2][4]`를 모두 받는다).
    MSVC 미지원·C11 선택 기능·커널의 VLA 제거까지 정직하게 달았다.
  - 예제 2개 신설(양판) — `ch40/arrptr.c`, `ch40/params.c`.
  - ★ `-Wsizeof-array-argument`를 예제 안에서 만나게 하고, 왜 그 경고가 옳은지를
    문답으로 붙였다.

- **59장 보강 — 우경식(牛耕式) 읽기**(저자 지시: "boustrophedonically … 적절한
  한글 번역 필요"). 선언을 좌우로 번갈아 읽는 방식에 붙은 이름이다.
  - 번역어를 **우경식(牛耕式, boustrophedon)**으로 정했다 — 그리스어 βοῦς(소) +
    στρέφειν(돌다), *소가 쟁기를 끌고 밭 끝에서 돌아오는 모양*. 어원과 고대 그리스
    비문의 서법까지 붙였다.
  - ★ **우선순위 규칙 A/B.1/B.2/B.3/C**를 표로 실었다. 특히 **C항** — `const`·
    `volatile`이 타입 지정자 옆이면 *타입*에, 아니면 *바로 왼쪽 별표*에 붙는다.
    이 책에 없던 규칙이고, 실무에서 가장 자주 사고를 낸다.
  - 예제 `ch59/precedence.c` 신설(양판) — `const int *` / `int *const` /
    `const int *const`를 실행으로 갈랐고, 반대쪽이 `assignment of read-only
    location '*pci'`와 `assignment of read-only variable 'cpi'`로 막히는 것도 확인.
  - 「동쪽 const」(east const) 관행 소개, **지워 가며 읽기** 절 신설
    (`char *const *(*next)();`를 일곱 걸음 표로 분해 — `telnet` 소스의 실물).

- **59장 보강 — `typedef` 를 어디에 쓸 것인가**(저자 지시). 널리 인용되는 어림규칙
  셋을 소개하고, 셋을 오늘 기준으로 하나씩 검토해 붙였다. ★ 그중 하나는 갱신이
  필요했고(오늘은 `<stdint.h>` 가 그 일을 한다), 다른 하나는 **지금 당장 필요한
  이유**가 있었다 — 태그 없는 `typedef` 로는 자기 참조 구조체를 못 만든다
  (`error: unknown type name 'node'` 실측).

- **54장 「여러 파일」 보강 — 링크의 세 단계·ABI·PIC·가로채기**(저자 지시).
  - **세 이름을 정확히 갈랐다** — link-editing / loading / runtime linking. 정적은
    앞 둘, 동적은 셋.
  - ★ **게으른 결합**(lazy binding) — 런타임 링커는 `main` 전에 공유 객체를 들이되
    *함수 심볼은 실제 호출까지 미룬다.* 그래서 안 부를 라이브러리를 링크해도
    벌점이 없고, 대신 `symbol lookup error`가 한참 뒤에 날 수 있다.
    `LD_BIND_NOW`·`-z now`·`-z relro`를 처방으로 붙였다.
  - ★ **동적 링크의 진짜 목적은 크기가 아니라 ABI다.** 크기는 실측으로 보이되
    (16 KB vs 758 KB) 부산물로 못박고, ABI가 정하는 것 다섯을 표로.
    `soname`과 심볼 버저닝까지.
  - ★ **정적 링크는 죽지 않았다** — 1990년대의 "폐물" 판정이 오늘 뒤집힌 사정
    (컨테이너·재현 가능 빌드·musl)과, 그 대가(보안 갱신 시 재빌드)를 함께.
  - ★ **PIC 절 신설** — 왜 필요한가부터. *코드 페이지를 고치면 공유가 깨진다*는
    것이 이유이고, 해법은 「코드를 고치지 않고 주소를 표에 모으는 것」. **GOT**(데이터)
    와 **PLT**(함수)를 실제 `objdump` 출력으로 보였다. ★ GOT 접근이 *읽기 두 번*
    이라는 것이 40장의 배열/포인터 구조와 같다는 점을 이었다.
  - ★ 오늘 기준 갱신 넷 — RIP 상대 주소로 비용이 준 것, **x86-64에서는 `-fPIC`
    없이 `.so`를 만들 수 없다는 것**(링커가 실제로 거부한다 — 실측), 배포판이
    실행 파일까지 **PIE**로 짓는 이유는 성능이 아니라 **ASLR**이라는 것,
    `-K pic`/`-G` → `-fPIC`/`-shared`.
  - ★ **가로채기(interpositioning)** 절 신설 — 기본이 전역인 성질과 「먼저 찾은
    것을 쓴다」가 겹쳐 라이브러리 함수가 조용히 갈아치워진다. 컴파일러가 막지
    않는 이유(프로그래머가 옳다고 가정하는 C의 태도)까지. `LD_PRELOAD`의 정당한
    용법과 위험을 함께 적었다.

- **참고문헌 확장**(저자 지시: "각주로 어디서 인용했는지 … 참고문헌 항목을 독립하여").
  - ★ **「이 책이 개념을 빌린 책들」 절 신설** — 착안점을 얻은 문헌을 정식 서지로
    올렸다. 아울러 문장·표·예시를 옮기지 않았고 서술과 측정값은 이 책의 것임을
    명시했다.
  - ★ **「링크·적재·ABI」 절 신설** — Levine _Linkers and Loaders_(1999),
    Drepper _How To Write Shared Libraries_, System V ABI(AMD64), ELF 규격 v1.2,
    `ld.so(8)`·`ld(1)`·`dlopen(3)`, GCC·Clang 코드 생성 옵션 문서.
  - 본문에는 해당 자리마다 각주를 달아 출처를 밝혔다(59장 2곳, 40장 2곳, 54장 1곳).

### Changed
- 38·39장 끝에 40장으로 잇는 문장을 넣었다. ★ 39장에 **옛 재배치의 잔재로 남아
  있던 중복 마무리 문단**(「다음 장은 … 문자열이다」)을 발견해 정리했다.
- 장 수 96 → **97**. 한국어판 727쪽, 영어판 779쪽. 예제 154개.

## [v0.35.0] - 2026-08-09

### Added
- **44장 「`malloc`을 어떻게 적을 것인가 — 오래된 논쟁」 신설**(저자 지시) — 30년째
  이어지는 캐스트 논쟁을 양쪽 1차 자료와 실측으로 정리했다.
  - 세 표기를 갈랐다 — ① `p = malloc(n * sizeof *p)` ② `p = (T *)malloc(n * sizeof(T))`
    ③ 절충 `p = (T *)malloc(n * sizeof *p)`.
  - **①의 근거 넷**: 타입이 바뀌어도 고칠 자리가 하나(리눅스 커널 코딩 스타일 원문 인용
    — 「읽기를 해치고 … `sizeof` 가 바뀌지 않을 때 생기는 버그의 기회를 만든다」),
    타입 이름을 아예 안 적으니 어긋날 수 없음, ★`sizeof *p` 는 피연산자가 *평가되지
    않으므로* `p` 가 널이어도 안전(실측), 그리고 옛 캐스트가 버그를 숨기던 사정.
    ★**넷째 논거는 오늘날 힘을 잃었다는 것을 정직하게 적었다** — C23 에서는 캐스트
    유무와 무관하게 `error: implicit declaration` 이다(실측).
  - **반대편도 1차 자료로**: SEI CERT C 의 *MEM02-C* 는 캐스트를 *권장*한다(「뒤이은
    부주의한 포인터 변환을 컴파일러가 잡을 수 있다」). ★CERT 의 예제를 실제로 컴파일해
    주장이 성립함을 확인했다 — 캐스트가 *경고를 오류로 격상*시킨다. 다만 ①은 그 버그를
    *쓸 수조차 없고*, CERT 자신도 위험도를 P3/L3 로 매긴다.
  - ★**「C++ 호환」이라는 이유를 실측으로 따졌다**: 캐스트를 성실히 붙인 현대 C 파일을
    C++ 로 넘기면 *지정 초기화 순서*와 *`_Generic`* 에서 그대로 막힌다 — **캐스트가
    사는 것은 호환이 아니라 호환의 착각**이다. 게다가 C 와 C++ 가 실제로 공유하는 것은
    헤더인데 *헤더는 `malloc` 을 부르지 않는다.*
  - **그래도 캐스트가 맞는 자리**(`.c` 를 C++ 로 빌드하는 프로젝트, CERT 규약 아래)를
    적고 그때는 ③을 권했다 — 양쪽에서 다 통과함을 실측했다.
  - ★반례 상자 「두 표기가 함께 지는 진짜 위험」 — `n * sizeof *p` 의 **곱셈 넘침**.
    감아 돈 결과로 `malloc` 이 *성공*하는 것을 실측으로 보이고, 세 처방(개수 선검사·
    `calloc`·`<stdckdint.h>`)을 제시했다. 「캐스트를 붙일지보다 훨씬 값진 논쟁」.
  - 저자의 결론: ① — *틀릴 방법이 없는 표기가, 틀리면 잡아 주는 표기보다 낫다.*
    다만 규칙으로 강요하지 않으며, 「알고 고르는 것과 관행을 물려받는 것은 다르다」.
  - 예제 `ch44/malloc_style.c` 신설(양판).

## [v0.34.0] - 2026-08-09

### Fixed
- **전면 감사(RFC-0020)로 확정한 결함 13건을 고쳤다** — 96장 25,756줄을 네 축(사실 오류·
  연결·지침 준수·친절함)으로 훑고, *거짓 양성 관문*(1차 자료/실측/책 내부 대조 중 하나)을
  통과한 것만 손댔다.
  - ★**사실 오류 1건**: 42장의 `Morris worm(1992)` → **1988**. 그리고 그 *원인*이 더
    중요했다 — 단순 오타가 아니라 **재배치 스크립트가 만든 오류**였다. 한국어 패턴
    `((?:\d+[·~,])*\d+)\s*장` 이 `Morris worm(1988, 39장)` 에서 「1988, 39」를 한 묶음으로
    물어 연도까지 밀었고, 재배치를 세 번 거치며 1988→1990→1991→1992 가 되었다.
    묶음의 각 수를 세 자리 이하로 제한해 재발을 막았고, 양판 연도를 전수 대조해 훼손된
    곳이 이 한 자리뿐임을 확인했다.
  - ★**잘못된 장 참조 11건**(양판 동시 수정): 표준 라이브러리 지도 53→**61**장,
    고정 폭 타입 74→**26·27**장, `setjmp` 네 문맥 71→**76**장, `register` 10→**11**장,
    텍스트 파일 8→**9**장, `argv` 28→**52**장, Duff's device 31→**32**장, 최적화
    14→**13**장, 임베디드 도구 18→**95**장(3곳). 특히 76장의 선행 항목은
    *존재하지 않는 장 제목*(「41장 스택과 호출」)을 가리키고 있었다 — 43장으로 바로잡았다.

### Added
- **`scripts/check-promises.py`** — 「N장에서 다룬다」는 *약속이 그 장에서 지켜졌는지*
  대조한다. `check-xrefs.py` 가 두 판의 번호 일치만 보는 것과 다르다. 대조 어휘를
  아무 낱말이 아니라 *이 책이 개념어로 인정한 것*(용어 사전·색인·코드 이름)으로 좁혀
  거짓 양성을 399건에서 101건으로 줄였고, 그중 11건이 진짜였다.
- 사설 문서 둘 — `docs/AUDIT-FINDINGS.md`(발견 목록과 *고치지 않은 91건의 이유*, 그리고
  검증을 통과한 사실 11건의 기록), `docs/EVALUATION-v0.33.md`(아홉 물음에 근거를 붙인 평가).

## [v0.33.0] - 2026-08-08

### Changed
- **20장 제목을 「수식과 상수 — 값이 되는 것」으로**(저자 제안) — 이 장 열한 절 가운데
  여덟이 상수 표기인데 제목이 「수식」뿐이라 *상수 표기법을 여기서 다룬다는 것이 차례에서
  보이지 않았다.* 서두의 「이 장이 끝나면」도 「소스에 값을 적는 법이 절반이고, 나중에
  「이 표기가 뭐였더라」 싶을 때 돌아올 자리」라고 고쳐 적었다. 21·30·33·48장의 선행
  항목 표기도 새 제목으로 맞췄다(양판).
- **22장 「출력」을 보강**(저자 지적 — 내용이 짧고 정리가 덜 됐다) — 136줄 → 227줄.
  - 「빈칸의 종류」 신설 — `%d`·`%f`·`%c`·`%s`·`%x`·`%%`. 20장에서 배운 상수 네 갈래에
    빈칸이 하나씩 대응한다는 것으로 이었다.
  - 「자리를 맞추는 법 — 폭과 정밀도」 신설 — `%6d`·`%-6d`·`%06d`·`%.2f`·`%8.2f`·`%.3s`,
    그리고 읽는 순서(맞춤 → 폭 → 정밀도 → 변환 글자). ★오해 상자: *폭은 바이트로 센다* —
    영문 표는 가지런한데 한글 표가 어긋나는 것을 시연으로 보인다(9장 UTF-8 과 연결).
  - 「출력의 창구는 하나가 아니다」 신설 — `puts`·`putchar`·`fprintf`, 그리고 **표준
    출력과 표준 오류를 가르는 이유**(리다이렉션·파이프). ★실측 문답: 시연에서 `stderr`
    줄이 맨 위에 나온 것은 *표준 오류가 버퍼링되지 않기* 때문이다.
  - 예제 `ch22/convert.c` 신설(양판) — 제4부의 「변수 없이 간다」 설계를 지켜 재료는
    전부 상수와 수식이다.
- **25장 「입력」을 보강** — 111줄 → 174줄.
  - 「`fgets`의 계약」 신설 — 세 약속(그릇을 넘기지 않는다 / NUL 을 붙인다 /
    ★*줄바꿈까지 담아 준다*)과, **잘렸는지 아는 법**: 끝에 개행이 없으면 잘린 것이다.
    개행 떼기 관용구(`line[strcspn(line, "\n")] = '\0'`)와 「그릇은 얼마나 크게」 문답
    (POSIX `getline` 언급 포함).
  - 예제 `ch25/lines.c` 신설(양판) — 그릇을 16으로 일부러 작게 잡아 긴 줄이 세 번에
    나뉘어 들어오는 것을 개행 기호(`@`)로 눈에 보이게 했다.

### Note
- 22장과 25장을 한 장으로 합치자는 제안을 검토했으나 *합치지 않기로 했다.* 입력은
  `scanf("%d", &n)` 처럼 값을 담을 변수가 있어야 하는데 변수 선언은 23장이고, 실제로
  19\~22장 예제는 변수를 하나도 쓰지 않는다 --- 제4부의 「변수 없이 간다」는 설계가
  의도된 것이라, 짧다는 문제는 합치기가 아니라 *채우기*로 풀었다.

## [v0.32.0] - 2026-08-08

### Added
- **용어 병기와 색인 검사 `scripts/check-terms.py`**(저자 지시, RFC-0019) — 규율을 한 줄로
  세웠다: **개념을 정의하는 자리에 영어 병기와 색인 표시가 함께 있어야 한다.**
  「정의하는 자리」는 *절 제목 → 굵은 강조 → 첫 등장* 순으로 기계가 판정하고,
  `gloss-missing`·`gloss-late`·`index-missing` 셋을 잡는다. 용어 사전은 원고에서 뽑은 것
  (이미 병기된 쌍, 색인 표제어)에 `docs/terms.tsv`(정본)를 얹어 만든다.
- **`docs/TERMS.md` 신설** — 개념어 209개의 *한국어 ↔ 영어 ↔ 정의 장 ↔ 병기·색인 여부*
  대조표. 한↔영 용어 사전을 겸한다. README 양판에 연결하고 `release.sh` 게이트에 넣었다.

### Changed
- **용어 병기를 전면 보강** — 지면의 `한국어(English)` 병기가 **337곳**이 되었다.
  포인터(pointer)·배열(array)·스트림(stream)·저장 기간(storage duration)처럼 개념이 처음
  서는 자리에 원어를 붙였고, 절·장 제목에만 있던 용어(부호 확장·컴파일 과정·암묵적 변환·
  비지역 점프 등)는 제목에서 병기했다.
- **색인을 두 배로** — 한국어판 표제어 **135종 → 245종**, 영어판 **134종 → 176종**.
  각 개념을 *설명하는 자리*에 표시가 놓이도록 정의 장 기준으로 배치했다.

### Fixed
- 색인의 중복 표제어 **51종을 병합**했다 — 「복합 리터럴」과 「복합 리터럴(compound
  literal)」이 따로 잡히던 것을 하나로 모으고 쪽번호를 합쳤다.
- 자동 삽입이 만든 사고를 잡아내고 도구에 방지책을 넣었다 — 색인 표시가 `#prereq`·표 셀
  같은 *인자 목록 안*으로 들어가 빌드를 깨뜨린 자리(4곳)를 되돌리고, 병기가 `#idx()`
  *안*으로 들어간 55곳을 떼어냈으며, 정규식이 문 문장 조각(「계산하고」·「에서도 값」 등)
  40여 개를 사전에서 제외했다. ★조사로 끝나는 정상 용어(오버플로·부수효과·구현 정의·힙)를
  조각으로 오인해 지웠다가 되살린 일이 있어, *사전에 오른 낱말은 어떤 어미로 끝나든
  용어로 본다*는 규칙을 넣었다.

## [v0.31.0] - 2026-08-08

### Changed
- **15장 「헬로 월드」 재구성**(저자 지시) — 소스를 보자마자 결과를 함께 들이밀던 구성을
  *소스 → 실행 환경 → 컴파일 → 결과 → 한 줄씩 읽기* 로 바꿨다.
  - **15.1 첫 프로그램** — 이제 *소스 코드만* 보인다(`demo(.., show-output: false)`).
    그리고 저자의 문장 그대로 「C언어 문법으로 작성한 일종의 작업 계획서 같은 것으로
    소스 코드라 하고, 컴파일러라는 번역기로 기계어로 번역해 미리 작업된 기계어
    코드(라이브러리)와 합쳐 묶어야 실행 파일이 된다」를 놓았다.
  - **15.2 터미널과 셸 — 프로그램을 실행하는 자리 (신설)** — *터미널은 유리창, 셸은 그
    안의 일꾼*이라는 구별부터. 플랫폼별 창과 셸의 표(윈도 `cmd`·PowerShell, 리눅스
    `bash`·`zsh`, macOS `zsh`), 셸이 한 줄을 처리하는 세 걸음(읽고·가르고·실행),
    내장 명령 대조표(`pwd`/`cd`/`ls`/`echo`/`clear` 네 셸), **내장 명령과 실행 파일을
    갈라야 하는 세 자리**, 내가 만든 실행 파일을 실행하는 법과 ★`./hello` 가 필요한
    이유(`PATH` 에 현재 디렉터리가 없는 유닉스의 관행 — PowerShell 도 같고 `cmd` 만
    다르다), 그리고 **인자 넘기기**(값·짧은 옵션·값이 붙는 옵션·긴 옵션·`--`,
    공백은 따옴표).
  - **15.3 명령행에서 컴파일하고 실행하기 (신설)** — GCC·Clang·MSVC 세 갈래를 각각
    실제 명령으로. 옵션 대조표(`-std=c23` 대 `/std:c17`, `-Wall -Wextra` 대 `/W4`,
    `-o` 대 `/Fe:`), ★MSVC 가 전용 「x64 Native Tools」 창을 요구하는 이유는 파일이
    없어서가 아니라 `PATH` 가 준비되지 않아서라는 것, MSVC 의 C 지원이 `clatest` 까지라
    윈도에서 이 책의 예제를 돌리는 세 가지 현실적 선택(MinGW·윈도용 Clang·WSL).
  - **15.4 그래서 첫 프로그램은 이렇게 나온다** — 실행 결과를 여기서 처음 보인다.
    새 장치 `demo-output()` 을 `lib.typ` 에 추가했다(소스와 결과를 떼어 놓는 자리용).
  - 15.5 「한 줄씩 읽기」로 이어진다 — 도입 한 줄을 새로 썼다.
  - 서두의 「먼저 알아야 할 것」에 3장(프로그램과 프로세스)을 더했다.

## [v0.30.0] - 2026-08-08

### Added
- **절 단위 지도 `scripts/section-map.py`**(저자 지시, RFC-0018) — 96장의 2단계 절
  **490개**를 분해해 절마다 *앞질러 참조·되돌아 참조·난이도(0\~3)·성격·분량*을 재고
  `docs/SECTIONS.md` 와 `docs/section-heat.svg` 를 만든다. README 양판에 연결하고
  `release.sh` 게이트에 넣어 배포마다 원고와 다시 맞춘다.

### Changed
- **설명 순서 전면 점검(RFC-0018)** — 490개 절을 재고, 근거가 확인된 것만 고쳤다.
  - ★**핵심 발견: 순서 문제로 보이던 것의 대부분이 *문장* 문제였다.** 기계가 뽑은
    앞질러 참조 46건 중 40건은 이 책의 설계인 **예고**("…는 43장에서 다룬다")였고,
    실제 결함은 **12건의 「배운 척하는」 의존 어법**("51장에서 *배운* 미정값")이었다.
    12건 전부를 예고형으로 고쳤다(4·7·31·40·41×3·42·51·52·66장, 양판).
    검증: 「장 뒤에 오는 의존 어법」 **0건**.
  - **94장 절 재배치** — 「업계의 규약 — MISRA」를 「오늘도 C로 도는 것들」 *뒤*로 옮겼다.
    자동차·항공·의료라는 무대를 먼저 보이고 그 현장의 규칙을 말하는 인과가 서고,
    도구 이야기 한복판에 무거운 규약이 끼어들던 것도 풀린다. 이음새 문단을 새로 썼다.
  - **큰 절 쪼개기** — 12장의 「계약서에 없는 자리 — 회색지대」와 「C 밖의 표준이 대신
    보장할 때」, 20장의 상수 여섯 갈래, 89장의 「어느 형식화 함수를 쓸 것인가」를
    2단계로 승격했다(499절). 책 전체가 참조하는 개념이 *차례에서 보이게* 된다.
    반면 56장(두 겹 우회)·81장(자유 사용 구현)은 *한 논증의 단계*라 묶어 두었다.

### Fixed
- 판정 기준의 오탐 넷을 잡아 도구를 정교화했다 — 「지도」는 장 첫머리면 *예고*,
  「정리」는 「뒷정리(cleanup)」일 수 있고, 낱말이 제목 *속*에 묻힌 것은 성격 판정에서
  제외하며, 큰 절도 *3단계 절이 둘 이상이면* 차례에서 속이 보이므로 통과시킨다.
  (오탐으로 「고칠 뻔한」 자리: 27·60·66장의 예고 지도, 75장의 자원 뒷정리 절.)

## [v0.29.0] - 2026-08-08

### Added
- **장 사이의 의존 관계도**(저자 지시) — 각 장 서두의 「먼저 알아야 할 것」이 곧 의존
  선언이므로, 손으로 목록을 베끼는 대신 *원고에서 뽑아* 그린다.
  `scripts/make-depgraph.py` 가 넷을 만든다.
  - `docs/dependency-graph.svg` · `docs/dependency-graph-en.svg` — **호 다이어그램**.
    가로축이 1\~96장, 호 하나가 의존 하나(왼쪽=기댄 곳 → 오른쪽=그 장)다. 호가 길수록
    멀리서 끌어오고, 점이 클수록 여러 장이 기대며, *뒤 장에 기대는 자리는 붉은 점선*으로
    드러난다. 축 아래에 부(部) 띠를 깔아 어느 부의 장인지 함께 보인다.
    (선형으로 읽는 책이라 호 다이어그램을 골랐다 — 상자·화살표 그래프는 96개 마디에서
    실뭉치가 된다.)
  - `docs/DEPENDENCIES.md` · `-en.md` — 부별 전체 표(장 / 제목 / *기댄 곳* / *이 장에
    기대는 곳*), 기둥이 되는 장 순위, 역방향 의존 목록, 아무도 기대지 않는 장 목록.
  - README 양판에 관계도와 링크를 넣고, `release.sh` 게이트에 생성 단계를 더해
    *배포 때마다 원고와 다시 맞춘다*.

### Fixed
- ★관계도가 첫 실행에서 잡아낸 것 둘을 고쳤다.
  - **68장(로케일 ②)이 뒤에 오는 73장(시간)을 선행으로 걸고 있었다** — 선형으로 읽는
    책에서 성립하지 않는 자리라, 선행 항목에서 빼고 본문에서 「정식 취급은 73장」이라는
    앞질러 참조로 바꿨다. 이제 모든 장이 자기보다 앞선 장에만 기댄다.
  - **새 26장(타입의 지도)에 아무 장도 기대지 않는 것으로 잡혔다** — 27장과 29장이
    실제로 기대는 장이므로 두 장의 선행 항목에 명시했다.

## [v0.28.0] - 2026-08-08

### Added
- **40장 「반복의 기법 — 중첩, 탈출, 그리고 블록 만들기」 신설**(저자 지시, RFC-0017) —
  지시하신 대로 *다차원 배열(39장) 바로 다음* 자리다. 옛 40\~95장은 41\~96장으로
  밀렸다(총 96장). 32장이 「루프란 무엇인가」라면 이 장은 「루프를 어떻게 부리는가」다.
  - **중첩 루프와 순회 순서** — ★실측: 4000×4000 `int` 합산이 행 우선 11\~14 ms,
    열 우선 101\~113 ms로 *여덟 배* 차이. 11장의 캐시 이야기가 숫자로 회수된다.
  - **다중 루프에서 빠져나오기** — ★`break` 는 한 겹만 벗는다(실측: 20칸 표에서 18칸을
    훑었다)는 것과 *C 에는 레이블 붙은 `break` 가 없다*는 것(Java·Go·Rust·Perl 비교표).
    네 가지 방법(플래그 / `goto` / 함수+`return` / 바깥 조건)의 우열을 표로 갈랐고,
    권고는 *함수로 빼기 → `goto` → 플래그* 순서.
  - **`goto` 가 정당한 두 자리** — ①여러 겹 탈출 ②오류 처리 정리(리눅스 커널의
    `goto free_a;` 무늬, 정리 코드가 한 벌만 남는다). ★다익스트라의 1968년 편지가
    *실제로* 반대한 것(무절제한 점프이지 낱말이 아니다)과 「considered harmful」이
    편집자 비르트의 제목이라는 사실. 규율 넷(아래로만·가까이·이름은 하는 일로·초기화를
    건너뛰지 않기, VLA 스코프로 뛰어들면 제약 위반).
  - **매크로를 한 문장으로 — `do { } while (0)`** — ★실측: 중괄호만 쓴 매크로가
    `if …; else` 와 만나면 `'else' without a previous 'if'` 로 깨진다. 후보 셋 비교표
    (벌거벗은 문장 / 중괄호만 / `do-while(0)`)와, *값을 내는 매크로에는 쓸 수 없다*는
    반례(수식 매크로·`static inline`·`_Generic` 이 대안, GCC 문장 수식은 확장).
    빈 do-while 로 「아무 일도 안 하지만 문장인」 매크로를 만드는 커널 관행까지.
  - **관용구와 함정** — ★`for (size_t i = n-1; i >= 0; i--)` 는 무한 루프(실측),
    바른 꼴 `for (size_t i = n; i-- > 0; )`. `for (;;)` vs `while (1)`, 쉼표 연산자,
    센티널 루프의 괄호, 루프 조건 속 `strlen`, 부동소수점 카운터, 그리고
    ★**빈 몸통 경고의 범위**(`-Wempty-body` 는 `if (x);` 는 잡지만 `for (…);` 는 잡지
    않는다 — 실측).
  - 예제 3종 신설(양판) — `ch40/nested_exit.c`(세 방식이 같은 답을 낸다),
    `ch40/macro_block.c`(`do{}while(0)` 와 `goto` 정리), `ch40/loop_idioms.c`.
  - 32·39장에서 새 장을 가리키도록 연결했다.

## [v0.27.0] - 2026-08-07

### Added
- **26장 「타입의 지도 — 표준이 가른 것들」 신설**(저자 지시, RFC-0016) — 데이터형을 정리하는
  독립 장이다. 위치는 지시하신 두 제약이 결정했다: *구조체(44장)보다 앞*, *승격(29장)보다
  앞* → 제6부 「값과 흐름」의 맨 앞. 옛 26\~94장은 27\~95장으로 밀렸다(총 95장).
  - **표준 §6.2.5 의 분류를 통째로** 세웠다 — 객체 타입/함수 타입, 완전/불완전(그리고
    `void` = *완성될 수 없는* 불완전 객체 타입), **기본 타입**(`char` + 부호 있는/없는 정수
    + 부동소수점), 문자 타입 셋, **정수·실수·산술 타입**, 파생 타입과 파생 선언자 타입,
    스칼라·집합체, 한정자.
  - ★**이 책이 이미 써 온 어휘가 정의되는 자리**다 — 47장 연산자 계약 표의 「피연산자:
    실수 타입 또는 포인터」 같은 칸이 이제 정확히 읽힌다.
  - ★C23 정정 셋: *`bool` 은 표준 부호 없는 정수 타입*(§6.2.5p8), *스칼라에 `nullptr_t`
    포함*, *공용체는 집합체가 아니다*(배열+구조체만 — C++ 와 갈리는 자리).
    그리고 *열거 타입은 기본 타입이 아니다*.
  - **도해 `type-tree` 신설**(양판) — 분류 나무. 점선 상자로 「겹쳐 부르는 이름」을 가지와
    구별했다.
  - **`<stdint.h>` 를 제대로** — 세 갈래(정확 폭=*선택 사항*·패딩 없음 / 최소 폭=필수 /
    가장 빠른 폭=필수)와 `intptr_t`·`intmax_t`, 그리고 `size_t`·`ptrdiff_t` 가
    `<stddef.h>` 소속이라는 것.
  - ★**`uint8_t` 의 함정 셋**을 반례 상자로: ① 대개 `unsigned char` 라 `%c`·`putchar` 로
    *글자가 되어 샌다* ② 산술 전에 `int` 로 승격되어 *8비트 산술이 아니다*(실측:
    `200+100`이 44가 아니라 300) ③ *별명일 뿐 새 타입이 아니라* `_Generic` 도 구별하지
    못한다(실측). 「언제 무엇을 쓰는가」 판단 표와 *기본값은 `int`* 라는 결론까지.
  - 예제 2종 신설(양판) — `ch26/type_map.c`(`_Generic` 으로 분류를 컴파일러에게 직접
    물어본다), `ch26/stdint_kinds.c`(세 갈래의 실제 크기와 함정 셋).
    ★실측: 이 기계의 `uint_fast32_t` 는 8바이트였다 — 「빠른」은 「작은」이 아니다.
  - 27·29·44·46장에서 새 지도를 가리키도록 연결했다.

### Fixed
- 장 재배치 뒤 어긋난 부 구성표(제7\~13부)와 부 마무리 문단의 장 범위를 바로잡았다.
  영어판 빌드 목록에 95장을 더했다.

## [v0.26.0] - 2026-08-07

### Changed
- **「상수를 적는 모든 방법」을 46장에서 20장으로 옮겼다**(저자 지시) — 리터럴을 처음
  소개하는 20장 「수식 — 값이 되는 것」의 「리터럴 — 소스에 적는 값」 바로 다음 자리다.
  독자가 실제로 궁금해하는 시점이고 장의 주제와도 맞는다. 절 서두에 *참조용이니 지금
  전부 외울 필요는 없다*는 안내를 붙였다.
- **데이터형에 기대는 부분은 26장으로 갈랐다**(저자 지시) — 20장은 타입을 배우기 전이라,
  타입에 의존하는 서술을 그대로 두면 앞질러 가게 된다.
  - 20장에는 *표기*만 남겼다 — 네 진법, 접미어가 「타입을 바꾸는 표시」라는 사실,
    자리 구분자, 문자 상수의 접두어, 이스케이프, 부동소수점의 형태, 문자열 리터럴 규칙,
    상수처럼 보이는 것들. 타입 이름이 나오는 표에는 8·9·26·47장으로의 앞질러 참조를
    달았다.
  - **26장 「정수 상수의 타입 — 같은 값도 표기에 따라 다르다」 신설** — 후보 목록을
    진법별로 갈라 표로 정리했다(★10진은 부호 없는 타입이 후보에 없고, 8진·16진·2진은
    중간중간 끼어든다). 실측 `sizeof(0xFFFFFFFF)`=4(`unsigned int`) 대
    `sizeof(4294967295)`=8(`long`), 비트 마스크를 십진으로 적는 것의 반례,
    그리고 후보에 없는 큰 수는 고정 폭 타입 매크로로 못박으라는 문답.
- 46장에는 「상수 표기는 20장, 정수 상수의 타입은 26장」이라는 안내 한 줄을 남기고,
  예제를 `examples/ch46/constants.c` → `examples/ch20/constants.c` 로 옮겼다(양판).

## [v0.25.0] - 2026-08-07

### Added
- **46장 「전위와 후위 — 같은 일을 하고 다른 값을 준다」 신설**(저자 지시) — 두 연산자의
  차이가 *수식이 내놓는 값 하나뿐*임을 표로 못박고, 표준의 계약(§6.5.3.5 후위 /
  §6.5.4.1 전위)을 나란히 놓았다.
  - ★**값이 정해지는 때와 메모리가 바뀌는 때는 다른 사건이다** — 후위는 「결과의 값 계산이
    저장 부수효과보다 *먼저* 순서 지어진다」(§6.5.3.5p2), 전위는 `++E` ≡ `(E += 1)` 이므로
    「대입의 갱신은 양쪽 값 계산 *뒤*」(§6.5.17.1p3). 표준은 *상대 순서*만 정하고 *시각*은
    정하지 않는다.
  - ★**오해 정정**: 「후위는 세미콜론에서 증가한다」는 표준 어디에도 없다. 실제 저장은
    *다음 시퀀스 포인트 전 아무 때나*이고, 그래서 `i = i++ + 1` 은 여전히 UB(§6.5.1p2).
  - ★**「전위가 빠르다」의 역사 정정** — 리치의 「C 언어의 발전」 원문 확인: PDP-11 어드레싱
    모드 때문이라는 통설은 *역사적으로 불가능*(B 개발 당시 PDP-11 이 없었다). PDP-7 의
    자동 증가 셀이 힌트였고, 더 큰 동기는 *`++x` 의 번역이 `x=x+1` 보다 작다*는 관찰이었다.
    즉 원래의 비교 대상은 `x++` 가 아니었다.
  - ★실측: 값을 쓰지 않는 루프에서 `i++` 와 `++i` 의 어셈블리는 `-O0` 에서도 *한 바이트도
    다르지 않다*. 값을 쓰는 자리는 `-O0` 에서 9 대 11(후위가 오히려 적었다), `-O2` 에서 3 대 3.
  - ★**C++ 와의 차이 둘**: ① C 에서는 전위·후위 *둘 다* 좌변값이 아니지만 C++ 는 전위만
    좌변값이다(`&++x`·`++x = 5` 가 C 에서 오류, C++ 에서 통과. `&x++` 는 양쪽 다 오류).
    ② 사용자 정의 타입의 후위는 복사를 만든다 — 반복자 1000회 전진에 전위 0회 / 후위 1000회
    (`-O2` 에서도). C 의 스칼라에는 해당 없으므로 「전위를 쓰라」를 C 에 그대로 옮기면
    근거 없는 규칙이 된다.
  - 피연산자 제약의 *실수 타입*(real type)이 「부동소수점」이 아니라 *정수 + 실수 부동소수점*
    (복소수만 제외)임을 오해 상자로 못박고, 기존 표의 표기도 정확히 고쳤다.
- **46장 「상수를 적는 모든 방법」 신설**(저자 지시) — 정수·문자·부동소수점·문자열 상수의
  표기를 한자리에 모았다.
  - 정수: 네 진법(★`0b` 는 C23), 접미어 `u`/`l`/`ll`/★`wb`(C23 `_BitInt`), 타입 결정 규칙
    (★10진은 부호 없는 타입을 후보에 넣지 않는다 — 실측 `sizeof(0xFFFFFFFF)`=4 대
    `sizeof(4294967295)`=8).
  - ★**C23 자리 구분자 `'`** — 표준 예제의 함정을 실측으로 확인: `12'34` ○,
    `0b11'10'11'01` ○, `0x1'2'3'4AB'C'D` ○, `0x'FF` ✗("digit separator after base
    indicator"), `'1'2` ✗(문자 상수로 읽힌다).
  - 문자: 접두어별 타입 표, ★**C 에서 `'a'` 는 `int`(크기 4) — C++ 는 `char`(1)**,
    이스케이프 전체 표, ★16진 이스케이프는 *가장 긴 열*을 먹는다(`"\x411"` 함정과
    문자열 쪼개기 해법).
  - 부동소수점: 십진 형태의 필수 요소, 접미어(★C23 `df`/`dd`/`dl`), ★16진 부동소수점
    `0x1p-3` 과 *`p` 가 필수인 이유*(16진에서 `e` 는 숫자 14다), 번역에서 반올림이 없다는
    쓸모, `(double)0.1f == 0.1` 이 거짓인 것.
  - 문자열: 접두어 5종, NUL 한 칸, 인접 연결(★접두어를 섞으면 컴파일 오류), 안의 `\0`,
    ★수정은 UB(실측 SIGSEGV) → `const char *` 로 받기.
  - 상수처럼 보이는 것들(열거 상수·`nullptr`·`true`/`false`·복합 리터럴·매크로·C23
    `constexpr`), 그리고 ★`const int` 가 어디서 상수 수식이 아닌지를 실측 표로
    (블록 안 배열은 VLA 로 통과, 파일 스코프·`static`·`case` 는 오류).
  - 예제 `ch46/constants.c` 신설(양판).

## [v0.24.0] - 2026-08-07

### Added
- **소스 문자 집합과 실행 문자 집합**(저자 지시 — "C에서는 소스 코드랑 결과물 문자셋을
  서로 다르게 취급하잖아. 정확한 표준에서의 이름") — 개념은 9장, 도구는 17장에 나눠 실었다.
  - **9장 「C가 가르는 두 문자 집합 — 소스와 실행」 신설** — 표준의 정확한 용어를 표로
    확정했다: *source character set* / *execution character set* / *basic character set* /
    *extended characters* / *extended character set*(§5.2.1). ★§5.2.2 는 실행 문자 집합의
    멀티바이트 인코딩이 *소스 문자 집합의 것과 같을 것을 요구하지 않는다*고 명시한다.
    두 집합이 만나는 자리는 번역 단계 1(물리적 소스 → 소스 문자 집합, 구현 정의)과
    5(리터럴을 실행 문자 집합으로 변환, 대응이 없으면 *조용히 다른 글자*)로 못박혀 있다
    (§5.1.1.2).
  - ★**C23 이 새로 붙인 이름**: §6.2.9 「Encodings」의 *literal encoding* 과
    *wide literal encoding*. 「실행 문자 집합」이 어떤 글자가 있는가라면 「리터럴 인코딩」은
    그 글자를 어떤 값으로 적는가다.
  - ★**흔한 오해 정정**: 「C23 이 translation character set 으로 개명했다」는 사실이 아니다.
    그 개명은 *C++20/23* 의 것이고, C23(N3220) §5.2.1 은 여전히 source/execution character
    set 을 쓴다 — 두 표준을 함께 읽을 때의 함정이라 플랫폼 상자로 실었다.
  - 리터럴 접두어 표(`"…"` / `u8"…"` / `u"…"` / `U"…"` / `L"…"`)와 각각을 *누가* 보장하는가
    (`__STDC_UTF_16__`·`__STDC_UTF_32__`·`__STDC_ISO_10646__`).
  - **17장 「②와 ③을 컴파일러에 못박는 법」 신설** — `-finput-charset`/`-fexec-charset`/
    `-fwide-exec-charset` 과 MSVC `/source-charset`/`/execution-charset`/`/utf-8` 대응표,
    기본값(GCC·Clang=양쪽 UTF-8, MSVC=시스템 코드 페이지)의 차이.
    ★실측 표: *바이트가 똑같은 EUC-KR 소스 파일*이 `-finput-charset` 을 주었느냐에 따라
    `EA B0 80` 과 `B0 A1` 로 갈린다 — 틀리게 알려 줘도 오류 없이 통과한다.
    ★실측: `-fexec-charset=EUC-KR` 로 빌드해도 `u8"가"` 만은 UTF-8 을 지켰다.
  - ★실제 사례 「`__STDC_ISO_10646__` 이 참말만 하지는 않는다」 — `-fwide-exec-charset=EUC-KR`
    로 와이드 리터럴 인코딩을 유니코드가 아닌 것으로 바꿔도(`L"가"[0]` = `U+A1B0`) 매크로는
    여전히 `201706L` 로 정의되어 있었다. 기능 검사 매크로는 「구현이 그렇다고 말한 것」이지
    「이 빌드에서 참인 것」이 아닐 수 있다.
  - 예제 `ch17/charset.c` 신설(양판) — 리터럴 인코딩·`u8`·`u`/`U`/`L`·보장 매크로를 한 번에.

## [v0.23.0] - 2026-08-07

### Added
- **이름 공간 두 장 신설**(저자 지시, RFC-0015) — 51장 뒤에 끼워 넣고 옛 52\~92 장을
  54\~94 로 밀었다. 제10부가 아홉 장이 된다.
  - **52장 「이름의 세계 — 네 이름 공간과 세 축」** — 「C에는 이름 공간이 없다」는 말이
    절반만 맞다는 것부터(표준 §6.2.3 은 *네 개*를 정의한다. 없는 것은 *사용자가 만드는*
    이름 공간이다), 레이블·태그·멤버·보통 식별자의 네 마당, **스코프 × 연결 × 이름
    공간의 3축 표**, 태그와 `typedef` 가 갈리는 자리와 세 갈래 관행(공개 API / 리눅스
    커널 / 불투명 타입), 열거 상수가 보통 식별자라서 생기는 충돌과 C++ `enum class`,
    가림(shadowing), 예약된 이름 표(§7.1.3)와 `_t` 접미사의 회색지대.
    ★실측: `struct`·`union`·`enum` 은 태그 마당을 *셋이서 함께* 쓴다(`enum status` 가
    있으면 `struct status` 는 만들 수 없다 — "defined as wrong kind of tag").
    ★실측: 가림은 `-Wall -Wextra` 로 한 건도 잡히지 않고 `-Wshadow` 라야 세 건 모두 잡힌다.
  - **53장 「이름 충돌을 다루는 법 — 접두어에서 `namespace`까지」** — 충돌이 *조용히*
    일어나는 세 자리(정적 라이브러리 링크 순서·약한 심볼·`LD_PRELOAD`)부터, 네 무기
    (① 내보내지 않기 ② 접두어 규약 ③ 심볼 가시성 ④ 이름 자체를 줄이기), 큰 프로젝트의
    실제 접두어 표(SQLite·libcurl·zlib·OpenSSL·GLib/GTK·SDL·리눅스 커널),
    그리고 **C++ 의 이름 공간**(문법·익명 이름 공간·`using namespace` 규약·ADL·
    맹글링·`extern "C"`). ★실측: `nm` 으로 시연의 외부 심볼이 `main`·`textbuf` 둘뿐임을
    확인했고, `namespace app { int f(int); }` 가 `_ZN3app1fEi` 로 맹글링되어
    *이름 공간이 심볼에 새겨지는* 것을 보였다. ★실측: `int f(int class, int new);` 는
    C 에서 통과하고 C++ 에서 거절된다.
  - 예제 3종 신설 — `ch52/four_spaces.c`(같은 철자를 네 마당에 동시에),
    `ch52/tag_typedef.c`(태그·typedef·열거 상수), `ch53/prefix.c`(모듈 하나 = 외부
    심볼 하나).
  - 24·51·78·92장의 이름 공간 언급을 새 두 장으로 연결했다.

### Changed
- 영어판 빌드 목록에 93·94장을 더하고, 「94장」으로 통계를 갱신했다(README 양판 포함).

## [v0.22.0] - 2026-08-07

### Added
- **12장 「C 밖의 표준이 대신 보장할 때」 신설**(저자 지시) — C 표준이 비워 둔 자리를
  IEEE 754·POSIX·유니코드가 대신 메우는 구조를 표로 정리하고, 저자의 판단을 세 문장으로
  못박았다: 표준의 결정에는 합리적 이유가 있고, C 밖의 약속에도 그런 이유가 있으며 서로
  충돌할 수 있다 — 그러므로 프로그래머는 이것을 분명히 알고, 각자의 선택을 내리되 그것을
  *눈에 보이게* 하고 *버전업·이식·플랫폼 변화에도 옳게 남도록* 준비해야 한다.
- **12장 「경중에 따른 안전장치 사다리」**(저자 지시 — "주석 한 줄이면 된다는 너무 가볍다")
  — 회색지대를 쓰기로 했을 때 무엇을 얼마나 두를지를 5단으로 갈랐다: ① 주석 한 줄
  ② 문서 ③ 빌드가 알리게(`static_assert`·기능 검사 매크로·컴파일 오류) ④ 시험
  ⑤ 격리와 대안 준비. 오를 기준은 「어디서 컴파일되는가·얼마나 오래 사는가·깨지면 무엇이
  무너지는가·대안의 비용은 얼마인가」.
- 12장 회색지대 목록에 **함수 포인터를 `void *` 로 바꾸는 자리**(POSIX 가 요구한다)를 추가.
- **90장 「업계의 규약 — MISRA C와 그 이웃들」 신설**(저자 지시) — MISRA 의 출신(1994 년
  차량 소프트웨어 지침의 "표준 프로그래밍 언어의 제한된 부분집합")과 현재(MISRA C:2023 =
  제3판 2차 개정, C11·C18 대상, 2021 년부터 The MISRA Consortium 이 관리), 규칙 등급
  (Mandatory/Required/Advisory), **일탈(deviation) 절차**를 「선택을 금지하는 것이 아니라
  선택을 눈에 보이게 만드는 것」으로 읽었다. 이웃 표준 표(IEC 61508 · ISO 26262 ·
  DO-178C · IEC 62304 · EN 50128/50657 · SEI CERT C · ISO/IEC TS 17961 · JPL Power of
  Ten · BARR-C), 「무엇이 금지되는가」를 이 책의 장에 대응시킨 표, 준수의 실제 비용 사례.

### Changed
- **40장을 「안전한 입력 — 넘침을 막고, 실패를 다루기」로 재구성**(저자 지시 — proven 의
  등장이 이 자리에서는 생뚱맞다). 라이브러리 소개를 걷어내고, *입력 해석의 실패를 다루는
  다섯 규율*을 표준 C 만으로 세웠다: ① 실패를 값으로 돌려준다 ② `[[nodiscard]]` 로 확인을
  강제한다 ③ 어디까지 읽었는지 알려준다 ④ 잘림을 성공으로 치지 않는다 ⑤ 실패하면 출력
  인자를 건드리지 않는다. 표준 도구들(`fgets`·`scanf`·`strtol`·`atoi`)이 실패를 알리는
  방식의 비교표와 「`scanf` 반환값만 보면 안전하다」 오해 상자를 더했다. 라이브러리 이야기는
  방향만 남기고 제12부(81장 에러는 값이다, 84장 문자열)로 넘겼다.
  - 예제 `ch40/scan.c`(proven) → **`ch40/parse.c`(표준 C 만)** 로 교체.
  - 17·39·48·56·59장의 40장 참조 문구를 함께 고쳤다.
- **각 장 서두 소제목 개명**(저자 지시) — 「선행학습」 → **「먼저 알아야 할 것」**,
  영어판 "Prerequisites" → **"What to know first"**.

## [v0.21.0] - 2026-08-07

### Added
- **포인터 주소를 인쇄하는 법**(저자 지시, RFC-0014) — 표준·POSIX·컴파일러·커널 문서를
  조사하고 실측한 뒤 두 장에 나눠 실었다.
  - **34장 「주소를 인쇄하는 법」 신설** — `%p` 의 정확한 계약(★C23 에서 *문자 타입
    포인터*가 추가됐다), 캐스트를 습관으로 삼아야 하는 이유(§6.2.5p33), 출력 모양이
    구현 정의라는 것(실측: glibc 는 널을 `(nil)` 로), `uintptr_t`+`PRIxPTR` 로 문자열
    만들기와 왕복 보장(§7.22.1.4), `%p` vs `PRIxPTR` 비교표, 그리고 **주소를 로그에
    남기는 일의 함의**(ASLR·정보 누출). ★실측: `-Wall -Wextra` 로는 캐스트 누락이
    잡히지 않고 `-Wformat=2` 라야 잡힌다.
  - 34장 실제 사례 「리눅스 커널은 `%p` 를 해시해서 찍는다」 — 커널 4.15 이후 맨 `%p`
    가 해시되는 이유와 `%pS`·`%px`·`%pK`·`%pe` 의 자리(kernel.org 문서 확인).
  - **54장 「함수 포인터를 인쇄하는 법」 신설** — `%p` 에 넘길 수 없는 두 겹의 이유,
    ★실측 `-Wpedantic` 경고("ISO C forbids conversion of function pointer to object
    pointer type"), 세 가지 흔한 오답(특히 `(void *)&f` 는 *변수의 주소*), POSIX 가
    `dlsym` 때문에 이 구멍을 메운다는 것(회색지대), `memcpy` 로 바이트를 뜨는
    이식성 있는 길, 그리고 **가장 좋은 답 — 이름표를 함께 들고 다닌다**.
  - 54장 실제 사례 「주소에서 이름을 되찾는 길, 그리고 그 한계」 — ★`dladdr` 실측:
    `static` 함수는 못 찾고, 전역 함수도 `-rdynamic` 없이는 못 찾으며, `printf` 는
    내부 별칭 `_IO_printf` 로 나온다.
  - 54장 플랫폼 노트 — 함수 포인터가 주소 하나가 아닌 기계들(세그먼트 x86, AIX·
    Itanium 의 디스크립터, 하버드 구조).
  - 예제 2종(`print_ptr`·`print_funcptr`). **주소 값 자체는 싣지 않고 성질만 인쇄**해
    매 빌드 출력이 같도록 했다.

### Changed
- **HTML 언어 전환 링크가 같은 장으로 간다**(저자 지시) — 상단 바의 한국어↔English 가
  차례가 아니라 *지금 보고 있는 장의 번역본*으로 이동한다(장 페이지 이름이 두 판에서
  같다는 점을 이용). 앞·뒷부속은 판마다 쪽이 달라 그쪽 차례로 보낸다.

## [v0.20.0] - 2026-08-07

### Added
- **도해 다섯 개 신설**(저자 지시, RFC-0013) — 「표나 문자로 되면 그쪽이 먼저」라는
  기준으로 후보를 판정한 뒤, 공간 관계가 본질인 자리에만 그렸다.
  - `int-wheel`(7장) — 부호 없는 정수가 원 위를 돌고, 같은 비트를 부호 있는 타입으로
    읽으면 반대쪽이 음수가 되는 것을 한 그림에.
  - `sign-extend`(7장) — 좁은 그릇에서 넓은 그릇으로 옮길 때 앞칸을 0으로 채우는지
    부호 비트로 채우는지.
  - `conversions`(28장) — 통상 산술 변환의 네 단계 결정 흐름과 `int + unsigned` 예.
  - `decay`(37장) — 배열이 포인터로 무너질 때 「같은 주소, 다른 타입」과 잃는 것.
  - `serialize`(44장) — 통째로 쓴 12바이트와 필드별로 쓴 7바이트의 대조.
- **12장에 절 「계약서에 없는 자리 — 회색지대」 신설**(저자 지시) — 표준이 말하는 세
  갈래(약속·구현 정의·무언) 밖에 실무의 네 번째 자리가 있다는 것, 왜 생기는지(표준은
  최소공통분모를 적고 구현은 넉넉히 준다), 이 책에서 만날 회색지대 목록, 판단의 네
  기준, 그리고 **어느 쪽을 고르든 양쪽을 다 대비해야 한다**는 규율(쓰면 표시·검증·
  대안 확보, 안 쓰면 그 대가를 알고 치른다). 최악은 회색지대인 줄 모르고 쓰는 것.

### Changed
- **부록 D와 부록 F를 하나로 통합**(저자 지시) — 「부록 D — 더 읽을거리와 도구,
  그리고 표준 문서」. 책·서지(ACM 서식)·온라인 참고서에 이어 브라우저 도구·시각화
  도구·연습 사이트·무료 자료·낡은 자료 가려내기·표준 문서·이 책 다음의 길 순서로.
  본문의 「부록 F」 참조도 함께 고쳤다.
- **책 전체의 URL에 클릭 링크**(저자 지시) — 원고의 도메인 표기 88곳을 실제 링크로
  바꾸고, 바깥 주소로 가는 링크는 색으로 구분해 눈에 보이게 했다(안쪽 링크는 그대로).
  PDF 에서 바깥 링크 50개가 클릭된다.

## [v0.19.0] - 2026-08-07

### Added
- **제8부(구조체·공용체) 전면 보강**(저자 지시, RFC-0012) — 계획을 세우고 43·44·45장을
  단계적으로 두껍게 했다. 새 장은 만들지 않았다.
  - **43장 「`sizeof`의 첫 놀라움」 신설** — 멤버 크기의 합과 구조체 크기가 왜 다른지,
    패딩의 세 규칙, `offsetof` 로 배치 확인, 순서만 바꿔 12→8바이트(원소 100만 개면
    11 MiB→7 MiB). **개념도 `padding` 신설**(빗금이 패딩).
  - 43장 「대입은 되는데 비교는 안 되는 이유」 — 패딩 때문에 `==` 가 없고 `memcmp` 도
    답이 아니라는 것, 멤버별 비교 함수.
  - **43장 「유연 배열 멤버」 신설**(flexible array member) — `sizeof` 제외, `offsetof`
    로 크기 셈, 넘침 검사, 옛 「구조체 해킹」(`char data[1]`)과의 차이.
  - **★44장 「통째로 저장하거나 보내면 안 되는 이유」 신설** — 네 축(패딩 값 미정·
    배치 차이·엔디안·타입 크기)을 표로 가르고, **필드별 직렬화**의 네 요령(고정 폭
    타입·바이트 순서를 코드에 명시·길이 먼저·읽는 쪽이 검증), 실측 시연(통째 12바이트에
    `AA` 패딩이 그대로 나가고, 필드별은 7바이트), 커널 정보 누출 사례, 파일 저장 반례,
    텍스트 형식과의 갈림.
  - **44장 「이름 없는 멤버와 `container_of`」 신설** — C11 익명 구조체·공용체,
    `offsetof` 로 바깥 구조체를 되찾는 매크로와 그 회색지대.
  - **★45장 「활성 멤버와 타입 펀닝」 신설** — C 는 허용하고 **C++ 는 정의되지 않은
    동작**이라는 차이, 세 방법(공용체·`memcpy`·포인터 캐스트)의 계약 비교,
    엄격한 앨리어싱과 트랩 표현, 공용체의 크기·정렬·공통 초기 시퀀스.
  - 45장 「비트 필드에서 구현이 정하는 것들」 — 비트 순서·단위 경계·부호·주소 불가.
    결론: *프로토콜 파싱에 비트 필드를 쓰지 않는다.*
  - 예제 5종 신설(`sizeof_first`·`flexible`·`serialize`·`container_of`·`punning`).
- **18장 Pelles C 에 「한 묶음에 들어 있는 것」 절 신설**(저자 지시) — 공식 사이트가
  밝힌 구성(자원 편집기: 대화 상자·메뉴·문자열 표·아이콘·매니페스트 등, IDE: 프로파일러
  포함, 대상: x64·x86·ARM64, C23·OpenMP 3.1·AVX-512·NEON)을 표로. **Visual C++ 6.0 과의
  비교** — 「작고 빠른 통합 환경」의 감각을 유지하면서 컴파일러는 C23 까지 온 도구.
  공평하게 VC6 에 있고 여기 없는 것(MFC·클래스 위저드)과 그 반대도 적었다.

### Fixed
- **PDF 상자의 첫 문단 들여쓰기 불일치**(저자 지적) — 원인은 Typst 의 함정이었다:
  본문이 *한 문단뿐이면* 그 내용이 `par` 요소가 되지 않아 `first-line-indent` 가 적용될
  대상이 없다. `_indent-body` 가 *순수 인라인 내용일 때만* `par()` 로 감싸도록 고쳤다
  (코드 블록·목록·표가 섞이면 감싸지 않는다 — 감싸면 그 요소가 사라진다).
  실제 사례·반례·수학 박스·흔한 오해·플랫폼 노트가 모두 함께 통일된다.

## [v0.18.0] - 2026-08-07

### Added
- **부록 F 「온라인 도구와 학습 자료」 신설**(저자 지시) — 브라우저에서 바로
  돌려 보는 곳(Compiler Explorer·OnlineGDB·Wandbox·Coliru·Replit·TIO), 눈으로
  보는 도구(Python Tutor C 모드·`cdecl.org`), 문제를 주는 곳(Exercism·Advent of
  Code·Codewars·HackerRank/LeetCode·Project Euler), 무료 자료(cppreference·
  Beej's Guide·Modern C·comp.lang.c FAQ·SEI CERT C·WG14·컴파일러 매뉴얼),
  그리고 **낡은 자료를 가려내는 신호 표**(`void main()`·`gets`·`conio.h` 등).
  링크는 전부 실제로 응답을 확인했다.
- **17장에 절 「설치가 부담이면 — 웹에서 바로 해 보기」 신설** — 위 도구들의
  표, 학습용 옵션(`-std=c23 -Wall -Wextra -Werror` 를 웹 도구의 어느 칸에
  적는지), 온라인 도구의 한계(시간·파일·입력·비공개 코드·판 차이).
  **개념도 두 개 신설**(`explorer`·`viz`) — 남의 화면을 캡처하는 대신 이 책이
  직접 그린 도해다(저작권과 수명 때문).
- **18장 Pelles C 절 보강** — 공식 홈페이지 `pellesc.se`·포럼 `forum.pellesc.de`,
  그리고 ★**위키백과에 단독 문서가 없다**는 사실(「LCC (compiler)」 항목의 절로
  실려 있다 — Pelles C 가 LCC 의 대폭 개작판이기 때문). 새 절 「윈도 앱을 C로
  짠다는 선택」 — Win32 의 강점과 대가를 표로, 마이크로소프트가 새 앱에 권하는
  것은 WinUI 3/Windows App SDK 라는 점을 분명히 하되 UI 스택의 세대교체
  (MFC→WinForms→WPF→UWP→WinUI) 밖에 Win32 가 있다는 것, 그리고 보일러플레이트를
  AI 가 맡아 주는 시대라 그 대가가 예전만큼 무겁지 않다는 것.
- **18장에 절 「공식 자료가 있는 곳」 신설** — GCC·Clang/LLVM·MSVC·Pelles C·tcc 의
  공식 사이트와 영어 위키백과 항목명(전부 응답 확인).
- **부록 D 보강** — 서지 사항을 **ACM 참고문헌 서식**으로 정리(저자·연도·제목·
  판·출판사·ISBN). Modern C 는 2판(2019, ISBN 978-1-61729-581-2)과 **C23 을 다루는
  3판(2024, ISBN 978-1-63343-777-7)** 을 함께, King 2판(2008, ISBN
  978-0-393-97950-3), Preschern (2022, ISBN 978-1-4920-9733-4). 온라인 참고서·표준
  문서의 주소를 표로.

### Changed
- **HTML `index.html` 에서 머리말 본문 중복 제거**(저자 지시) — 머리말은 제
  페이지가 따로 있으므로 차례 아래에 다시 싣지 않는다(색인 페이지 32KB → 20KB).

## [v0.17.0] - 2026-08-07

### Added
- **「소스의 0」과 「기억 속의 비트」를 네 자리에서 분명히 갈랐다**(저자 지시) —
  정수 0이 널 포인터 상수로 쓰이고 비교·대입에 통하지만, *담긴 뒤의 표현이
  모든 비트 0이라는 보장은 없다*는 사실을 초보자도 놓치지 않도록.
  - **6장**에 오개념 상자 「널 포인터는 0이다」 신설 — 표기의 층과 표현의 층을
    표로 가르고, `memset`·`calloc` 이 어느 층의 작업인지 못박았다(35·43장 예고).
  - **35장**에 절 「어디까지 0을 써도 되는가 — 널 포인터 상수의 정확한 정의」
    신설 — 표준의 정의(값이 0인 *정수 상수식*, 또는 그것을 `void *` 로 캐스트),
    `0`·`0L`·`'\0'`·`(void *)0`·`NULL`·`nullptr` 가 모두 자격이 있고
    **값이 0인 `const int` 변수는 자격이 없다**는 것(실측: 컴파일 오류),
    그리고 어느 연산이 어느 층인지 정리한 표.
  - 35장 예제 `nullrep.c` 신설 — 다섯 표기의 바이트를 나란히 덤프하고,
    `{ 0 }` 구조체와 `memset` 구조체를 견주고, `calloc` 이 준 포인터의 바이트를
    보인다. *이 기계에서는 전부 일치하고, 약속은 다르다*를 실물로.
  - **35장에 플랫폼 노트 「C++의 사정 — 같은 0, 다른 규칙」** — C++ 에는
    `void *` → 다른 포인터의 암묵 변환이 없어 `NULL` 을 `((void *)0)` 으로
    정의할 수 없다는 뿌리, 그래서 생기는 **오버로딩 사고**(실측: `NULL` 이 `0`
    이면 `f(int)` 가 뽑히고 g++ 의 `__null` 에서는 모호하다는 오류),
    C++03 → C++11 에서 「0인 정수 리터럴」로 좁아진 정의(실측: `-std=c++03` 통과,
    `-std=c++11` 오류), C++11 `nullptr` 와 C23 `nullptr` 의 관계.
  - **42장**에 오개념 상자 「`calloc` 으로 잡으면 포인터가 널로 초기화된다」 신설.
  - 43장의 `{0}` vs `memset` 문답에서 35장 시연으로 가는 다리를 놓았다.

## [v0.16.0] - 2026-08-07

### Added
- **62장을 여섯 장으로 분리**(저자 지시, RFC-0011) — 로케일과 `wchar_t` 를
  각각 두 장씩, 실무 처방을 한 장 더. 총 **13부 92장**(옛 63~87 → 68~92).
  - **62장 「문자 분류 — `<ctype.h>`」** — 함수 열두 개의 지도와 `isdigit` 의
    로케일 독립성, `(unsigned char)` 규율, `EOF` 와 `int` 인자.
  - **63장 「로케일 ① — 프로그램의 지역 설정」** — 왜 있는지(번역이 아니라
    관습), 전역 상태와 스레드 경쟁(§7.11.1.1p5), 여섯 범주와 POSIX 확장 범주,
    `setlocale` 의 정확한 계약(★반환값이 널이면 실패·아무것도 바뀌지 않는다),
    **로케일 이름의 문법** `언어[_지역][.문자집합][@변종]` 과 그 근거
    (C23 §7.11 각주 258 → **ISO/IEC 9945**(POSIX), **ISO 639**·**ISO 3166**,
    IANA 문자집합 등록부, 웹·윈도의 **BCP 47**=RFC 5646/4647, **CLDR**/UTS \#35),
    환경 변수 우선순위(`LC_ALL` > `LC_*` > `LANG`), `localedef`·`LOCPATH`.
  - **64장 「로케일 ② — 숫자·통화·시간·정렬」** — `struct lconv` 24멤버 전수와
    `""`/`CHAR_MAX` 관례, `grouping` 인코딩 규칙(★en_IN 의 `3 2` 실측 — 3자리
    고정이 아니다), 통화 조립(`cs_precedes`·`sep_by_space`·`sign_posn` 조합표,
    `int_curr_symbol`=**ISO 4217**), `LC_NUMERIC` 데이터 파손, `strftime` 의
    로케일 의존/불변 서식(★`%F %T` 만 여섯 로케일에서 동일), `strcoll`·
    `strxfrm`(키 크기 6바이트 vs 43바이트 실측), 스레드 지역 로케일.
  - **65장 「와이드 문자 ① — `wchar_t` 와 멀티바이트 변환」** — 표준이 정하지
    않은 것, `__STDC_ISO_10646__` 계열 매크로, 다섯 가지 상수·문자열의 타입과
    바이트(★U+1F600 에서 `char16_t` 만 요소가 둘), `MB_CUR_MAX`(로케일) vs
    `MB_LEN_MAX`(★glibc 가 6인 이유=RFC 2279 의 흔적), 변환 함수 세 층,
    `mbstate_t` 의 존재 이유, **`mbrtowc` 단계별 실측**(1/3/4바이트 소비,
    `(size_t)-2` 는 오류가 아니라 "더 필요", 2+1 로 나눠 넣어도 이어 붙음).
  - **66장 「와이드 문자 ② — 플랫폼의 현실과 와이드 입출력」** — 4바이트 UTF-32
    와 2바이트 UTF-16 으로 갈린 이유(1996년 유니코드 2.0), **서러게이트 쌍의
    계산 실측**, 윈도 `A`/`W` 함수와 UTF-16LE·매니페스트 UTF-8 코드페이지·
    콘솔 `_O_U16TEXT`, 짝 없는 서러게이트와 파일 이름(WTF-8), glibc 가 크지만
    쓰이지 않는 이유, GTK(UTF-8 `char*`)와 Qt(UTF-16 `QString`),
    **스트림의 방향**(`fwide` — 한 번 정해지면 못 바꾼다, 섞으면 UB).
  - **67장 「실무 — 유니코드와 멀티바이트를 다루는 법」** — UTF-8 우선 설계의
    근거 다섯, **RFC 3629 검증**(과잉 인코딩·서러게이트·범위 초과를 직접 만든
    디코더로 거절), 「글자 수」의 네 층위와 그래핌 클러스터(UAX \#29, 이모지
    가족 18바이트/5코드포인트/1글자 실측), 정규화(UAX \#15, macOS NFD 사례),
    터키어 `i` 문제, **레거시 2바이트 인코딩**(EUC-KR·CP949·Shift_JIS·Big5·
    GBK 의 선행/후행 바이트 표와 ★`0x5C` 다메모지 사고 재현), 변환 수단 비교.
  - 예제 11종 신설(`locale_probe`·`lconv`·`time_locale`·`collate`·`wide_lit`·
    `mbrtowc_step`·`surrogate`·`wide_io`·`utf8_scan`·`graphemes`·`legacy_lead`).
    로케일이 필요한 예제는 **없으면 "없음"을 인쇄**해 독자의 기계에서도 통과한다.

### Changed
- `scripts/verify-examples.sh` 가 저장소 밖 `../usr/locale` 을 찾으면 `LOCPATH`
  로 잡아 준다 — 로케일 예제가 풍부한 출력을 내도록(없어도 통과).

### Changed
- **깃 히스토리 재작성**(저자 지시, 2026-08-06) — 공개 저장소의 `main` 을 현재
  내용 그대로의 **단일 커밋**으로 접었다. 판마다 배포물이 쌓여 팩이 200MB 를
  넘었고, GitHub Pages 가 저장소를 통째로 받느라 빌드가 10분 제한에 걸리던
  문제를 없앤다. 새로 clone 하면 `.git` 이 **200.7MiB → 12MiB**.
  - 릴리스 묶음(`dist/*.zip`)은 이제 저장소에 담지 않는다 — 깃허브 릴리스
    자산으로만 배포한다(판마다 23MB 절약). PDF 사본은 그대로 둔다.
  - 이전 히스토리(main 20개·manuscript 144개 커밋과 옛 태그 전부)는 로컬
    `proven_c_book_private/git-history-backup-2026-08-06.git` 미러에 보존했다.
  - 원고 브랜치(`manuscript`, 로컬 전용)는 손대지 않았다.

## [v0.15.1] - 2026-08-06

### Changed
- **README 전면 재작성**(저자 지시) — 책의 실제 내용을 근거로 다시 썼다.
  서두 인용(정수는 왜 감아 도는가·포인터는 왜 숫자가 아닌가), 「어떤 책인가」,
  부별 구성표(각 부에서 어떤 장을 만나는지), 「이 책이 다른 점」(출력 전부
  실측·주장을 코드로 재기·C23 기본값·두 판 동시 발행). 낡은 수치도 바로잡았다
  (13부 87장, ko 553쪽·en 583쪽, 예제 120개).
- `CONTRIBUTING.md` 삭제(저자 판단: 불필요) — README 의 「기여」 절도 없애고,
  오류 신고 안내 한 줄만 맨 끝에 남겼다.
- `docs/.nojekyll` 추가 — GitHub Pages 레거시 빌드가 Jekyll 로 200개 넘는
  파일을 훑다 10분 제한에 걸리던 것을 막는다.
- **HTML 상단 고정 바 재배치**(저자 지시) — 제목 `Proven C Book`은 왼쪽 끝에
  붙이고, 오른쪽 끝에 언어 전환(한국어/English)과 GitHub 링크를 둔다. 그 왼쪽에
  이전·목차·다음을 **화살표 세 개**(`←` `↑` `→`)로만 표시한다(제목은 `title`·
  `aria-label` 로 유지). 첫 장·마지막 장에서는 해당 화살표가 흐리게 남아 자리가
  밀리지 않는다.

## [v0.15.0] - 2026-08-06

### Added
- **66·67장에 「기억」과 「레지스터」 층 추가** (사용자 요청: 메모리 할당·해제와
  레지스터 저장·복구가 두 장치에서 어떤 문제를 일으키는지, 그리고 이것을 쓰는
  라이브러리가 무엇이고 왜 쓰는지).
- 66장 「처리기와 기억 — 왜 `malloc`이 목록에 없는가」 — 자유 목록이 반쯤
  갱신된 순간, 크기 정보를 쓰기 직전, 그리고 **할당자의 잠금을 쥔 순간**의 세
  가지 실패. 처리기에서 `longjmp`로 뛰면 *잠금을 쥔 채* 나가므로 그 뒤의 첫
  `malloc`이 멈춘다는 점. 처방=미리 잡아 두기·정적 버퍼.
- 66장 「신호가 저장하고 되돌리는 것 — 레지스터 문맥과 `errno`」 — 커널이
  신호 프레임에 **레지스터 전부**를 담았다가 `sigreturn`으로 되돌린다는 것,
  `setjmp`(여덟 칸)와의 대조표, `errno`만은 손으로 저장·복원해야 한다는 규율,
  붉은 구역(red zone)과 `sigaltstack`. 예제 `sig_context.c`(errno 오염 2→9
  실측, 계산 한복판에서 신호를 맞아도 결과 동일).
- 66장 「어떤 소프트웨어가, 왜 신호를 쓰는가」 — nginx·PostgreSQL·레디스·
  HotSpot JVM·웹어셈블리 런타임·보엠 GC·libuv·CPython·libcurl 의 용도와 이유를
  표로. 세 갈래(바깥의 지시 / 하드웨어 트랩 올리기 / 신호를 사건으로).
- 67장 「여덟 칸을 들여다보면」·「이것이 곧 `volatile` 규칙이다」 — glibc
  `jmp_buf` 200바이트의 내역(레지스터 64 + 마스크 여부 8 + 마스크 128)과
  **관측한 순서**(RBX·RBP·R12\~R15·SP·복귀 주소 = x86-64 SysV 피호출자 보존
  목록), 세 칸이 가드값과 섞여 저장된다는 것, 그리고 변수가 있던 자리
  (스택/피호출자 보존/호출자 보존)에 따라 결과가 갈리는 표.
- 67장 「더 조용한 사고 — 포인터가 옛 값으로 되돌아간다」 — 예제
  `jmp_leak.c`. **실측: `-O1` 이상에서 비 `volatile` 포인터가 널로 되돌아가
  `free`가 아무 일도 하지 않는다**(ASan 이 64바이트 직접 누수로 확인). 반대
  방향인 이중 해제 반례도 함께.
- 67장 「이들은 기억을 어떻게 감당하는가 — 답이 전부 같다」 — libjpeg·libpng·
  Lua·PostgreSQL·시험 프레임워크가 **전부 아레나**로 우회한다는 관찰,
  PostgreSQL `PG_TRY`/`PG_CATCH`와 기억 문맥 실제 사례(그 소스의 `volatile`
  규약 포함), `alloca`·VLA 는 스택과 함께 정리되지만 가변 변경 타입 스코프를
  벗어나는 점프는 정의되지 않은 동작이라는 점.

## [v0.14.0] - 2026-08-06

### Added
- **67장 「비지역 점프 — `<setjmp.h>`」 신설** — 65장의 한 절이던 것을 장으로
  세웠다(옛 67장 이후 한 칸 밀림, 총 87장).
  - *왜 있는가* — 예외가 없는 언어의 궁여지책. 유닉스 V7 의 유래, 표준 각주가
    말하는 "낮은 층의 이례적 상황", 그리고 예외와 다른 세 가지(정리 없음,
    타입 없음, 지역 제한).
  - *두 낱말의 정확한 모양* — `setjmp` 의 반환값 규칙(직접 0, 되돌아오면 `val`,
    `val`이 0이면 1), `longjmp` 의 `[[noreturn]]`, 그리고 표준 §7.13.1.1 이
    허용하는 **네 가지 문맥**(제어식·상수 비교·`!`·수식문).
  - *`jmp_buf` 의 정체* — 배열 타입인 이유, 안에 들어가는 것(스택 포인터·복귀
    주소·피호출자 보존 레지스터), 그리고 표준이 명시적으로 저장하지 *않는* 것
    (부동소수점 환경, 열린 파일).
  - *`volatile` 규칙* — §7.13.2.1 을 옮기고, 같은 코드를 `-O0`·`-O2` 로 빌드해
    비 `volatile` 지역 변수가 실제로 되돌아가는 것을 **실측**으로 보인다.
  - *자원 문제* — 뛰어넘은 자리의 `free`·`fclose` 가 실행되지 않는 반례와,
    잡고 푸는 자리를 `setjmp` 를 부른 함수 안에 모으는 규율.
  - *실제 사례* — libjpeg 의 error manager(왜 그 모양이 되었나), libpng,
    Lua 의 `LUAI_THROW`, 코루틴 구현, 시험 프레임워크. POSIX
    `sigsetjmp`/`siglongjmp` 와 신호 처리기에서 뛰는 것의 위험, 그리고 오늘의
    대안(반환값·`goto cleanup`).
  - 예제 3종(`jmp_basic`·`jmp_volatile`·`jmp_error`).

### Changed
- 65장의 「비지역 점프」 절을 67장으로 가는 안내로 줄였다(66장 신호와 같은 처리).

## [v0.13.0] - 2026-08-06

### Added
- **66장 「신호 — `<signal.h>`」 신설** — 65장의 한 절이던 것을 장으로 세웠다
  (옛 66장 이후 한 칸 밀림, 총 86장).
  - *역사* — 유닉스의 "믿을 수 없는 신호", 4.2BSD 의 수정, POSIX `sigaction`,
    그리고 C 표준이 왜 고쳐진 쪽이 아니라 공통분모를 담았는지(OS 없는 곳에서도
    돌아야 한다).
  - *함수의 모양* — `signal`·`raise` 의 선언, 인자별 의미(`SIG_DFL`·`SIG_IGN`·
    함수 포인터), 반환값(이전 처리 방식, `SIG_ERR`), 표준의 여섯 신호와 기본 동작.
  - *처리기 계약* — 표준 §7.14.1.1 의 허용 목록을 표로 옮기고, `printf` 가 왜
    금지인지(버퍼·잠금·교착), `sig_atomic_t` 와 `volatile` 이 각각 무엇을 막는지.
  - *자료형 속* — `struct sigaction` 의 네 멤버와 공용체로 겹치는 두 처리기,
    주요 깃발 넷, `siginfo_t` 의 `si_code`·`si_addr` 등.
  - *실제 사용 사례* — `SIGTERM` 정상 종료(컨테이너 유예 시간), `SIGHUP` 설정
    재적재(nginx), `SIGPIPE` 무시가 정답인 이유, `EINTR` 재시도, `SIGWINCH`,
    자기 파이프 요령과 `signalfd`·`kqueue`, 스레드에서의 배달 규칙과 `sigwait`.
  - 예제 3종(`sig_basic`·`sig_flag`·`sig_action`). `signal` 이 처리 뒤
    `SIG_DFL` 로 되돌아가는 구현 정의 동작을 실측으로 보인다.

## [v0.12.1] - 2026-08-06

### Added
- **55장에 「실물 — 실무에서 실제로 쓰인 선언들」 절** — 연습용이 아니라
  *출하된 라이브러리의 선언*을 그대로 가져와 절차대로 읽는다.
  - 표준의 `signal`(§7.14.1.1) — 다섯 걸음짜리 표로 한 겹씩 벗겨 "신호 번호와
    핸들러를 받아 *이전 핸들러*를 돌려주는 함수"라는 한 문장에 이른다. 반환
    타입이 험한 이유(되돌릴 수 있어야 한다)까지 설명한다.
  - X11 의 `XSetErrorHandler` — 매뉴얼 원문 그대로. 무늬가 `signal` 과 같다는
    것을 보이고, `typedef` 로 층을 나눈 판과 나란히 실행한다.
  - `qsort`·`bsearch`·`pthread_create` 로 이어지는 같은 발상("동작을 값으로
    넘긴다")을 실제 사례로 묶었다.
  - 인터넷 퀴즈용 선언(`char *(*(**foo[][8])())[]`)도 같은 절차로 풀되,
    *읽을 값어치가 있는 선언과 없는 선언*의 차이를 분명히 했다.
  - 오개념 상자 「복잡한 선언은 천재만 읽는다」 — 걸음 수는 괄호의 겹 수로
    정해지지 재능으로 정해지지 않는다.
  - 예제 `examples/ch55/monster.c` 가 네 무늬를 실제로 컴파일·실행한다.

## [v0.12.0] - 2026-08-06

### Added
- **38장 「다차원 배열」 신설**(RFC-0010) — 37장이 1차원까지만 다루고 다차원은
  두 줄로 스쳐 지나갔던 자리를 한 장으로 세웠다. 옛 38장 이후는 한 칸 밀렸다
  (총 85장).
  - *실체* — `int a[3][4]`는 "`int[4]`가 3개". 행 우선(row-major)은 표준이
    못박은 배치이고, 기억은 격자가 아니라 한 줄이다.
  - *첨자 전개* — `a[i][j]` → `*(*(a+i)+j)`를 단계마다 타입·걸음 크기와 함께
    표로 풀고, 오프셋 `i×16 + j×4`를 실측으로 확인한다. $n$차원 일반식과
    "가장 바깥 차원만 계산에 쓰이지 않는다"까지.
  - *매개변수* — 바깥 차원만 벗겨지는 이유, `int **`가 왜 아닌지(배치가 다르다),
    VLA 매개변수가 권장 기본값인 이유.
  - *평탄화의 계약* ★ — 같은 주소를 다른 타입으로 보는 문제를 **엄격한
    앨리어싱과 프로버넌스로 나눠** 설명한다. 원소의 유효 타입은 어느 쪽이든
    `int`라 앨리어싱은 걸리지 않고, 걸리는 것은 *포인터 산술의 범위*다. 반대로
    `malloc` 기억은 선언된 타입이 없어 2차원 뷰가 정공법이라는 비대칭을 밝힌다.
  - *실무 무늬* — 고정 폭·VLA·평탄+선행 차원(BLAS 의 `lda`, 부분행렬·전치·정렬이
    공짜가 되는 이유)·행 포인터 배열(`argv`, libjpeg `JSAMPARRAY`, 들쭉날쭉한 행),
    그리고 스트라이드로의 일반화(NumPy `strides`, OpenCV `step`, C++ `mdspan`).
  - *순회와 캐시* — 같은 자료를 같은 횟수 읽어도 순서로 갈리는 이유를 캐시 라인
    수로 실측(행 우선 3584 대 열 우선 0). 행렬 곱의 `ijk` 대 `ikj`, 타일링.
  - *배경 지식* — 포인터가 곧 이터레이터라는 관념, 선행 차원·스트라이드가
    "다음으로 가는 법"을 값으로 만든 것이라는 연결.
  - 예제 4종(`md_layout`·`md_param`·`md_flat`·`md_stride`)과 개념도 1종.

## [v0.11.3] - 2026-08-06

### Fixed
- **실행 결과의 긴 줄이 상자를 뚫고 나가던 것**(PDF) — 조판의 `raw` 는 스스로
  줄을 바꾸지 않아, 81장의 깊이 200 출력이 상자 밖으로 흘렀다. 실행 결과와
  표준 입력 칸은 글자 단위로 강제 접기를 한다(UTF-8 을 깨뜨리지 않는다).
  소스 코드 칸은 접지 않는다 — 낱말 한복판에서 잘리면 읽기 나쁘기 때문이다.

### Changed
- **찾아보기도 차례처럼 점선으로** — 표제어와 쪽 번호를 점선으로 잇는다(PDF).

## [v0.11.2] - 2026-08-06

### Added
- **위 고정 바에 장 이동 링크**(HTML) — GitHub 링크 오른쪽에 「← 이전 · 목차로 ·
  다음 →」을 두었다. 긴 장에서 바닥까지 내려가지 않고도 옮겨 다닐 수 있다.
  첫 장의 이전, 마지막 장의 다음은 흐리게 비활성으로 둔다(좁은 화면에서는 숨긴다).

## [v0.11.1] - 2026-08-06

### Added
- **81장에 세 번째 판 — 중첩을 받는 확장 proven 구현** — 객체·배열의 중첩을
  읽고 다시 쓰되 *재귀를 쓰지 않는다.* 파싱도 출력도 명시적 스택을 돌리는
  루프라, 깊이가 배열의 길이가 되어 한계를 넘으면 스택이 무너지는 대신 값으로
  거부한다. 예제가 200겹 입력을 한계 32에서 거부하고 한계 256에서 통과시키는
  것을 실제로 보인다.
  - 쓴 재료: 아레나(한 판의 기억을 뭉텅이로), 풀(같은 크기 노드 재활용),
    침습적 리스트(자식 잇기 — 새 할당 없음), 동적 배열(명시적 스택),
    `view`(원문에서 빌리기), 검사 산술(자리 올림마다), `proven_err_t`(깊이
    초과·자리 부족·형식 오류를 값으로).
  - 「깊이 폭탄」이 실제 공격 유형이라는 실제 사례와, "재귀를 없애면 스택
    오버플로가 사라진다"는 오해(자원이 사라지는 것이 아니라 볼 수 있는 곳으로
    옮겨 온다)를 함께 다뤘다.
  - 장 제목을 「세 판으로 짜 보기」로 고쳤다.

## [v0.11.0] - 2026-08-06

### Added
- **81장 「두 판으로 짜 보기 — 간이 JSON」(신설, 제12부의 마지막 장)** — 같은
  프로그램을 순수 C 와 proven 으로 두 번 짜서 나란히 놓는다. 범위는 평평한
  객체 하나와 값 네 갈래(문자열·정수·참거짓·널)로 좁혔다. 순수 C 판은 짧지만
  그릇 크기·쌍의 최대 수·반환값 확인·`errno`·정적 배열 다섯 가지를 사람이
  기억해야 하고, proven 판은 그 다섯이 타입·매개변수·반환값으로 옮겨 온다.
  예제가 그 차이를 실물로 보인다 — 순수 C 판은 긴 값을 조용히 잘라 담고
  (159자 중 127자), proven 판은 자리 부족과 수의 넘침을 값으로 거부한다.
  「무엇이 달라졌는가」 절에서 길이의 차이가 곧 *원래 있었어야 할 검사*임을
  설명하고, 순수 C 판이 언제 옳은 선택인지(조건이 좁을 때)도 함께 적었다.
  장 번호는 옛 81~83장이 82~84장으로 한 칸 밀렸다.

### Changed
- **부록 D 의 평가를 정확하게** — "C는 입문서 다음 칸이 유난히 비어 있는
  언어다"를 "얇고, 그나마도 오래된 언어다"로 고치고, 그 칸을 실제로 메우는
  책의 상당수가 중급 C 책이 아니라 시스템 프로그래밍 책이라는 사정을 덧붙였다
  (Gustedt·Seacord 처럼 최근 판이 실재하므로 "비어 있다"는 과한 표현이었다).

## [v0.10.23] - 2026-08-06

### Fixed
- 앞 판에서 함께 들어갔어야 할 서두 본문 여백 규칙(`.open-body`)이 빠져 있었다.
  이제 네 칸의 위·아래 여백이 실제로 같다.

## [v0.10.22] - 2026-08-06

### Fixed
- **「이 장이 끝나면」의 본문이 표제 줄에 바짝 붙던 것**(HTML) — 이 칸만 본문이
  요소로 감싸이지 않은 맨 글이라 여백 규칙이 걸리지 않았다. 서두 넷의 본문을
  모두 한 겹(`.open-body`)으로 감싸 위·아래 여백을 통일했다.

## [v0.10.21] - 2026-08-06

### Fixed
- **답 표지 앞에 들여쓰기가 붙던 것**(HTML) — 「돌아보기」 안의 답이 아직 옛
  경로로 나와 표지("답")와 본문이 두 문단으로 갈렸고, 그 탓에 본문 문단이
  들여쓰기를 받았다. 이제 표지와 본문이 한 문단에 오고, 들여쓰기는 그 다음
  문단부터 붙는다.

## [v0.10.20] - 2026-08-06

### Changed
- **「이 장에서 답할 질문」을 번호 목록으로**(HTML) — 문단으로 흩어 놓아 여백만
  넓어 보이던 것을 PDF 처럼 번호가 붙은 목록으로 바꾸고 항목 간격을 좁혔다.
- **상자 안의 문단 간격을 한 줄 사이 정도로**(HTML) — 문답·실제 사례 등 상자
  안에서 본문 문단 간격을 그대로 쓰던 탓에 헐렁해 보이던 것을 좁혔다.

## [v0.10.19] - 2026-08-06

### Fixed
- **흔한 오해 상자 뒤의 문답이 오해 상자 안에 든 것처럼 보이던 결함**(HTML) —
  장치를 두 조각(표제 상자 + 본문 상자)으로 내던 시절의 CSS 규칙
  (`.misconception + div`)이 남아, 뒤따르는 상자를 오해 상자의 아랫칸처럼
  그리고 있었다. 규칙을 걷어냈다.

### Changed
- **상자형 장치를 문답 상자와 같은 서식으로 통일**(HTML) — 흔한 오해·실제 사례·
  반례·수학·플랫폼 노트·복습 정리가 모두 표제 줄과 본문을 가로선으로 가르는
  같은 모양이고, 좌우 여백도 상자가 준다. 갈래는 테두리 모양으로 구별한다
  (반례는 파선, 플랫폼 노트는 겹선).

## [v0.10.18] - 2026-08-06

### Fixed
- **장 서두 상자의 좌우 여백이 칸마다 달랐다**(HTML) — 「이 장이 끝나면」은 본문이
  요소로 감싸이지 않은 맨 글이라 여백이 0 이었고, 「이 장에서 답할 질문」은 문단
  마다 여백이 겹쳐 지나치게 넓었다. 좌우 여백을 자식이 아니라 *상자*가 주도록
  바꿔 넷이 같아졌다. 복습 정리 상자도 같은 방식으로 통일했다.

## [v0.10.17] - 2026-08-06

### Fixed
- **HTML 문답 바깥에 테두리가 하나 더 생기던 것** — 장치가 스스로 마크업을 내게
  바꾼 뒤에도 후처리 래퍼가 남아 `qa-box` 를 한 번 더 감쌌다. 래퍼를 없앴다.

### Changed
- **복습 정리도 문답과 같은 상자로** — 표제 줄과 본문을 가로선으로 가르는
  1×2 상자다(HTML).
- **장 서두 넷을 각각 하나의 상자로**(HTML) — 「이 장이 기대는 것」·「돌아보기」·
  「이 장이 끝나면」·「이 장에서 답할 질문」이 저마다 테두리를 갖고, 표제 줄과
  내용이 가로선으로 갈린다. 넷을 한 테두리로 묶던 방식은 71장처럼 사이에 다른
  문단이 끼면 구조가 어그러졌다.

## [v0.10.16] - 2026-08-06

### Fixed
- **HTML 에서 장치 상자가 통째로 서식을 잃던 결함(72장 등)** — 조판 출력에서
  `<div>` 를 훑어 클래스를 붙이는 사후 방식이라, 조판 쪽 구조가 조금만 바뀌어도
  실제 사례·플랫폼 노트·복습 정리·코드 상자가 맨 글로 떨어졌다. 이제
  `lib.typ` 의 장치들이 *HTML 마크업을 스스로 낸다* — 문답·오개념·실제 사례·
  반례·수학·복습·플랫폼 노트·시연 상자·장 서두 넷 모두. 클래스가 원고에서
  결정되므로 깨질 자리가 없다.

### Changed
- **실행 결과·복습 정리 서식을 문답과 같은 모양으로** — 표제 줄(산세리프 굵은
  글씨)과 내용을 가로선으로 가르는 1×2 상자다.

## [v0.10.15] - 2026-08-06

### Changed
- **시연 상자를 두 판에서 같은 모양으로** — HTML 도 PDF 처럼 1×2 배치가 되어,
  위 칸에 파일 경로(굵게, 고정폭) 또는 "실행 결과"(굵은 산세리프)가 오고 가로선
  아래 칸에 내용이 온다. 상자 안에 상자를 겹치지 않는다. PDF 의 실행 결과 상자
  왼쪽 굵은 선은 소스와 같은 가는 실선으로 바꿨다.
- **README 의 내려받기 링크를 릴리스 직접 링크로** — 한국어·영어 PDF 를 한 번에
  받을 수 있는 주소를 앞머리에 둔다. `scripts/sync-version.py` 가 파일 이름뿐
  아니라 `/releases/download/<태그>/` 의 태그까지 갱신하므로, 새 판을 낼 때마다
  링크가 저절로 새 판을 가리킨다(릴리스가 이 검사를 통과해야 진행된다).
- **저장소 정리** — 지난 판의 산출물과 이전 히스토리를 저장소에서 덜어 내고
  저자 로컬에 보관한다. 공개 저장소에는 현재 판의 결과물만 둔다.

## [v0.10.14] - 2026-08-06

### Added
- **44.4.1 「실물 하나 — 2바이트 조합형 한글」** — 구조체·공용체·비트 필드를
  섞는 무늬의 실제 사례로 조합형 한글 부호를 넣었다. 16비트를 표시 1비트 +
  초성·중성·종성 5비트씩으로 자르는 배치, 낱자에 붙은 번호와 중성의 빈자리,
  완성형 2,350자의 한계와 대비, 그리고 한계 셋 — ① 둘째 바이트가 ASCII 와
  겹쳐 바이트 검색이 글자 속을 짚는 사고(`가` = `88 61`, 뒤 바이트가 `'a'`),
  ② 비트 필드 배치는 구현 정의라 외부 형식에는 시프트·마스크가 정공법,
  ③ 유니코드가 같은 발상을 곱셈으로 이어받았다는 것. 예제
  `examples/ch44/johab.c` 가 셋을 실제로 보여 준다(비트 값은 실측으로 확인).

### Fixed
- **상자와 장 서두에서 표제 다음 첫 문단이 들여쓰기되지 않던 것** — 오개념·
  실제 사례·플랫폼 노트 등 모든 장치 상자와 장 서두 소제목 아래 첫 문단이
  이제 본문과 같이 한 글자 들여쓰기된다(목록과 표지 줄은 그대로).

## [v0.10.13] - 2026-08-06

### Fixed
- **인라인 코드가 조판에서 두 동강 나던 결함** — 원고에서 백틱 한 쌍 안에
  줄바꿈이 들어가면 Typst 가 그 자리를 공백으로 읽어 거기서 줄을 끊는다.
  42.3절의 `const struct point *p` 가 "const struct / point *p" 로 인쇄된
  이유다. 전 원고를 훑어 14곳(한국어 9·영어 5)을 한 줄로 붙였다.
- 84자짜리 인라인 호출(77장) 하나를 코드 블록으로 옮겼다.

### Added
- **`scripts/check-inline-code.py`** — 인라인 코드 안의 줄바꿈을 오류로 잡고,
  지나치게 긴 인라인 코드는 참고로 알린다. 빌드에서 알리고 릴리스에서 막는다.

## [v0.10.12] - 2026-08-06

### Fixed
- **41장 「동적 메모리」의 서두가 앞 장 마지막 쪽에 얹혀 인쇄되던 결함** —
  원고에서 `#prereq` 가 장 제목(`= `)보다 앞에 있었다. 제목 다음으로 옮겼고,
  `scripts/check-chapter-openings.py` 에 "서두 장치는 장 제목보다 뒤에 온다"는
  규칙을 넣어 같은 실수를 기계가 잡게 했다(전 장 점검, 이 한 곳뿐이었다).

### Changed
- **장 서두 표지와 그 아래 내용 사이 여백**을 살짝 넓혔다(7pt → 9.5pt).
- **코드 시연 상자**(PDF) — 파일 경로가 적힌 표제 줄과 코드 본문을 가로선으로
  갈라 1×2 상자로 만들었다. 경로는 굵게 쓰고, 앞에 붙던 들여쓰기 공백을 없앴다.
  「표준 입력으로 준 것」·「실행 결과」 상자도 같은 구조로 통일했다.

## [v0.10.11] - 2026-08-06

### Fixed
- **문단 첫 줄 들여쓰기가 지켜지지 않던 자리들** — HTML 판에는 들여쓰기 규칙이
  아예 없었고(머리말을 포함한 모든 본문), PDF 판에서는 장치 상자(실제 사례·
  오개념·플랫폼 노트·수학·복습·문답) 안의 문단이 들여쓰기를 꺼 두고 있었다.
  이제 두 판 모두 본문 문단의 첫 줄을 한 글자만큼 들여쓴다. 상자 안에서는
  표제 줄에 붙는 첫 문단만 들여쓰지 않는다(표지와 붙어 읽히도록).
  표지·차례·캡션·표·목록·메타 목록·시연 상자는 그대로 들여쓰지 않는다.

### Changed
- **차례** — 제목과 쪽 번호를 가운데 점선으로 잇는다(PDF).

## [v0.10.10] - 2026-08-06

### Changed
- **머리말에 「이 책의 의도와 이용 방법」 절을 저자 문안으로 새로 실었다** —
  이 책이 무엇을 길러 주려 하는지(기초 지식과 함정을 알아보는 눈), 그것이 코드를
  만들어 내는 능력과 어떻게 다른지, 연습문제를 두지 않은 이유, 첫 프로그램이
  늦은 이유, 기계 검증과 AI 보조 집필 사실을 한 절에 모았다.
- **머리말 문장 다듬기** — 주어와 서술어가 어긋나던 자리("의도한 것은 독자가 …
  길러 주는 것이었다" → "독자에게 … 갖추게 하고 … 길러 주는 것"), 구어체 축약,
  중복 어휘를 손봤다. 영어판도 같은 취지로 다시 썼다.

## [v0.10.9] - 2026-08-06

### Changed
- **판권면을 없애고 머리말과 합쳤다** — 표지 다음의 별도 판권면을 걷어내고,
  머리말 끝에 「저작권과 연락처」 절을 두어 지은이·연락·저장소·판·최종 수정과
  라이선스 안내를 한자리에서 읽게 했다. HTML 목차도 표지 → 안내 → 차례 →
  머리말(저작권 포함) 순으로 단순해졌다.
- **머리말을 저자 문안으로 다시 썼다** — 이 책이 proven 입문서이자 C 입문서인
  이유, 아는 사람에게도 모르는 사람에게도 설명이 먼저 필요한 까닭, C의 역사에
  남은 흔적을 따라가는 즐거움, 그리고 읽는 이에게 미리 전하는 감사.

### Added
- **`metalist` 장치** — 이름표–값 목록(지은이·연락·저장소 …)을 PDF 에서는
  격자로, HTML 에서는 `<dl>` 로 낸다. `grid` 를 그대로 쓰면 HTML 에서 통째로
  사라지는 함정(메모리 도해와 같은 종류)을 피한다.

## [v0.10.8] - 2026-08-06

### Changed
- **장치 표제에서 아이콘을 없앴다** — `◉`·`∑`·`✗`·`☰`·`⊞` 대신 뜻을 적은 낱말과
  마침표를 쓴다: "실제 사례." · "수학." · "반례." · "복습 정리" · "플랫폼 노트."
  (영어판은 In practice · The mathematics · Counter-example · Recap · Platform note).
- **실제 사례 상자** — 위아래 선을 지우고 왼쪽 세로선 하나로 범위를 보인다.
- **왼쪽 세로선 굵기 통일** — 장 서두·문답·실제 사례가 같은 2.2pt 를 쓴다
  (서두는 3pt 에서 조금 얇아졌다).
- **장 제목 아래 여백**을 넓혔다(머리말 포함).
- **「이 장에서 답할 질문」 목록**의 행간과 항목 간격을 본문 리듬에 맞췄다.
- **판권·연락 문구**를 저자 문안으로 교체하고 문체를 본문과 통일했다
  (영어판도 같은 뜻으로).

## [v0.10.7] - 2026-08-06

### Fixed
- **HTML 에서 메모리 도해(그림)가 캡션만 남고 사라지던 결함** — `memrow` 가
  조판 전용 요소(grid·box)로만 그려져 있었다. 5장의 그림 5.1~5.3 이 그랬다.
  이제 HTML 에서는 표로 내보낸다.
- **영어판 82·83장이 빌드에서 통째로 빠져 있던 결함** — 장 번호를 재배치한 뒤
  `book-en/main.typ` 의 `translated` 목록이 옛 마지막 번호(81)에 멈춰 있었다.
  원고는 있는데 PDF 에도 HTML 에도 나오지 않았다.

### Added
- **`scripts/check-editions.py`** — ① 두 판의 장 파일 집합이 같은지, ② 영어판
  `translated` 목록이 존재하는 모든 장을 담는지, ③ 원고의 표·도해·그림 개수가
  HTML 산출물과 일치하는지 검사한다. 위 두 결함을 모두 잡는 검사이며 릴리스
  게이트에 걸었다.

## [v0.10.6] - 2026-08-06

### Changed
- **HTML 표 머리행** — 산세리프 굵은 글씨에 옅은 회색 음영을 넣었다(PDF 와 같은
  처리). 색이 아니라 반투명 회색이라 밝은 테마와 어두운 테마 양쪽에서 자연스럽다.

## [v0.10.5] - 2026-08-06

### Fixed
- **HTML 표가 여전히 가로폭을 다 쓰고 오른쪽에 빈 칸이 보이던 문제** — 가운데
  정렬을 flex 로 잡은 것이 브라우저에 따라 표를 늘리거나 왼쪽을 자르는 원인이
  됐다. 이제 감싼 상자는 `text-align:center`, 표는 `display:inline-table` 로
  두어 언제나 내용만큼만 넓어진다(shrink-to-fit).

### Changed
- **판 번호 단일 출처** — `scripts/sync-version.py` 가 `book/main.typ` 의
  `book-version` 을 읽어 `book-en/main.typ`·`VERSION.md`·`README.md`·
  `README-en.md` 를 맞춘다. 릴리스는 `--check` 를 통과해야 진행된다
  (README 가 v0.9.0 에 멈춰 있던 사고의 재발 방지).

## [v0.10.4] - 2026-08-06

### Fixed
- **HTML 표가 가로폭을 다 쓰면서 셀 오른쪽에 빈 칸이 생기던 결함** — 표를
  `display:block; width:100%` 로 두어 가로 스크롤을 얻은 대가였다. 이제 표는
  내용에 필요한 만큼만 폭을 쓰고 가운데에 서며(`width:auto`), 넘칠 때만 감싼
  상자(`.tblwrap`)가 가로로 스크롤한다. 테두리가 내용에 밀착한다.

## [v0.10.3] - 2026-08-06

두 판의 서식을 각각의 매체에 맞게 다시 손봤다. 내용 변경은 없다.

### Changed
- **HTML — 장 서두** — 네 칸이 따로 놀던 테두리를 없애고 *하나의 테두리를 공유*
  하게 묶었다(`chapter-head`). 안에서는 가는 가로선으로 갈린다. 제목 위아래 여백을
  줄이고 표지 뒤에 남던 빈 문단을 제거했다.
- **HTML — 문답** — 왼쪽 선만 있던 형태를 버리고, 문과 답을 *한 상자*로 두르고
  둘 사이에 가로선을 두었다(`qa-box`).
- **HTML — 예제와 실행 결과** — 소스·입력·출력 상자에 선명한 검은 테두리를 준다
  (`demo-src`·`demo-out`).
- **PDF — 장 서두** — 항목마다 제각각이던 아래 여백을 없애고 문단 행간과 블록
  간격을 하나의 값으로 묶어 리듬을 고르게 했다. 「돌아보기」 답의 왼쪽 굵은 회색
  선을 가는 검은 선으로 바꿨다.
- **PDF — 표** — 머리행은 산세리프 굵은 글씨에 옅은 음영을 깔고, 열이 셋 이상인
  표는 1열도 같은 방식으로 키 노릇을 하게 했다(`keycol` 인자로 강제 가능).

## [v0.10.2] - 2026-08-06

서식을 한 차례 더 다듬었다. 회색 음영을 전부 걷어내고 선으로만 구분하며,
구문 강조에 절제된 색을 들였다. 내용 변경은 없다.

### Fixed
- **여러 문단짜리 답이 서식 밖으로 떨어지던 결함** — HTML 에서 "답."·"A." 처럼
  표지에 마침표가 붙은 뒤로 장치 판정이 실패해, 답이 본문처럼 인쇄되던 자리가
  한국어판 55곳·영어판 54곳 있었다. 표지 판정이 마침표를 받도록 고쳤고, 이제
  모든 질문 뒤에 답 서식이 따라온다(기계 점검 0건 예외).

### Added
- **표·그림 번호와 캡션** — 모든 표와 그림이 장별 번호("표 5.1"·"그림 5.1")를 받고,
  캡션은 대상 아래 가운데에 붙는다. 번호는 사람이 적지 않는다 — `dtable`·
  `figure-svg`·`memrow` 장치가 카운터로 매기고, 새 `scripts/check-floats.py` 가
  ① 날것의 `#table`/`#image`/`#figure`, ② 손으로 적은 번호, ③ 두 판의 개수
  불일치를 막는다(빌드와 릴리스 게이트에 연결). 현재 표·그림 183개.

### Changed
- **회색 음영 폐지** — 문답의 질문 줄, 오개념 표제, 표 머리행, 장치 상자, 코드
  블록의 바탕에서 회색을 모두 없앴다. 구분은 외곽선·왼쪽 세로선·아래 가이드선
  으로만 준다.
- **문답** — 문과 답 사이의 빈칸을 없애고, 왼쪽 굵은 세로선 하나가 둘을 통째로
  잇도록 위치·굵기·색을 통일했다.
- **장 서두** — 「이 장이 기대는 것」 등 네 표지를 본문보다 큰 굵은 검은 산세리프로
  키우고 행간을 본문 수준으로 되돌렸다. 본문 안의 모든 글자 크기를 10pt 이상으로
  올렸다.
- **표** — 굵은 외곽선이 표 너비에 밀착하도록 감싸는 블록 대신 표 자신의 바깥
  선으로 그린다. 머리행 아래에는 굵은 선을 둔다.
- **차례** — 행간을 본문보다 살짝 넉넉하게 넓혔다.
- **장치 표제** — 실제 사례·반례·수학 상자·복습·플랫폼 노트·시연의 표제를 산세리프
  굵은 글씨로 통일해 세리프 본문과 갈라 보이게 하고, 상자 여백을 조금 좁혀 밀도를
  높였다.
- **구문 강조** — 흑백 제한을 풀고 절제된 색을 넣었다. 주석·키워드·타입 이름·
  문자열·수·전처리기·구두점을 색과 굵기·기울임으로 구분하되, 모든 색의 명도를
  낮게 묶어 흑백으로 변환해도 흐려지지 않게 했다.

## [v0.10.1] - 2026-08-06

읽는 눈에 걸리던 서식 셋을 손봤다. 내용 변경은 없다.

### Changed
- **장 서두** — 「이 장이 기대는 것」·「돌아보기」·「이 장이 끝나면」·「이 장에서 답할
  질문」 표지 밑의 짧은 밑선을 없앴다. 굵은 산세리프 표지만으로 충분히 구별된다.
- **표** — 바깥은 굵은 테두리, 안쪽은 가는 선, 머리행은 굵은 글씨에 옅은 음영으로
  통일했다(PDF·HTML 동일).
- **오개념 상자** — 같은 두께의 가는 검은 테두리 상자로 바꾸고, 표제 줄에만 음영을
  두었다. "흔한 오해." 뒤에 줄을 바꾸지 않고 오해 문장을 같은 줄에 이어 쓴다.

## [v0.10.0] - 2026-08-06

연산자를 부록에서 본문으로 되가져왔다. 대입을 부수효과의 관점에서 다루는 장과
연산자를 계약으로 읽는 장을 새로 세우고, 그에 맞춰 장 번호를 다시 매겼다.

### Added
- **33장 「대입과 부수효과」(신설)** — 대입이 수식이라는 사실에서 시작해, 좌변도
  평가된다는 것(순서는 미지정), 복합 대입이 왼쪽을 한 번만 평가한다는 것,
  `a[i] = ++i + i++` 같은 형태가 왜 계약 밖인지까지 다룬다. 예제
  `examples/ch33/assign.c` 가 좌변·우변의 호출 횟수를 실제로 세어 보인다.
- **45장 「수식과 연산자」(신설)** — 수식이 지닌 네 가지(값·타입·값 범주·부수효과),
  우선순위·결합과 그 이유, 연산자별 계약(피연산자·결과·회색지대), 평가 순서와
  시퀀스 포인트, 회색지대 총람. 부록 A 에 있던 연산자 카드를 전부 이리로 옮겼다.
- **37장** — `[static N]` 절을 크게 늘렸다. `[static 1]` 이 "널이 아님"을 선언에
  적는 관용구이며 GCC 가 이를 `nonnull` 로 취급한다는 것(실측 진단 인용), 그리고
  대괄호 안의 `const`·`restrict` 는 *포인터* 에 붙는다는 것을 오개념 상자와 함께.
- **42장** — 구조체 제로 초기화 절을 새로 넣었다. `{0}` 과 C23 의 `{}` 가 포인터
  멤버를 (중첩된 것까지 재귀적으로) *널* 로 만든다는 표준의 기본 초기화 규칙,
  `memset` 과 약속이 다르다는 것, 그리고 `{}` 만이 패딩까지 0 비트로 만든다는
  차이. 예제 `examples/ch42/zeroinit.c`.

### Changed
- **문답 서식** — "문."·"답." 을 줄을 바꾸지 않고 본문과 같은 줄에 둔다. 질문은
  산세리프 굵은체, 답은 표지만 산세리프 굵은체이고 본문은 세리프다.
- **부록 A** — 연산자 완전 참조에서 한 장짜리 조회표로 줄이고, 설명은 45장을
  가리킨다.
- **장 번호** — 두 장이 끼면서 옛 33장 이후가 한 칸, 옛 44장 이후가 두 칸 밀렸다.
  `scripts/renumber-chapters.py` 가 원고·예제·경로·동기 기록을 한꺼번에 옮겼다.

## [v0.9.0] - 2026-08-06

머리말을 저자의 말로 다시 쓰고, 읽는 눈에 걸리는 서식 셋을 개편했으며,
개념도(SVG)를 들이고 포인터를 수의 세 갈래로 설명하는 절을 새로 썼다.

### Added
- **개념도(SVG)** — `scripts/make-figures.py` 가 판별로 생성한다(글자가 든 그림은
  판마다 따로). 이번 판에 셋을 실었다: 2장 기억의 구역 지도, 8장 IEEE 754 비트
  배치, 36장 포인터 산술의 보폭. 33장에는 포인터 값에 붙어 다니는 세 가지
  (주소·타입·출처) 그림을 넣었다. PDF 는 `image()`, HTML 은 `<figure><img>` 로
  내보내며(Typst 의 HTML 내보내기가 아직 이미지를 내지 않는다), 다크 모드에서는
  선화를 반전한다.
- **33장 「주소는 어떤 수인가 — 이름·크기·순서」** — 이름수·순서수·집합수로
  주소의 쓰임을 갈랐다. *이름수로는 늘 쓸 수 있고, 순서수와 집합수로는 한 객체
  안에서만 쓸 수 있다* — 상등 비교는 어디서나 되고 크기 비교·뺄셈은 같은 배열
  안에서만 뜻이 있다는 규칙이 여기서 하나로 묶인다. 이어서 *왜 하필 정수였나*
  (기계가 이미 그렇게 하고 있었고, 그 선택으로 배열 문법이 공짜로 따라왔다)와
  *추상화가 시간이 갈수록 두꺼워진 경위*(기억의 층·겹쳐 도는 실행·편집자가 된
  컴파일러)를 썼다. 결론은 두 문장이다 — 주소는 정수로 표현된다, 그러나 포인터는
  정수가 아니다.

### Changed
- **머리말** — 저자의 문장으로 교체했다. 이 책이 C 입문서이기도 하다는 문단,
  실습을 뺀 것이 "결함이지만 의도적 선택"이라는 문단, 그리고 C 에 대한 애정과
  분량에 대한 사과를 담은 문단.
- **문답 서식** — 문과 답을 왼쪽 굵은 세로선 하나로 잇고, 질문 줄을 옅은 바탕과
  굵은 고딕으로 도드라지게 했다.
- **오개념 서식** — "흔한 오해" 머리와 함께 오개념 문장 자체를 크게·굵게 세우고
  상자로 감쌌다.
- **장 서두 라벨** — 산세리프·굵게·크게 쓰고 짧은 밑선을 깔아, 본문에 묻히던
  문제를 고쳤다.
- HTML 본문 폭은 **저자 결정으로 확정**했다(창 너비와 배율로 독자가 조절한다).
  앞으로의 검토에서 다시 논쟁하지 않는다.

## [v0.8.0] - 2026-08-06

v0.6.0 재평가(8.2/10)의 P0 를 갚고, 장 서두를 정형화해 기계가 지키게 했다
(RFC-0008).

### Fixed
- **44장 IEC 60559 기능 검사 매크로** — `__STDC_IEC_60559_BF16_TYPES__` 를 들었던
  것을 `__STDC_IEC_60559_BFP__` 로 바로잡았다. BF16 은 bfloat16 타입 지원에 관한
  별개 표시이고, *타입의 형식이 IEC 60559 와 같다는 것과 연산 의미가 부속서 F 를
  따른다는 것은 다른 질문*이라는 문장을 덧붙였다. (v0.6.0 에서 새로 들어간 오류)
- **33장 CHERI** — "정수로 바꿨다 되돌리면 권한을 잃는다"를 갈랐다. `uintptr_t`
  를 거친 *공식 왕복은 출처를 보존해 지원되고*, 막히는 것은 출처 없는 정수에서
  포인터를 위조하는 일이다. 35·72장의 서술과의 충돌도 해소됐다.
- **5장 주소=정수** — 단순 모형(번호로 그려도 좋다)과 C 의 포인터 값(정수 타입이
  아니다)을 처음부터 두 층으로 갈랐다. 장 결론도 함께 고쳤다.
- **릴리스 게이트** — 배포 스크립트가 서두 정형 검사와 판 동기(stale 0)를
  요구하고, 어긋나면 배포를 중단한다. 경고를 안고 배포되던 회귀를 막았다.
- 15장 요약 문답도 호스트 프로그램으로 한정했다.

### Changed
- **장 서두 정형** — 순서를 *기댄 것 → 인출 문답 → 이 장이 끝나면 → 답할 질문*
  으로 통일했다(두 판 160파일 재배치). 선행 개념을 표시하는 데 그치지 않고
  곧바로 인출까지 시키는 순서다. 1장만 예외(기댈 선행 장이 없다).
- **서두 시각 개편** — 굵은 왼쪽 세로선 하나가 네 칸을 묶고, 라벨은 회색
  대문자풍으로 통일했으며, 굵은 가로선이 본문과 가른다. 항목당 한 줄로 압축해
  서두가 길어지던 문제도 함께 줄였다. HTML 도 같은 구조를 쓴다.
- **참고 문헌과 출처를 부록에서 뺐다** — 찾아보기 앞의 정식 항목이 되고, 문법
  부록은 F → E 로 당겼다. HTML 목차에서 머리말이 「부록과 찾아보기」에 섞이던
  것도 고쳐, 앞부속은 제1부 앞에 둔다.
- 5장 워드/64비트 동일시를 좁혔다 — 일반 목적 레지스터의 대표 폭을 가리키는
  느슨한 말이고, x86 문서의 word 는 16비트다.
- 5·33장의 사례 열거에 "외울 필요 없다"는 표지를 넣었다.
- 요구사항 R12 를 실제 교수 설계에 맞게 개정했다(핵심 개념마다, 최소 부 단위로
  고르게).

### Added
- **`scripts/check-chapter-openings.py`** — 서두 네 장치의 존재·개수·순서·위치와
  문답 유무, prereq 의 장 이름 병기를 검사한다. 빌드·`project-check`·릴리스
  게이트에 물렸다. 옛 `check-questions.py` 를 대체한다.

## [v0.7.0] - 2026-08-06

연산자를 *계약*으로 다시 썼다(RFC-0007). 근거는 ISO/IEC 9899:2024 작업 초안
N3220 §6.5 원문이다.

### Added
- **부록 A 「연산자 완전 참조」** — 연산자마다 피연산자 제약·결과 타입과 값
  범주·동작·회색지대(UB/미지정/구현정의)·본문 참조를 카드로 적었다. 평가 순서와
  시퀀스 포인트를 절로 세워 순서가 보장되는 다섯 자리를 못박고, 회색지대 총람을
  세 표로 모았으며, '연산자가 아닌 것들'(인자 목록의 쉼표·복합 리터럴·선언의
  별표 등)을 정리했다.
- **27장** 이 장의 연산자들이 거는 계약 표. 시프트의 회색지대 셋을 따로 표로.
- **36장 「포인터의 덧셈은 보통의 덧셈이 아니다」** — 원소 단위인 세 이유,
  뺄셈이 원소 수를 주는 것과 `ptrdiff_t`, 어디까지 허용되는지의 표(one past the
  end 는 만들 수 있으나 역참조 금지), 역방향 루프 반례, 최적화가 이 규칙을 쓰는
  사례. 예제 `ch36/ptrmath.c`.
- **8장 「비트가 실제로 어떻게 나뉘는가」** — float·double 의 부호·지수·가수
  배치, 바이어스와 숨은 비트, 지수 양 끝의 예약(영·비정규수·무한대·NaN).
- **44장 「비트를 직접 열어 본다」** — 예제 `ch44/bits.c` 가 1.0·0.1·0.1+0.2·
  ±0.0·inf·NaN·비정규수의 비트를 찍는다. ULP 가 비트 하나라는 것과 점진적
  언더플로를 지면에서 확인한다.
- **40장 「1차원으로 빌려 2차원으로 쓰기」** — 첨자 매크로, 크기 계산의 넘침
  검사, `int **` 오해. 예제 `ch40/flat2d.c`.
- **문답 신설** — 문답이 하나도 없던 여섯 장(58~63)에 실제 독자의 질문을 썼다.
- **`scripts/check-questions.py`** — 모든 장이 *서두에* 질문 모음을 제시하는지
  기계적으로 검사한다(호출 1회, 첫 절보다 앞, 문답 1개 이상). 두 빌드
  스크립트와 `project-check.sh` 에 물렸다.

### Changed
- 질문 모음의 임계값을 1로 낮춰 **81장 전부**에 실린다(이전 36장).
- 시프트 서술을 원문대로 바로잡았다 — C23 에서도 부호 있는 음수의 좌시프트는
  UB 이고, 음수의 우시프트는 구현 정의다. "두 보수 강제로 해결됐다"는 통설을
  오개념 블록으로 정면 반박했다.

## [v0.6.0] - 2026-08-06

타 세션의 종합 평가(7.7/10)를 받아 RFC-0006 을 세우고 일곱 단계로 나누어
고쳤다. 평가의 지적 가운데 **설계 의도인 것과 실제 결함인 것을 먼저 가른**
것이 이 판의 뼈대다.

### 설계로 지킨 것 (RFC-0006 §1.1)
- 실습·연습을 넣지 않는다. 부담 없이 읽히는 쪽을 택한 교환이다.
- 첫 프로그램이 늦은 순서를 바꾸지 않는다.
- HTML 본문 폭을 가두지 않는다 — 독자가 창으로 조절할 수 있고 PDF 판이 있다.
- 답을 접거나 숨기지 않는다.
그 대신 **머리말에서 한계를 정직하게 선언**했다(아래 Added).

### Added
- **머리말 「이 책이 주는 것과 주지 않는 것」** — 읽기만 해서는 프로그래밍
  능력이 자라지 않는다는 것, 그것이 분량과 부담을 줄이려는 선택이라는 것,
  첫 프로그램이 제3부에서야 나온다는 예고, 그리고 이 책이 주는 것은 빠른
  결과가 아니라 단단한 기반이라는 것.
- **장 서두 3종 세트**(두 판 81장) — `prereq`(이 장이 기대는 것: 번호가 아니라
  '번호 + 장 이름 + 개념'), `chapter-questions`(그 장의 문답에서 질문만 모아
  서두에 제시, 답은 싣지 않는다. 목록은 손으로 적지 않고 메타데이터 조회로
  만든다).
- **5장 「이 번호는 진짜 주소인가」** — 가상 주소, 세그먼트·하버드, 정렬이
  남긴 하위 비트와 TBI/MTE 의 상위 바이트, NaN 박싱, CHERI capability.
- **11장 「또 하나의 사다리 — 가상기억」** — 세그먼트에서 페이징으로, 4단계
  쪽 표와 TLB, 쪽 부재가 여는 것들(요구 쪽매김·스왑·copy-on-write·게으른
  할당), 쪽 권한과 W^X, 널 참조가 즉시 죽는 이유, MMU 없는 세계.
- **33장 「포인터의 크기는 하나가 아니다」** — 표준이 실제로 약속하는 것,
  워드 주소 기계·세그먼트 x86·하버드 MCU·AS/400 의 사례, CHERI/Morello·
  TBI/MTE·x32/wasm 처럼 지금도 갈리는 사례, `void *`·`char *` 의 두 특권.
- **35장** `void *` 와 `char *` 의 특권 대조표와 오개념 블록(바이트로 읽는 것과
  포인터를 되찾는 것은 다른 문제다).
- **78장 「이 라이브러리의 자리」** — 확인된 것과 확인되지 않은 것의 대조표,
  API/ABI 안정성, 아직 없는 것(성능 자료·외부 사용 사례·플랫폼 매트릭스).

### Fixed
- 44장 부동소수점 0 나누기를 IEC 60559 조건부로 정정(C 핵심 언어는 정의하지
  않는다).
- 33장 포인터 크기·주소=정수 단정을 표준 수준으로 좁혔다.
- 72장 `uintptr_t` 순서 비교의 플랫폼 전제를 명시했다.
- 15장 "모든 C 프로그램은 main 에서"를 호스트 구현으로 한정했다.
- 40장 `free` 뒤 널 대입과 ASan 의 보호 범위를 좁혔다.
- 부록 D 의 N3220 문단 순서를 갈라 오독을 막았다.

### Changed
- **proven 의 위치를 다시 썼다** — 최근 합의가 아니라 그 합의를 C23 로
  구현하려는 하나의 시도(제12부 도입부·78장).
- **조판 가시성** — 모든 글자를 10pt 이상으로(장치 제목·코드·판권·색인),
  회색조에서도 구분되는 구문 강조 신설(주석 회색 기울임·키워드 굵게·문자열
  남색·전처리기 자주), 장치와 문답의 여백 확대, 목차 부 제목 고아 방지.
- 부록 인쇄 순서를 A~F 정순으로 바로잡은 v0.5.2 의 결과로 거의 빈 쪽이 사라졌다.
- 쪽 수: 한국어판 423 → 456, 영어판 439 → 474.

## [v0.5.2] - 2026-08-05

읽기 편하도록 조판 여백을 손보고, 차례에서 빠져 있던 부록을 채웠다.

### Fixed
- **차례에 부록과 찾아보기가 없었다.** 차례는 번호가 붙은 장만 모으고 있어서,
  번호를 매기지 않는 머리말·부록 A~F·찾아보기가 통째로 빠져 있었다. 이제
  번호 없는 1단계 제목도 쪽 위치로 앞부속과 뒷부속을 갈라 싣는다 — 머리말은
  차례 첫 줄에, 부록과 찾아보기는 「부록과 찾아보기」 묶음으로. 전부 눌러서
  본문으로 가는 링크다.
- **부록의 차례가 A·B·C·D·F·E 순이었다.** 차례를 만들자 드러난 것으로,
  두 판 모두 부록 F(문법 전문)가 부록 E(참고 문헌)보다 먼저 인쇄되고 있었다.
  글자 차례대로 바로잡았다.

### Changed
- **차례 행간을 넓혔다.** 항목 간격 0.2em→0.5em, 줄 간격 0.9em, 부 제목 위
  여백 0.9em→1.5em. 빽빽해서 읽기 어렵던 것을 풀었다.
- **절 제목(`1.2` 꼴) 아래 여백을 주었다.** 기본값은 제목이 본문에 붙어 있어
  단락의 시작이 눈에 들어오지 않았다. 2단계 제목은 위 1.9em·아래 1.05em,
  3단계는 위 1.5em·아래 0.85em.
- 여백이 늘어난 만큼 쪽 수가 늘었다 — 한국어판 417→423쪽, 영어판 431→439쪽.

## [v0.5.1] - 2026-08-05

### Changed
- **PDF 접근성 태그를 다시 켰다.** v0.1.0 때 "태그 PDF 가 링크마다 쪽을 하나씩
  늘린다"고 보고 `--no-pdf-tags` 로 내보내 왔으나(상류 이슈 typst/typst#8722),
  같은 바이너리(typst 0.15.1, 9dfd3a08)로 엄밀히 재측정한 결과 **사실이 아니다**.
  최소 재현물은 태그·무태그 모두 2쪽이고, 이 책은 링크 346개를 담고도 양쪽 다
  417쪽(영어판 431쪽)이며, `--pages` PNG 내보내기 결과는 바이트 단위로 같다
  (md5 일치). 상류 관리자의 지적대로 이 깃발은 조판이 끝난 뒤 PDF 변환에만
  전달되므로 쪽 나눔에 영향을 줄 수 없다. 태그를 되살리니 화면 낭독기가 읽을 수
  있는 PDF 가 되고, 대신 파일이 2.3MB→5.9MB 로 늘었다.

## [v0.5.0] - 2026-08-05

제목을 확정하고, 한국어판 PDF 의 글꼴 회귀를 고쳤으며, **예제 트리를 판마다
따로 두어** 영어판 독자가 영어 주석과 영어 출력을 보게 했다.

### Added
- **영어판 예제 트리 `examples-en/`** — 91개 예제의 주석·문자열·출력 문구를
  영어로 옮겼다(전수 컴파일·실행 검증). 유니코드·인코딩을 설명하느라 한글이
  필요한 두 곳(`ch37/str.c` 의 `"안녕"` 바이트 수, `ch59/ctype.c` 의 CP949
  `0xC7`)만 한글을 남겼다. 검증 스크립트는 트리를 인자로 받아
  `build/examples-out/` 와 `build/examples-out-en/` 에 따로 캡처하고,
  `#demo` 는 경로 접두사를 보고 제 판의 캡처를 읽는다. 영어판 빌드가 자기
  트리를 전수 검증한 뒤 조판한다.
- **영어판 판권면** — 한국어판에만 있던 지은이·연락·저장소·판·라이선스 면을
  영어판 PDF 에도 넣었다.
- **HTML 첫 화면의 표지·머리말·판권** — 목차 앞에 PDF 표제면과 같은 표지
  (제목·부제·초안 표시·판 번호·최종 수정일·지은이·연락처)를, 목차 뒤에
  머리말 전문과 판권·저작권·연락 절을 실었다. 판 번호·성격·수정일은
  `main.typ` 에서 읽어 온다.

### Fixed
- **한국어판 PDF 글꼴 회귀(v0.3.0·v0.4.0)** — 세 빌드 스크립트 중 한국어판
  것만 글꼴 경로 기본값이 없어, 환경변수 없이 빌드하면 Noto 를 못 찾고 대체
  글꼴로 조판됐다. 실제로 v0.3.0·v0.4.0 한국어판 PDF 에는 Noto 대신 WenQuanYi
  Zen Hei(중국어 자형)와 IPAGothic(일본어)이 박혀 있다. 기본값을 맞추고
  **typst 의 `unknown font family` 경고를 오류로 승격**시켜 대체 글꼴 PDF 는
  만들어지는 즉시 지우고 빌드를 실패시킨다. `check-tools.sh` 도 글꼴 파일의
  존재를 미리 알린다.
- 내보낸 HTML 끝에 비어 버린 차례 뼈대가 남던 것을 걷어 냈다.

### Changed
- **책 제목 확정** — 주제목은 두 판 모두 `Proven C Book`(고유명사이므로
  음차하지 않는다), 부제는 한국어 `프로븐 C 라이브러리와 함께하는 현대적 C
  입문`, 영어 `An Introduction to Modern C with the Proven C Library`.
  옛 영어 부제(`Modern C, built on the proven library`)는 소문자 `proven` 이
  형용사로 읽혀 "증명된 라이브러리"라는 오독을 낳을 수 있었다. 제목 대문자
  `the Proven C Library` 는 `the GNU C Library` 와 같은 꼴이라 제품 이름으로만
  읽힌다.
- **판별 조판 글꼴** — 영어판은 본문 `Noto Serif`, 제목 `Noto Sans`, 코드
  `Noto Sans Mono` 로 통일했다(D2Coding 을 쓰지 않는다). 한국어판은 그대로
  Noto CJK KR + D2Coding. HTML 도 판마다 같은 스택을 쓴다.

## [v0.4.0] - 2026-08-05

제12부(proven)를 매뉴얼에서 *교재*로 다시 썼고, 부록 F에 C 표준 문법 전문을
실었으며, 외부 검토 보고서의 지적을 전수 반영했다.

### Added
- **제12부 전면 확장(69~78장)** — "함수 나열"에서 벗어나, 각 장에 ① 자료구조
  원문(구조체 필드와 그 뜻), ② 객체의 한살이 표(만들기·쓰기·되돌리기),
  ③ 계약(무엇이 실패이고 무엇이 정상인가), ④ 잘못된 사용 예를 넣었다. 각 장이
  138~183행에서 232~353행으로 늘었다. 검증 예제 8종 신설 — 객체 한살이
  (`ch70/first`), 에러 코드 전수와 정리 관용구(`ch71/codes`·`cleanup`, 일부러
  실패하는 할당자로 시연), 기억 어휘(`ch72/mem`), 같은 코드가 힙·아레나·풀에서
  도는 것(`ch73/three`), 문자열 편집(`ch74/edit`), 서식 문법 전부와 사용자 타입
  렌더러(`ch75/spec`), 네 컨테이너 비교(`ch76/tour`), 파일 한살이(`ch77/fslife`).
- **부록 F에 C23 문법 전문** — 부속서 A 전체(어휘 문법·구문 문법·전처리 지시문,
  규칙 193개)를 절마다 해설과 함께 수록. 244행 → 1389행.
- **37장에 문자열 리터럴 네 절** — 리터럴이 배열이라는 것(`sizeof "abcdef"`=7,
  그리고 `3["abcdef"]`가 합법인 이유), 인접 리터럴의 이어 붙이기와 그 용법
  (버전 문자열 조립, 서식 조각 매크로, `PRIu64`가 그 규칙 위에 선 이유),
  인코딩 접두사(`L""`·`u8""`·`u""`·`U""`, 실측 표), 그리고 *타입이 `const`가
  아니라는 것*과 C++와의 차이·그 역사적 이유·향후 표준 전망.
- **49장 전처리기 보강** — 두 겹 우회(double expansion)를 단계표로 전개하고
  표준의 `glue`/`xglue`·`xstr(INCFILE(2).h)`를 실행되는 예제로 수록. 표준이 든
  기묘한 예 세 가지(자리표시자, 표준이 "미지정"이라 못 박은 전개, `__VA_OPT__`의
  판정 시점). 매크로 인자 괄호 규칙을 절로 신설해 함정 4종을 실제 전개로 제시.
- **46장에 파일 모양의 UB** — 소스 파일이 개행으로 끝나야 한다는 조항(C23에도
  유효, 부속서 J.2의 두 번째 항목)과 그 실무 파급(깃 차이·`cat`·`wc -l`),
  기묘한 UB 표(218종 명시), 그리고 `memcpy(NULL, NULL, 0)`이 C2y에서 정의되기로
  한 사연(N3322).
- **30장에 표준의 `switch` 예제** — 도달하지 않는 선언 뒤에서 초기화되지 않은
  값을 읽는 그 예제.
- **47장에 플랫폼 상자** — 인자를 *받는 것*(argc/argv, 표준)과 *해석하는 것*
  (파서, 표준에 없음)을 가르고, 유닉스의 `execve` 배열 대 윈도우의 단일 문자열,
  `GetCommandLineW`·`CommandLineToArgvW`를 설명.
- **`scripts/check-headings.py`** — 줄바꿈된 인라인 코드가 우발적 장 제목이 되는
  사고를 빌드가 잡는다(양쪽 빌드에 연결).

### Changed
- **HTML 판이 창 가로를 다 쓴다** — 본문 폭 상한과 가운데 정렬 제거, 목차 단수
  자동.
- 장 제목의 구분 부호를 em dash 로 통일(양판 14파일).
- README의 예제 수(94종·파일 98개)와 공개 스크립트 범위를 실제와 맞췄다.

### Fixed
외부 검토 보고서(14건)를 현재 원고와 전수 대조해 반영했다.
- `realloc(p, 0)` — C17까지 구현 정의·폐기 예정, **C23에서는 정의되지 않은
  동작**(N2464). 반대로 쓰여 있던 것을 정정.
- 31장에서 인라인 코드가 줄바꿈되며 실제 1단계 제목이 돼 있던 것.
- 기본 인자 승격이 항상 `int`가 아님(`unsigned int`가 될 수 있다), 정수 변환
  등급에서 `bool`이 문자 타입들보다 낮다는 것, 좁히기 변환이 부호 있는 목적
  타입에서는 구현 정의라는 것.
- 객체 표현을 읽을 수 있는 문자 타입에 `signed char` 추가, 배열 무너짐 예외에서
  `alignof` 제거, `%.*s`의 `*` 인자가 `int`라는 것, `stderr`를 "완전 버퍼링이
  아니다(대개 무버퍼)"로 축소.
- 자유 사용 구현의 필수 헤더에 C23이 더한 `<stdbit.h>`·`<stdckdint.h>` 반영.
- N3220을 "C23 발행 직후의 C2y 작업 초안"으로 계보 정정(정확한 인용은
  ISO/IEC 9899:2024).

## [v0.3.0] - 2026-08-05

13부 81장 + 부록 A~F + 찾아보기. **영어판이 전권 완역됐고**, 제1부와
제3부·제9부·제13부에 장이 늘었다. 장 번호는 이 판에서 확정된 것을 적는다.

### Added
- **영어판 완역** — 50~81장, 제11·12부 도입부, 부록 A~F, 찾아보기까지 옮겨
  전권을 마쳤다(`sync-status`: 93 동기화 · 0 대기). 영어판 PDF에 부록과
  찾아보기를 붙이고 표지의 "translation in progress" 표시를 걷었다.
- **2·3장 신설(제1부)** — 「기억의 구역들」(문법 없이 코드·정적·스택·힙과
  bss), 「프로그램과 프로세스」(OS 가 주는 것, `fork`/`exec` 대
  `CreateProcess`, 표준 밖이라는 경계).
- **18장 「컴파일러의 지형」 신설** — 현역 C 컴파일러 총람(GCC·Clang/LLVM·
  MSVC, 윈도우용 Pelles C, 벤더 성능 컴파일러 icx·AOCC·nvc·Open XL·CCE),
  *고전 `icc`는 은퇴(oneAPI 2024.0에서 제거)* 명시, "새 컴파일러는 대부분
  LLVM 기반"이라는 흐름.
- **80장 「임베디드의 도구 상자」 신설(닫는 부)** — 임베디드 컴파일러 표
  (IAR·Arm Compiler for Embedded·TI Arm Clang·MPLAB XC·Renesas CC-RX/RL·
  Green Hills·Wind River Diab·SDCC·GCC 계열)와 make·git 곁의 연장 8묶음
  (빌드 생성기, 디버그 프로브와 OpenOCD/gdb, UART·SWO·RTT 로그,
  QEMU·Renode, 정적 분석과 MISRA, Unity·Ceedling, 크기 측정 도구,
  로직 애널라이저, Compiler Explorer).
- **47장 「`main`의 세 얼굴」 신설** — 표준 3형태, `void main()` 반례,
  argc/argv 계약, 종료 방법 표, 하위 8비트 잘림, 리눅스·윈도우·임베디드 관례.
- **52장 「선언을 읽는 법」 신설** — 두 가지 독법(식별자에서 바깥으로 =
  오른쪽-왼쪽 규칙, 그리고 바깥에서 안으로), 영어 어순으로 이어 읽기,
  이름이 없는 추상 선언자, `typedef`의 문법적 정체와 한계(별명일 뿐 새 타입이
  아니다)·포인터 숨기기 반례, 그리고 `cdecl`. 예제 1종 검증.
- **67·68장 신설(제11부 끝)** — 「프로그램의 기억 지도」: 실행 파일 구역과
  주소 공간, 리눅스/윈도우 대조(가드 페이지·ASLR·mmap 문턱·스레드 스택),
  그리고 자유 사용 구현의 기억(Arm Cortex-M 벡터 테이블과 MSP/PSP, AVR의
  하버드 구조와 `PROGMEM`, PIC의 뱅크와 컴파일된 스택·재귀 불가).
  「할당자의 속」: 블록 헤더와 경계 태그, 빈과 갈래별 캐시, 단편화와 RSS,
  대안 할당자(jemalloc·TCMalloc·mimalloc·snmalloc·scudo)와 대안 libc
  (musl·picolibc·newlib·Bionic), 아레나·풀, 그리고 proven의 메모리 모델로
  잇는 다리.
- **39장에 「표준의 네 축」** — 저장 기간·유효범위·연결·저장 클래스 지정자,
  `static`의 두 얼굴, 선언과 정의. 여기에 기억의 지도를 주소로 확인하는
  예제와 리눅스 8 MiB / 윈도우 1 MiB 스택 한도를 더했다.
- **40장 보강** — `malloc`이 돌려주는 주소의 정렬(`max_align_t`,
  `aligned_alloc`)과 할당이 비싼 이유 다섯 가지, 실측(회당 ~11ns vs 재사용
  0.4ns). `free`가 OS 반납이 아니라는 오개념 포함. 예제 1종 검증.
- **79장 「실전의 C」 도구 절 전면 보강** — 빌드·의존성, 디버거와 기록·재생,
  프로파일러, 검사·시험, 서식·문서, 임베디드 관측.
- **부록 F 신설** — EBNF 표기법과 C 문법 참고(선언자·문장·식 계층, `A * B;`).
- **`scripts/check-xrefs.py`** — 한국어 원본과 영어 번역의 *장 참조 집합*을
  대조해 어긋난 곳을 빌드에서 드러낸다. 영어판 빌드에 연결했다.

### Changed
- 목차를 *부 단위로 묶어* 낸다(PDF·HTML 양쪽). 장만 나열하던 긴 목록 대신
  제1부…제13부 아래에 장을 들여 쓴다.
- **HTML 판이 창 가로를 다 쓴다** — 본문 폭 상한(105ch)과 가운데 정렬을
  없애고 좌우 여백을 줄였다. 목차는 들어가는 만큼 단이 늘어난다.
- 제9부 부제를 「정밀」에서 「깊은 구석들」로.
- 표지 소개글을 존댓말로 다듬고, 판권면(2쪽)을 재조판.
- 8장의 부동소수점 흡수 예제를 `float` 8.000000001 류에서 `double`의
  $10^16 + 1$ 로 교체하고 $2^53$ 한계를 덧붙였다.
- 18장에서 Pelles C 를 임베디드 뒤 곁다리 절로 옮기고 사용자 평을 반영,
  은퇴한 이름에 Dev-C++·TDM-GCC 추가.
- 장 신설로 이후 장이 밀려 전체가 81장이 됐다.

### Fixed
- 반복된 재번호로 어긋나 있던 장 참조를 전수 정정 — 범위 표기(`N–M장`)와
  대문자 `Chapter N`, 그리고 표 안의 숫자와 본문 괄호 참조가 이동 스크립트를
  비켜 간 자리들(2·3·51·78·79장, 부록 E·F). 이제 `check-xrefs` 가 통과한다.
- CHANGELOG 끝에 통째로 중복돼 있던 예전 섹션(구 [Unreleased]·v0.2.0) 제거.

## [v0.2.0] - 2026-08-05

### Added
- **39장 「구조체를 부리는 법」 신설** — 중첩 구조체 참조와 `.`/`->` 혼용,
  복합 리터럴(임시 구조체)의 세 성질(lvalue·블록 수명·주소 반환 금지),
  옵션 구조체 + 지정 초기화로 만드는 *순서 없는 이름 붙은 인자* 관용구
  (기본값 설계·API 확장 안전성·평가 순서 함정), 패딩의 세 규칙과 멤버
  재배열, `#pragma pack`·`__attribute__((packed))`(비표준)의 위험과
  `alignas`(표준) 강제 정렬, 그리고 *배열을 값으로 넘기는 유일한 방법*인
  구조체 감싸기와 그 대가(스택 사용량·호출 규약·복사 생략). 예제 2종 검증.
  이후 장은 40~73장으로 이동했다.
- **제11부에 세 장 신설(58~60장)** — `<stdatomic.h>`(원자적 연산·데이터 경쟁·
  `volatile`과의 차이·메모리 순서·CAS 관용구), `<stdckdint.h>`(검사 산술,
  사후 검사가 지워지는 이유, 할당 크기 계산), 그리고 매크로에서 키워드로
  승격된 낱말들(`bool`·`nullptr`·`static_assert`·`constexpr`·`typeof` — 도입
  배경, 무엇을 막는가, 규칙과 제한, 기존 코드 이전 순서). 예제 3종 검증.
  이에 따라 proven 부는 61~70장, 닫는 부는 71~72장으로 이동했다.
- **영어판 번역 착수** — 머리말, 제2부 도입부, 1~7장. 장 번호는 한국어판과
  동일하게 고정한다(미번역 장은 부 표지에 명시).
- **한국어판·영어판 동기화 장치** — `scripts/sync-status.py` 가 각 영어 파일이
  어느 한국어 원본 해시에서 번역됐는지 기록하고 synced/stale/pending 을
  판정한다. 두 빌드가 이를 실행하므로 원본이 바뀌면 다음 빌드에서 해당 영어
  장이 stale 로 드러난다. `TRANSLATION.md` 는 이 기록에서 자동 생성한다.
- 서술 장치 라벨을 `book/lib.typ` 한 곳에서 지역화(`--input lang=`) — 영어판이
  라이브러리 사본을 갖지 않으므로 장치를 고치면 양판이 함께 바뀐다.

### Fixed
- HTML 목차·`<title>` 에서 장 제목이 이중 이스케이프되던 문제(`<stdio.h>` 가
  `&lt;stdio.h>` 로 보임). 내보낸 HTML 에서 뽑은 제목을 한 번 되돌린 뒤
  이스케이프한다.
- HTML 장치 판정이 접두 일치라 `문` 이 "문자열…" 을, `A` 가 "A bundle…" 을
  물 수 있던 문제 — 라벨 뒤에 공백을 요구하도록 조였다.

## [v0.1.1] - 2026-08-05

### Added
- **제11부 표준 라이브러리 정독 신설(48~57장)** — 헤더 전체 지도(31종),
  `<stdio.h>` 두 장(스트림·읽고 쓰기), `<string.h>`, `<stdlib.h>`,
  문자와 로케일, 수, 시간, 진단과 제어, 그리고 새 헤더와 부속서 K(`*_s`)의
  실패사. 함정 중심으로 쓰고 정례·반례를 붙였다.
- **46장 함수 포인터** — 이름의 무너짐과 `(*****f)()`의 비대칭, 타입 호환,
  `void *`와의 비호환(하버드 구조·`dlsym`), 디스패치 표, 손수 짓는 vtable과
  GObject·커널 `file_operations`·COM 사례.
- 예제 15종 추가(전부 실제 빌드·실행 검증). 수학 예제를 위해 검증
  스크립트가 `-lm`을 자동으로 링크한다.

### Changed
- proven 부가 제12부(58~67장)로, 실전·총정리가 68·69장으로 이동.
- 13부 69장 구성.

### Fixed
- 13장에서 낱말 안쪽 `*` 강조가 HTML 페이지를 잘라먹던 문제(`stdio` 설명)
  수정, "검은 상자" 표현 정정.
- 찾아보기 조판을 2열 표제어/쪽번호로 바로잡고, HTML 색인에 본문 링크 추가.
- **PDF 태그 비활성화(`--no-pdf-tags`)** — Typst 0.15.1이 링크마다 빈 쪽을
  만들어 책이 235쪽에서 570쪽으로 부풀던 문제(typst/typst#8722). 이로써
  쪽수가 정상화되고 색인의 쪽 번호 링크도 살아났다.
- 인쇄용 흑백 전환 — 상자 채움·검은 출력 상자·구문 강조 색 제거.

## [v0.1.0] - 2026-08-05

첫 공개 판. 12부 58장 + 부록 A~E + 찾아보기, 약 235쪽.

### Added
- 제11부 proven 매뉴얼부(47~56장) — 다섯 가지 버그 제기부터 프리스탠딩까지.
- 부록 A~C 확장(연산자·서식 문자열 완전 정리·암묵 변환), 부록 D(더 읽을거리와
  표준 문서), 부록 E(참고 문헌), 그리고 쪽 번호가 붙는 찾아보기.
- 15장 디버거(빌드 옵션·맹점·릴리스 전용 버그), 46장 printf/scanf 해부,
  45장 가변 인자 승격과 `_Generic`·`PROVEN_ARG` 구조, 23장 기본 자료형 지도,
  33장 VLA·`[static N]`·배열 매개변수, 39장 비트 필드, 44장 X 매크로,
  57장 C와 C++의 관계.
- 수록 예제 60여 종, 전부 실제 빌드·실행 검증(GCC 14 기준, Clang 교차 확인).
